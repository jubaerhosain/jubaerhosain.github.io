---
import { site, nav } from '../data/site';
---

<header>
  <nav class="flex flex-wrap gap-x-4 gap-y-1 text-sm" aria-label="Main">
    {nav.map((item) => <a href={item.href}>{item.label}</a>)}
    <a href={site.resume} target="_blank">Resume</a>
  </nav>
</header>
