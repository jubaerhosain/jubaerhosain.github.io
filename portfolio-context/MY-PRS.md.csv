# Pull Request Portfolio — Md. Jubaer Hosain

**Repository:** `skavl-media/subrite` (Subrite — multi-tenant subscription-management SaaS for
Nordic markets; NestJS + PostgreSQL/TypeORM + Temporal backend, Next.js frontend)
**Author:** Md. Jubaer Hosain (`jubaerhosain`)
**Period covered:** 2025-03-07 → 2026-07-28 (~17 months)
**Generated:** 2026-07-29

| | Count |
| --- | --- |
| Merged pull requests | **251** |
| Open pull requests | 2 |
| In progress (branch, not yet raised) | 1 |
| **Total described here** | **254** |
| Production hotfixes (merged straight to `main`, or `hotfix/*`) | 56 |
| Distinct Linear issues delivered | 175+ |

---

## How to read this document

Entries are **ranked by impact, highest first**, and every entry carries its merge date so you can
place it on a timeline. Three tiers group the ranking:

- **Tier 1 — Flagship (ranks 1-25).** Revenue-, security- or data-integrity-critical work. These are
  the CV candidates; drafted résumé bullets for them are in **Appendix B**.
- **Tier 2 — Significant (ranks 26-105).** Real production problems solved, meaningful features,
  performance work.
- **Tier 3 — Supporting (ranks 106-253).** Smaller fixes, copy/translation corrections, UI polish.
  Included for completeness and to show sustained delivery volume.
- **In progress.** The SUBRITE-632 branch, described after Tier 3. Not ranked, since it has not shipped.

Every entry — in all three tiers — has the same four sections: **Problem**, **Root cause**,
**What I changed**, **Impact**. Where a small change genuinely has a one-line root cause, the section
says so rather than being padded.

If you would rather read chronologically, **Appendix A** lists everything by month.
**Appendix C** groups the multi-PR programmes (tenant isolation, error-handling compliance, Datadog
noise reduction, performance/scale) — these read stronger on a CV than the individual PRs.

### How the impact score was derived

The score is a 0-10 composite, computed from the signals below and then hand-adjusted after reading
each PR's actual diff. It is a **ranking aid, not a metric** — the ordering matters, the decimals do
not.

| Signal | Weight | What earns it |
| --- | --- | --- |
| Subsystem criticality | up to 3.0 | Payments, invoicing, auth, tenant isolation > subscription lifecycle > communication/automation > admin UI > copy/branding |
| Production incident | up to 2.8 | Merged to `main` as a hotfix, a Datadog-raised error, or a customer-support escalation — proof it hit real users |
| Failure class | up to 2.6 | Cross-tenant/cross-user data exposure and money-correctness bugs > silent failures > performance > UX polish |
| Linear priority & size | up to 1.5 | `Urgent`/`High` priority, `M`/`L` estimate |
| Breadth & durability | up to 1.5 | Cross-stack change, new shared utilities, new test coverage, DB migrations, API-contract changes |
| Systemic / architectural | up to 1.5 | Part of a deliberate multi-PR programme rather than a one-off |

### Sourcing and honesty notes

- **Problem** statements come from the Linear issue and the PR description I wrote at the time.
- **Root cause** and **What I changed** are read off the actual diff of each PR.
- **Impact** is stated in terms of what the change demonstrably does. Where no production metric was
  recorded, the impact is described qualitatively rather than invented — there are no fabricated
  percentages in this document.
- Linear issues numbered `SUBRITE-2xxx`/`SUBRITE-3xxx` (2025 work) no longer resolve: the workspace
  was renumbered in 2026 and those issues were not carried over. Those entries are reconstructed from
  the PR title, description and diff, and are marked accordingly.

## Impact-ranked index

All 253 PRs, highest impact first. This is the filtering surface: scan it, pick the rows worth putting on a
CV, then read those entries in full below. `H` = production hotfix. Linear keys shown only where the issue still
exists (2025 issues were not carried through the workspace renumbering).

| # | Date | PR | Linear | Type | Area | Score | Summary |
| --: | --- | --- | --- | --- | --- | --: | --- |
| 1 **T1** | 2026-06-29 | #5194 | [SUBRITE-394](https://linear.app/subrite/issue/SUBRITE-394) | Bug fix | Payments & providers | 8.2 | When we manually stop a subscription for the customer, do we also stop further attempts to c… |
| 2 **T1** | 2026-07-16 | #5317 | [SUBRITE-55](https://linear.app/subrite/issue/SUBRITE-55) | Security / tenant isolation | Tenant isolation | 8.05 | Tenant isolation: Product / Plan / Content / Segmentation |
| 3 **T1** | 2026-05-14 | #4893 | [SUBRITE-26](https://linear.app/subrite/issue/SUBRITE-26) | Security / tenant isolation | Tenant isolation | 7.9 | Tenant isolation: Foundations and Member module |
| 4 **T1** | 2026-06-19 | #5188 `H` | [SUBRITE-299](https://linear.app/subrite/issue/SUBRITE-299) | Bug fix | Subscriptions & billing lifecycle | 7.8 | Bug on Invoice Already Paid for Next Period |
| 5 **T1** | 2026-06-23 | #5200 | [SUBRITE-406](https://linear.app/subrite/issue/SUBRITE-406) | Bug fix | Invoicing & accounting | 7.45 | Credit note editor: editing a decimal price silently inflates the amount 10x or 100x, øre am… |
| 6 **T1** | 2026-06-29 | #5208 | [SUBRITE-446](https://linear.app/subrite/issue/SUBRITE-446) | Performance | Subscriptions & billing lifecycle | 7.2 | Optimize subscription/seat-member list for 3000+ members |
| 7 **T1** | 2026-05-21 | #5024 `H` |  | Recovery tooling | Payments & providers | 7.0 | Recover stripe recurring invoices |
| 8 **T1** | 2026-06-03 | #5068 | [SUBRITE-273](https://linear.app/subrite/issue/SUBRITE-273) | Security | Auth & access control | 6.9 | Access to survey list without admin access |
| 9 **T1** | 2026-07-06 | #5270 | [SUBRITE-557](https://linear.app/subrite/issue/SUBRITE-557) | Bug fix | Communication & notifications | 6.7 | Skip Logging duplicate SendGrid webhook events, no warning/error |
| 10 **T1** | 2026-07-17 | #5329 | [SUBRITE-53](https://linear.app/subrite/issue/SUBRITE-53) | Security / tenant isolation | Communication & notifications | 6.65 | Tenant isolation: Push notification |
| 11 **T1** | 2026-07-01 | #5229 | [SUBRITE-457](https://linear.app/subrite/issue/SUBRITE-457) | Bug fix | Subscriptions & billing lifecycle | 6.6 | Error when switching subscription |
| 12 **T1** | 2026-06-16 | #5158 | [SUBRITE-418](https://linear.app/subrite/issue/SUBRITE-418) | Data migration | Subscriptions & billing lifecycle | 6.45 | Psykologisk: Strange renewal-data |
| 13 **T1** | 2026-01-22 | #4432 `H` |  | Security / tenant isolation | Subscriptions & billing lifecycle | 6.4 | Prevent cross user data access |
| 14 **T1** | 2026-01-15 | #4402 `H` |  | Bug fix | Payments & providers | 6.3 | Error while recurring payment failed |
| 15 **T1** | 2026-02-10 | #4506 `H` |  | Bug fix | Subscriptions & billing lifecycle | 6.1 | Prevent subscription plan price update if not intendent |
| 16 **T1** | 2026-07-15 | #5316 | [SUBRITE-298](https://linear.app/subrite/issue/SUBRITE-298) | Security | Admin UI | 5.95 | Nonsensical and unauthorized error message |
| 17 **T1** | 2026-07-23 | #5351 | [SUBRITE-50](https://linear.app/subrite/issue/SUBRITE-50) | Security / tenant isolation | Invoicing & accounting | 5.9 | Tenant isolation: Accounting module |
| 18 **T1** | 2026-06-05 | #5082 | [SUBRITE-276](https://linear.app/subrite/issue/SUBRITE-276) | Bug fix | Payments & providers | 5.85 | E-invoice reactivation advances nextRenewal a full billing period beyond paid coverage (subs… |
| 19 **T1** | 2026-07-09 | #5278 | [SUBRITE-521](https://linear.app/subrite/issue/SUBRITE-521) | Bug fix | Subscriptions & billing lifecycle | 5.85 | Change cancellation blocked due to last invoice being CREDITED |
| 20 **T1** | 2026-07-20 | #5323 | [SUBRITE-585](https://linear.app/subrite/issue/SUBRITE-585) | Bug fix | Communication & notifications | 5.8 | Push notifications silently dropped when FCM returns transient internal errors |
| 21 **T1** | 2026-02-13 | #4528 `H` |  | Bug fix | Database migrations | 5.6 | Subrite 3912/prevent override discounted prices |
| 22 **T1** | 2026-07-02 | #5249 | [SUBRITE-489](https://linear.app/subrite/issue/SUBRITE-489) | Performance | Member portal (My Page) | 5.6 | Optimize My Account (mypage) subscription seat-member UI for 3000+ members |
| 23 **T1** | 2025-09-23 | #3776 `H` |  | Improvement | Admin UI | 5.55 | Hotfix / subrite 3223 / Support paid payment provider |
| 24 **T1** | 2026-07-24 | #5368 *(open)* | [SUBRITE-535](https://linear.app/subrite/issue/SUBRITE-535) | Feature | Admin UI | 5.5 | Align gift subscription member UI with the new group subscription design (admin + mypage) |
| 25 **T1** | 2025-07-22 | #3485 |  | Improvement | Invoicing & accounting | 5.45 | Subrite 2856/inoice reference on invoice address |
| 26 T2 | 2025-08-15 | #3620 `H` |  | Bug fix | Payments & providers | 5.4 | Fix / include tenantId in payment provider settings query |
| 27 T2 | 2026-02-24 | #4543 |  | Improvement | Subscriptions & billing lifecycle | 5.4 | Subrite 3904/adjust ongoing upgrade downgrade recurring price if subscription plan price upd… |
| 28 T2 | 2026-07-28 | #5374 *(open)* | [SUBRITE-661](https://linear.app/subrite/issue/SUBRITE-661) | Security / tenant isolation | Tenant isolation | 5.3 | Tenant isolation: dedicated TenantScopedRepository for TenantUsage |
| 29 T2 | 2026-05-08 | #4944 | [SUBRITE-70](https://linear.app/subrite/issue/SUBRITE-70) | Improvement | Payments & providers | 5.2 | Stripe user charged thrice during downtime via temporal |
| 30 T2 | 2026-06-22 | #5185 | [SUBRITE-402](https://linear.app/subrite/issue/SUBRITE-402) | Bug fix | Subscriptions & billing lifecycle | 5.2 | SubscriptionUpgradeDowngrade: Fix frontend state errors |
| 31 T2 | 2026-07-07 | #5272 | [SUBRITE-553](https://linear.app/subrite/issue/SUBRITE-553) | Improvement | Automation & workflows | 5.15 | Automation "No recipient email address" should be a silent traceable log, not a Datadog error |
| 32 T2 | 2026-06-03 | #5029 | [SUBRITE-197](https://linear.app/subrite/issue/SUBRITE-197) | Performance | Admin UI | 5.1 | UI: Optimize Plans & Products->Product packages |
| 33 T2 | 2025-06-17 | #3333 `H` |  | Improvement | Payments & providers | 5.0 | Hotfix / clear webhooksecret on payment mehtod update |
| 34 T2 | 2025-10-22 | #3863 `H` |  | Bug fix | Payments & providers | 5.0 | Hotfix / upgrade downgrade issue on immediate apply |
| 35 T2 | 2025-05-23 | #3213 `H` |  | Improvement | Admin UI | 4.85 | Hotfix/theme settings |
| 36 T2 | 2026-06-10 | #5095 | [SUBRITE-365](https://linear.app/subrite/issue/SUBRITE-365) | Bug fix | Invoicing & accounting | 4.85 | Fix invoice reactivation periods |
| 37 T2 | 2025-11-03 | #3924 `H` |  | Improvement | Payments & providers | 4.7 | Hotfix/optional phone number in invited member |
| 38 T2 | 2026-02-11 | #4526 `H` |  | Bug fix | Admin UI | 4.7 | Prevent updating member package prices |
| 39 T2 | 2026-06-11 | #5114 | [SUBRITE-379](https://linear.app/subrite/issue/SUBRITE-379) | Feature | Members, segments & import/export | 4.7 | List export |
| 40 T2 | 2026-07-28 | #5370 | [SUBRITE-669](https://linear.app/subrite/issue/SUBRITE-669) | Bug fix | Content & podcasts | 4.65 | Failed to fetch podcast feed |
| 41 T2 | 2025-08-21 | #3595 |  | Improvement | API contract | 4.6 | Subrite 3187/secure sender/schema update and data migration |
| 42 T2 | 2026-05-11 | #4947 | [SUBRITE-107](https://linear.app/subrite/issue/SUBRITE-107) | Improvement | Admin UI | 4.6 | Decouple subscription visibility between Admin UI and My Page for enterprise subscriptions |
| 43 T2 | 2026-07-13 | #5301 | [SUBRITE-586](https://linear.app/subrite/issue/SUBRITE-586) | Bug fix | Payments & providers | 4.6 | Recurring e-invoice: drop over-strict phone mandatory check (blocks EHF delivery for private… |
| 44 T2 | 2026-07-22 | #5346 | [SUBRITE-618](https://linear.app/subrite/issue/SUBRITE-618) | Feature | Admin UI | 4.6 | Members list — apply the Product Packages filter design (Filter bar + chips) |
| 45 T2 | 2025-09-17 | #3736 |  | Improvement | Payments & providers | 4.5 | Subrite 3259 / deprecate non functional fields from db and ui |
| 46 T2 | 2025-09-03 | #3705 `H` |  | Bug fix | Payments & providers | 4.4 | Subrite 3249 - user turned off autorenewal still receiving reminders |
| 47 T2 | 2026-06-19 | #5170 | [SUBRITE-400](https://linear.app/subrite/issue/SUBRITE-400) | Bug fix | Checkout | 4.4 | Checkout PaymentForm: Fix frontend state errors |
| 48 T2 | 2026-07-10 | #5282 | [SUBRITE-567](https://linear.app/subrite/issue/SUBRITE-567) | Bug fix | Automation & workflows | 4.4 | reconcileVippsCharge: Cannot read properties of null (reading 'tenantId') |
| 49 T2 | 2025-07-29 | #3509 |  | Improvement | Admin UI | 4.3 | Subrite 3134/Brandkit configuration in admin, brand specific sender and otp template |
| 50 T2 | 2025-09-29 | #3793 |  | Bug fix | Payments & providers | 4.3 | Fix / subrite 3223 / skip sending invoice automatically when the invoice is paid |
| 51 T2 | 2026-01-23 | #4400 |  | Improvement | Members, segments & import/export | 4.3 | 3827 bugs related to price change |
| 52 T2 | 2026-06-17 | #5166 | [SUBRITE-405](https://linear.app/subrite/issue/SUBRITE-405) | Bug fix | Members, segments & import/export | 4.2 | Export segmentation list not showing the dropdown options |
| 53 T2 | 2026-01-26 | #4439 |  | Bug fix | Subscriptions & billing lifecycle | 4.1 | Bugs on restart vipps payment |
| 54 T2 | 2026-02-09 | #4493 |  | Bug fix | Subscriptions & billing lifecycle | 4.1 | Fix / subrite-3903 / prevent-subscription-plan-price-update-if-not-intendent |
| 55 T2 | 2026-02-20 | #4556 |  | Improvement | Subscriptions & billing lifecycle | 4.1 | Subrite 3942/subscription plan price update affects vipps in progress invoice already sent |
| 56 T2 | 2026-07-24 | #5362 | [SUBRITE-664](https://linear.app/subrite/issue/SUBRITE-664) | Feature | Members, segments & import/export | 4.1 | Issues on members list filter |
| 57 T2 | 2025-11-26 | #3992 |  | Improvement | Communication & notifications | 4.0 | Subrite 3235/list quality hard bounced emails |
| 58 T2 | 2025-11-28 | #4118 `H` |  | Bug fix | Communication & notifications | 3.9 | Hotfix/email communication consent bug |
| 59 T2 | 2026-05-11 | #4922 | [SUBRITE-37](https://linear.app/subrite/issue/SUBRITE-37) | Bug fix | Members, segments & import/export | 3.9 | Feedback in import section |
| 60 T2 | 2026-07-10 | #5297 | [SUBRITE-576](https://linear.app/subrite/issue/SUBRITE-576) | Bug fix | Auth & access control | 3.9 | OIDC token endpoint: refresh-token rotation force-logs-out members (invalid_grant flood) |
| 61 T2 | 2025-09-26 | #3771 |  | Improvement | Admin UI | 3.85 | Subrite 3243/allow tenant to edit or save own text for period price on product package level |
| 62 T2 | 2025-12-30 | #4303 `H` |  | Improvement | Analytics & reporting | 3.85 | Show more in dashboard subscription chart |
| 63 T2 | 2026-06-03 | #5044 | [SUBRITE-158](https://linear.app/subrite/issue/SUBRITE-158) | Bug fix | Admin UI | 3.8 | Einvoice payment shows harcoded values instead of api |
| 64 T2 | 2026-01-27 | #4419 |  | Improvement | Checkout | 3.75 | Option to override cta text on checkout |
| 65 T2 | 2025-04-15 | #2952 |  | Improvement | Admin UI | 3.7 | Feat/subrite 2692 payment methods show in admin |
| 66 T2 | 2025-06-02 | #3242 |  | Improvement | Admin UI | 3.7 | Feat/subrite-2994-edit-payment-methods |
| 67 T2 | 2025-12-30 | #4300 `H` |  | Improvement | Analytics & reporting | 3.7 | Dashboard area chart |
| 68 T2 | 2026-01-02 | #4287 |  | Improvement | Admin UI | 3.7 | Subrite 2871/fixed discounted price on creation |
| 69 T2 | 2025-05-21 | #3194 |  | Improvement | Admin UI | 3.65 | Feat/subrite 2938 communication settings |
| 70 T2 | 2025-05-22 | #3119 |  | Improvement | Admin UI | 3.65 | Subrite - 2888: Theme settings |
| 71 T2 | 2025-07-24 | #3522 |  | Improvement | Database migrations | 3.6 | Subrite 3111 use company name on invoice |
| 72 T2 | 2026-07-03 | #5237 | [SUBRITE-401](https://linear.app/subrite/issue/SUBRITE-401) | Feature | Admin UI | 3.6 | SubscriptionSwitch wizard: token refresh silently resets quantities and discards hand edited… |
| 73 T2 | 2025-04-16 | #3032 |  | Improvement | Communication & notifications | 3.5 | Feat/subrite 2709 improve tenant setting flexibility on newsletter |
| 74 T2 | 2025-11-17 | #4003 `H` |  | Bug fix | Backend (other) | 3.5 | Subrite 3634/Bug on image delivery |
| 75 T2 | 2025-11-24 | #4064 `H` |  | Bug fix | Automation & workflows | 3.5 | Fix / Support duplicate email senders |
| 76 T2 | 2025-12-29 | #4292 |  | Improvement | Communication & notifications | 3.5 | 3758 handing of soft bounce dropped |
| 77 T2 | 2026-01-15 | #4296 |  | Improvement | Communication & notifications | 3.5 | Subrite 3711/more flexibility for consent |
| 78 T2 | 2025-09-29 | #3792 `H` |  | Bug fix | Admin UI | 3.4 | Subrite 3382/bug on saving subscription rules in admin |
| 79 T2 | 2026-01-08 | #4341 |  | Bug fix | Admin UI | 3.4 | Upgrade downgrade issue on manual price subscription |
| 80 T2 | 2026-06-12 | #5133 | [SUBRITE-284](https://linear.app/subrite/issue/SUBRITE-284) | Refactor / tech debt | Tooling, deps & docs | 3.35 | Clean up .ncurc.json in frontend |
| 81 T2 | 2025-03-10 | #2899 |  | Bug fix | Members, segments & import/export | 3.3 | Fix(Subrite-2606): Payment report missing company name and org. number |
| 82 T2 | 2025-06-13 | #3317 `H` |  | Bug fix | Invoicing & accounting | 3.2 | Hotfix/style issue in invoice generation |
| 83 T2 | 2025-06-26 | #3373 |  | Bug fix | Admin UI | 3.2 | Fix/subrite 3058 default preferences for ehf payment method |
| 84 T2 | 2025-10-24 | #3787 |  | Improvement | Members, segments & import/export | 3.15 | Subrite 2822/import tag |
| 85 T2 | 2025-10-14 | #3803 |  | Improvement | Admin UI | 3.05 | Subrite 2805/not possible to delete communication sender |
| 86 T2 | 2026-04-28 | #4888 | [SUBRITE-38](https://linear.app/subrite/issue/SUBRITE-38) | Bug fix | Members, segments & import/export | 3.05 | Support missing member and address fields during member import from admin UI |
| 87 T2 | 2026-04-30 | #4903 | [SUBRITE-58](https://linear.app/subrite/issue/SUBRITE-58) | Bug fix | Members, segments & import/export | 3.05 | CSV encoding issue in member import |
| 88 T2 | 2026-06-02 | #5006 | [SUBRITE-69](https://linear.app/subrite/issue/SUBRITE-69) | Bug fix | API contract | 3.05 | Send Email From "email" is incorrect on Auth Code Verification UI |
| 89 T2 | 2025-09-30 | #3797 |  | Bug fix | Payments & providers | 3.0 | FIx / don't send invoice while already paid |
| 90 T2 | 2025-10-28 | #3891 |  | Improvement | Payments & providers | 3.0 | Subrite 3543 / Reminder email sent for paid invoice |
| 91 T2 | 2025-11-03 | #3855 |  | Improvement | Members, segments & import/export | 3.0 | Subrite 3363/export for delivery address |
| 92 T2 | 2025-11-21 | #4047 |  | Bug fix | Payments & providers | 3.0 | Subrite 3604 / subscription-is-not-stopped-if-card-detaills-are-missing |
| 93 T2 | 2025-08-19 | #3640 `H` |  | Improvement | Shared platform code | 2.8 | Add postal address in tenant update process |
| 94 T2 | 2025-09-16 | #3754 `H` |  | Improvement | Shared platform code | 2.8 | Hotfix / subrite 3322 / retry on vipps purchase |
| 95 T2 | 2025-09-19 | #3768 |  | Improvement | Subscriptions & billing lifecycle | 2.8 | Subrite - 3029 / Add country name in addtion to country code in exports |
| 96 T2 | 2025-09-17 | #3760 |  | Improvement | Admin UI | 2.75 | Subrite 3252 / Show ocr import receiver email in admin |
| 97 T2 | 2025-09-26 | #3777 |  | Improvement | Admin UI | 2.75 | Subrite 3230/add enable vipps login settings in admin |
| 98 T2 | 2025-05-22 | #3143 |  | Improvement | Admin UI | 2.7 | Feat/Subrite 2903 - Tenant preference settings |
| 99 T2 | 2025-11-05 | #3925 |  | Improvement | Members, segments & import/export | 2.7 | Subrite 3537/add revenue and active days per member in registered member export |
| 100 T2 | 2025-11-12 | #3860 |  | Improvement | Widgets & branding | 2.65 | Subrite/3520 hosted widget form |
| 101 T2 | 2026-05-21 | #5004 | [SUBRITE-122](https://linear.app/subrite/issue/SUBRITE-122) | Data migration | Subscriptions & billing lifecycle | 2.65 | Psykologisk import |
| 102 T2 | 2026-06-04 | #5079 | [SUBRITE-150](https://linear.app/subrite/issue/SUBRITE-150) | Bug fix | Admin UI | 2.65 | Subscription missing start date |
| 103 T2 | 2025-06-13 | #3319 `H` |  | Bug fix | Admin UI | 2.6 | Hotfix/saving onboarding fields issue |
| 104 T2 | 2025-06-18 | #3339 `H` |  | Improvement | Admin UI | 2.6 | Fixes / bank account length and readonly options for the onboarding fields |
| 105 T2 | 2025-06-24 | #3388 `H` |  | Bug fix | Payments & providers | 2.6 | Remove sentry error on duplicate stripe credential |
| 106 T3 | 2025-09-08 | #3722 |  | Improvement | Payments & providers | 2.6 | Subrite 3222 - gdpr deleted member and invoice |
| 107 T3 | 2026-07-20 | #5331 | [SUBRITE-599](https://linear.app/subrite/issue/SUBRITE-599) | Security | Member portal (My Page) | 2.6 | Misleading "no permission" error from spurious 401 |
| 108 T3 | 2026-04-24 | #4862 |  | Improvement | Members, segments & import/export | 2.55 | Subrite 4126 keep member email and primary email synced |
| 109 T3 | 2025-03-07 | #2886 `H` |  | Bug fix | Payments & providers | 2.52 | Hotfix(subrite-2604): Reference filter on ledger not working as expected |
| 110 T3 | 2025-08-25 | #3679 `H` |  | Improvement | Automation & workflows | 2.52 | hotfix - communication sender receiver check |
| 111 T3 | 2026-01-16 | #4407 `H` |  | Bug fix | Communication & notifications | 2.52 | Activity log import member consent data feed email and fullname is incorrect |
| 112 T3 | 2026-01-09 | #4351 |  | Improvement | API contract | 2.5 | 3781 resend member invitation not working as expected v2 |
| 113 T3 | 2025-06-16 | #3321 |  | Improvement | Admin UI | 2.45 | Subrite 3031 - Simplify member invite |
| 114 T3 | 2026-07-06 | #5267 | [SUBRITE-552](https://linear.app/subrite/issue/SUBRITE-552) | Bug fix | Communication & notifications | 2.43 | SendGrid webhook drops events: communicationId FK violation on communication_log insert |
| 115 T3 | 2025-03-14 | #2926 |  | Bug fix | Subscriptions & billing lifecycle | 2.4 | Fix (Subrite - 2630): VIPPS charge check |
| 116 T3 | 2025-03-21 | #2949 |  | Improvement | Admin UI | 2.4 | Feat/subrite-2692: iteration 1 show tenants settings in admin panel |
| 117 T3 | 2025-05-21 | #2959 |  | Improvement | Admin UI | 2.4 | Subrite 2692 custom properties |
| 118 T3 | 2025-05-30 | #3214 |  | Improvement | Admin UI | 2.4 | Feat/subrite 2949 edit company info |
| 119 T3 | 2025-06-02 | #3227 |  | Improvement | Admin UI | 2.4 | Tenant settings feedbacks 2025-05-26 |
| 120 T3 | 2025-08-28 | #3704 `H` |  | Improvement | Admin UI | 2.4 | Hotfix/company details name org |
| 121 T3 | 2025-09-19 | #3712 |  | Improvement | Admin UI | 2.4 | Subrite 3229 create order form adjustment |
| 122 T3 | 2025-11-05 | #3926 |  | Bug fix | Admin UI | 2.4 | Subrite 3555/copy email bug |
| 123 T3 | 2025-11-12 | #3989 |  | Bug fix | Communication & notifications | 2.4 | Fixed hosted form translation issue |
| 124 T3 | 2026-01-12 | #4379 `H` |  | Bug fix | Members, segments & import/export | 2.4 | Quantity issue in subscription import |
| 125 T3 | 2026-04-10 | #4792 |  | Bug fix | Payments & providers | 2.4 | Error while recurring payment failed event |
| 126 T3 | 2026-04-17 | #4832 |  | Improvement | Members, segments & import/export | 2.4 | 4196 filter and josimar invoice not sent expected |
| 127 T3 | 2026-06-29 | #5231 | [SUBRITE-446](https://linear.app/subrite/issue/SUBRITE-446) | Performance | Subscriptions & billing lifecycle | 2.4 | Optimize subscription/seat-member list for 3000+ members |
| 128 T3 | 2026-06-03 | #5071 | [SUBRITE-195](https://linear.app/subrite/issue/SUBRITE-195) | Bug fix | Shared platform code | 2.35 | Fix Admin Create Order modal failing with notAvailableViaSlug |
| 129 T3 | 2025-09-01 | #3713 |  | Bug fix | Content & podcasts | 2.3 | Fix/podcast image update |
| 130 T3 | 2025-10-29 | #3898 |  | Bug fix | API contract | 2.3 | Subrite-3017: wrong-path-for/api/v1/communications/email/edit/id |
| 131 T3 | 2025-11-24 | #3934 |  | Improvement | Frontend (other) | 2.3 | Subrite 3552 / Email sending improvements |
| 132 T3 | 2025-12-01 | #4113 |  | Bug fix | Shared platform code | 2.3 | subrite-3673 / preview-text-bug-in-email |
| 133 T3 | 2026-04-23 | #4861 |  | Improvement | Database migrations | 2.3 | 4097 remove automation template id from preference |
| 134 T3 | 2026-06-03 | #5070 | [SUBRITE-272](https://linear.app/subrite/issue/SUBRITE-272) | Bug fix | Subscriptions & billing lifecycle | 2.28 | Wrong text on Confirm button |
| 135 T3 | 2025-03-11 | #2902 |  | Bug fix | Automation & workflows | 2.2 | Fix (Subrite - 2625): Order trigger list alphabetically |
| 136 T3 | 2025-04-16 | #3060 `H` |  | Improvement | Communication & notifications | 2.2 | Hotfix/subrite 2709 improve tenant setting flexibility on newsletter |
| 137 T3 | 2025-06-18 | #3336 |  | Bug fix | Automation & workflows | 2.2 | Fix/subrite 3059 communication email when image is added text shrinks |
| 138 T3 | 2025-10-13 | #3830 |  | Improvement | Automation & workflows | 2.2 | Subrite 3367 / Show CTA in send test email |
| 139 T3 | 2025-10-29 | #3903 |  | Improvement | Communication & notifications | 2.2 | Subrite-3550 / Avoid one unverified sender to stop all other email |
| 140 T3 | 2025-10-30 | #3914 `H` |  | Improvement | Translations & copy | 2.2 | Subrite 3062 / Hide invoice address on invited member view |
| 141 T3 | 2025-11-21 | #4034 `H` |  | Improvement | Frontend (other) | 2.2 | Subrite 3645 / page-crushed-for-invalid-url-on-cloudflare-image-component |
| 142 T3 | 2026-01-15 | #4405 |  | Bug fix | Communication & notifications | 2.2 | Add draft status in consent list |
| 143 T3 | 2026-01-22 | #4418 |  | Bug fix | Automation & workflows | 2.2 | Replace english text on cta |
| 144 T3 | 2026-04-21 | #4841 |  | Improvement | Database migrations | 2.15 | 4204 filternyheter plan clean up |
| 145 T3 | 2026-07-22 | #5354 | [SUBRITE-649](https://linear.app/subrite/issue/SUBRITE-649) | Bug fix | Admin UI | 2.15 | Postal code validation error not shown on Company details settings form |
| 146 T3 | 2025-08-28 | #3702 `H` |  | Bug fix | Backend (other) | 2.1 | Surbite 3250 issue on company info save |
| 147 T3 | 2025-08-28 | #3701 `H` |  | Improvement | Backend (other) | 2.1 | Hotfix/subrite 3250 failed to update brandkit settings |
| 148 T3 | 2025-09-26 | #3788 |  | Bug fix | Shared platform code | 2.1 | Fix / Subrite-3381 / tenant save and the icon |
| 149 T3 | 2025-11-03 | #3883 |  | Improvement | Admin UI | 2.1 | Subrite 3542 / Option to delete vipps login from admin |
| 150 T3 | 2025-11-19 | #4000 |  | Improvement | Shared platform code | 2.1 | Subrite 3628 / Remove hardcoded cdn.united.no |
| 151 T3 | 2025-12-01 | #4141 |  | Bug fix | Shared platform code | 2.1 | Fix / preview text |
| 152 T3 | 2025-09-12 | #3753 `H` |  | Improvement | Admin UI | 2.04 | Hotfix / subrite 3321 / cannot read null properites of "filter" in admin podcast |
| 153 T3 | 2025-11-19 | #4001 |  | Improvement | Shared platform code | 2.0 | Subrite 755 / When only one payment provider we should select it for the user |
| 154 T3 | 2026-04-16 | #4794 |  | Bug fix | Analytics & reporting | 2.0 | Fix/subrite 3869/adjust graph on dashboard |
| 155 T3 | 2026-04-30 | #4902 | [SUBRITE-44](https://linear.app/subrite/issue/SUBRITE-44) | Improvement | Admin UI | 2.0 | Default payment method should be EHF on Create order from admin |
| 156 T3 | 2025-09-11 | #3734 |  | Improvement | Checkout | 1.95 | Subrite 3295/move payment image above text on mobile |
| 157 T3 | 2025-11-12 | #3955 |  | Improvement | Translations & copy | 1.95 | Subrite 3594/search under mypage podcast |
| 158 T3 | 2026-06-03 | #5059 `H` |  | Recovery tooling | Payments & providers | 1.95 | Find default stripe card for psykologisk |
| 159 T3 | 2026-06-30 | #5238 | [SUBRITE-518](https://linear.app/subrite/issue/SUBRITE-518) | Performance | Subscriptions & billing lifecycle | 1.95 | UI feedback on seat optimization task |
| 160 T3 | 2025-06-12 | #3313 `H` |  | Bug fix | Invoicing & accounting | 1.9 | Fix/subrite 2955 css bug on invoice page |
| 161 T3 | 2025-08-25 | #3662 |  | Improvement | Admin UI | 1.9 | Subrite 1752 zero seat |
| 162 T3 | 2025-10-14 | #3844 |  | Improvement | Admin UI | 1.9 | Bugfix / Selected communication sender not showing |
| 163 T3 | 2025-11-03 | #3910 |  | Improvement | Admin UI | 1.9 | Subrite 3506 / Global search debounce |
| 164 T3 | 2025-09-30 | #3796 |  | Refactor / tech debt | Backend (other) | 1.85 | Subrite 3380 / Query read timeout on cleanup old request logs |
| 165 T3 | 2026-02-11 | #4450 |  | Data migration | Payments & providers | 1.82 | 3666 petro import |
| 166 T3 | 2026-04-10 | #4780 |  | Data migration | Payments & providers | 1.82 | Subrite 3861/minerva export api analysis and data investigation |
| 167 T3 | 2025-03-07 | #2884 |  | Bug fix | Payments & providers | 1.8 | Fix/subrite 2604 reference filter on ledger not working as expected |
| 168 T3 | 2025-03-07 | #2885 |  | Bug fix | Invoicing & accounting | 1.8 | Fix(subrite-2533): Click on email should be linked to ledger detail for user |
| 169 T3 | 2025-03-13 | #2906 `H` |  | Bug fix | Shared platform code | 1.8 | Fix (Subrite - 2604): All filter variant test on ledger |
| 170 T3 | 2025-03-13 | #2911 |  | Improvement | Frontend (other) | 1.8 | Subrite - 2632: Add support for help text in settings |
| 171 T3 | 2025-06-19 | #3341 |  | Bug fix | Frontend (other) | 1.8 | Fix/subrite 3057 missing logo url to be used in template |
| 172 T3 | 2025-07-23 | #3516 `H` |  | Improvement | Shared platform code | 1.8 | Reference on invoice address |
| 173 T3 | 2025-09-01 | #3689 |  | Improvement | Translations & copy | 1.8 | Subrite 3234/podcast frontend changes |
| 174 T3 | 2025-10-29 | #3911 `H` |  | Improvement | Shared platform code | 1.8 | Hotfix / Extra slash in package url |
| 175 T3 | 2025-09-19 | #3746 |  | Improvement | Admin UI | 1.75 | Subrite 3262/feedback newsletter section |
| 176 T3 | 2025-05-02 | #3062 |  | Data migration | Subscriptions & billing lifecycle | 1.73 | Subrite - 2761 - Export api analysis and data investigation for harvest magazine |
| 177 T3 | 2025-09-11 | #3459 |  | Data migration | Subscriptions & billing lifecycle | 1.73 | Subrite 2969/prepare import manifest media |
| 178 T3 | 2025-10-24 | #3816 |  | Data migration | Subscriptions & billing lifecycle | 1.73 | Subrite 3338/preparing import script for dog trainer |
| 179 T3 | 2025-12-22 | #4269 |  | Refactor / tech debt | Analytics & reporting | 1.7 | Refactor stats view for area chart |
| 180 T3 | 2026-07-23 | #5356 | [SUBRITE-97](https://linear.app/subrite/issue/SUBRITE-97) | Feature | Translations & copy | 1.7 | Use SMS terminology instead of newsletter in unsubscribe wording |
| 181 T3 | 2025-08-18 | #3623 |  | Improvement | Admin UI | 1.62 | Add confirmation before cancel subscription changes |
| 182 T3 | 2025-06-02 | #3252 |  | Improvement | Admin UI | 1.6 | Wcag standard check on colors settings |
| 183 T3 | 2025-06-26 | #3369 |  | Improvement | Admin UI | 1.6 | Feat/subrite 3031 simplify member invite |
| 184 T3 | 2025-08-20 | #3646 |  | Improvement | Shared platform code | 1.6 | Subrite 3209 - Show active subscription on top in admin |
| 185 T3 | 2025-08-22 | #3665 |  | Bug fix | Automation & workflows | 1.6 | Added env for request receiver |
| 186 T3 | 2025-09-12 | #3750 `H` |  | Improvement | Admin UI | 1.6 | Hotfix / Subrite 3320 / Makes teaser text optional |
| 187 T3 | 2025-09-30 | #3795 `H` |  | Improvement | Translations & copy | 1.6 | Hotfix/payment provider translation |
| 188 T3 | 2025-09-30 | #3799 |  | Improvement | Shared platform code | 1.6 | Subrite 3243 / Custom period text adjustment on product item |
| 189 T3 | 2025-10-23 | #3869 |  | Improvement | Admin UI | 1.6 | Subrite 3522 / Removed landing pages from admin |
| 190 T3 | 2025-11-24 | #4061 `H` |  | Bug fix | Admin UI | 1.6 | Hotfix / Logo blurr issue |
| 191 T3 | 2025-12-24 | #4291 |  | Improvement | Shared platform code | 1.6 | 3705 add thousand separator on checkout |
| 192 T3 | 2025-09-30 | #3800 |  | Bug fix | Tests | 1.55 | Fix request log test fail |
| 193 T3 | 2025-12-23 | #4274 |  | Improvement | Admin UI | 1.55 | subrite-3783 / subscription-navigator-get-active-when-tab-selected-inside-member |
| 194 T3 | 2025-09-12 | #3748 `H` |  | Improvement | Translations & copy | 1.5 | Hotfix/subrite 3319/seat member shows empty space if accepted member name empty |
| 195 T3 | 2025-09-18 | #3769 `H` |  | Bug fix | Frontend (other) | 1.5 | Hotfix imageinx undefined issue |
| 196 T3 | 2025-11-25 | #4074 `H` |  | Improvement | Admin UI | 1.5 | hotfix / logo-should-not-be-centered-in-iphone |
| 197 T3 | 2025-12-18 | #4265 |  | Improvement | Analytics & reporting | 1.5 | Subrite 3702 / Fixed cashflow feedbacks |
| 198 T3 | 2025-04-15 | #2976 `H` |  | Improvement | Admin UI | 1.4 | Hotfix/subrite 2692 company details input length |
| 199 T3 | 2025-05-23 | #3218 `H` |  | Improvement | Admin UI | 1.4 | Hotfix - Font preview on admin panel |
| 200 T3 | 2025-06-20 | #3361 |  | Bug fix | Admin UI | 1.4 | Fix/Subrite-3058 Feedback tenant seetings - Email template type and onboarding fields |
| 201 T3 | 2025-08-19 | #3639 |  | Bug fix | Admin UI | 1.4 | Subrite 3211 - Fix fixed save email on admin issue |
| 202 T3 | 2025-08-25 | #3681 `H` |  | Improvement | Automation & workflows | 1.4 | Hotfix - Set environment variable for sender approval request email in tests |
| 203 T3 | 2025-09-24 | #3773 |  | Bug fix | Admin UI | 1.4 | 3310 when uploading large image to podcast it fails with unclear error message |
| 204 T3 | 2025-10-10 | #3820 |  | Improvement | Admin UI | 1.4 | Subrite 3384/imporvement predefined query for inactive subscribers |
| 205 T3 | 2025-10-13 | #3824 |  | Improvement | Admin UI | 1.4 | Subrite 3258/whitespace as salestitle shouldnt alllow |
| 206 T3 | 2025-11-07 | #3951 `H` |  | Improvement | Translations & copy | 1.4 | Hotfix / unnecessary text in report |
| 207 T3 | 2025-12-23 | #4276 |  | Improvement | Admin UI | 1.4 | 3675 display tenant id in ui |
| 208 T3 | 2026-02-02 | #4467 |  | Improvement | Admin UI | 1.4 | 3853 show different info on product list |
| 209 T3 | 2025-08-04 | #3556 |  | Improvement | API contract | 1.38 | Subrite 1740 - include user id on api/v1/members/profile |
| 210 T3 | 2025-10-28 | #3884 |  | Improvement | API contract | 1.38 | Subrite 3503 / sort original send date not delivery status |
| 211 T3 | 2025-11-12 | #3987 |  | Bug fix | Communication & notifications | 1.32 | Fix / hosted widget form translation issue |
| 212 T3 | 2025-11-28 | #4121 |  | Bug fix | Communication & notifications | 1.32 | Subrite-3653 / communication-receivers-preview-error |
| 213 T3 | 2026-01-15 | #4169 |  | Data migration | Members, segments & import/export | 1.32 | Subrite 3613/filter nyheter preparing import script |
| 214 T3 | 2025-08-22 | #3671 |  | Refactor / tech debt | Automation & workflows | 1.3 | Refactor: Update communication sender approval message for clarity and specificity |
| 215 T3 | 2025-12-26 | #4288 |  | Improvement | Frontend (other) | 1.3 | 3788 adjust y axis to fit actual numbers |
| 216 T3 | 2026-04-23 | #4873 |  | Improvement | Frontend (other) | 1.3 | 3977 editor cta trouble |
| 217 T3 | 2025-10-28 | #3885 |  | Bug fix | Shared platform code | 1.26 | subrite-3016 / wrong-path '/api/v1/communications/email/statistics/id' |
| 218 T3 | 2025-10-28 | #3887 |  | Bug fix | Shared platform code | 1.26 | Subrite 3014 / wrong path api/v1/communications/{id}/test/sms |
| 219 T3 | 2025-09-29 | #3794 |  | Bug fix | Members, segments & import/export | 1.2 | Subrite 3364 / UI Error Blinking once Close the Automation Workflow |
| 220 T3 | 2025-11-12 | #3867 |  | Improvement | Widgets & branding | 1.2 | Subrite 3288 / feedback widget |
| 221 T3 | 2025-12-30 | #4304 `H` |  | Improvement | Analytics & reporting | 1.2 | Vertical space on show more |
| 222 T3 | 2026-01-13 | #4380 |  | Bug fix | Members, segments & import/export | 1.2 | Unsupported error not shown correctly |
| 223 T3 | 2026-05-19 | #5005 | [SUBRITE-151](https://linear.app/subrite/issue/SUBRITE-151) | Improvement | Member portal (My Page) | 1.2 | Describe the difference between change and switch subscription |
| 224 T3 | 2025-07-30 | #3544 |  | Improvement | Admin UI | 1.14 | Subrite 3049 - Show product package name instead of sales title in list |
| 225 T3 | 2025-08-21 | #3663 |  | Bug fix | Admin UI | 1.14 | Fix: Communication sender create update |
| 226 T3 | 2025-08-19 | #3644 |  | Improvement | Communication & notifications | 1.1 | Remove icons from push notificaton stats |
| 227 T3 | 2025-12-31 | #4309 `H` |  | Improvement | Analytics & reporting | 1.05 | Show more link not working in dashboard subscriptions |
| 228 T3 | 2025-06-12 | #3314 `H` |  | Bug fix | Tooling, deps & docs | 0.96 | Fix/Subrite 2110 - tooltiop issue |
| 229 T3 | 2025-09-19 | #3762 |  | Improvement | Shared platform code | 0.96 | Subrite 3318 / Show cta in preview |
| 230 T3 | 2025-11-03 | #3928 |  | Bug fix | Shared platform code | 0.96 | Fix / delivery address name issue |
| 231 T3 | 2025-06-12 | #3316 `H` |  | Improvement | Tooling, deps & docs | 0.9 | hotfix/Updated add tenants.md with subrite standard podcasts |
| 232 T3 | 2025-06-26 | #3393 |  | Bug fix | Backend (other) | 0.9 | Subrite-3058 - Add proper error message on user create |
| 233 T3 | 2025-10-14 | #3838 |  | Improvement | Admin UI | 0.84 | Subrite 2034 / not able to close modal popups with esc key |
| 234 T3 | 2025-10-23 | #3868 |  | Bug fix | Admin UI | 0.84 | Subrite 3042 / Copying package url missing https |
| 235 T3 | 2026-06-04 | #5073 |  | Bug fix | Admin UI | 0.84 | Double loading on sorting product package |
| 236 T3 | 2025-08-04 | #3557 |  | Improvement | Translations & copy | 0.8 | Subrite 1466 - Get stripe publishable key conditionally |
| 237 T3 | 2025-10-10 | #3826 |  | Bug fix | Translations & copy | 0.8 | Subrite 3476 / Member profile image pload issue for large size |
| 238 T3 | 2026-06-04 | #5075 | [SUBRITE-249](https://linear.app/subrite/issue/SUBRITE-249) | Bug fix | Translations & copy | 0.78 | Wrong breakpoint when changing subscription on my page |
| 239 T3 | 2025-09-15 | #3747 |  | Improvement | Admin UI | 0.7 | Subrite 3315 - Mypage and tenant name in admin layout |
| 240 T3 | 2025-10-29 | #3841 |  | Bug fix | Admin UI | 0.7 | Subrite 2471/missing a for search results |
| 241 T3 | 2026-05-07 | #4937 | [SUBRITE-95](https://linear.app/subrite/issue/SUBRITE-95) | Bug fix | Admin UI | 0.64 | Admin should be able to save onboarding rules without selecting a package |
| 242 T3 | 2026-07-03 | #5260 | [SUBRITE-399](https://linear.app/subrite/issue/SUBRITE-399) | Refactor / tech debt | Tooling, deps & docs | 0.63 | Clean up next-intl and eslint-plugin-react-hooks from .ncurc.json in frontend |
| 243 T3 | 2025-09-18 | #3763 |  | Improvement | Translations & copy | 0.48 | Subrite 3323 - Phone number requirements gift receivers |
| 244 T3 | 2025-10-14 | #3839 |  | Bug fix | Translations & copy | 0.48 | Bugfix translation |
| 245 T3 | 2025-10-14 | #3840 |  | Bug fix | Translations & copy | 0.48 | bugfix/translation fix 2 |
| 246 T3 | 2025-11-03 | #3913 |  | Improvement | Shared platform code | 0.48 | Subrite 3052 / Not able to add extra products on mobile |
| 247 T3 | 2025-11-13 | #3990 |  | Bug fix | Translations & copy | 0.48 | Fix/widget tag manager translation |
| 248 T3 | 2025-03-07 | #2876 |  | Refactor / tech debt | Admin UI | 0.42 | refactor(SubscriptionPlans): Remove duplicate showAsOptionOnSubscript… |
| 249 T3 | 2025-09-15 | #3745 |  | Improvement | Admin UI | 0.42 | Subrite 3264 / display member number for group members |
| 250 T3 | 2025-10-23 | #3866 |  | Improvement | Admin UI | 0.42 | Subrite 2912 / Upside down icons in subrite |
| 251 T3 | 2025-09-04 | #3720 |  | Improvement | Frontend (other) | 0.39 | Subrite 3228 / Extend due date "date picker" issues |
| 252 T3 | 2025-11-12 | #3988 |  | Bug fix | Widgets & branding | 0.3 | Fix/widget hosted form and version |
| 253 T3 | 2025-09-29 | #3790 |  | Improvement | Translations & copy | 0.24 | subrite-3230 feedbacks vipps settings |

**In progress:** `SUBRITE-632` — Communication module error-handling compliance
(branch `feature/subrite-632-…`, 5 commits, 28 files, +2927/-353 as of 2026-07-28). Described after Tier 3.

---

# Tier 1 — Flagship work (ranks 1-25)

Revenue-, security- and data-integrity-critical changes. These are the CV candidates; drafted résumé
bullets are in Appendix B.

---

### 1. #5194 — Stopped subscriptions kept getting charged and silently reverted to PAUSED
`Bug fix` · `Payments & providers` · **Impact 8.2/10**
**Merged** 2026-06-29 · **Linear** [SUBRITE-394](https://linear.app/subrite/issue/SUBRITE-394) (Urgent · Bug + Support) · **Base** `develop` · 17 files, +642/-136, 17 commits

**Problem.** When an operator manually stopped a subscription for a customer, the nightly recurring-payment
job kept trying to charge it. Every failed attempt wrote the package back from `STOPPED` to `PAUSED`, so
support staff had to stop the same subscription again and again — and the stop left no audit trail, so
nobody could see who had stopped it or when. Reported through customer support and flagged Urgent.

**Root cause.** Three independent defects compounded:
1. The Temporal recurring-payment workflow read the subscription status once, when the workflow was
   scheduled, and never re-checked it before charging. A stop that happened after scheduling was invisible.
2. `handleFailedRecurringPayment` unconditionally set the status to `PAUSED` for dunning, with no guard for
   terminal states — this is what reverted `STOPPED`.
3. `stopMemberPackage` did not cancel the in-flight Temporal workflow, so a charge attempt was already queued.
4. `setMemberPackageStatus` only wrote an activity-log entry when the *previous* status was `ACTIVE`, so
   `PAUSED → STOPPED` produced no audit record.

**What I changed.**
- Added a `getMemberPackageStatus` Temporal activity and made `recurring-payment.workflow` re-read the
  status immediately before every charge attempt, aborting on terminal states. `PAUSED` deliberately still
  charges, because that is the dunning path.
- Made `handleFailedRecurringPayment` a no-op on terminal states, eliminating the `STOPPED → PAUSED` revert.
- `stopMemberPackage` now best-effort cancels the in-flight renewal workflow via
  `buildRecurringPaymentWorkflowId`.
- Extracted the shared predicates (`canChargeRenewal`, `hasRecurringRetriesRemaining`, `isTerminalStatus`)
  into a new `recurring-payment.guards` module and applied them uniformly across **all four** card
  providers — Stripe, Nets, SwedbankPay and Vipps — which previously each had their own inline variant.
- `setMemberPackageStatus` now audits transitions from any prior status.
- Added 40 tests: workflow status re-check, failure-handler no-op, stop-cancels-workflow,
  `PAUSED → STOPPED` audit, and unit tests for the guards.

**Impact.** Ended a class of incident where customers who had been explicitly stopped were still being
charged — the most serious kind of billing bug in a subscription platform, with direct refund and trust
cost. Removed recurring manual work from the support team, gave stops a proper audit trail, and replaced
four divergent per-provider guard implementations with one shared, tested module so the next provider
inherits the correct behaviour for free.

---

### 2. #5317 — Tenant isolation for catalog, content and segmentation tables
`Security / tenant isolation` · `Tenant isolation` · **Impact 8.1/10**
**Merged** 2026-07-16 · **Linear** [SUBRITE-55](https://linear.app/subrite/issue/SUBRITE-55) (Medium · Improvement) · **Programme** *Tenant isolation: add tenantId to all tenant-scoped entities* · **Base** `develop` · 36 files, +614/-65, 12 commits

**Problem.** Six tables in a multi-tenant SaaS — `segment_member`, `package_product`,
`subscription_plan_product`, `product_content`, `pending_subscription_plan_price_update` and
`podcast_feed` — carried no `tenantId` of their own. They were scoped only indirectly, by joining to a
parent row. Any query that forgot the join, or any code path that reached these tables directly, could read
or write across tenant boundaries. The project's own security rules require every query to filter by
`tenantId`; these tables made that impossible to guarantee.

**Root cause.** Architectural: tenant scoping was an implicit property of the query rather than a property
of the data. Correctness depended on every author remembering the join, which is not a guarantee that scales.

**What I changed.**
- Two migrations: one adds `tenantId` (indexed, FK with cascade) to all six tables; the second backfills it
  from the parent row and then sets the column `NOT NULL`, so the schema itself now enforces the invariant.
- Converted the six repositories to extend `TenantScopedRepository`, so reads are automatically scoped and
  writes automatically stamped.
- Stamped `tenantId` explicitly on the upsert paths that bypass entity hooks (package products, plan
  products, pending price updates, segment members), and extended `TenantScopedRepository` to cover `upsert`.
- Fixed a genuine leak in the podcast-feed sweep: it enumerated feeds unscoped, so episodes could land under
  the wrong tenant. It now re-enters tenant context per podcast via `withTenantContext`.
- Added integration and unit tests asserting that tenant B cannot see or affect tenant A's catalog, content
  or lists.

**Impact.** Raised the share of tenant-scoped entities from 78 to 84 of 135 and moved six tables from
"correct if the developer remembers" to "correct by construction". Closed a real cross-tenant write path in
the podcast feed sweep. In a platform where every customer is a separate media brand on its own subdomain,
this is the difference between a structural guarantee and a convention.

---

### 3. #4893 — Tenant-isolation foundations: `TenantScopedRepository` and the member module
`Security / tenant isolation` · `Tenant isolation` · **Impact 7.9/10**
**Merged** 2026-05-14 · **Linear** [SUBRITE-26](https://linear.app/subrite/issue/SUBRITE-26) (High · Improvement) · **Programme** *Tenant isolation* · **Base** `develop` · 35 files, +3142/-229, 48 commits

**Problem.** Tenant isolation was enforced ad hoc: each repository and service was individually responsible
for adding `tenantId` to its `where` clauses. With 135 entities and dozens of modules, that had already
failed in specific places (see #3620, #4432) and would keep failing. There was no shared mechanism to make
isolation the default, and no way to test that it held.

**Root cause.** Missing abstraction. The platform had CLS-based tenant context available but nothing that
consumed it automatically at the data-access layer.

**What I changed.** This is the foundation the rest of the programme builds on.
- Built `TenantScopedRepository<T>`: a TypeORM repository base class that reads `tenantId` from CLS and
  injects it into every read (`find*`, `count`, `createQueryBuilder`) and stamps it onto every write
  (`save`, `insert`, `update`, `upsert`, `delete`). Added an explicit `.unscoped()` escape hatch so
  legitimately cross-tenant operations (background sweeps, reconciliation) have to declare themselves
  rather than happening by accident.
- Added `TenantScopedEntity`, an abstract entity supplying the indexed `tenantId` column and `Tenant`
  relation, so adopting isolation on a new entity is a one-line change.
- Added `withTenantContext(cls, tenantId, fn)` for entering tenant context in workers, queues and cron jobs
  — the paths that have no HTTP request to derive the tenant from.
- Applied it to the member module: `member`, `note` and `member_email_status` got `tenantId` via an
  add-then-backfill migration pair, with composite indexes (`idx_member_tenantId_createdAt_id`,
  `idx_note_tenantId_memberId_createdAt`) so the added predicate does not cost query performance.
- Updated the four package-purchase transactions to stamp tenant context correctly.
- Documented the pattern in `api-server/CLAUDE.md` so it is the documented default for future work.
- Added ~830 lines of unit and integration tests covering scoped reads, stamped writes, the `unscoped()`
  path, and genuine cross-tenant isolation against a real database.

**Impact.** Converted multi-tenant isolation from a per-query convention into an enforced platform
primitive, and made every subsequent module conversion (#5317, #5329, #5351, #5374) a small, low-risk
change instead of a bespoke audit. This is the highest-leverage piece of architecture in the portfolio: the
tenant-isolation work that follows is only cheap because this exists.

---

### 4. #5188 — Subscriptions wrongly reported "invoice already paid for next period"
`Bug fix` · `Subscriptions & billing lifecycle` · **Impact 7.8/10** · **Production hotfix**
**Merged** 2026-06-19 · **Linear** [SUBRITE-299](https://linear.app/subrite/issue/SUBRITE-299) (High · Bug) · **Base** `main` (hotfix) · 4 files, +85/-10

**Problem.** The system decided whether an invoice had already been issued for the *next* billing period,
and used that to suppress further invoicing and renewal actions. It was getting the answer wrong, so
subscriptions were treated as already-invoiced when they were not — suppressing invoices that should have
been sent. Shipped as a hotfix straight to `main`.

**Root cause.** Two mistakes in the same comparison, duplicated in three places:
1. It compared the invoice's `periodEndDate` against `nextRenewal`. The correct field is `periodStartDate`:
   an invoice belongs to the next period when its period *starts* at or after the next renewal date. Using
   the end date meant any current-period invoice looked like a next-period invoice as soon as its period
   extended past a renewal date that had been moved earlier.
2. The SQL variant compared raw timestamps, so time-of-day differences within the same calendar day
   produced inconsistent answers between the SQL path and the TypeScript path.

**What I changed.**
- Extracted one shared utility, `isInvoiceSentForNextPeriod(invoicePeriodStartDate, nextRenewal)`, in
  `billing-period.util.ts`. It compares `periodStartDate >= nextRenewal` at start-of-day granularity and
  returns `false` when either date is missing — the gift-subscription case, where `nextRenewal` is null
  until activation.
- Replaced the two duplicated inline comparisons in `member-package.service.ts` with calls to it.
- Rewrote the `invoice.repository.ts` query to compare `::date` values cast through the Oslo timezone, so
  SQL and application code now agree on what "the same day" means.
- Added tests for the reported scenario (renewal date moved earlier must not count as next-period), the
  genuine next-period and future-invoice cases, same-day-different-time equality, and null handling.

**Impact.** Restored correct invoicing for affected subscriptions — customers were not being billed when
they should have been, which is direct revenue leakage. Replaced three divergent copies of a
money-affecting date comparison with one tested utility, and eliminated a timezone inconsistency between
the SQL and application layers that would have kept producing subtle disagreements.

---

### 5. #5200 — Credit-note editor inflated prices 10× / 100×, and øre could not be entered
`Bug fix` · `Invoicing & accounting` · **Impact 7.5/10**
**Merged** 2026-06-23 · **Linear** [SUBRITE-406](https://linear.app/subrite/issue/SUBRITE-406) (Urgent) · **Base** `develop` · 7 files, +129/-33, 6 commits

**Problem.** In the credit-note editor, typing a decimal unit price silently produced an amount 10 or 100
times too large — `249,50` became `24950`. Sub-krone (øre) amounts could not be entered at all. Since credit
notes reduce what a customer owes, a 100× inflated credit note is money given away, and the operator got no
indication anything was wrong. Filed Urgent.

**Root cause.** Two layers:
1. **Frontend.** The price input displayed a locale-formatted value (Norwegian/French-style: comma decimal
   separator, non-breaking-space thousands separator) but parsed it back with logic that only understood a
   dot decimal separator. `249,50` had its comma stripped rather than interpreted, yielding `24950`.
   Reading and writing state on every keystroke also meant a partially-typed value was immediately reparsed.
2. **Backend.** `invoice.paidAmount` was a Postgres `integer`. Even a correctly parsed decimal amount could
   not be persisted, so øre were structurally impossible.

**What I changed.**
- Added `frontend/src/utils/parseFormattedPrice.ts`, a locale-tolerant parser: strips all whitespace
  including NBSP and narrow NBSP, converts comma decimals to dots, discards currency symbols and stray
  characters, and returns `NaN` for genuinely invalid input rather than a misleading number.
- Reworked `InvoiceOrderTable` to hold in-progress text in a `priceDrafts` state keyed by row, so a
  half-typed value is not reparsed mid-edit, and added a bounds-checked `updateItemAt` for per-row updates.
- Migrated `invoice.paidAmount` from `integer` to `numeric` with a `ColumnNumericTransformer`, including a
  reversible `down` migration that rounds back to integer.
- Added a Jest suite for the parser covering comma decimals, space/NBSP/narrow-NBSP group separators, and
  invalid input.

**Impact.** Closed a silent money-correctness bug in an operator tool that writes financial documents — the
failure mode was a credit note 100× too large with no warning. Made sub-krone amounts representable
end-to-end for the first time, and left behind a reusable, tested locale-aware price parser for the rest of
the admin UI.

---

### 6. #5208 — Subscription and seat-member lists made workable at 3000+ members
`Performance` · `Subscriptions & billing lifecycle` · **Impact 7.2/10**
**Merged** 2026-06-29 · **Linear** [SUBRITE-446](https://linear.app/subrite/issue/SUBRITE-446) (High · Improvement · estimate L) · **Base** `develop` · 37 files, +2980/-310, 17 commits

**Problem.** Enterprise customers with group subscriptions of 3000+ seats could not use the member and
subscription screens. Seat data was embedded in the subscription response, so every page load serialised
thousands of seat records with their member details in a single unbounded payload. Related code made an
individual `findOne` per invitation while building member alerts — a classic N+1 — so the cost grew with
seat count on several independent paths at once.

**Root cause.** The API had been designed for small subscriptions and returned nested collections in full,
with no pagination boundary. `GroupSubscriptionResponse` and `GiftReceiverResponse` built a complete
`seats` array purely to derive a count from `seats.length`. Alert generation resolved members one at a time
inside a loop.

**What I changed.**
- Added a paginated seats API — `GET .../subscriptions/product/:id/seats` on both the admin and My Page
  controllers — with a `SeatListQuery` DTO supporting search, an active/pending/inactive status filter, and
  ordering, backed by a purpose-built query in `member-package-product-seat.repository.ts`.
- Changed the response DTOs to accept a `seatCount` directly instead of materialising the whole `seats`
  array to call `.length` on it, and marked the embedded `seats` property `@ApiProperty({ deprecated: true })`
  so consumers migrate rather than break.
- Killed the N+1 in `MemberAlertService` by adding `MemberService.findManyByIds()` returning a
  `Map<string, MemberPersonalInfo>` and resolving all invitation members in one batch.
- Built the "Manage Members" card and modal on the frontend: on-demand paginated seat loading, seat
  selection, bulk removal, and invite resend.
- Added `GET has-blocking-subscription` plus `HasBlockingSubscriptionResponse` so hard-delete actions can
  check for active/pending/paused subscriptions safely instead of inferring it from a large payload.
- Corrected seat counting for group *and* gift subscriptions, and added unit plus integration coverage for
  seat listing, counting and alert batching.

**Impact.** Turned screens that were effectively unusable for the platform's largest accounts into paginated
views with bounded cost, and removed an N+1 whose cost scaled with seat count. Established pagination as the
contract for seat data — with a deprecation path rather than a breaking change — which the My Page
counterpart (#5249) then reused. The largest single engineering effort in this portfolio.

---

### 7. #5024 — Operator recovery for Stripe charges that succeeded remotely but failed locally
`Recovery tooling` · `Payments & providers` · **Impact 7.0/10** · **Production hotfix**
**Merged** 2026-05-21 · **Base** `main` (hotfix) · 8 files, +472/-0

**Problem.** Recurring Stripe charges had succeeded at Stripe — customers' cards were debited — but the
local half of the transaction failed, so no order or invoice was recorded in Subrite. The result was the
worst possible state for a billing system: money taken with no local record of what it paid for. Affected
subscriptions could not simply be re-charged, because that would double-bill the customer.

**Root cause.** The recurring-payment flow performed the remote charge and the local invoice write as
separate steps with no reconciliation path. When the local step failed after a successful charge, nothing
retried or repaired it, and there was no tooling to detect or fix the divergence.

**What I changed.**
- Added `findLatestSucceededRecurrentPaymentIntent()` to `stripe-api.service.ts`, which lists the customer's
  payment intents and matches on `status === 'succeeded'` *and* `metadata.memberPackageId` — so recovery is
  keyed to the specific subscription rather than guessing from amounts or timing.
- Added `recoverStuckRecurringStripeInvoice()` to the Stripe provider. It returns an explicit
  `succeeded | already_processed | skipped` status, so re-running is safe and the operator can see what
  actually happened per subscription. Reconstructing the local invoice from the confirmed remote intent
  means no customer is charged a second time.
- Exposed it as an operator-only endpoint, `POST admin/recover-stripe-recurring-invoices`, with a validated
  DTO (`ArrayMinSize(1)`, `ArrayMaxSize(200)`, `IsInt({each: true})`) capping batch size, and a per-item
  result response so partial failures are visible instead of silent.
- Ran each recovery inside `withTenantContext` so tenant scoping holds on an operator-initiated path with no
  HTTP tenant to inherit.
- Added a dry-run-by-default data-ops script for the associated member-address repair.

**Impact.** Recovered real customer payments that had been debited but not recorded, resolving the
reconciliation gap without double-charging anyone. Idempotent status reporting and the batch cap made a
high-risk production repair safe to run incrementally. Left a reusable recovery mechanism rather than a
one-off SQL fix.

---

### 8. #5068 — Survey data readable without survey permission
`Security` · `Auth & access control` · **Impact 6.9/10**
**Merged** 2026-06-03 · **Linear** [SUBRITE-273](https://linear.app/subrite/issue/SUBRITE-273) (Urgent · Bug + **Security concern**) · **Base** `develop` · 8 files, +96/-19, 8 commits

**Problem.** The admin surveys area could be reached by users who had no survey permission. Survey responses
are member-submitted data, so this was an authorisation hole reported as an Urgent security concern.

**Root cause.** Surveys had no permission section of their own. Every endpoint on `SurveyController` was
guarded with `@CheckPermissions([..., SECTIONS.COMMUNICATIONS])` — reusing the communications section —
so any user with communications access implicitly got survey access. Compounding it, the surveys entry in
the frontend sidebar was marked `unrestricted: true` and the surveys route had no server-side access check
at all, so the UI advertised and rendered a page the user should not have reached.

**What I changed.**
- Added `SECTIONS.SURVEYS` and a migration that inserts the `read`/`update` survey permissions and grants
  them to the appropriate roles, with `ON CONFLICT` so it is idempotent.
- Re-guarded every `SurveyController` endpoint on `SECTIONS.SURVEYS` instead of `SECTIONS.COMMUNICATIONS`.
- Added a server-side check in `surveys/layout.tsx` using `getServerAuthToken` + `checkAdminAccess`,
  rendering `Forbidden` when the user lacks permission — enforcement on the server, not just the client.
- Removed `unrestricted: true` from the sidebar entry so navigation reflects actual permissions, and added
  `'surveys'` to the `Section` type so the check is type-checked rather than stringly-typed.

**Impact.** Closed a real authorisation gap on member-submitted data and corrected a permission model that
conflated two unrelated capabilities. Enforcement was added at both the API and the server-rendered route,
following the project rule that client-side checks are UX only. Note the deployment required a profile
cache clear, which was called out in the PR.

---

### 9. #5270 — Expected SendGrid webhook duplicates were flooding error tracking
`Bug fix` · `Communication & notifications` · **Impact 6.7/10**
**Merged** 2026-07-06 · **Linear** [SUBRITE-557](https://linear.app/subrite/issue/SUBRITE-557) (Medium) · **Programme** *Datadog cleanup* · **Base** `develop` · 5 files, +135/-12, 4 commits

**Problem.** The SendGrid email-activity webhook handler logged Postgres duplicate-key violations (SQLSTATE
`23505`) at `error` level. These duplicates are expected and harmless, but they produced 114 Datadog error-
tracking hits in 14 days, burying genuine failures in the same signal.

**Root cause.** A find-then-insert race on `communication_log`. Each webhook POST starts its own Temporal
workflow, so two events for the same message — `processed` and `delivered` arriving in concurrent batches —
can both pass the existence check and both attempt the insert; the loser hits a unique constraint. I
verified in Datadog which constraint was actually being hit: 87 violations on `(messageId, emailProvider)`
versus 27 on `reference`. That ruled out keying the detection on a constraint name and pointed at the
SQLSTATE instead.

**What I changed.**
- Added a shared `isUniqueViolation()` helper and `POSTGRES_UNIQUE_VIOLATION` constant in
  `common/utils/database-error.util.ts`, checking SQLSTATE `23505` on both the `QueryFailedError` and its
  nested `driverError` — TypeORM puts the code in different places depending on the path.
- Used it in `handleEmailActivityWebhook` to log duplicates at `warn` with `tenantId` and `sgEventId` for
  traceability, deliberately *without* the error object so error tracking will not ingest it. All other
  failures still log at `error`. The batch continues exactly as before.
- Removed the duplicated private copy of the same logic from `sms-short-link.service.ts` and pointed it at
  the shared helper.
- Added unit tests for both SQLSTATE placements and for the webhook path skipping a duplicate while
  continuing the batch.

**Impact.** Removed a persistent source of false-positive production alerts, which is what makes the
remaining alerts trustworthy. Kept full traceability (the event is still logged, at the right level) rather
than swallowing it. Deduplicated the detection logic into one tested utility now used by both the email and
SMS paths.

---

### 10. #5329 — Tenant isolation for push notifications
`Security / tenant isolation` · `Communication & notifications` · **Impact 6.7/10**
**Merged** 2026-07-17 · **Linear** [SUBRITE-53](https://linear.app/subrite/issue/SUBRITE-53) (Medium · Improvement) · **Programme** *Tenant isolation* · **Base** `develop` · 6 files, +215/-66, 8 commits

**Problem.** Push-notification content, subscribers and setup records were stored in plain TypeORM
repositories with no automatic tenant scoping. Push notifications go directly to end-users' devices, so a
cross-tenant mistake here means one media brand's notification reaching another brand's subscribers — a
visible, unrecoverable incident.

**Root cause.** `PushContentRepository`, `PushSubscriberRepository` and `PushSetupRepository` extended
TypeORM's `Repository` directly, so scoping depended entirely on each caller remembering to filter. The
dispatch service, which by design walks queue items across tenants, was reading content through the same
unscoped repositories, so there was no distinction between "intentionally cross-tenant" and "accidentally
cross-tenant".

**What I changed.**
- Converted the three repositories to extend `TenantScopedRepository`, so reads are scoped and newly created
  push content is stamped with the active tenant automatically.
- Reworked `push-dispatch.service.ts` to make its cross-tenant behaviour explicit: the reconciliation sweep
  now calls `.unscoped()` deliberately where it must span tenants, and each individual delivery is executed
  inside `withTenantContext` so the per-member work runs under the correct tenant.
- Documented the intentional cross-tenant queue-item reconciliation in the code, so the next reader does not
  "fix" it back into a bug.
- Added an integration test suite asserting cross-tenant isolation and correct tenant stamping for all three
  push repositories.

**Impact.** Removed the possibility of push notifications leaking across tenants while preserving the one
place that legitimately needs to span them — and made that place self-documenting. The `unscoped()` +
`withTenantContext` pairing here became the reference pattern for background workers elsewhere in the
codebase.

---

### 11. #5229 — Subscription switching rejected valid group plans and could revoke the owner's own access
`Bug fix` · `Subscriptions & billing lifecycle` · **Impact 6.6/10**
**Merged** 2026-07-01 · **Linear** [SUBRITE-457](https://linear.app/subrite/issue/SUBRITE-457) (High · Support) · **Base** `develop` · 17 files, +936/-31, 28 commits

**Problem.** Customers switching subscription plans hit an error telling them the target plan did not
support group members — on plans that demonstrably held four seats. Separately, and more seriously, the
switch could silently flip the owner's own `hasAccess` flag, so the person who paid could lose access to
what they had just bought. Raised via customer support.

**Root cause.** Two distinct bugs:
1. Group capability was determined solely by whether a *digital* product had `maxQuantityPerOrder > 1`. But
   a plan carries its extra seats as an **optional** product whose capacity lives in `quantityRange`, not
   `maxQuantityPerOrder`. That path was never inspected, so a genuinely group-capable plan looked like a
   single-seat plan.
2. During the switch, the owner's `hasAccess` was recomputed from the target product definition instead of
   being carried over from the existing `member_package_product` row — so any difference between plans could
   toggle it.

**What I changed.**
- Rewrote group-capability detection to derive capacity from optional-product rules including `quantityRange`
  and unlimited ranges, on both the backend (`member-package.service.ts`) and the frontend seat-transfer
  utilities, so the two agree.
- Made seat-transfer handling preserve the owner's `hasAccess` across mapped transfers, carry-overs and
  consolidated targets, rather than re-deriving it.
- Recalculated occupancy with owner-seat requirements included, and made gift subscriptions skip
  group-capability validation entirely (they are structurally different and were being wrongly rejected).
- Exposed whether a seat is assigned to the owner on the subscription-plan product DTO, so the frontend no
  longer has to infer it.
- Widened the `ApiNotFoundResponse` descriptions on the affected controllers from a bare `notFound` to the
  specific `subscriptionPlanNotFound | memberPackageNotFound`, so the client can tell the cases apart.
- Added four test suites — DTO mapping, backend seat transfer, frontend seat-transfer utilities, and
  package-change seat transfer.

**Impact.** Unblocked plan switching for group subscriptions, which had been failing on valid configurations,
and removed a silent access-revocation path affecting the paying owner. The capability rule now lives in one
understood place on each side of the API instead of being inferred from the wrong field.

---

### 12. #5158 — Import set the wrong renewal anchor day, drifting every monthly renewal
`Data migration` · `Subscriptions & billing lifecycle` · **Impact 6.5/10**
**Merged** 2026-06-16 · **Linear** [SUBRITE-418](https://linear.app/subrite/issue/SUBRITE-418) (Urgent · Support) · **Base** `develop` · 3 files, +233/-0, 3 commits

**Problem.** After a customer migration on 20 May 2026, monthly subscriptions for that tenant showed wrong
renewal data — every renewal had snapped to the 20th of the month regardless of the customer's actual Stripe
billing day. Renewal dates drive both billing and customer-facing "next payment" information, so the data was
wrong in a way customers could see. Urgent, raised through support.

**Root cause.** The import wrote `member_package.renewalAnchorDate` as the *import date* rather than the
subscription's true billing date. Because `calculateNextMonthlyRenewalWithAnchor()` keys off
`anchorDate.getDate()` — the day-of-month only — every affected monthly subscription computed its next
renewal as the 20th. The bug was in the imported data, not in the renewal logic, so no code change would
fix the existing rows.

**What I changed.** Built a reviewable, reversible data correction rather than running ad-hoc SQL:
- A **read-only preview** script showing old vs new `renewalAnchorDate` and `nextRenewal` per affected
  subscription, making no changes, so the correction could be reviewed before being applied.
- An **apply** script that snapshots every affected row to a `subrite_418_renewal_anchor_backup` table for
  rollback, performs the update inside a transaction, and runs a verification query before commit.
- The correction rule derives the true anchor from evidence in the data: where a renewal had already
  occurred, `renewalAnchorDate = MAX(recurrent order.createdAt)` and `nextRenewal = that + 1 month`; where
  none had, the existing `nextRenewal` is used as the anchor and left unchanged.
- A wrapper shell script that is **dry-run by default** and requires an explicit `--apply` flag, with
  `set -euo pipefail` and a required `DATABASE_URL`.
- Every file documents the context and the rule in a header comment, so the change is auditable after the fact.

**Impact.** Corrected customer-visible billing dates for an entire tenant's monthly subscriptions. The
preview/snapshot/verify structure meant a production data mutation on billing fields could be reviewed
before execution and rolled back after it — this became the template for later data-ops fixes in the repo.

---

### 13. #4432 — Cross-user access to subscription data
`Security / tenant isolation` · `Subscriptions & billing lifecycle` · **Impact 6.4/10** · **Production hotfix**
**Merged** 2026-01-22 · **Base** `subrite/develop` (hotfix branch) · 1 file, +55/-20, 8 commits

**Problem.** A logged-in member could retrieve subscription data belonging to another user. Small diff,
direct authorisation hole.

**Root cause.** `getMemberPackage` looked up the package by `id` alone — `where: { id: memberPackageId }` —
with no tenant or ownership predicate, then derived the tenant *from the record it had just fetched* rather
than from the authenticated user. The subsequent ownership check compared `memberPack.member.tenantId`
against the CLS current user, but by then the row had already been loaded and the check did not cover the
seat-invitation case, so it was applied inconsistently.

**What I changed.**
- Took `tenantId` from the authenticated user (`currentUser?.tenantId`) and rejected the request with `401`
  when absent, instead of trusting data derived from the requested record.
- Added the tenant predicate to the query itself — `where: { id: memberPackageId, member: { tenantId } }` —
  so a row belonging to another tenant is never loaded in the first place.
- Replaced the inline comparison with `validateOwnershipOrAdmin(currentUser, tenantId, memberId)`, falling
  back to an explicit `hasAccessViaSeatInvitation()` check for members who legitimately reach a subscription
  through an accepted seat invitation, and returning `404` (not `403`) when neither holds, so the endpoint
  does not confirm the existence of other users' records.
- Restructured the surrounding logic so the group-subscription and invitation lookups happen before the
  authorisation decision rather than after it.

**Impact.** Closed a direct cross-user data-exposure path on subscription records. Moved the isolation
predicate into the query rather than a post-fetch check — the pattern the tenant-isolation programme later
generalised — and kept legitimate seat-invitation access working, which a blunt ownership check would have
broken.

---

### 14. #4402 — Recurring payment failures crashed for customers without stored card details
`Bug fix` · `Payments & providers` · **Impact 6.3/10** · **Production hotfix**
**Merged** 2026-01-15 · **Linear** SUBRITE-3833 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/main` (hotfix) · 10 files, +72/-40, 5 commits

**Problem.** When a recurring payment failed, the code that fires the payment-failed event threw an
exception for Stripe customers paying via `link` (and other methods that store no card), because it assumed
card information was always present. The failure handler itself failing meant the dunning path broke exactly
when it was needed: the customer was neither charged nor properly notified, and the queue job churned.

**Root cause.** Unguarded property access on optional payment data throughout the provider layer.
`memberPackage.memberPackageProviderPayment.paymentInfo` and `.cardInfo` were dereferenced without null
checks across the Nets, Stripe and Vipps providers and the shared card-comparison helper — whose signature
did not even admit `null`. Separately, the Bull consumer only suppressed retries for
`Stripe.errors.StripeCardError`, so a missing-payment-info error was retried repeatedly instead of failing
once.

**What I changed.**
- Widened the card-comparison helper to accept `null | undefined` and treat a missing side as "changed",
  rather than throwing.
- Added optional chaining and explicit guards on every `paymentInfo` / `cardInfo` access across the Nets,
  Stripe and Vipps providers, the recurrent-payment and update-card transactions, and the
  change-payment-provider transaction.
- Made Vipps throw a defined `ERROR_MESSAGES.PAYMENT_INFO_NOT_FOUND` with a logged member-package id when
  the agreement is missing, instead of failing on a null dereference with no context.
- Extended the Stripe recurrent-payment consumer to treat `PAYMENT_INFO_NOT_FOUND` like a card error:
  prevent retries and let Bull fail the job once, so `@OnQueueFailed` runs a single time.
- Recorded the fix in `CHANGELOG.md`.

**Impact.** Restored the payment-failure/dunning path for customers using card-less Stripe payment methods —
previously a hard crash on the very path meant to handle failure. Removed pointless queue retries on an
unrecoverable error, and hardened optional payment data consistently across all three providers rather than
patching the single reported call site.

---

### 15. #4506 — Editing a subscription plan silently repriced every existing subscriber
`Bug fix` · `Subscriptions & billing lifecycle` · **Impact 6.1/10** · **Production hotfix**
**Merged** 2026-02-10 · **Linear** SUBRITE-3903 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/main` (hotfix) · 1 file, +21/-3

**Problem.** Saving a subscription plan in the admin UI pushed the plan's product prices onto every
existing member package on that plan — including subscribers on individually negotiated or discounted
prices. An operator editing an unrelated field (a name, a description, a product's visibility) silently
repriced live subscriptions. Because the recurring price drives the next charge, the damage was monetary
and invisible until customers were billed the wrong amount.

**Root cause.** `SubscriptionPlanService` called
`updateRecurrentPricingOfMemberPackageAndProducts(...)` unconditionally on every plan update. It never
compared the incoming prices with the stored ones, so "the plan was saved" was treated as "the prices
changed".

**What I changed.**
- Added a private `hasProductPriceChanged(existingProducts, newProducts)` guard. It builds a
  `Map<productId, price>` from the stored plan products and returns true only if an incoming product both
  exists in that map and carries a different price — so genuinely new products, and products whose price is
  unchanged, do not trigger a reprice.
- Gated the `updateRecurrentPricingOfMemberPackageAndProducts` call on that guard, inside the existing
  transaction.

**Impact.** Stopped unintended repricing of live subscriptions from ordinary plan edits — a 21-line change
that removed a direct route to charging customers the wrong amount. Part of a short series of related
price-protection hotfixes (#4493, #4526, #4528) shipped over the same weeks.

---

### 16. #5316 — Nonsensical and "unauthorized" error messages across admin money flows
`Security` · `Admin UI` · **Impact 6.0/10**
**Merged** 2026-07-15 · **Linear** [SUBRITE-298](https://linear.app/subrite/issue/SUBRITE-298) (High) · **Programme** *Clear error messages* · **Base** `develop` · 12 files, +291/-107, 6 commits

**Problem.** When a credit-note, refund, payment or member operation failed, the admin UI showed messages
that were either meaningless to the operator or actively wrong — including "unauthorized" for errors that
had nothing to do with authorisation. Each screen also kept its own hardcoded list of "errors I know
about", so any backend error outside that list fell through to a generic or misleading message. Stale
inline validation errors persisted after an unrelated failure, so an operator could see a field error
belonging to a previous attempt.

**Root cause.** No shared error-normalisation layer. Each form independently mapped backend responses to
UI text via local arrays (`possibleApiErrorsForCreditNote`, `possibleApiErrorsForRefund`, …). A backend
`messageCode` not present in a given form's array produced whatever the fallback happened to be, and
because field-level and banner-level errors were tracked in ad-hoc component state, they were not cleared
consistently between attempts.

**What I changed.**
- Added `frontend/src/utils/getActionError.ts`: one centralised normaliser that turns a backend response —
  `messageCode`, field-level `errors`, or neither — into a structured result the UI can render, with a
  sensible localised fallback instead of leaking a raw code or a wrong message.
- Deleted the per-form hardcoded error arrays and pointed credit notes, general credit, refunds, payment
  refund confirmation, member creation and member details at the shared normaliser.
- Extended the shared `ui/Alert` component with a warning variant and `AlertWarningIcon`, replacing the
  ad-hoc red bold `<span>` each form had rolled itself, so all these screens now present failures
  consistently.
- Fixed the stale-error bug: inline amount validation is now cleared when an unrelated refund failure
  occurs, so an operator never sees a field error from a previous submission.
- Added `apiMessageCode` translations in English and Norwegian, and a Jest suite covering normalisation and
  every fallback branch.

**Impact.** Operators working on money-moving screens now get accurate, localised, consistently presented
errors instead of misleading "unauthorized" text — which matters because a misread error on a refund or
credit note leads to the wrong corrective action. Replaced six divergent per-form error mappings with one
tested utility, which the follow-up work in this programme (#5331, #5229, SUBRITE-632) then built on.

---

### 17. #5351 — Tenant isolation for the accounting module
`Security / tenant isolation` · `Invoicing & accounting` · **Impact 5.9/10**
**Merged** 2026-07-23 · **Linear** [SUBRITE-50](https://linear.app/subrite/issue/SUBRITE-50) (Medium · Improvement) · **Programme** *Tenant isolation* · **Base** `develop` · 16 files, +254/-36, 10 commits

**Problem.** The accounting module — `account`, `account_entry`, `account_balance` and
`aggregated_account_balance` — had no tenant column. This is the ledger: the tables that hold each
tenant's financial position. Beyond the isolation risk, `account` carried a **unique index on
`(name, memberId)` with no tenant component**, so two tenants could not use the same account name; the
schema was actively coupling unrelated customers' charts of accounts.

**Root cause.** The module was built before tenant scoping was a platform primitive and had never been
revisited. Balances were aggregated without a tenant predicate, so a missing tenant context produced
silently wrong figures rather than an error.

**What I changed.**
- Migration adding `tenantId` to all four accounting entities — `NOT NULL` on `account`, indexed
  (`IDX_account_entry_tenantId`) with `Tenant` relations — and made `aggregated_account_balance` include
  `tenantId` in its **primary key**, since a balance is only meaningful per tenant.
- Replaced the unique index on `(name, memberId)` with a tenant-aware one, so account names can be reused
  across tenants without collision.
- Converted all four repositories to `TenantScopedRepository`.
- Made `create-account-entries.transaction.ts` refuse to proceed without a tenant context, so a missing
  tenant fails loudly instead of writing unattributed ledger rows.
- Made missing aggregated balances display as zero rather than as absent/undefined, so reports do not show
  blanks where a real zero belongs.
- Added service tests using `ClsServiceMock` with an explicit tenant id.

**Impact.** Brought the financial ledger under the same isolation guarantee as the rest of the platform,
taking tenant-scoped entities from 86 to 90 of 136. Fixed a schema-level constraint that had been leaking
one tenant's naming decisions into another's, and converted a silent-wrong-number failure mode in
accounting transactions into a hard error.

---

### 18. #5082 — E-invoice reactivation pushed the next renewal a full period past what the customer paid for
`Bug fix` · `Payments & providers` · **Impact 5.9/10**
**Merged** 2026-06-05 · **Linear** [SUBRITE-276](https://linear.app/subrite/issue/SUBRITE-276) (**Urgent** · Bug) · **Base** `develop` · 3 files, +194/-8, 4 commits

**Problem.** When a member paid an e-invoice to restart a lapsed subscription, the reactivation advanced
`nextRenewal` by a whole billing period *on top of* the period the invoice had already paid for. Annual
subscriptions ended up with roughly two years until their next renewal — meaning a year of free service and
a year of missed revenue per affected subscription. Filed Urgent.

**Root cause.** `reactivateEInvoiceSubscription()` derived the new period from the subscription's *stored*
`nextRenewal` — `getPeriodNextDate(plan, memberPackage.nextRenewal)` — and set `currentPeriodStart` to
`new Date()`. It never looked at the invoice the member had actually paid. When the paid invoice was a
restart invoice already covering the current period (its `periodEndDate` equal to the stored
`nextRenewal`), advancing from the stored value double-counted one period. A second defect compounded it:
`setMemberPackageStatus` resets `renewalAnchorDate` by reading `memberPackage.nextRenewal` from the
in-memory entity, which still held the stale pre-reactivation value, so the anchor was also written wrong.

**What I changed.**
- Changed `reactivateEInvoiceSubscription()` to take the paid `Invoice` and anchor both period dates to it:
  `currentPeriodStart = paidInvoice.periodStartDate`, `nextRenewal = paidInvoice.periodEndDate`. The
  membership period now matches the invoiced/revenue period by construction rather than by arithmetic.
- Added an explicit fallback for legacy invoices with no recorded period dates: log a warning naming the
  invoice and member package, then keep the old stored-`nextRenewal` behaviour — rather than writing null
  period dates.
- Updated the in-memory entity (`memberPackage.nextRenewal`, `currentPeriodStart`) before calling
  `setMemberPackageStatus`, so the `renewalAnchorDate` reset stores the advanced value instead of the stale
  one.
- Threaded the paid invoice through from `UpdateEInvoicePaymentTransaction`, which is the caller that knows
  which invoice was settled.
- Added a test suite with fake timers pinning renewal-anchoring behaviour across billing scenarios.

**Impact.** Stopped roughly a full billing period of free service being granted on every e-invoice
reactivation — direct, quantifiable revenue leakage on the platform's invoice-based payment method.
Replaced date arithmetic derived from mutable subscription state with anchoring to the immutable financial
record, which is the correct source of truth for what a customer has paid for.

---

### 19. #5278 — A credited next-period invoice permanently blocked subscription cancellation
`Bug fix` · `Subscriptions & billing lifecycle` · **Impact 5.9/10**
**Merged** 2026-07-09 · **Linear** [SUBRITE-521](https://linear.app/subrite/issue/SUBRITE-521) (Medium) · **Base** `develop` · 8 files, +178/-16, 6 commits

**Problem.** If the last invoice on a subscription had been **credited** (cancelled by a credit note),
customers could no longer cancel a pending subscription change. The system also kept showing a payment link
and a forced-stop date for invoices that were in fact fully settled, so members were prompted to pay
invoices that no longer required payment.

**Root cause.** The code had one concept — "is this e-invoice paid?" — where two were needed. It tested
`status === PAID || status === OVER_PAID`, which is the right question for "did money arrive". But the
question governing cancellation, payment links and forced-stop dates is "is this invoice **settled**, i.e.
does it still require action?" — and a `CREDITED` invoice is settled without ever having been paid. Because
`CREDITED` failed the paid test, the invoice looked outstanding forever, and the change-cancellation guard
treated it as an unresolved obligation.

**What I changed.**
- Introduced the missing distinction explicitly in `invoice-status.enum.ts`:
  `isEInvoicePaid(status)` → `PAID | OVER_PAID`, and `isEInvoiceSettled(status)` → paid **or** `CREDITED`.
  Two named predicates instead of one overloaded inline comparison.
- Added `hasActiveInvoiceForNextPeriod` to `EInvoicePaymentInfo`, so consumers can ask whether a
  next-period invoice is still live rather than inferring it from status combinations.
- Applied the right predicate at each call site in `MemberPackageService`: settlement governs the
  change-cancellation guard, payment-link display and forced-stop date; paid governs anything that means
  money actually arrived.
- Updated the member-facing `CancelSubscriptionChange` component and the admin `SubscribedPackage` table to
  use the new indicator.
- Added a test suite covering paid vs settled vs credited vs refunded across e-invoice scenarios.

**Impact.** Unblocked cancellation for customers whose invoice had been credited — previously a dead end
requiring manual intervention — and stopped payment links and forced-stop warnings appearing for invoices
that needed no payment. The underlying gain is conceptual: an overloaded boolean was split into two named,
tested predicates, so future code has to choose deliberately between "paid" and "settled".

---

### 20. #5323 — Push notifications silently dropped on transient FCM failures
`Bug fix` · `Communication & notifications` · **Impact 5.8/10**
**Merged** 2026-07-20 · **Linear** [SUBRITE-585](https://linear.app/subrite/issue/SUBRITE-585) (Medium) · **Programme** *Datadog cleanup* · **Base** `develop` · 11 files, +549/-48, 14 commits

**Problem.** When Firebase Cloud Messaging returned a transient failure — HTTP 500/503,
`server-unavailable`, `internal-error`, `quota-exceeded` — the queue item was marked `FAILED` and never
retried. Deliverable push notifications were silently discarded: the member never received the
notification and nothing indicated a recoverable error had occurred.

**Root cause.** The dispatch path treated every FCM error identically. There was no error classification,
so an invalid device token (permanently undeliverable) and a momentary FCM outage (retry in a minute) both
terminated the item. There was also nowhere to record retry state — `push_queue_item` had no attempt
counter — so even recognising the distinction would not have helped.

**What I changed.**
- Classified FCM errors into three outcomes: **invalid token** → drop the subscriber; **retryable** →
  requeue; **terminal** → fail. Each has a distinct action instead of one blanket failure.
- Added a migration for `push_queue_item.attempts`. On a transient error the item stays `QUEUED` and
  `attempts` is incremented, retrying up to `MAX_ATTEMPTS = 3` before failing for real.
- Added `findQueuedItemIdsReadyForDispatch(limit, backoffMs)`, which gates requeued items behind a 5-minute
  backoff computed in SQL (`updatedAt` against the database's own `now()`), so a retried item waits for a
  later cron minute rather than being re-attempted in the same drain.
- Moved logging into the dispatch layer, which knows the attempt count and can therefore log a retry at
  `warn` and a final give-up at `error` — the right levels for alerting.
- Deliberately chose per-row retry state over Temporal activity retry, and documented why in the PR: the
  cron runs one activity that drains many items per minute, so retrying the whole activity would re-send to
  subscribers already delivered in that batch. Retry state has to be per-item and durable across cron
  fires.
- Added tests for requeue-on-transient, fail-after-exhaustion, and skip-until-backoff-elapsed.

**Impact.** Converted silent, total loss of affected push notifications into bounded, backed-off retry with
correct alert levels. The design note is the substantive part: identifying that the natural framework
retry (Temporal) would have caused duplicate deliveries, and placing retry state on the row instead, is the
difference between a fix and a new bug.

---

### 21. #4528 — Plan price updates overwrote individually discounted subscriptions
`Bug fix` · `Subscriptions & billing lifecycle` · **Impact 5.6/10** · **Production hotfix**
**Merged** 2026-02-13 · **Linear** SUBRITE-3912 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/main` (hotfix) · 9 files, +39/-2, 9 commits

**Problem.** Subscribers on a negotiated or discounted recurring price had that price overwritten when the
underlying subscription plan's price was updated. The customer's agreed discount silently disappeared and
they were charged the standard rate. This is the deeper half of the same production issue as #4506 and
#4493: #4506 stopped *unintended* repricing, but a *deliberate* plan price change still had no way to
recognise which subscriptions must be left alone.

**Root cause.** Nothing recorded that a member package's price was deliberately different from its plan's
price. The reprice logic compared `memberPackage.recurringPrice !== newRecurringPrice` and updated on any
difference — which is exactly backwards: a difference is the *signal* that the price was individually set
and should be preserved.

**What I changed.**
- Added a `hasFixedDiscount` boolean column to `member_package` (migration written with
  `ADD COLUMN IF NOT EXISTS` and a matching `down`, so it was safe to apply on a hotfix path).
- Set the flag where a discount genuinely originates: `FakturaPackageTransaction` computes
  `hasFixedDiscount = memberPackage.recurringPrice !== calculatedRecurringPrice` at creation, and
  `MemberPackageService` sets it explicitly when an operator fixes a price.
- Cleared the flag in `MemberPackageChangeService` when a subscription changes plan or product, since the
  old individual price no longer applies.
- Made `SubscriptionPlanService` select `mp.hasFixedDiscount` and skip repricing those packages, replacing
  the `recurringPrice !== newRecurringPrice` heuristic with an explicit, stored intent.
- Exposed the field through the API contract so the admin UI can show that a subscription is on a fixed
  price.

**Impact.** Protected negotiated customer pricing from being silently reset by routine plan maintenance —
previously a direct overcharge with no audit trail and no way to detect it except a customer complaint.
Replaced an inference ("the prices differ, so update") with recorded intent ("this price was set
deliberately"), which is what made the fix durable rather than another special case.

---

### 22. #5249 — My Page seat management rebuilt for 3000+ member subscriptions
`Performance` · `Member portal (My Page)` · **Impact 5.6/10**
**Merged** 2026-07-02 · **Linear** [SUBRITE-489](https://linear.app/subrite/issue/SUBRITE-489) (High · Improvement) · **Base** `develop` · 21 files, +1027/-690, 6 commits

**Problem.** The member-facing counterpart of #5208. A subscription owner on My Page — the customer's own
self-service portal — could not manage a large group subscription: the invited-members view loaded all seats
at once, so for 3000+ seats the page was unusable for the very customers paying the most.

**Root cause.** `MemberInvites` fetched the full seat list client-side using an auth-token hook and held it
all in component state, with no pagination, search or filter. Navigation was also subscription-scoped
rather than product-scoped, so after inviting or removing a member the user was returned to the
subscription overview and had to navigate back into the product to continue.

**What I changed.**
- Added a dedicated product-scoped route,
  `/account/subscriptions/[id]/product/[product-id]/manage-members`, so seat management has its own page
  with its own paginated data rather than being embedded in the subscription overview.
- Added two server actions — `getProductSeats` (paginated, searchable, status-filtered, consuming the API
  from #5208) and `resendProductSeatInvitation` — so seat data is fetched on the server with the session
  token rather than from the client.
- Built a reusable `MemberManagement` component set (`ManageMembersCard`, `ManageMembersModal`,
  `ManageMembersPanel`, `SeatRow`) shared across the card, modal and full-page presentations, replacing
  ~200 lines of bespoke logic in `MemberInvites`.
- Fixed post-mutation navigation: invite completion, member removal, resend and back now all return to the
  correct product manage-members screen, and used `updateTag` with
  `MEMBER_SUBSCRIPTIONS_PRODUCT_SEAT_TAG` so the list refreshes after a mutation instead of showing stale
  data.
- Added translations for the new screens in English, Norwegian, Danish and Swedish.

**Impact.** Made self-service seat management workable for the platform's largest subscriptions, removing
support load that would otherwise land on staff doing it on customers' behalf. Reused the paginated API
built in #5208 rather than adding a second data path, and left behind a shared member-management component
set instead of a page-specific implementation. Server-side data fetching also removed a client-side
token-handling path.

---

### 23. #3776 — Admin orders marked as already paid recorded the wrong payment provider
`Bug fix` · `Payments & providers` · **Impact 5.6/10** · **Production hotfix**
**Merged** 2025-09-23 · **Linear** SUBRITE-3223 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/main` (hotfix) · 9 files, +102/-16, 4 commits

**Problem.** When an operator created an order in the admin UI and marked it as already paid — the customer
had paid by bank transfer, cash or another out-of-band method — the order was still recorded against the
e-invoice provider. The payment records therefore misattributed how the money had actually arrived, which
corrupts payment reporting and reconciliation for exactly those manually handled cases.

**Root cause.** The faktura/e-invoice creation path hardcoded the provider: `EInvoicePaymentProvider` wrote
`paymentProvider: PAYMENT_PROVIDER.EINVOICE` and `FakturaPackageTransaction` wrote
`PAYMENT_PROVIDER.EXTERNAL`, in both cases as literals. There was no field on the request in which the
operator could state which provider had actually been paid, so the information was unrepresentable.

**What I changed.**
- Added an optional `paidPaymentProvider` to `FakturaPackageSubscriptionDto` and
  `EInvoicePackageSubscriptionDto`, exposed through the API contract.
- Replaced both hardcoded literals with `data.isPaid ? (data.paidPaymentProvider || <default>) : <default>`
  — the override applies only when the order is actually marked paid, so the normal unpaid e-invoice flow is
  untouched.
- Threaded the value through `MemberPackageService` into the transaction.
- Extended the admin `AddNewOrder` form with payment-date and payment-provider fields (with validation and
  translations) so the operator can record how the payment was made.
- Renamed `subscribeViaFaktura` to `subscribeViaFakturaFromAdmin`, making the admin-only nature of the
  endpoint explicit at the call site, and updated the controller tests.

**Impact.** Corrected payment attribution for manually registered payments, which is what reporting and
reconciliation depend on — the previous behaviour produced financial records that were quietly wrong. Also
made an admin-only endpoint say so in its name, and the operator-facing form now captures the information
the system needs rather than defaulting silently.

---

### 24. #5368 — Gift subscription member UI aligned with the group subscription design
`Feature` · `Admin UI` · **Impact 5.5/10**
**Merged** 2026-07-24 · **Linear** [SUBRITE-535](https://linear.app/subrite/issue/SUBRITE-535) (Medium · Improvement) · **Base** `develop` · 40 files, +1368/-1084, cross-stack

**Problem.** Gift subscriptions and group subscriptions are near-identical from an operator's and a
customer's point of view — both distribute seats to other people — but they had separate, divergent UIs in
both the admin panel and My Page. Gift subscriptions had missed the seat-management improvements built for
group subscriptions (#5208, #5249), so the same task behaved differently depending on which product the
customer had bought.

**Root cause.** Historical divergence: the two features were built at different times and never
reconciled, so each carried its own components, its own data-loading approach and its own copy.

**What I changed.**
- Migrated the gift-subscription member screens onto the shared `MemberManagement` components and the
  paginated seats API introduced by #5208/#5249, in both the admin panel and My Page.
- Removed the duplicated gift-specific components and flows — visible in the near-balanced diff
  (+1368/-1084 across 40 files): this was substantially a consolidation rather than an addition.
- Aligned copy and translations across the affected locales so both product types describe the same action
  the same way.
- Added test coverage for the converted flows.

**Impact.** Gift subscriptions inherited the pagination, search and seat-management capability built for
group subscriptions, including the 3000-member scalability work, without that work being reimplemented.
Consolidating two divergent implementations into one shared component set means future seat-management
changes land in both places by default — the maintenance cost of the duplication was the real problem being
solved.

---

### 25. #3485 — Customer invoice references could not be captured, so invoices were unmatched
`Feature` · `Invoicing & accounting` · **Impact 5.5/10**
**Merged** 2025-07-22 · **Linear** SUBRITE-2856 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 28 files, +181/-4, 13 commits

**Problem.** Business customers require a reference — a purchase-order number, cost-centre code or
contact name — to be printed on an invoice so their accounts-payable system can match and approve it. The
platform had nowhere to store one. Invoices went out without it, and customers' finance departments could
not process them, delaying payment on exactly the highest-value B2B accounts.

**Root cause.** A genuine gap rather than a defect: the `address` entity had no invoice-reference field, so
it was absent from the address DTOs, the invoice-creation path, the rendered invoice document and every
address form on both sides of the product.

**What I changed.** A single field, threaded correctly through 28 files:
- Migration adding `invoiceReference` to the `address` table.
- Added it to the DTO chain — `MemberAddressDto`, `ProfileAddress`, `BillingAddress` (with `@IsOptional`
  and `@IsString` validation), `AddressInfo` and `OrderInvoiceDto` — and through `TenantInfo`, so both
  member and tenant addresses carry it.
- Included it in `invoice-generate.service.tsx` so the value actually appears on the rendered invoice
  document, which is the point of the whole change.
- Wired it through `PaymentService`, `FakturaPackageTransaction` and `PurchasePackageTransaction`.
- Added the field to every address surface: member-facing add/change invoice address, admin
  add/change/view invoice address, the checkout billing-address form, the e-invoice payment form and the
  payment-link SMS flow — so the reference can be captured wherever an address is entered, not just in one
  place.

**Impact.** Removed a concrete blocker to getting paid by business customers, whose AP systems reject or
stall invoices without a reference. The value is in the completeness: a half-threaded field — stored but not
rendered, or capturable in one form but not the others — would not have solved the problem. Rendering on the
invoice document and capture on all six address surfaces is what made it usable.

---

# Tier 2 — Significant work (ranks 26-105)

Real production problems solved, substantial features, and performance work. Same four-section treatment
as Tier 1.

---

### 26. #3620 — Payment provider settings readable across tenants
`Bug fix` · `Payments & providers` · **Impact 5.4/10** · **Hotfix branch**
**Merged** 2025-08-15 · **Base** `subrite/develop` · 1 file, +3/-1, 3 commits

**Problem.** Payment-provider settings — the per-tenant configuration for Stripe, Vipps, Nets and
SwedbankPay — could be fetched by id without any tenant check. Any authenticated user who supplied another
tenant's configuration id received that tenant's payment settings. The branch name says it plainly:
`hotfix/prevent-random-access-of-payment-provider-settings`.

**Root cause.** `PaymentConfigService` looked the record up with `where: { id: id }` — id only. The
authenticated user's tenant was available in CLS but was never consulted, so the query had no isolation
predicate at all.

**What I changed.**
- Read the caller's tenant from CLS: `const tenantId = this.cls.get('currentUser')?.tenantId as number`.
- Added it to the lookup: `where: { id: id, tenantId }`, so a configuration belonging to another tenant is
  simply not found.

**Impact.** A three-line change closing direct cross-tenant exposure of payment configuration — among the
most sensitive per-tenant data in the platform. Its significance is out of all proportion to its size, and
it is one of the concrete failures that motivated building `TenantScopedRepository` (#4893) so this class of
omission could not recur.

---

### 27. #4543 — Plan price changes never reached subscriptions already invoiced for the next period
`Improvement` · `Subscriptions & billing lifecycle` · **Impact 5.4/10**
**Merged** 2026-02-24 · **Linear** SUBRITE-3904 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 19 files, +670/-56, 48 commits

**Problem.** When an operator changed a subscription plan's price, subscriptions that had already been
invoiced for their next period could not be repriced — the invoice was issued, so changing the recurring
price then would have created a mismatch between the invoice and the subscription. The price change was
therefore simply lost for those subscribers, who stayed on the old price indefinitely rather than moving to
the new one at their following renewal. The same applied to Vipps subscriptions with a charge already due.

**Root cause.** The reprice operation was strictly synchronous: apply now, or not at all. There was no
mechanism to defer a price change until the subscription reached a point where applying it was safe, and no
record that a change was outstanding.

**What I changed.** Built a deferred-application pipeline:
- New `pending_subscription_plan_price_update` entity, table (migration) and repository, holding the
  intended price change plus a `PendingMemberPackageProduct` breakdown and an `isProcessed` flag.
- Added `PlanProductPriceSnapshot` to the change model so the intended per-product prices are captured at
  the moment the operator makes the decision, not re-derived later when the plan may have moved on again.
- Added a Bull queue job (`PENDING_SUBSCRIPTION_PLAN_PRICE_UPDATE_JOB`) and wired
  `subscription-plan-update.consumer.ts` to process it.
- Added the two bulk queries needed to decide safely and cheaply, both returning sets to avoid N+1:
  `InvoiceRepository.getMemberPackageIdsWithSentInvoiceForNextPeriod(ids)` and
  `VippsPaymentRepository.getDueChargeAgreementIds(ids)`.
- Hooked `RecurrentPaymentTransaction` and `RenewEInvoiceSubscriptionTransaction` so that when a
  subscription passes the point where the block applied, the pending update is enqueued and applied.
- Surfaced pending state in the admin `ProductTable` so operators can see a price change is queued rather
  than assuming it silently failed.

**Impact.** Made plan price changes eventually consistent instead of silently dropped — subscribers now move
to the new price at their next safe renewal rather than staying on the old one forever, which is revenue the
platform was previously forfeiting. The bulk set-returning queries mean the check scales with a batch rather
than per-subscription.

---

### 28. #5374 — Tenant-scoped repository for tenant usage records
`Security / tenant isolation` · `Tenant isolation` · **Impact 5.3/10** · **OPEN — in review**
**Opened** 2026-07-28 · **Linear** [SUBRITE-661](https://linear.app/subrite/issue/SUBRITE-661) (Medium · **Security concern**) · **Programme** *Tenant isolation* · **Base** `develop` · 5 files, +146/-4, 3 commits

**Problem.** `TenantUsage` — the records tracking each tenant's consumption of the platform, which feed
usage-based decisions — had no dedicated repository at all. The module declared no providers and no exports,
so callers reached the entity through a generic manager with no tenant scoping. Labelled a security concern
in Linear.

**Root cause.** The module had never been given a repository, so it was excluded from the isolation
mechanism by omission rather than by decision.

**What I changed.**
- Added `TenantUsageRepository extends TenantScopedRepository<TenantUsage>`, so reads are scoped to the
  active tenant and writes are stamped with it automatically.
- Registered it in `tenant-usage.module.ts` providers and exports (previously both empty arrays), making it
  the sanctioned access path, and in `testing.module.ts` so tests exercise the real repository.
- Added an integration test suite running against a real database, covering isolation between two tenants,
  tenant-aware reads, automatic tenant stamping on writes and updates, and the deliberate `unscoped()`
  retrieval path.

**Impact.** Closes the last known gap of this kind in the tenant-isolation programme. Because #4893 built
the primitive, bringing an entire module under the guarantee is a five-file change with real integration
coverage rather than a bespoke audit — which is the payoff of the foundation work. **Still open and in
review at the time of writing.**

---

### 29. #4944 — Stripe customer charged three times during a service outage
`Bug fix` · `Payments & providers` · **Impact 5.2/10**
**Merged** 2026-05-08 · **Linear** [SUBRITE-70](https://linear.app/subrite/issue/SUBRITE-70) (Medium) · **Base** `develop` · 4 files, +239/-20, 5 commits

**Problem.** During a period of downtime, a Stripe customer was charged three times for the same
subscription renewal. Temporal retried the recurring-payment activity after the outage, and each retry
created a fresh payment intent at Stripe — so every retry took real money.

**Root cause.** `createPaymentIntentForRecurral` created payment intents with no idempotency guarantee.
From Stripe's point of view each retry was a legitimately new, distinct charge request, so there was nothing
to deduplicate against. Retry-on-failure — normally the correct behaviour for a transient outage — was
therefore unsafe on this specific call, and the same weakness existed in the SwedbankPay charge path.

**What I changed.**
- Derived a deterministic idempotency key for each recurring charge from the subscription identity and the
  billing period, formatted in the platform's timezone (`DateUtil`, `DEFAULT_TIMEZONE`, `formatInTimeZone`)
  so the same renewal always produces the same key regardless of when the retry occurs. Stripe then
  collapses repeats of the same renewal into one charge instead of creating new ones.
- Applied the equivalent treatment to `SwedbankpayApiService.chargeSubscription`, so the second card
  provider with the same exposure was fixed at the same time rather than being left as a latent duplicate
  of the bug.
- Added unit tests for both services asserting the key is derived as expected and passed to the provider.
- Flagged honestly in the PR description that the full recurring workflow had not been exercised end-to-end.

**Impact.** Removed the mechanism by which an outage plus normal retry behaviour produced duplicate customer
charges — the most damaging kind of payment bug, since it takes money and requires refunds and apologies.
Fixing SwedbankPay alongside Stripe closed the same hole in both providers rather than waiting for it to
recur.

---

### 30. #5185 — Upgrade/downgrade form fought the user and issued stale price requests
`Bug fix` · `Subscriptions & billing lifecycle` · **Impact 5.2/10**
**Merged** 2026-06-22 · **Linear** [SUBRITE-402](https://linear.app/subrite/issue/SUBRITE-402) (High) · **Base** `develop` · 3 files, +150/-101, 9 commits

**Problem.** In the admin subscription upgrade/downgrade form, optional-product quantities and shipping
countries behaved erratically: edits were lost or reverted, adding and removing products did not take
effect reliably, and the recurring price shown could belong to an earlier state of the form. An operator
could therefore confirm a subscription change against a price that was not the price for the current
selection.

**Root cause.** Classic duplicated-state and race problems in React:
- `OptionalProduct` kept its own `useState` copies of `quantity`, `shippingCountries` and
  `shouldTakeProduct`, initialised from props, and pushed changes upward through `useEffect`. Parent and
  child each believed they owned the value, so a parent update could be overwritten by a stale child
  effect.
- The price recalculation fired an unthrottled request on every keystroke with no sequencing, so a slower
  earlier response could land after a faster later one and overwrite the correct price.
- Admin edits in progress were re-initialised whenever the baseline context re-rendered, discarding work.

**What I changed.**
- Made `OptionalProduct` fully controlled: removed all three `useState` hooks and both `useEffect`
  push-ups, taking `quantity` and `shippingCountries` as props and deriving `shouldTakeProduct` from them.
  Single source of truth in the parent.
- In `ProductChange`, built a `useMemo`'d `Map` of selected optional products by id, so lookup is by key
  rather than a linear scan per row.
- Replaced the price-fetch `useEffect` with a `useDebouncedCallback`, and added an `AbortController` plus a
  `requestSequenceRef` so in-flight requests are cancelled and out-of-order responses are discarded — the
  displayed price always corresponds to the current selection.
- Guarded against `NaN` quantities (a partially cleared input) triggering a request, and blocked
  confirmation while any selected quantity is invalid.
- Used a `baselineBuiltRef` so optional selections initialise only once per baseline context, preserving
  in-progress edits.
- Made plan-detail loading report a clear error and always reset its loading state.

**Impact.** Removed the possibility of confirming a subscription change against a stale price — a
correctness problem, not a cosmetic one, because the displayed price is what the operator agrees with the
customer. Also cut request volume from one-per-keystroke to one-per-pause. Part of a deliberate series
fixing the same class of React state bug across the three most important money-handling forms (#5170
checkout, #5185 upgrade/downgrade, #5237 switch wizard).

---

### 31. #5272 — Automation workflows failed hard when a member had no email address
`Improvement` · `Automation & workflows` · **Impact 5.2/10**
**Merged** 2026-07-07 · **Linear** [SUBRITE-553](https://linear.app/subrite/issue/SUBRITE-553) (Medium · Improvement) · **Programme** *Datadog cleanup* · **Base** `develop` · 8 files, +215/-33, 3 commits

**Problem.** When an automation workflow reached a "send email" or "send SMS" step for a member who had no
email address or phone number, the step was treated as an error. That both logged a Datadog error for a
completely expected situation — not every member has every contact channel — and **stopped the whole
workflow**, so every subsequent step for that member was abandoned.

**Root cause.** The executor interface had only two outcomes: success or error. "Nothing to do here, carry
on" was not expressible, so a missing recipient had to be encoded as a failure, which the workflow engine
correctly treated as fatal.

**What I changed.**
- Added a third outcome to the model: `createSkippedResult(reason, metaData)` on `BaseNodeExecutor`,
  returning `{ success: true, skipped: true, ... }`, and extended `ExecutionResult` in
  `node-executor.interface.ts` accordingly.
- Changed `SendEmailExecutor` and `SendSmsExecutor` to return a skipped result — with the reason and
  traceable metadata retained — instead of `createErrorResult` when recipient contact info is absent.
- Updated `automation-activities.service.ts` so a skipped step completes successfully and the workflow
  continues to the next node.
- Added tests asserting skipped behaviour and, specifically, that **no error logging occurs** in these
  cases.

**Impact.** Two distinct fixes in one change: workflows no longer abort part-way through for members
missing a contact channel — previously silent loss of every downstream automation step — and a predictable
condition stopped generating false alerts. Introducing "skipped" as a first-class outcome removed the need
to misuse the error path, which is why the fix generalises to any future executor.

---

### 32. #5029 — Product-packages list: filtering, sorting and search at scale
`Performance` · `Admin UI` · **Impact 5.1/10**
**Merged** 2026-06-03 · **Linear** [SUBRITE-197](https://linear.app/subrite/issue/SUBRITE-197) (High · Improvement) · **Base** `develop` · 24 files, +1727/-146, 20 commits

**Problem.** The admin Plans & Products → Product packages list had a slow price-filtering query and lacked
the filtering, sorting and search operators needed to work with a real catalogue. Operators could not narrow
by price range, billing period or publication status.

**Root cause.** Price filtering was implemented as an expensive SQL query. As I noted in the PR, the
alternative was to load all product packages into memory and filter there, since the row count per tenant is
small — the fix required deciding deliberately where the filtering should happen rather than optimising the
existing query in place.

**What I changed.**
- Rebuilt the backend filter contract: a `PackageFilterDto` supporting recurring-price and intro-price
  min/max bounds, billing-period filtering, status filtering (draft/published/unpublished) and sorting by
  recurring price, intro price and updated date, with a rewritten `package.repository.ts` query.
- Added a reusable `IsLessThanOrEqualToField(otherField)` class-validator decorator, so "min must not exceed
  max" is enforced declaratively in the DTO rather than by hand-rolled checks — usable by any future
  range filter.
- Built the frontend filter framework: `NameFilter`, `PriceRangeFilter`, `StatusFilter`,
  `SubscriptionDurationFilter`, a `useProductPackageFilter` hook, a `ProductPackageFilterLoadingContext`,
  and URL helpers (`generateProductPackageListUrl`, `getPreservedProductPackageFiltersFromUrl`,
  `productPackageFilterKeys`) so filter state round-trips through the URL and survives navigation and
  refresh.
- Added a `/` keyboard shortcut to focus search, and an `isBlank` utility.
- Added test coverage for the filter DTO, the controller and the new filtering/sorting behaviour.

**Impact.** Made a core catalogue screen genuinely usable and shareable — filter state in the URL means an
operator can bookmark or send a filtered view. More importantly this is where the reusable filter framework
was built; the Members list was later migrated onto the same framework (#5346, #5362), so this PR set the
pattern for list filtering across the admin panel rather than solving one screen.

---

### 33. #3333 — Webhook secrets survived a payment-provider credential change
`Bug fix` · `Payments & providers` · **Impact 5.0/10** · **Production hotfix**
**Merged** 2025-06-17 · **Base** `subrite/main` (hotfix) · 3 files, +23/-1, 3 commits

**Problem.** When a tenant updated their Stripe or Vipps payment-method configuration, the previously stored
webhook secret was left in place. A webhook secret is the shared key used to verify that an incoming webhook
genuinely came from the provider; keeping a stale one after the credentials change means provider callbacks
either fail verification or are verified against a key that no longer corresponds to the configuration — so
payment status updates arriving by webhook are unreliable.

**Root cause.** `PaymentConfigService` updated the provider configuration without invalidating the
dependent secret. The two were stored separately and had no lifecycle relationship, so nothing tied
"credentials replaced" to "secret no longer valid".

**What I changed.**
- On configuration update, detect the providers where this applies —
  `shouldRemoveWebhookSecret = name === STRIPE || name === VIPPS` — and call
  `removeWebhookSecret(tenantId, name)`, so the stale secret is cleared and regenerated against the new
  configuration rather than silently retained.
- Hid the webhook-events section from the tenant payment-method settings form, since it exposed
  provider-internal state that tenants should not be editing, and closed the modal properly after save.
- Recorded the change in `CHANGELOG.md`.

**Impact.** Restored reliable webhook verification after a credential rotation — without it, payment status
updates for that tenant silently stop being trustworthy, which in a subscription platform means
subscriptions not activating or not renewing. Tied the secret's lifecycle to the credentials it belongs to,
rather than leaving them independently mutable.

---

### 34. #3863 — Immediate upgrade/downgrade lost its link to the pending change record
`Bug fix` · `Payments & providers` · **Impact 5.0/10** · **Production hotfix**
**Merged** 2025-10-22 · **Base** `subrite/main` (hotfix) · 1 file, +21/-0, 2 commits

**Problem.** Applying a subscription upgrade or downgrade immediately failed to associate the resulting
order with the pending `MemberPackageChange` record. The change was executed and charged but the record
tracking it was not linked, so the subscription's change history and downstream processing were left
inconsistent — the system could not tell that the pending change had been fulfilled.

**Root cause.** `PurchaseOrderTransaction` relied on `order.memberPackageChange` already being populated by
the caller. On the immediate-apply path it was not, and there was no fallback, so the association was
simply skipped without any error.

**What I changed.**
- Added a fallback in `PurchaseOrderTransaction`: when `order.memberPackageChange` is absent, look up the
  member package's most recent `MemberPackageChange` with `status = PENDING_FOR_NEXT_RENEWAL` and
  `isImmediate: false`, ordering by `createdAt DESC`, and attach it to the order.
- Scoped the lookup precisely to the pending-not-immediate case, so it recovers the association only where
  one genuinely exists and cannot attach an unrelated change record.

**Impact.** Restored consistency between orders and the subscription-change records they fulfil, which is
what the change history and any downstream reconciliation depend on. Twenty-one lines, but the failure was
silent — no error, just a missing link — which is the kind of defect that surfaces much later as unexplained
data.

---

### 35. #3213 — Tenant settings rewritten around a single settings endpoint
`Improvement` · `Admin UI` · **Impact 4.9/10** · **Hotfix branch**
**Merged** 2025-05-23 · **Base** `subrite/develop` · 45 files, +1140/-883, 10 commits

**Problem.** The tenant settings area of the admin panel was structurally broken. Every settings form
required the caller to pass a `tenantId` explicitly and each form saved through its own path, so a tenant
could in principle address another tenant's settings and each form handled auth, caching and revalidation
differently. Server-side auth-token helpers were being used from client components, settings did not
refresh after saving, and the onboarding field list did not update when the underlying tenant data changed.

**Root cause.** Accumulated inconsistency rather than one defect: the settings screens had been added over
time, each with its own save action, its own token acquisition and no shared cache-invalidation strategy.
Passing `tenantId` from the client made the tenant a request parameter rather than a property of the
session.

**What I changed.**
- Added `UpdateTenantSettingsDto` (extending `UpdateTenantDto` with an optional `preference` block) and a
  single `TenantService.updateSettings()` that takes the tenant from CLS —
  `this.cls.get('currentUser').tenantId` — instead of from the request. The client can no longer nominate a
  tenant.
- Consolidated the controller surface accordingly, including replacing the id-taking `uploadImage` with
  `uploadSettingsImage`, and removed the now-unnecessary `ParseIntPipe` on tenant ids.
- Converted ~12 settings forms — invoice fields, member-card fields, maintenance, onboarding field config,
  onboarding rules, subscription rules, communications — from server-side `getServerAuthToken` to the
  client-side `useAuthToken` hook, and gave them a uniform
  `revalidateApiTag(tenantSettingsTag)` invalidation so saved settings actually appear.
- Added a `useEffect` so `OnboardingInputFieldConfigList` re-syncs when tenant preferences change, instead
  of showing stale config.
- Fixed a breadcrumb depth bug that mis-rendered on newsletter-integration detail pages, and corrected a
  misspelled utility import (`getFiledErrors` → `getFieldErrors`).
- Updated controller tests to the new signatures.

**Impact.** Replaced a dozen divergent save paths with one endpoint that derives the tenant from the
session — removing both a class of tenant-targeting mistake and the "saved but still showing the old value"
bug operators were hitting. The near-balanced diff (+1140/-883 over 45 files) reflects that this was mostly
consolidation.

---

### 36. #5095 — Data remediation for e-invoice subscriptions with corrupted billing periods
`Data migration` · `Invoicing & accounting` · **Impact 4.9/10**
**Merged** 2026-06-10 · **Linear** [SUBRITE-365](https://linear.app/subrite/issue/SUBRITE-365) (Bug) · **Base** `develop` · 2 files, +308/-0, 12 commits

**Problem.** The code fix in #5082 stopped e-invoice reactivation from over-advancing renewal dates, but it
did not repair the subscriptions already corrupted by the old behaviour. Those member packages were sitting
in production with billing periods spanning around two years, meaning no renewal and no revenue until the
inflated date arrived. The affected rows spanned **all tenants**.

**Root cause.** Historical data damage from the bug fixed in #5082. Repairing it was genuinely risky: some
affected subscriptions had since been legitimately renewed or extended, so blind re-anchoring would have
overwritten correct data with a reconstruction.

**What I changed.** A carefully scoped, idempotent remediation rather than a blanket update:
- A **read-only preview** script whose output doubles as the rollback record — I ran it and saved the
  results before applying anything.
- An **apply** script that re-anchors period fields to the last paid invoice, with `nextRenewal` floored at
  `now() + 23 days`, and tags every row it touches with `metaData.fix_pr` so the change is attributable
  afterwards.
- Deliberately narrow scope, documented in the file header: only rows whose corrupted period spans **≥ 400
  days** *and* that have **no** post-reactivation `next_renewal_updated` history extension *and* **no**
  post-reactivation `subscription_renewed` event. Rows with competing renewal signals are left untouched.
- Made it idempotent, so already-correct rows are skipped and the script can be re-run safely.

**Impact.** Restored correct billing periods — and therefore renewals and revenue — for the subscriptions
damaged by #5082's bug, across all tenants. The engineering judgement is in the exclusions: choosing to fix
only the demonstrably safe subset and leave ambiguous rows alone is what made a cross-tenant production data
mutation on billing fields defensible.

---

### 37. #3924 — Gift purchases blocked because the recipient's phone number was mandatory
`Bug fix` · `Checkout` · **Impact 4.7/10** · **Hotfix branch**
**Merged** 2025-11-03 · **Base** `subrite/develop` · 5 files, +7/-28, 4 commits

**Problem.** Buying a gift subscription required entering the recipient's phone number. A gift buyer often
does not know or have the recipient's mobile number, so checkout could not be completed — a hard stop on a
revenue path, and a poor experience at the point of purchase.

**Root cause.** The requirement was enforced in two places at once: `CheckoutStateService` threw
`400 giftReceiverInformationIsRequired` via `checkGiftReceiverDataCompletion()` when the phone was absent,
and the frontend form independently rejected an empty phone (`if (!formData.phone.length)`). Neither
treated the field as optional, and downstream display code assumed a phone was always present.

**What I changed.**
- Removed the server-side completeness check and its `checkGiftReceiverDataCompletion()` helper, plus the
  now-inaccurate `giftReceiverInformationIsRequired` API error declaration.
- Changed frontend validation from "must be present and valid" to "valid **if** present":
  `if (formData.phone && !validateMobilePhoneNr(formData.phone))`, and deleted the empty-value rejection.
- Guarded the display component so it only renders the phone line when one exists
  (`productMemberData.createdMember?.phone`), rather than showing an empty field.
- Guarded the Welcome step against incomplete member data.

**Impact.** Unblocked gift-subscription checkout for buyers without the recipient's phone number. Note the
shape of the fix: −28/+7 — the correct change was to *remove* the over-strict validation in both places
plus its API contract declaration, not to add a workaround.

---

### 38. #4526 — Plan prices were editable in the UI even when the plan was in use
`Bug fix` · `Admin UI` · **Impact 4.7/10** · **Production hotfix**
**Merged** 2026-02-11 · **Base** `subrite/main` (hotfix) · 1 file, +28/-15, 2 commits

**Problem.** The frontend half of the price-protection incident (#4493, #4506, #4528). The subscription-plan
product table presented a freely editable price input even when the plan already had active subscribers.
Operators had no indication that editing it would affect live subscriptions, so the dangerous action looked
routine.

**Root cause.** The `ProductTable` rendered the price `Input` unconditionally, with no reference to whether
the plan was in use. The safety property existed in the operator's head, not in the UI.

**What I changed.**
- Made the price cell conditional on `isPlanInUse`: when the plan has subscribers, the input is disabled and
  visually marked (`opacity-50 cursor-not-allowed`).
- Wrapped it in a `TooltipWrapper` showing the `planIsInUseText` explanation on hover, so the operator
  learns *why* it is disabled rather than just finding it unresponsive.

**Impact.** Closed the last hole in the price-protection series at the point of interaction: #4506 and #4528
made the backend refuse to reprice live subscriptions incorrectly, and this made the UI stop inviting the
action. Defence in depth — the guard the operator sees, and the guard the server enforces.

---

### 39. #5114 — List export with selectable fields
`Feature` · `Members, segments & import/export` · **Impact 4.7/10**
**Merged** 2026-06-11 · **Linear** [SUBRITE-379](https://linear.app/subrite/issue/SUBRITE-379) (High · Feature) · **Base** `develop` · 29 files, +1252/-75, 13 commits

**Problem.** Operators could not export the members of a segment or list to CSV with a chosen set of
columns. Getting member data out of the platform — for a mailing house, an agency, an offline campaign —
required a developer, so a routine marketing operation was a support request.

**Root cause.** Feature gap. The export module had member-level export but nothing segment/list-scoped, and
no way to enumerate the available columns — which matters because the exportable fields are partly dynamic:
tenant-defined custom properties and consent fields differ per tenant, so they cannot be a static list.

**What I changed.**
- Added `GET list-export-fields` returning `ListExportFieldsResponseDto` — key plus human-readable label per
  field — built by combining fixed member fields with the tenant's dynamic custom-property and consent
  fields, so the UI renders whatever that tenant actually has.
- Added a picker API returning published segments/lists **with member counts**, so operators see how many
  members an export will contain before running it.
- Added `SegmentExportDto` with `ArrayNotEmpty` validation, enforcing at the contract level that at least one
  field is selected — an export with no columns is a silent no-op otherwise.
- Guarded the endpoint with dual permissions (`[READ, SEGMENTS], [READ, DATA]`).
- Built the "Export list" modal, preselecting the list the operator is currently viewing.
- Fixed a long-standing typo in a filename that was part of the module's import graph
  (`segment-export.-filter.dto.ts` → `segment-export-filter.dto.ts`).
- Added English and Norwegian translations and unit tests for export-field construction and published-segment
  counting.

**Impact.** Turned a developer-assisted task into self-service for marketing operators. The design decision
that made it genuinely usable was deriving the field list from tenant configuration at request time rather
than hardcoding columns — a static list would have excluded exactly the custom properties tenants care most
about.

---

### 40. #5370 — Podcast feed outage alerting rewritten to time-based escalation
`Bug fix` · `Content & podcasts` · **Impact 4.7/10**
**Merged** 2026-07-28 · **Linear** [SUBRITE-669](https://linear.app/subrite/issue/SUBRITE-669) (High) · **Programme** *Datadog cleanup* · **Base** `develop` · 2 files, +149/-42

**Problem.** 133 production errors appeared in Datadog when `PodcastFeedService` fetched RSS from
`omny.fm` — a mixture of upstream HTTP 503s and 30-second timeouts. The alerting made a third-party outage
look like an internal emergency, and the volume drowned out real errors. The Linear issue was raised
automatically by Datadog.

**Root cause.** Escalation was driven by a **consecutive-failure counter**. Because the feed check runs on a
schedule, a sustained upstream outage re-fired an alert on every single check. Worse, the counter was keyed
per failure type, so an outage that alternated between 503 and timeout — which is exactly what a struggling
upstream does — reset the counter each time and never escalated properly.

**What I changed.**
- Replaced the consecutive-failure counter with **continuous outage duration**: the service records when the
  feed *started* failing and reasons from elapsed time rather than attempt count.
- Transient failures stay at `warn` for the first two hours, then escalate to `error`, with follow-up errors
  every two hours instead of on every check.
- Included the outage duration in the alert text, so whoever reads it knows whether this is two minutes old
  or two days old without cross-referencing.
- Made a change of transient failure type **not** reset the outage timer — the feed is still down, so the
  clock keeps running.
- Made successful fetches and non-transient failures correctly reset the escalation state.
- Added test coverage for the escalation window, the follow-up interval, failure-type switching and reset
  behaviour.

**Impact.** Reduced a sustained third-party outage from one error per scheduled check to one per two-hour
window, while making genuinely long outages escalate reliably — the previous behaviour could keep a
multi-day outage at warning level indefinitely because the type kept changing. Duration in the message
turns an alert into information.

---

### 41. #3595 — Communication senders extracted into a first-class, approvable entity
`Improvement` · `Communication & notifications` · **Impact 4.6/10**
**Merged** 2025-08-21 · **Linear** SUBRITE-3187 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 56 files, +3979/-2521

**Problem.** Sender identities — the from-name and from-address used for a tenant's email and SMS — were
stored as loose string columns scattered across the `tenant` and `brandkit` tables
(`tenantSenderName`, `tenantSenderEmail`, `brandKitSenderName`, `brandKitSenderEmail`, `smsSenderName`).
There was no way to model the same sender being used in several places, and — the reason this mattered — no
way to record whether a sender had been **verified/approved** with the email provider. Sending from an
unapproved address damages domain reputation and gets mail rejected.

**Root cause.** Sender identity had been modelled as text fields on whatever entity happened to need it,
duplicated per table, with no shared lifecycle. Approval state had nowhere to live.

**What I changed.** A schema migration in four ordered steps, so production data moved safely:
1. `AddSenderIdAndIsApprovedFlag` — introduce `emailSenderId` / `smsSenderId` foreign keys and add
   `isApproved` plus `requestSentAt` to `communication_sender`.
2. `MigrateTenantSenderInfoToCommunicationSenderTable` — move existing tenant sender strings into real
   `communication_sender` rows and link them.
3. `MigrateBrandkitSenderInfoToCommunicationSenderTable` — same for brandkit senders.
4. `DeprecateOldSenderProperties` — only then drop the old columns.
- Updated the automation module, controllers, services, DTOs and admin UI to reference senders by id.
- Updated `ADDING_TENANTS.md` so tenant provisioning no longer sets the removed columns — the operational
  documentation stayed in step with the schema.

**Impact.** Turned sender identity into a modelled entity with an approval lifecycle, which is what
subsequent sender-approval work (#3679, #3681, #3803, #3903, #4064) built on. The staged
add → backfill → link → drop migration sequence is the notable part: the same change done in one step
would have risked losing every tenant's sender configuration.

---

### 42. #4947 — Enterprise plans appearing in the customer-facing switch list
`Improvement` · `Admin UI` · **Impact 4.6/10**
**Merged** 2026-05-11 · **Linear** [SUBRITE-107](https://linear.app/subrite/issue/SUBRITE-107) (**Urgent**) · **Base** `develop` · 30 files, +148/-5, 7 commits

**Problem.** Subscription visibility was a single flag shared by two audiences. A plan an admin needed to
select when switching a customer's subscription — a negotiated enterprise plan, for instance — was therefore
also offered to members on My Page, so customers could see and self-select plans intended only for
administrator use. Filed Urgent.

**Root cause.** One boolean (`showAsOptionOnSubscriptionSwitch`) governed both the admin subscription-switch
list and the member-facing list. The two audiences have genuinely different requirements, so no single value
could be correct for both.

**What I changed.**
- Migration adding `showAsOptionOnAdminSubscriptionSwitch` to the subscription-plan entity, separating admin
  visibility from member visibility.
- Threaded the new field through the API contract and the admin UI so operators control the two
  independently.
- Updated 20+ controller test fixtures across podcast, member, profile, e-invoice, invoice, ledger and
  payment-provider suites to carry the new field — the unglamorous but necessary part of adding a required
  property to a widely-used entity.

**Impact.** Stopped administrator-only plans being exposed to customers, which is both a commercial problem
(a customer self-selecting a negotiated plan) and a confusing experience. Splitting one overloaded flag into
two audience-specific ones removed the impossible constraint rather than papering over it.

---

### 43. #5301 — Recurring e-invoice validation noise for members without a phone number
`Bug fix` · `Payments & providers` · **Impact 4.6/10**
**Merged** 2026-07-13 · **Linear** [SUBRITE-586](https://linear.app/subrite/issue/SUBRITE-586) (Medium · Bug) · **Base** `develop` · 1 file, +1/-1, 5 commits

**Problem.** The recurring e-invoice (EHF) path treated a missing phone number as an error-level failure.
Private individuals frequently have no phone number recorded, and EHF delivery does not require one — so the
platform was logging errors for an entirely normal situation on its invoice-based payment method, obscuring
genuine EHF delivery problems.

**Root cause.** A validation-failure branch logged at `error` level regardless of whether the failure was
actually blocking. The phone check was stricter than the EHF specification requires, so a condition that
should have been informational was reported as a fault.

**What I changed.**
- Changed the single log call from `this.logger.error({...})` to `this.logger.log({...})` — a one-line change
  that reclassifies an expected outcome without altering any control flow. The validation and its existing
  error handling behaviour are untouched, which is what made this safe to ship on a payment path.

**Impact.** Removed a recurring source of false error signal from the e-invoice flow. Worth including for
the diagnosis rather than the diff: identifying that the over-strict check was reporting rather than
blocking meant the correct fix was one line at the log call, not a change to payment validation logic —
which on an EHF delivery path would have carried real risk.

---

### 44. #5346 — Members list migrated onto the shared filter framework
`Feature` · `Admin UI` · **Impact 4.6/10**
**Merged** 2026-07-22 · **Linear** [SUBRITE-618](https://linear.app/subrite/issue/SUBRITE-618) (High) · **Base** `develop` · 45 files, +1872/-3105, 10 commits

**Problem.** The Members list — the most-used screen in the admin panel — had all ten of its filters
implemented as the old design: a flat row of separate dropdown buttons, each with bespoke state and URL
handling. It looked and behaved differently from the Product Packages list (#5029), and adding a filter meant
writing another one-off component.

**Root cause.** Two independently evolved filtering implementations. Members predated the generic framework
and had never been migrated, so every filter carried its own state, URL serialisation and popover code.

**What I changed.**
- Migrated Members onto the generic `FilterBar` framework: one search row, a Filter button, removable chips
  and a two-column popover, matching Product Packages.
- Extended the framework rather than special-casing Members, adding two genuinely new editors —
  `ConsentEditor` and `CustomPropertyEditor` — so tenant-defined custom properties and consent are now
  filterable, and improved `TextEditor`.
- Reworked the backend filter mapping in `member-filter.service.ts`: added a `SEARCH` filter field
  (name/company/email) and fixed multi-value email-status and inactivity mapping plus open-ended postcode
  ranges, where only a min or only a max is supplied.
- Extended the framework's `numberComparator` with `disableGrouping` and documented it in the
  `Filters/README.md`, so the next person using it does not have to read the implementation.
- Added debounced search with a `/` keyboard shortcut, matching the Product Packages behaviour.
- Added a test suite for the backend filter-to-query mapping, and lowered the repo's ESLint threshold from
  36 to 35 — the migration removed a warning rather than adding one.

**Impact.** The diff tells the story: **+1872/-3105** across 45 files, a net removal of over 1200 lines. Two
filtering systems became one, with new capability (consent and custom-property filtering) added to the
shared framework where every list benefits. Fixing the open-ended postcode range and multi-value mapping also
corrected filters that had been quietly returning wrong result sets.

---

### 45. #3736 — Non-functional payment-provider visibility fields removed
`Refactor / tech debt` · `Payments & providers` · **Impact 4.5/10**
**Merged** 2025-09-17 · **Linear** SUBRITE-3259 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 27 files, +41/-254, 12 commits

**Problem.** `payment_provider_config` carried two boolean columns, `showOnMyPage` and `showOnAdmin`, that
appeared to control where a payment provider was offered but no longer did anything. They were still
exposed through the API, still settable in the admin UI, and still filterable — so operators could toggle
them and reasonably expect an effect. Configuration that lies about what it does is worse than no
configuration.

**Root cause.** The fields had been superseded by other logic but never removed, leaving a
believable-looking control with no effect anywhere in the codebase.

**What I changed.**
- Migration `DeprecateShowOnMyPageAndShowOnAdmin` dropping both columns, after confirming nothing read them.
- Removed them from the entity, the settings response DTO and `payment-config.service.ts`.
- Deleted `PaymentProviderFilter` and the query-parameter filtering it supported from
  `payment-provider.controller.ts`, since the only thing it filtered on was the dead fields — so the API
  stopped advertising a capability it did not have.
- Removed the corresponding admin UI controls and updated the affected controller tests and fixtures.

**Impact.** −254/+41 across 27 files: removed a misleading configuration surface from the database, the API
contract and the UI in one coordinated pass. The value is in preventing wasted operator time and support
questions about a toggle that did nothing — and in not leaving the next developer to work out whether it was
safe to touch.

---

### 46. #3705 — Payment reminders still sent to members who had turned auto-renew off
`Bug fix` · `Payments & providers` · **Impact 4.4/10** · **Production hotfix**
**Merged** 2025-09-03 · **Linear** SUBRITE-3249 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/main` (hotfix) · 2 files, +19/-0, 2 commits

**Problem.** Members who had explicitly disabled auto-renewal continued to receive
payment-failure/renewal reminder emails. From the member's point of view the platform was chasing them for a
payment they had deliberately opted out of — the kind of message that generates complaints and unsubscribes.

**Root cause.** Two places selected the member packages to act on without filtering on `autoRenew`. The
Vipps recurring path and `TriggerService.triggerRecurrentPaymentFailedEvent` both queried by subscription
state only, so packages with auto-renew off were still picked up. `triggerRecurrentPaymentFailedEvent` also
dereferenced `memberPackage` without checking it existed.

**What I changed.**
- Added `autoRenew: true` to the query criteria in both `vipps-payment.provider.ts` and
  `trigger.service.ts`, so subscriptions with auto-renew disabled are excluded from recurring charging and
  from the payment-failed event that generates the reminder.
- Added `if (!memberPackage) { ...log...; return; }` guards in both places, so a missing package logs
  context and returns instead of throwing on a null dereference.

**Impact.** Stopped the platform contacting members about payments they had opted out of — respecting an
explicit customer preference, which is both a trust and a deliverability concern. The null guards removed a
crash on the same code path.

---

### 47. #5170 — Checkout payment form discarded buyer-edited quantities and showed stale totals
`Bug fix` · `Checkout` · **Impact 4.4/10**
**Merged** 2026-06-19 · **Linear** [SUBRITE-400](https://linear.app/subrite/issue/SUBRITE-400) (**Urgent**) · **Base** `develop` · 9 files, +848/-57, 8 commits

**Problem.** In checkout — the point where customers actually pay — optional product selections and
buyer-edited quantities were overwritten whenever the checkout state refreshed or a load failed. The total
price could also lag behind the current selection during rapid quantity changes. A buyer could therefore see
one price and be charged for a different basket. Filed Urgent.

**Root cause.** The same duplicated-state and race pattern as #5185, on the customer-facing side.
`OptionalProduct` held its own `useState` copies of quantity and shipping countries and synced upward via
`useEffect`, so a checkout-state refresh re-initialised the component and destroyed the buyer's edits. The
total-price recalculation had no request sequencing, so an earlier slow response could overwrite a later
correct one. `NaN` quantities from partially cleared inputs were passed through rather than normalised.

**What I changed.**
- Made `OptionalProduct` fully controlled — removed its internal state and the `useEffect` push-up, taking
  `quantity` and `shippingCountries` as props so a refresh cannot clobber buyer input.
- Added abort/ignore handling for in-flight total-price recalculations so stale results are discarded and the
  displayed total always matches the current selection.
- Extracted two tested utilities: `normalizeProductQuantities` (treats `NaN` as `0` and omits non-positive
  selections from the payload, so the request never carries a meaningless quantity) and
  `hasInvalidOptionalProductQuantity` (blocks submission when a quantity is invalid or below its minimum,
  respecting `isRequired`).
- Added substantial test coverage: unit tests for both utilities plus React Testing Library suites for
  `OptionalProduct` and `PaymentForm` (~350 test lines) — the checkout form had had none.
- Reduced the repo ESLint threshold from 46 to 44.

**Impact.** Removed the possibility of a buyer being charged for a basket different from the one displayed,
on the platform's primary revenue path. The lasting contributions are the two extracted quantity utilities —
now shared rather than reimplemented per form — and the first real test coverage for the checkout payment
form.

---

### 48. #5282 — Vipps reconciliation crashed on abandoned checkouts
`Bug fix` · `Automation & workflows` · **Impact 4.4/10**
**Merged** 2026-07-10 · **Linear** [SUBRITE-567](https://linear.app/subrite/issue/SUBRITE-567) (Medium · Bug) · **Programme** *Datadog cleanup* · **Base** `develop` · 1 file, +7/-22, 8 commits

**Problem.** `reconcileVippsCharge` threw `Cannot read properties of null (reading 'tenantId')` repeatedly in
production. Because reconciliation is what recovers Vipps charges whose webhook did not arrive, a crashing
reconciler means genuinely missed charges go unrecovered.

**Root cause.** I traced it to a scope error rather than a null-safety error. Reconciliation exists for
**recurring** charges, but its query selected both `PENDING` and `DUE` Vipps payments. `PENDING` rows are
created only by the initial checkout/order-intent flow and can legitimately have a null `packageId`/`memberId`
— an abandoned or failed checkout whose terminal webhook never arrived stays `PENDING` forever. Reconciliation
then dereferenced `payment.package.tenantId` on a null package. The correct fix was therefore not a null
guard, which would have silently skipped rows and hidden the mismatch, but narrowing the query to what
reconciliation is actually for.

**What I changed.**
- Narrowed the fetch from `In([PENDING, DUE])` to `status: VIPPS_PAYMENT_STATUS.DUE`, and simplified the
  corresponding guard from a two-status check to a single one.
- Removed the now-unreachable `PENDING`-only branch that created an invoice via `PurchasePackageTransaction`
  when a charge came back `CHARGED` — dead code once `PENDING` rows are out of scope — along with its now
  unused import and injected dependency.

**Impact.** Restored Vipps charge reconciliation, which had been crashing rather than recovering missed
charges — a payment-recovery mechanism that silently does not run is a revenue problem. The net −15 lines
reflect that the fix was to correct the query's scope and delete the code that only existed to service the
wrong rows.

---

### 49. #3509 — Brand kits: per-brand senders, templates and configuration
`Feature` · `Widgets & branding` · **Impact 4.3/10**
**Merged** 2025-07-29 · **Linear** SUBRITE-3134 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · **100 files**, +4638/-668, 85 commits

**Problem.** A tenant could only have one visual and communication identity. Media companies typically run
several brands — different publications or shows — from one Subrite tenant, and each needs its own logo,
colours, sender identity and OTP email template. Everything was tenant-level, so multi-brand customers could
not be served properly.

**Root cause.** The data model had no concept of a brand below the tenant. Branding, sender details and
templates were all columns on `tenant`.

**What I changed.** The largest single feature delivery in this portfolio — 100 files:
- Introduced the `brand_kit` entity and table via three ordered migrations: `BrandKit` (the entity itself,
  with logo, colours, `defaultLogoutUrl`), `BrandKitSenderAndOtpTemplate` (per-brand sender name/email and OTP
  template reference), and `SetDefaultOtpTemplateId` — a data migration that iterates every existing tenant
  and assigns a default template, so no tenant was left in a broken state after deploy.
- Added `validateMJML()`, which compiles submitted MJML with `validationLevel: 'strict'` and returns a
  structured result, so an invalid email template is rejected at the API boundary rather than failing later
  when an email is actually sent to a customer.
- Added a `ParseJson()` transform decorator raising `BadRequestException` on malformed JSON, needed because
  brand-kit configuration arrives as JSON in multipart form uploads alongside the logo.
- Wired brand kits through the automation module, communication sender and template controllers, and built
  the admin configuration UI.
- Corrected a mislabelled `@ApiTags('CommunicationTemplate')` on the sender controller, which had been
  grouping sender endpoints under the wrong section in the API documentation.

**Impact.** Enabled multi-brand tenants — a commercial capability the platform could not previously offer,
and the foundation for the later per-brand sender approval work (#3595). Two details make it durable rather
than merely shipped: strict MJML validation at the boundary, so a broken template cannot reach a customer's
inbox, and a data migration that brought every existing tenant to a valid default instead of leaving
migration to chance.

---

### 50. #3793 — Invoices automatically sent for orders already paid
`Bug fix` · `Payments & providers` · **Impact 4.3/10**
**Merged** 2025-09-29 · **Linear** SUBRITE-3223 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 1 file, +12/-2, 2 commits

**Problem.** When an operator created an order that was already paid — the follow-up to #3776 — the e-invoice
provider still generated and submitted an invoice document to the customer. The customer received a demand
for money they had already handed over.

**Root cause.** `EInvoicePaymentProvider.submitDocument()` had no knowledge of payment state. It generated and
submitted a document on every order creation, because "already paid" was not information the submission path
received.

**What I changed.**
- Threaded an `isPaid: boolean = false` parameter through `submitDocument` and `submitDocumentForInitial`,
  defaulting to `false` so every existing caller keeps its current behaviour.
- Added an early `if (isPaid) return;` so no document is generated or submitted for an already-paid order.
- Required both `isPaid && !!paymentDate` for the initial-submission path, so the skip applies only when the
  payment is genuinely recorded with a date rather than on a bare flag.

**Impact.** Stopped customers being invoiced for orders they had already settled — a direct customer-trust
problem and an accounting one, since the invoice would sit outstanding against a paid order. Defaulting the
new parameter to `false` meant this shipped without touching any other call site's behaviour.

---

### 51. #4400 — Owner seat access wrong for products with no additional seats
`Bug fix` · `Subscriptions & billing lifecycle` · **Impact 4.3/10**
**Merged** 2026-01-23 · **Linear** SUBRITE-3827 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 15 files, +69/-24, 17 commits

**Problem.** A cluster of bugs around price changes and seat access. The core defect: products configured
with `maxQuantityPerOrder = 0` — meaning the product is owner-only, with no additional seats to distribute —
did not grant the owner access. The person who bought the subscription could not use what they had paid for.
Related, exports treated owner access inconsistently and such products were misclassified as group
subscriptions.

**Root cause.** `hasAccess` was derived solely from `product.assignSeatToOwner`. That flag is meaningful for
products that *have* distributable seats, but for a product with `maxQuantityPerOrder = 0` there are no seats
to assign, so the flag was never set and the owner got nothing. The export service compounded it by requiring
both `assignSeatToOwner` **and** `hasAccess`, so it disagreed with the stored value.

**What I changed.**
- Changed the derivation in both mapping functions in `order-utility.ts` to
  `assignSeatToOwner || maxQuantityPerOrder === 0`, with a comment recording why — a product with no
  distributable seats is by definition owner-access-only.
- Simplified the export condition to trust the stored `hasAccess` (`isDigital && memPackProd.hasAccess`)
  instead of re-deriving it from `assignSeatToOwner`, so export and application agree.
- Corrected the group-subscription classification so `maxQuantityPerOrder = 0` products are not treated as
  group subscriptions, and updated the affected controller test fixtures with explanatory comments.
- Added a test asserting `groupInfo` is reported correctly for a genuine group subscription.

**Impact.** Restored access for owners of single-seat digital products — a customer paying and receiving
nothing. Also removed a disagreement between the export path and the application about what owner access
means, which is the sort of inconsistency that makes exported data quietly untrustworthy.

---

### 52. #5166 — Segment export dropdown empty when a member-count query failed
`Bug fix` · `Members, segments & import/export` · **Impact 4.2/10**
**Merged** 2026-06-17 · **Linear** [SUBRITE-405](https://linear.app/subrite/issue/SUBRITE-405) (High · Bug) · **Base** `develop` · 7 files, +707/-42, 6 commits

**Problem.** The export segmentation list showed no dropdown options at all. Operators could not export any
segment, because the list they needed to choose from was empty.

**Root cause.** Building the option list required a member count per segment. Dynamic segments compute their
counts through a generated union query, and if that query failed — or was simply slow — the failure
propagated and took out the whole options response. One bad segment therefore removed every segment from the
dropdown: a total outage of the feature caused by a partial failure.

**What I changed.**
- Made the dynamic member-count query degrade gracefully in `segment-member.repository.ts`: failures are
  logged and the affected segment's count is omitted, so the remaining options still render. A partial
  failure now produces a partial result rather than nothing.
- Added stale-while-revalidate caching to `CacheService` (`SwrCacheEntry`, a per-key lock to prevent a
  revalidation stampede) and applied it to segment export options under a new
  `CacheConstant.SEGMENT_EXPORT_OPTIONS`. Operators get an immediate response from cache while the
  expensive counts refresh in the background — addressing the slowness that made the fragility visible.
- Added test suites for the SWR behaviour including the lock, the repository's dynamic-count path and the
  service's caching.

**Impact.** Restored a feature that was completely unavailable, and made it resilient: a single
malfunctioning segment can no longer take down the whole list. The reusable stale-while-revalidate
implementation added to the shared `CacheService` is available to any other expensive read in the platform.

---

### 53. #4439 — Vipps restart button shown while the agreement status was still loading
`Bug fix` · `Subscriptions & billing lifecycle` · **Impact 4.1/10**
**Merged** 2026-01-26 · **Linear** SUBRITE-3843 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 4 files, +13/-4, 8 commits

**Problem.** In the admin subscription view, the "restart Vipps payment" action appeared while the Vipps
agreement status was still being fetched, and also appeared when the status fetch had failed. Operators could
therefore trigger a restart based on an unknown agreement state — acting on information the UI did not
actually have.

**Root cause.** On a failed status fetch the code set the status to an empty string cast to the enum
(`setVippsAgreementStatus('' as VIPPS_AGREEMENT_STATES)`). An empty string is falsy but is not a valid
member of the enum, so downstream "is the agreement inactive?" logic evaluated it as inactive and showed the
restart action. There was no explicit representation of "we could not determine the status", and the
loading state was not consulted at all.

**What I changed.**
- Added `ERROR` to the `VIPPS_AGREEMENT_STATES` enum, so "the status could not be determined" is a real
  value rather than an invalid cast, and set it on fetch failure.
- Added `!isGettingVippsAgreementStatus` to the restart-visibility condition, so the action does not appear
  while the status is still loading.
- Required a truthy `vippsAgreementStatus` before evaluating agreement state.

**Impact.** Stopped operators being offered a payment-restart action based on unknown or failed status —
small in size but it removed a genuine "act on data you do not have" path. Replacing an invalid enum cast
with a real `ERROR` member is the kind of fix that prevents the same confusion recurring anywhere the enum is
used.

---

### 54. #4493 — First fix for unintended subscription-plan repricing
`Bug fix` · `Subscriptions & billing lifecycle` · **Impact 4.1/10**
**Merged** 2026-02-09 · **Linear** SUBRITE-3903 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 1 file, +24/-4, 9 commits

**Problem.** The `develop`-targeted original of the fix later hotfixed to `main` as #4506: saving a
subscription plan repriced every existing subscriber regardless of whether any price had actually changed.

**Root cause.** `SubscriptionPlanService` called `updateRecurrentPricingOfMemberPackageAndProducts()`
unconditionally on plan update, treating "the plan was saved" as "the prices changed".

**What I changed.**
- Added the `hasProductPriceChanged(existingProducts, newProducts)` comparison and gated the reprice call on
  it.
- Reordered the operations so `addProducts` runs before the reprice decision — the ordering matters, because
  the comparison must see the plan's final product set rather than a partially updated one.

**Impact.** The original fix for the repricing incident. It is listed separately from #4506 because it is a
distinct PR against a different base: #4493 fixed `develop` for future releases, #4506 carried the same fix
to `main` as a hotfix so production stopped repricing subscriptions immediately rather than at the next
release.

---

### 55. #4556 — Plan price updates corrupted Vipps subscriptions with a charge already in flight
`Improvement` · `Subscriptions & billing lifecycle` · **Impact 4.1/10**
**Merged** 2026-02-20 · **Linear** SUBRITE-3942 *(pre-migration issue, no longer in Linear)* · **Base** `subrite-3904/...` (stacked on #4543) · 6 files, +108/-13, 10 commits

**Problem.** The Vipps-specific gap in the deferred-repricing work. Vipps charges are created in advance of
the charge date, so a subscription can have a charge already submitted to Vipps for an amount based on the
old price. Repricing at that moment left the platform's recurring price and the amount Vipps was about to
collect disagreeing — the customer would be charged one amount while the system believed another.

**Root cause.** #4543 built the pending-price-update pipeline but keyed its "is it safe to reprice now?"
decision on invoices only. It had no equivalent check for Vipps agreements with a due charge, so
Vipps subscriptions bypassed the deferral and were repriced immediately.

**What I changed.**
- Added `VippsPaymentRepository.getDueChargeAgreementIds(agreementIds)`, returning a `Set` of agreement ids
  with a charge already due, so the check is one bulk query rather than per-subscription.
- Extended `SubscriptionPlanService` to consult both signals — sent invoices *and* due Vipps charges — before
  repricing, injecting `VippsPaymentRepository` and `InvoiceRepository` and reading the Vipps agreement id
  from `VippsPaymentInfo`.
- Simplified the queueing in `MemberPackageChangeService`, replacing the inline delayed-job configuration
  with the shared `enqueuePendingPriceUpdateJob` path used by both the recurrent-payment and e-invoice-renewal
  transactions.

**Impact.** Closed the provider-specific hole in the deferred-repricing mechanism, so Vipps subscribers with a
charge in flight get the new price at their following renewal instead of a mismatch between what the platform
records and what Vipps collects. Being stacked directly on #4543 kept the two halves of one behaviour
reviewable together.

---

### 56. #5362 — Members list filter defects after the framework migration
`Bug fix` · `Members, segments & import/export` · **Impact 4.1/10**
**Merged** 2026-07-24 · **Linear** [SUBRITE-664](https://linear.app/subrite/issue/SUBRITE-664) (High · raised from Datadog) · **Base** `develop` · 8 files, +374/-44, 24 commits

**Problem.** Follow-up to #5346. In use, the migrated Members filter bar had real defects: changing a filter
did not refresh the results, the company and inactivity filters allowed multi-select where only one choice is
meaningful, "Save as list" was offered when no members matched, the layout was unusable on small screens, and
there was no way to filter by member type (paying vs invited). Combined member-type filtering also returned
duplicate members. A Datadog error was attached to the issue.

**Root cause.** Two things. The filter navigation used `push(url)` alone; with Next.js App Router caching,
pushing the same-shaped URL did not always re-run the server component, so results went stale. And the
`TYPE` (paying/invited) filter did not exist in the backend filter-to-query mapping — the two member-type
sources are different relations, so combining them without deduplication yields the same member twice.

**What I changed.**
- Added `refresh()` alongside `push()` in `useFilterUrlState`, so a filter change always re-fetches.
- Implemented the `TYPE` filter (paying / invited / both) in `member-filter.service.ts`, mapping it into the
  segmentation JSON query with correct `AND` combination and deduplication so a member appearing in both
  sources is returned once.
- Added `singleSelect?: boolean` to the `MultiSelectEnumField` framework type and applied it to the company
  and inactivity filters, so the constraint is expressed in the schema rather than enforced ad hoc.
- Reworked `MultiSelectEditor` with explicit **Apply** and **Clear** actions instead of applying on every
  click, replacing the previous `applyKeepOpen` behaviour.
- Hid "Save as list" when the result set is empty, and fixed the layout for narrow viewports.
- Added a database-backed integration test for the type filter and unit tests for the JSON-query conversion.

**Impact.** Fixed a stale-results bug on the admin panel's most-used screen — operators were making decisions
from filtered lists that no longer matched the filters. Added member-type filtering, and pushed the
single-select constraint into the shared framework so other lists get it declaratively. A good illustration
of finishing a migration properly rather than declaring it done at merge.

---

### 57. #3992 — Hard-bounced email addresses tracked and excluded
`Feature` · `Communication & notifications` · **Impact 4.0/10**
**Merged** 2025-11-26 · **Linear** SUBRITE-3235 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 47 files, +1254/-110, 51 commits

**Problem.** The platform did not track which member email addresses had **hard bounced** — permanently
undeliverable. It therefore kept sending to dead addresses, which is the single fastest way to destroy sender
reputation with mailbox providers and get a domain's legitimate mail filtered or blocked. It also had no way
to report list quality to tenants.

**Root cause.** SendGrid bounce and blocked events were recorded as generic log events with no distinction
between hard and soft bounces, and there was no per-member email-status record — so bounce information existed
in logs but was not actionable.

**What I changed.**
- Added the `member_email_status` entity and table (migration `MemberEmailStatus`) with a repository and
  service, giving each member a durable email-deliverability status.
- Distinguished `HARD_BOUNCE` from `SOFT_BOUNCE` in `SENDGRID_MAIL_EVENTS`, added a
  `SendGridWebhookEventType` (`'bounce' | 'blocked'`) to the webhook DTO, and introduced
  `getSendGridEventStatus(event)` so `CommunicationLogService` classifies the event rather than storing
  SendGrid's raw string.
- Reworked `SENDGRID_EVENT_PRIORITY`, since events arrive out of order and a later-arriving lower-priority
  event must not overwrite a more definitive one.
- Added `totalHardBounced` to the email stats response so tenants can see list quality.
- Added a `HARD_BOUNCED_EMAILS` cache constant, so exclusion checks during a send do not hit the database per
  recipient.

**Impact.** Gave the platform a durable, per-member record of permanently undeliverable addresses and stopped
sending to them — directly protecting email deliverability for every tenant, which is infrastructure-level
value rather than a feature. The `member_email_status` entity introduced here was later brought under tenant
isolation in #4893.

---

### 58. #4118 — Marketing SMS could be sent without a consent basis
`Bug fix` · `Communication & notifications` · **Impact 3.9/10** · **Hotfix branch**
**Merged** 2025-11-28 · **Base** `subrite/develop` · 5 files, +38/-11, 8 commits

**Problem.** A marketing communication could be saved and sent without a consent record attached. Under
GDPR and Norwegian marketing rules, marketing messages require a lawful basis, so sending without one is a
compliance breach rather than a cosmetic omission. Separately, updating a communication could null out its
`consentId` as a side effect of an unrelated edit.

**Root cause.** In `CommunicationService`, the consent assignment was spread unconditionally into the update
payload. Any update that did not carry a `communicationType` therefore evaluated the marketing branch and
wrote `consentId: null` — silently removing the consent basis from an existing marketing communication. And
the SMS form did not validate that a marketing communication had a consent selected, so one could be created
without it.

**What I changed.**
- Made the consent assignment conditional on the field being present:
  `...(updateCommunicationDto.communicationType && { ... })`, so an update that says nothing about
  communication type leaves the existing consent alone.
- Inverted the branch to `!== MARKETING ? null : consentId`, making the intent explicit — only a
  non-marketing type clears the consent.
- Added frontend validation in the SMS `CommunicationForm` rejecting a marketing communication with no
  `consentId`, with a translated `sms.emptyConsentId` error.
- Cleaned up a stray `display: inline-block` and a trailing-whitespace bug in the Slate email serialiser
  encountered in the same area.

**Impact.** Closed a compliance gap on marketing SMS — both the "created without consent" and the more
insidious "consent silently removed by an unrelated edit" paths. The conditional-spread fix matters
generally: unconditionally spreading a derived field into an update payload will overwrite stored data
whenever the source field is absent.

---

### 59. #4922 — Import failures reported without saying what was wrong
`Bug fix` · `Members, segments & import/export` · **Impact 3.9/10**
**Merged** 2026-05-11 · **Linear** [SUBRITE-37](https://linear.app/subrite/issue/SUBRITE-37) (Medium · Bug) · **Base** `develop` · 14 files, +624/-335, 8 commits

**Problem.** When a member, subscription or payment import failed, the operator was told the import had
failed but not which row or which field was at fault. Imports are how new tenants get their data into the
platform, so an unexplained failure on a large file means guessing — a slow, frustrating loop during
onboarding. Payment-provider values in import files were also rejected unless they matched the internal enum
exactly, so a file saying "Stripe" or "vipps" failed on capitalisation.

**Root cause.** Validation errors were collected and reported without field or row attribution, and the
`paymentProvider` column was validated with a strict `IsEnum`/`IsIn` against internal identifiers, with no
tolerance for the labels customers actually export from their previous systems.

**What I changed.**
- Added a `field-error-collector` utility (with tests) that accumulates errors per field and per row, and
  used it across the member, subscription and payment import services, so the operator is told what failed
  and where.
- Added a `PAYMENT_PROVIDER_IMPORT_ALIASES` constant and a `Transform` on the import DTO's `paymentProvider`,
  mapping common external spellings and casings to the internal enum, with a test enumerating each accepted
  alias and its expected result — including which inputs correctly map to `null`.
- Surfaced the structured errors in the two admin file-uploader components, so the detail reaches the
  operator rather than the logs.

**Impact.** Turned import failures from "it did not work" into "row 412, field `email`, invalid format",
which removes most of the guesswork from tenant onboarding. The alias mapping fixed a common, avoidable class
of failure — a file rejected purely because a provider name was capitalised differently.

---

### 60. #5297 — OIDC grant errors classified so expected outcomes stop alerting
`Bug fix` · `Auth & access control` · **Impact 3.9/10**
**Merged** 2026-07-10 · **Linear** [SUBRITE-576](https://linear.app/subrite/issue/SUBRITE-576) (High · Bug) · **Programme** *Datadog cleanup* · **Base** `develop` · 4 files, +89/-1, 3 commits

**Problem.** The embedded OIDC provider logged every `grant.error` event at `error` level, producing a flood
of `invalid_grant` entries in Datadog. `invalid_grant` on refresh-token rotation is an ordinary OAuth outcome
— a token already used, a stale tab retrying — so genuine authentication faults were buried in expected noise.

**Root cause.** The provider's `grant.error` handler made no distinction between an expected client-facing
OAuth error and a real server fault, even though the error object carries exactly that information: `oidc-provider`
sets `expose: true` on errors intended for the client and `false` on internal faults.

**What I changed.**
- Added `frontend/src/server/oidc/grantErrorLogger.ts` with `logGrantError(error, clientId)`, keying on
  `expose`: `expose === true` logs at `warn`, `expose === false` at `error`, and a null/undefined error
  defaults to `error` rather than throwing inside a logger.
- Wired it into `getProvider.ts`, passing `ctx.oidc?.client?.clientId` with optional chaining so the logging
  path cannot itself fail when client information is unavailable.
- Added a spec covering all three branches, and a `@/lib/*` module mapping in `jest.config.js` that the test
  needed.

**Impact.** Reclassified a high-volume expected condition out of error tracking while keeping genuine OIDC
server faults at `error`, so the remaining signal is actionable. Scope note: the delivered change is the
classification and its logging — it does not alter refresh-token rotation behaviour, despite what the issue
title suggests.

---

### 61. #3771 — Tenants could not write their own billing-period wording
`Feature` · `Admin UI` · **Impact 3.9/10**
**Merged** 2025-09-26 · **Linear** SUBRITE-3243 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 17 files, +243/-8, 19 commits

**Problem.** The period/price text on a product package — the "kr 199 / month" style line customers read
before buying — was generated from a fixed template. Tenants could not adjust the wording to match how they
actually describe their offer ("199 kr per måned, faktureres årlig", "13 issues a year"), so the most
commercially sensitive copy on the purchase page was not theirs to control.

**Root cause.** The text was derived from billing-period fields with no override. There was nowhere to store
tenant-authored copy, and because Subrite serves both Norwegian and English audiences it needed to be
translatable, not a single string.

**What I changed.**
- Migration adding `customPeriodText` as a `jsonb` column on `package`, typed as the platform's `Translation`
  structure so the override is per-language rather than one string.
- Added it to `CreatePackageDto` with a `Transform` that parses a JSON string when the value arrives as one —
  necessary because package creation is a multipart form submission — and to `PackageResponse` with
  `@ValidateNested()`.
- Added per-language inputs (English and Norwegian) to `PackageSettings` and the package creation form, with
  their own validation error slot (`customPeriodErrorText`).
- Tagged the package detail page's fetch with `productPackagesTag` so an edited override appears immediately
  rather than being served from cache.

**Impact.** Handed control of customer-facing pricing copy to the tenants who own the commercial
relationship, in both supported languages. The `jsonb`/`Translation` choice is what makes it correct for a
Nordic multi-language platform — a plain `varchar` would have forced tenants to pick one language.

---

### 62. #4303 — Dashboard subscription chart consolidated onto the shared stats view
`Refactor / tech debt` · `Analytics & reporting` · **Impact 3.9/10** · **Production hotfix**
**Merged** 2025-12-30 · **Base** `subrite/main` (hotfix) · 4 files, +18/-842, 1 commit

**Problem.** The dashboard's historical-subscriptions card carried **three** separate bespoke chart
implementations — `ChartAreaInteractive`, `HistoricalSubscriptionsChartBar` and
`HistoricalSubscriptionsChartLine` — each with its own Recharts setup, memoisation and card layout. The
"show more" behaviour was broken (see #4309), and fixing anything meant fixing it three times in three places
that had already drifted apart.

**Root cause.** Each chart variant had been added by copying the previous one, so the dashboard accumulated
three near-duplicate implementations of the same thing while a shared `StatsView` component already existed
elsewhere in the codebase.

**What I changed.**
- Deleted all three bespoke chart components (~620 lines) and pointed the card at the existing shared
  `StatsView`, driven by `GraphType` / `StatsViewType` so the chart type is a parameter rather than a
  separate component.
- Kept the card shell and improved the loading skeleton to take translations, so it no longer rendered
  untranslated placeholder text.

**Impact.** **+18/−842** — a net removal of 824 lines from a production hotfix. Three divergent chart
implementations became one shared component, which is what made the follow-up dashboard fixes (#4300, #4309,
#4304, #4288) single-place changes instead of triple ones. Worth noting as a case where the right fix was to
delete code.

---

### 63. #5044 — E-invoice payment settings were hardcoded rather than configurable
`Bug fix` · `Payments & providers` · **Impact 3.8/10**
**Merged** 2026-06-03 · **Linear** [SUBRITE-158](https://linear.app/subrite/issue/SUBRITE-158) · **Base** `develop` · 12 files, +254/-37, 15 commits

**Problem.** The e-invoice/Avtalegiro payment settings shown in the admin UI were hardcoded display values.
Tenants saw numbers — due days, activation policy, deactivation policy — that looked like their configuration
but were the same constants for everyone, and could not be changed. Since these values determine when
invoices fall due and when a subscription deactivates for non-payment, tenants were shown incorrect
information about their own billing behaviour.

**Root cause.** `PaymentConfigService` always wrote `DEFAULT_EINVOICE_PAYMENT_PREFERENCE` on creation and the
settings DTO explicitly **omitted** `eInvoicePaymentPreference`
(`PartialType(OmitType(PaymentProviderSettingsResponse, ['eInvoicePaymentPreference']))`) — so the field could
not be submitted even if the UI had offered it. The API contract ruled the capability out.

**What I changed.**
- Removed the `OmitType` exclusion so `eInvoicePaymentPreference` became part of the settings DTO, and made
  the service respect a submitted value: `createPaymentProviderSettingsDto.eInvoicePaymentPreference ?? DEFAULT_...`
  — preserving the default only when nothing is supplied.
- Added validation on the preference model itself (`@IsEnum(INITIAL_ACTIVATION)`,
  `@IsEnum(DEACTIVATION_POLICY)`, `@IsInt()`, `@Min(0)`), so a negative due-day count is rejected at the
  boundary rather than producing nonsensical billing dates.
- Added `@ValidateNested()` on the response DTO and forwarded relevant updates to tenant settings.
- Built the editable UI in `PaymentMethodsFrom` with per-field error mapping, and added two frontend
  utilities: a fix to `getFieldErrors` (which previously overwrote `keyValue` on each iteration, so only the
  last field error survived — now it accumulates) and `stripNestedFieldPrefix`, which maps nested DTO error
  paths like `eInvoicePaymentPreference.dueDaysAfterOrder` back to the flat form field names.
- Disabled updates for providers where the settings do not apply, and added service tests for
  preference-forwarding.

**Impact.** Turned a misleading read-only display into working per-tenant configuration of billing timing.
The incidental `getFieldErrors` fix is worth noting on its own: it had been silently discarding all but the
last field error across every form using it.

---

### 64. #4419 — Tenants could not change the checkout call-to-action wording
`Feature` · `Checkout` · **Impact 3.8/10**
**Merged** 2026-01-27 · **Linear** SUBRITE-3755 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 21 files, +561/-12, 16 commits

**Problem.** The checkout button said the same thing for every tenant and every product. A newspaper selling
a subscription, a podcast selling membership and a shop selling a one-off order all need different words on
the button customers click to pay — and that wording measurably affects conversion.

**Root cause.** The label came from platform translations with no override at either tenant or package level.

**What I changed.**
- Migration adding `orderCtaLabel` as `jsonb` to **both** `package` and `preference`, giving a two-level
  override: a tenant-wide default in preferences, and a per-package override where a specific product needs
  different wording. Package-level takes precedence.
- Used the `Translation` structure again, so each override carries its Norwegian and English forms.
- Threaded it through `CreatePackageDto`, `UpdatePackageDto`, `PackageService`, the preference entity and
  `TenantPreferenceUpdateDto`.
- Added the editing UI to the theme/checkout settings page and the package forms.

**Impact.** Gave tenants control of the single highest-leverage piece of copy in the purchase flow. The
design decision that makes it useful is the two-level fallback — a tenant sets their wording once and only
overrides it for the products that genuinely differ, rather than having to configure every package.

---

### 65. #2952 — Payment provider settings surfaced in the admin panel
`Feature` · `Admin UI` · **Impact 3.7/10**
**Merged** 2025-04-15 · **Linear** SUBRITE-2692 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 19 files, +991/-8, 34 commits

**Problem.** Tenants had no visibility of their own payment-provider configuration. Which providers were
enabled and how they were configured was only knowable by asking Subrite staff, so every question about
payment setup was a support request.

**Root cause.** Feature gap: no read API for provider settings and no admin UI for them.

**What I changed.**
- Added `PaymentConfigService.getPaymentProviderSettings()` and a `PaymentProviderSettingsResponse` DTO that
  maps each provider's configuration model (`StripeConfig`, `VippsConfig`, `NetsConfig`, `OrbytConfig`) into a
  consistent response shape — necessary because each provider's configuration is genuinely different.
- Extended `PaymentProviderController` with list and by-id endpoints, throwing `NotFoundException` for an
  unknown id.
- Built the admin UI: a payment-methods list, a per-provider detail page, and proper Next.js route
  companions — `loading.tsx` using the shared `NavProgressBar` and `not-found.tsx` using the shared
  `SubriteNotFound` — so navigation and missing ids behave like the rest of the panel rather than hanging or
  showing a raw error.

**Impact.** Made payment configuration self-service and visible, removing a recurring class of support
question. This is the first of the payment-settings series — #3242 made the settings editable, #3736 removed
the dead fields, and #5044 fixed the hardcoded e-invoice values — so it established the shape the rest built
on.

---

### 66. #3242 — Payment provider settings made editable
`Feature` · `Admin UI` · **Impact 3.7/10**
**Merged** 2025-06-02 · **Linear** SUBRITE-2994 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 20 files, +1105/-226, 14 commits

**Problem.** The follow-up to #2952: tenants could now *see* their payment configuration but not change it.
Rotating a Stripe key, updating a Vipps client secret or correcting a merchant id still required Subrite
staff, which is both an operational bottleneck and a poor position for a self-service platform — key rotation
is a security operation that should not depend on a support ticket.

**Root cause.** No update endpoint, and no way to express the update in a single API contract: each provider
has a structurally different configuration, so one flat DTO could not describe all of them.

**What I changed.**
- Added an update endpoint with `UpdatePaymentProviderConfigDto` and `PaymentProviderSettingsDto`.
- Solved the polymorphic-configuration problem in the API contract with `@ApiExtraModels(StripeConfig,
  VippsConfig, NetsConfig, OrbytConfig)` and `getSchemaPath()`, so the generated OpenAPI describes each
  provider's real configuration shape rather than an unhelpful generic object — meaning the frontend's
  generated client is correctly typed per provider.
- Added a `ProviderConfigKey` model and a `cleanObject()` utility stripping `undefined`/`null` entries, so a
  partial update leaves unspecified credentials untouched instead of blanking them — important when a secret
  is write-only and not returned to the client for round-tripping.
- Moved `payment-provider-settings-response.dto.ts` from the `product` module to `payment`, where it belongs.
- Built the editing UI.

**Impact.** Made payment-provider credential management self-service, including key rotation. The two
notable pieces of engineering are the discriminated OpenAPI schema — which gives the frontend real types per
provider — and `cleanObject`, without which a partial settings update would have destroyed credentials the
form never displayed.

---

### 67. #4300 — Dashboard chart rebuilt on the shared stats view
`Improvement` · `Analytics & reporting` · **Impact 3.7/10** · **Production hotfix**
**Merged** 2025-12-30 · **Base** `subrite/main` (hotfix) · 2 files, +197/-7, 2 commits

**Problem.** The historical-subscriptions dashboard card needed to present subscription trends over selectable
periods, with the year-over-year comparison operators actually use. Its bespoke chart could not do it, and its
loading skeleton was a bare grey rectangle with untranslated text.

**Root cause.** The card was built on a one-off chart component rather than the shared `StatsView` used by the
platform's other statistics screens, so it did not inherit period selection, comparison or consistent styling.

**What I changed.**
- Rebuilt the card on the shared `StatsView`, driven by `GraphType`, `StatsViewType` and a `YEAR_BASE_VALUE`
  for the year-over-year comparison, with `useMemo` for the derived series so period switching does not
  recompute unnecessarily.
- Added a `className` pass-through to `StatsView` (via `cn`) so a consumer can adjust chart height without the
  component hardcoding it for everyone.
- Replaced the grey-rectangle skeleton with one that takes `tStatistics` translations, so the loading state is
  localised.

**Impact.** Brought the dashboard's main chart in line with the rest of the platform's statistics UI, with
period selection and year-over-year comparison. Together with #4303 this replaced three bespoke chart
implementations with one shared component — the reason the subsequent dashboard fixes were each a single small
change.

---

### 68. #4287 — Manually priced products lost their price on order creation
`Bug fix` · `Admin UI` · **Impact 3.7/10**
**Merged** 2026-01-02 · **Linear** SUBRITE-2871 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 17 files, +681/-50, 24 commits

**Problem.** When an operator created an order in the admin panel and set a manual (negotiated or discounted)
price on a product, that price was not carried through to the created subscription — the standard catalogue
price was used instead. The customer was billed the wrong, higher amount, and the operator had no indication
the override had been discarded.

**Root cause.** Manual pricing was only representable on *optional* products. There was no
`MainProductWithManualPrice` type, so a manual price on the main product had nowhere to travel in the request,
and the recurring/total price calculations always derived from catalogue prices rather than checking for
overrides.

**What I changed.**
- Added a `MainProductWithManualPrice` DTO and threaded it through `FakturaPackageSubscriptionDto`, the
  faktura transaction and `MemberPackageService`, so a manual main-product price survives the whole path.
- Extracted three shared helpers in `order-utility.ts` — `hasManualPrices()`,
  `calculateRecurringPriceFromProducts()` and `calculateTotalPriceFromProducts()` — so the totals are computed
  once from whichever prices actually apply, instead of each caller re-deriving them from the catalogue.
- Added `areManualPricesValid()` and validation on the optional-product DTO (`@IsNumber()`, `@IsOptional()`),
  so an invalid override is rejected rather than silently ignored.
- Updated the admin `AddNewOrder` main-product and optional-product components to submit the override.

**Impact.** Stopped negotiated prices being silently replaced by catalogue prices at the moment of order
creation — an overcharge the operator could not see. Extracting the price calculation into shared, reusable
helpers is what made the fix hold across the faktura, e-invoice and subscription paths rather than only the
reported one.

---

### 69. #3194 — Communication settings consolidated into the admin panel
`Feature` · `Communication & notifications` · **Impact 3.7/10**
**Merged** 2025-05-21 · **Linear** SUBRITE-2938 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 42 files, +1960/-47, cross-stack

**Problem.** Tenants had no self-service control over their communication configuration — system sender
details, newsletter behaviour and related settings were not exposed in the admin panel, so changes required
Subrite staff.

**Root cause.** Feature gap. Communication configuration existed in the data model but had no administrative
surface.

**What I changed.**
- Built the communication settings area of the admin panel across 42 files: the settings pages, forms and
  save actions, together with the API endpoints and DTOs behind them.
- Added test coverage for the new backend surface, and English/Norwegian translations for the UI.

**Impact.** Moved another category of configuration from support-mediated to self-service. Part of the
sustained 2025 push to make tenant settings manageable by tenants — alongside #2949, #2959, #3119, #3143 and
#3214 — which collectively removed a large recurring support load.

---

### 70. #3119 — Theme settings for tenant-specific branding
`Feature` · `Widgets & branding` · **Impact 3.7/10**
**Merged** 2025-05-22 · **Linear** SUBRITE-2888 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · **73 files**, +4535/-21, cross-stack

**Problem.** Tenants could not control the visual appearance of their customer-facing surfaces — checkout, My
Page, emails. Every tenant looked like the platform default unless Subrite staff intervened, which for media
brands whose identity is their product is a serious limitation.

**Root cause.** Feature gap: no theme configuration model and no administrative surface for it.

**What I changed.**
- Built theme settings end-to-end across 73 files — the second-largest single delivery here — covering the
  configuration model and API, the admin settings pages and forms, and the application of theme values to
  customer-facing rendering.
- Added test coverage and English/Norwegian translations.

**Impact.** Gave tenants control of their own branding, a prerequisite for selling to media companies whose
brand identity is central to their offer. This work led directly into the brand-kit programme (#3213, #3509),
which extended the same idea from one theme per tenant to multiple brands per tenant.

---

### 71. #3522 — Invoices showed the tenant's display name instead of its legal company name
`Bug fix` · `Invoicing & accounting` · **Impact 3.6/10**
**Merged** 2025-07-24 · **Linear** SUBRITE-3111 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 11 files, +58/-7, 10 commits

**Problem.** Generated invoices — both the PDF and the EHF/BIS3 electronic document — used the tenant's
display name as the supplier. A tenant's display name is its brand ("Psykologisk"), not its registered legal
entity ("Psykologisk Forlag AS"). An invoice must name the legal entity: it is a financial document, and for
EHF the supplier registration name is validated against the organisation register, so the wrong name can cause
outright rejection.

**Root cause.** No separate legal company name existed. `tenant.name` was the only name available, so the
invoice generator and the BIS3 builder both used it for the supplier `partyName` and `registrationName`.

**What I changed.**
- Migration `CopyTenantNameToCompanyName` introducing `companyName` on the tenant address and backfilling it
  from `tenant.name`, so every existing tenant had a valid value immediately — the invoice generator could not
  be allowed to encounter a null supplier name.
- Added `companyName` to `AddressInfo` and `tenantCompanyName` to `OrderInvoiceDto`.
- Changed the EHF builder to use `tenantAddress.companyName` for both
  `accountingSupplierParty.party.partyName.name` and `partyLegalEntity.registrationName`, and the PDF
  generator to render `data.tenantCompanyName`.
- Added the field to the admin company-details form with help text distinguishing the display name from the
  legal name — so tenants understand which is which rather than guessing.
- Updated `ADDING_TENANTS.md` so tenant provisioning sets `companyName`, defaulting to `tenant.name` when
  there is no separate legal name.

**Impact.** Made invoices legally correct — a compliance requirement for Norwegian invoicing, and a
concrete cause of EHF rejection when wrong. Backfilling in the same migration meant no tenant was ever in a
state where invoice generation could fail on a missing name.

---

### 72. #5237 — Subscription-switch wizard lost hand-edited seat maps on token refresh
`Bug fix` · `Admin UI` · **Impact 3.6/10**
**Merged** 2026-07-03 · **Linear** [SUBRITE-401](https://linear.app/subrite/issue/SUBRITE-401) (High) · **Base** `develop` · 8 files, +625/-87, 36 commits

**Problem.** In the multi-step subscription-switch wizard, an auth-token refresh — or simply switching browser
tab and back, which triggers a refresh on focus — silently reset optional-product quantities and discarded the
seat-transfer map the operator had built by hand. On a large group subscription that could be many minutes of
work, destroyed with no warning and no way to recover it.

**Root cause.** The third instance of the same pattern as #5170 and #5185. `OptionalProduct` held its own
state initialised from props, and the wizard re-derived its baseline whenever the access token changed. A
token refresh therefore looked identical to a fresh mount and re-initialised everything.

**What I changed.**
- Made the wizard's `OptionalProduct` fully controlled: removed its internal `useState` and `useEffect`,
  taking `quantity` as a prop.
- Prevented plan details and optional-product values from being reset on session refresh or window-focus
  events, so a token change no longer counts as a reason to rebuild state.
- Extracted `getMinOptionalProductQuantity` as a shared utility and reused the
  `hasInvalidOptionalProductQuantity` helper introduced in #5170, so the minimum-quantity rule is defined once
  across all three forms instead of three times.
- Made the minimum-quantity rule visible to the operator for required optional products, and added
  shipping-country updates for physical items.
- Added a dedicated regression test — `SubscriptionSwitch.tokenRefresh.test.tsx` — that changes the mocked
  access token mid-test and asserts state survives, plus an `OptionalProduct` suite (~280 test lines
  together).
- Reduced the repo ESLint threshold from 38 to 36.

**Impact.** Stopped silent loss of operator work on a wizard used for the platform's largest subscriptions.
The token-refresh regression test is the durable part: it pins the exact scenario that had broken three
different forms, so the pattern cannot quietly return.

---

### 73. #3032 — Consents could not default to subscribed
`Feature` · `Communication & notifications` · **Impact 3.5/10**
**Merged** 2025-04-16 · **Linear** SUBRITE-2709 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 21 files, +507/-181, 31 commits

**Problem.** Every consent — newsletter, marketing, service messages — started unsubscribed for new members,
with no per-consent control. Tenants had legitimate cases where a consent should default to subscribed
(a service-message consent that is part of the product, or a newsletter where the tenant has a lawful basis),
but the platform could not express it, so newsletters started from an empty list after every migration or
signup.

**Root cause.** `Consent` had no field describing its default state, and consent handling was spread across
`MemberService` with no dedicated owner, so adding the behaviour had nowhere natural to live.

**What I changed.**
- Migration `AddDefaultSubscribedColumnInConsent` adding `defaultSubscribed` (boolean, default `false`) — so
  existing consents keep their current behaviour and only explicitly configured ones change.
- Added it to the consent entity, DTO and repository, with a `Transform` handling the `'true'` string form
  since consent settings arrive via form submission.
- Extracted a dedicated `MemberConsentService` and moved consent resolution out of `MemberService`, wiring it
  into both `MemberController` and `ProfileController` so the admin and member-facing paths share one
  implementation.

**Impact.** Gave tenants per-consent control over the default subscription state, which is what makes consent
configuration usable for real communication strategies. Defaulting the new column to `false` meant no existing
consent silently became opt-in on deploy — the safe direction for anything consent-related. Extracting
`MemberConsentService` also gave consent logic a single home.

---

### 74. #4003 — Uploaded files with non-ASCII names failed to be delivered
`Bug fix` · `Shared platform code` · **Impact 3.5/10** · **Production hotfix**
**Merged** 2025-11-17 · **Linear** SUBRITE-3634 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/main` (hotfix) · 1 file, +34/-4

**Problem.** Images and files uploaded through the admin panel sometimes could not be served afterwards. The
upload appeared to succeed but the resulting asset was inaccessible — broken images in the admin UI and on
customer-facing pages.

**Root cause.** The S3 object key was built directly from the user's original filename:
`${dir}/${uuidv4()}_${file.originalname}`. Norwegian filenames routinely contain `æ`, `ø` and `å`, and
uploaded files often contain spaces, `#`, `?` and other characters that are unsafe in an S3 key or a URL. The
object was stored under a key that could not be reliably referenced afterwards — so the file existed but was
undeliverable.

**What I changed.**
- Added a `sanitizeFileName()` helper that splits the filename at the last dot to preserve the extension,
  replaces any character outside `[a-zA-Z0-9_-]` with a hyphen, collapses consecutive hyphens, trims leading
  and trailing hyphens, and falls back to `'file'` if sanitising leaves the name empty — so a filename of
  entirely non-ASCII characters still produces a valid key.
- Applied it at all three key-construction sites: the main upload path, the mock/test path, and the CSV export
  path.

**Impact.** Fixed broken asset delivery for the platform's Norwegian users, whose filenames trigger this by
default — an everyday failure rather than an edge case. Preserving the extension matters because content type
and rendering depend on it, and the empty-name fallback covers the case a naive sanitiser would break
entirely.

---

### 75. #4064 — Duplicate sender emails, null billing periods and logo scaling
`Bug fix` · `Communication & notifications` · **Impact 3.5/10** · **Production hotfix**
**Merged** 2025-11-24 · **Base** `subrite/main` (hotfix) · 9 files, +23/-33, 6 commits

**Problem.** A hotfix bundling three unrelated production issues, as noted in its `CHANGELOG.md` entry:
tenants could not create two communication senders sharing one email address (a legitimate need — the same
address with different display names for different brands); a member order list threw when a subscription had
no billing entity; and tenant logos rendered at low resolution.

**Root cause.** Three independent causes. `CommunicationSenderService` enforced uniqueness on `senderEmail`
per tenant, which was never a real constraint — the meaningful uniqueness is on sender *name*.
`BillingPeriodInfo`'s constructor required a non-null billing entity and dereferenced it unconditionally.
The logo used `h-full max-h-10 w-auto`, which sizes by height and lets width follow, producing a scaled-up,
soft image.

**What I changed.**
- Removed the `senderEmail` uniqueness checks from both the create and update paths, and corrected the API
  error declarations from `senderNameAlreadyExists, senderEmailAlreadyExists` to just
  `senderNameAlreadyExists` — so the contract matches the behaviour.
- Made `BillingPeriodInfo`'s parameter optional and used optional chaining throughout, so a subscription with
  no billing entity yields undefined fields instead of throwing.
- Changed the logo classes to `object-scale-down max-h-10 w-full` in both the brand-kit header preview and the
  member-facing `TenantIntro`, fixing it in both places rather than only the reported one.

**Impact.** Unblocked a legitimate multi-brand sender configuration, removed a crash from the member order
list, and fixed logo rendering. Listed with an honest note: this is three small fixes shipped together
because they were all needed in production at once, not one substantial change — which is why it sits here
rather than higher.

---

### 76. #4292 — Soft bounces recorded as hard failures
`Bug fix` · `Communication & notifications` · **Impact 3.5/10**
**Merged** 2025-12-29 · **Linear** SUBRITE-3758 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 12 files, +132/-80, 11 commits

**Problem.** Follow-up to #3992. Soft bounces — a full mailbox, a temporarily unreachable server — were being
handled as though they were hard bounces. A soft bounce is transient and the address remains valid, so
treating it as permanent means suppressing a contactable member and reporting list quality as worse than it is.

**Root cause.** SendGrid distinguishes bounce types through a `bounce_classification` field
(`Invalid Address`, `Technical`, `Content`, `Reputation`, …), which the webhook DTO did not model. Without it,
both bounce kinds looked the same to the classifier, so the more severe interpretation won.

**What I changed.**
- Added `SendGridBounceClassification` to the webhook DTO, modelling SendGrid's actual classification values.
- Reworked event classification in `communication-log.service.ts` and the shared
  `communication/utils/index.ts`, replacing the previous `updateCommunicationLogWithEvent` handling so a soft
  bounce is recorded as a soft bounce and only genuinely permanent classifications count as hard.
- Added `relations: { events: true }` to the communication-log repository query, so classification can
  consider the log's existing events rather than deciding on the incoming one in isolation — necessary because
  SendGrid events arrive out of order.
- Surfaced the distinction in the member communication-activities response, the admin communication table and
  the shared `StatusBadge`, so operators can see the difference between "temporarily undeliverable" and
  "permanently dead".

**Impact.** Stopped contactable members being treated as permanently undeliverable, which both preserved
reach and made list-quality reporting accurate. Modelling SendGrid's classification explicitly is what turned
an unreliable heuristic into a correct one.

---

### 77. #4296 — Consents could not be ordered or targeted at the right audience
`Feature` · `Communication & notifications` · **Impact 3.5/10**
**Merged** 2026-01-15 · **Linear** SUBRITE-3711 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 28 files, +942/-99, 22 commits

**Problem.** Consents appeared to members in an order the tenant could not control — sorted by `purpose`,
effectively arbitrary — and every consent was shown to everyone. Tenants needed some consents visible only to
paying subscribers and others only to non-subscribers, and needed the most important one to appear first.
Consent presentation directly affects opt-in rates and therefore the tenant's ability to communicate legally.

**Root cause.** `Consent` had neither an ordering field nor an audience field, and the repository ordered by
`purpose: 'ASC'` — a semantic field being used as a sort key because nothing better existed.

**What I changed.**
- Migration `AddSortOrderAndVisibilityToConsent` adding `sortOrder` (`integer NOT NULL DEFAULT 0`) and
  `visibility`, with a `ConsentVisibility` enum of `SUBSCRIBER | NON_SUBSCRIBER | ALL` — defaults chosen so
  existing consents keep behaving as before.
- Changed the repository ordering to `sortOrder ASC, id ASC` — the `id` tiebreak matters, because equal sort
  orders would otherwise produce a non-deterministic list that changes between page loads.
- Added `PUT bulk-sort-order` with a validated `BulkUpdateConsentSortOrderDto`
  (`@IsArray`, `@ValidateNested`, `@IsInt`, `@Min`), so a drag-and-drop reorder is one atomic request rather
  than one request per consent — which would have left a partially reordered list on failure.
- Threaded visibility filtering through the consent service so members see only consents applicable to them,
  and built the admin ordering and visibility UI.

**Impact.** Gave tenants real control over how consents are presented and to whom, which is what determines
opt-in rates and hence lawful reach. The bulk atomic reorder endpoint and the deterministic `id` tiebreak are
the details that make the ordering trustworthy rather than approximately right.

---

### 78. #3792 — Subscription rules settings would not save
`Bug fix` · `Admin UI` · **Impact 3.4/10** · **Production hotfix**
**Merged** 2025-09-29 · **Linear** SUBRITE-3382 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/main` (hotfix) · 1 file, +66/-28

**Problem.** The subscription-rules settings form in the admin panel did not save. These rules control whether
members can buy new or gift subscriptions from My Page and whether they may buy the same package or multiple
subscriptions — so tenants could not configure their own purchasing policy.

**Root cause.** The form read its values straight from `tenant.preference` on each render and had no
form state of its own, so user changes were not held anywhere to be submitted. The select options were also
rebuilt on every render with no placeholder option, so the two permission-level dropdowns had no representation
of "not yet chosen".

**What I changed.**
- Introduced a typed `SubscriptionRulesFormData` interface and real `useState` form state initialised from
  `tenant.preference`, so edits are captured and can be submitted.
- Built both permission-level option lists with `useMemo`, each including an explicit empty-value placeholder
  (`selectPermissionLevelForBuyingSamePackage`, `selectPermissionLevelForBuyingMultipleSubscriptions`) so an
  unset value is representable, and derived the options from `Object.values(...)` of the enums rather than a
  hand-maintained list.

**Impact.** Restored a settings form that was entirely non-functional — tenants had no way to configure
subscription purchasing rules at all. Typing the form data and deriving options from the enums means a new
permission level appears in the UI automatically instead of being silently missing.

---

### 79. #4341 — Upgrade/downgrade priced manually-priced subscriptions from the catalogue
`Bug fix` · `Subscriptions & billing lifecycle` · **Impact 3.4/10**
**Merged** 2026-01-08 · **Base** `subrite/develop` · 12 files, +192/-72, 10 commits

**Problem.** When a subscription on a manually set (negotiated) price was upgraded or downgraded, the new
price was calculated from catalogue prices, discarding the customer's agreed pricing. The same defect appeared
in the member-facing change-products flow on My Page. Companion to #4287, which fixed the same class of
problem at order creation.

**Root cause.** `getPriceByQuantity` had no way to know which subscription it was pricing for. It received
only the plan and the selected products, so it could only use `product.price` from the catalogue — the
member's own `memberPackageProduct.price` was not available to it. `MemberPackageService` compounded this by
reading `memberPackProduct.product.price` instead of the stored `memberPackProduct.price`.

**What I changed.**
- Added an optional `memberPackageId` to `SubscriptionPlanPriceByQuantityDto` and threaded it through the
  controller into `SubscriptionPlanService`, so the calculation can look up what this member actually pays.
- Reworked the pricing to build a `Map<number, { recurringPrice: number }>` of the member's existing product
  prices and prefer those over catalogue prices, falling back to the catalogue only where the member has no
  stored price.
- Fixed `MemberPackageService` to read `memberPackProduct.price` rather than `memberPackProduct.product.price`,
  and added `price: true` to the relevant select so the field is actually loaded.
- Passed `memberPackageId` from both call sites — the admin upgrade/downgrade component and the member-facing
  `SubscriptionChangedProducts` — so admin and self-service agree.

**Impact.** Stopped negotiated prices being silently replaced by catalogue prices when a subscription changed
plan — the customer would have been billed more than agreed, with no visible indication. Passing the member
package into the price calculation is the structural fix: without it the function had no way to be correct.

---

### 80. #5133 — Frontend dependency upgrade backlog cleared
`Refactor / tech debt` · `Tooling, deps & docs` · **Impact 3.4/10**
**Merged** 2026-06-12 · **Linear** [SUBRITE-284](https://linear.app/subrite/issue/SUBRITE-284) (High) · **Base** `develop` · 4 files, +14721/-12871, 10 commits

**Problem.** `frontend/.ncurc.json` held a growing list of packages excluded from the nightly automatic
dependency upgrade. Each pin was there because upgrading required a code migration nobody had done, and none
recorded *why* — so the list only ever grew, and the frontend drifted further from current versions, including
security patches.

**Root cause.** Pinning is the cheap response to a breaking upgrade. Without a recorded rationale each pin
becomes permanent, because the next person cannot tell whether it is still needed.

**What I changed.**
- Unpinned four packages and performed the migrations each required, rather than deferring again:
  - **`typeorm` 0.3.30 → 1.0.0** — v1 removed the `string[]` form of `relations`/`select`; migrated six call
    sites in `src/server/getTenant.ts` and `src/server/nextauth/typeorm/index.ts` to the object form.
  - **`mjml` ^4.18 → ^5.3** — `mjml2html()` became async in v5; the single call site (the OTP email in
    `getNextAuthOptions.ts`) now awaits it.
  - Plus `next-sanity` and `eslint-plugin-react-hooks`.
- Documented the rationale for **every remaining pin**, so the list is now auditable and the next person knows
  what work each one represents.
- The large line counts are the regenerated `package-lock.json`; the hand-written change is small and
  targeted.

**Impact.** Reduced the frontend's excluded-upgrade list and brought a major ORM version and the email
templating library up to date, so the nightly automation can keep them current instead of permanently
skipping them. Documenting the remaining pins is what stops the list resuming its growth. A follow-up (#5260)
cleared two more.

---

### 81. #2899 — Payment export missing company name and organisation number
`Bug fix` · `Members, segments & import/export` · **Impact 3.3/10**
**Merged** 2025-03-10 · **Linear** SUBRITE-2606 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 1 file, +6/-0

**Problem.** The payment report export omitted the paying member's company name and organisation number. For
business customers those two fields are what let a finance team reconcile a payment against a company, so the
export could not be used for its main purpose.

**Root cause.** `PaymentExportResponseDto` simply did not declare the fields. The data existed on the member;
the export DTO had never been extended to include it.

**What I changed.**
- Added `companyName` and `organizationNo` to `PaymentExportResponseDto`, each marked `@Exportable()` so the
  export framework picks them up as columns, and populated them from `payment?.member?.` with optional
  chaining — private members have neither field, so unguarded access would have thrown on every personal
  payment in the report.

**Impact.** Made payment reports usable for B2B reconciliation. Six lines, but the export was previously
unfit for the task it existed to serve. This is the earliest PR in the portfolio, from the first week.

---

### 82. #3317 — Invoice PDF generation crashed on styling fields
`Bug fix` · `Invoicing & accounting` · **Impact 3.2/10** · **Production hotfix**
**Merged** 2025-06-13 · **Base** `subrite/main` (hotfix) · 2 files, +14/-3

**Problem.** Invoice PDF generation failed for tenants whose custom invoice-field configuration omitted style
information. Since this is the code path that produces the actual invoice document sent to customers, a
failure means the invoice cannot be issued at all.

**Root cause.** The PDF generator dereferenced `property.style.fontSize`, `property.style.color` and
`property.style.fontFamily` without checking that `style` existed. `style` is optional in a custom invoice
field, so any field configured without it threw during rendering. `parseInt(undefined)` would also have
produced `NaN` as a font size even if the access had been safe.

**What I changed.**
- Added optional chaining on all three accesses (`property.style?.…`).
- Gave `fontSize` a real fallback to the document's own `styles.text.fontSize?.toString()` rather than letting
  `parseInt` receive `undefined`, so an unstyled field renders at the default size instead of producing `NaN`.
- Kept the existing colour and font-family fallbacks.
- Also extended `ADDING_TENANTS.md` with the podcasts My Page card settings needed for new tenant
  provisioning.

**Impact.** Restored invoice PDF generation for affected tenants — with this failing, no invoice document can
be produced. Falling back to the document's own default font size rather than a hardcoded number means
unstyled fields look consistent with the rest of the invoice.

---

### 83. #3373 — E-invoice preferences overwritten by client-supplied values
`Bug fix` · `Payments & providers` · **Impact 3.2/10**
**Merged** 2025-06-26 · **Linear** SUBRITE-3058 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 7 files, +50/-75, 6 commits

**Problem.** When a tenant created or updated an EHF/e-invoice payment method, the e-invoice payment
preferences — due days, activation and deactivation policy — came from whatever the client sent. The frontend
carried its own `defaultEhfPreferences` constant, so backend and frontend each had their own idea of the
defaults and they could disagree. Values controlling when invoices fall due and when subscriptions deactivate
for non-payment were effectively client-determined.

**Root cause.** `PaymentConfigService` wrote `createPaymentProviderSettingsDto.eInvoicePaymentPreference`
directly from the request, and the DTO accepted the field, so there was no server-side authority over billing
policy defaults.

**What I changed.**
- Made the server authoritative: `PaymentConfigService` now writes
  `DEFAULT_EINVOICE_PAYMENT_PREFERENCE` from a shared backend constant.
- Excluded the field from the settings DTO —
  `PartialType(OmitType(PaymentProviderSettingsResponse, ['eInvoicePaymentPreference']))` — so it cannot be
  submitted at all.
- Removed the frontend's duplicate `defaultEhfPreferences` constant, eliminating the second source of truth.

**Impact.** Moved billing-policy defaults from client-supplied to server-controlled, with one definition
instead of two that could drift. Worth reading together with #5044: that PR later re-opened the field
deliberately, with proper validation and a real editing UI. This PR closed an unintended write path; #5044
added an intended one — the sequence is correct in that order.

---

### 84. #3787 — Imports had no reference, so a batch could not be traced or undone
`Feature` · `Members, segments & import/export` · **Impact 3.2/10**
**Merged** 2025-10-24 · **Linear** SUBRITE-2822 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 17 files, +125/-26, 40 commits

**Problem.** Imported records carried nothing identifying which import created them. When a tenant onboarding
import went wrong — wrong column mapping, duplicate file, stale data — there was no way to find the affected
members afterwards, so cleaning up meant guessing from timestamps.

**Root cause.** `import_history` had no external reference, and imported members were not linked back to the
batch that created them. Traceability had simply never been built.

**What I changed.**
- Migration `AddTagOnImportHistory` adding a `reference` column (`varchar(255)`), with a **unique index on
  `(reference, tenantId)`** so a reference identifies exactly one import within a tenant.
- Added `generateImportReference()` to the repository — 12 hex characters from `crypto.randomBytes` — chosen so
  the reference is short enough for a human to quote in a support conversation while still being collision-safe.
- Threaded the reference through `SaveImportHistoryJobData`, the `DataImportConsumer` job payload and
  `MembersImportService`, using `jobData.reference ?? generate…()` so a caller can supply its own reference or
  get a generated one.
- Stamped it onto imported members and exposed it in `ImportHistoryResponse` so it is visible in the admin
  import history.

**Impact.** Made imports traceable and reversible: given a reference, every record from a bad batch can be
found. In a platform where onboarding a new tenant means bulk-importing their members, that difference decides
whether a mistaken import is recoverable or a manual cleanup.

---

### 85. #3803 — Communication senders could not be removed
`Feature` · `Communication & notifications` · **Impact 3.1/10**
**Merged** 2025-10-14 · **Linear** SUBRITE-2805 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 26 files, +685/-84, 24 commits

**Problem.** A communication sender, once created, could not be deleted. Senders accumulated — typos,
decommissioned addresses, senders for brands the tenant no longer ran — and all of them stayed in the
selection list. Hard deletion was not an option either: historical communications reference their sender, so
deleting one would orphan the audit trail.

**Root cause.** The sender model had only a boolean `isApproved`. A boolean cannot express three states —
awaiting approval, approved, retired — so "retired" was unrepresentable and no removal mechanism could be
built on it.

**What I changed.**
- Migration `RenameIsApprovedToStatusOnCommunicationSender` replacing the boolean with a
  `COMMUNICATION_SENDER_STATUS` enum of `PENDING | APPROVED | ARCHIVED`, converting existing rows so
  approved/unapproved senders kept their meaning.
- Added `PATCH :id/archive` with a `CommunicationSenderArchiveDto`, so retiring a sender is an explicit state
  transition that preserves the historical record rather than a destructive delete.
- Added `@ValidateIf((o) => o.communicationChannel === COMMUNICATION_CHANNEL.EMAIL)` to the create DTO, so
  email-only fields are validated only for email senders — SMS senders have no email address.
- Added a `replyTo` field to the response DTO.
- Fixed the shared `TransformToNumber` decorator, which converted any string with `Number()` — so the literal
  string `'null'` became `NaN`. It now returns `null` for `'null'` and only converts genuine numeric strings
  via `isNumberString`. This affected every DTO using the decorator, not just senders.
- Added controller tests covering create → approve → archive.

**Impact.** Gave tenants a way to retire senders without destroying the communication audit trail — the
reason a soft archive was the right design rather than a delete. Replacing the boolean with an enum removed the
modelling constraint that had blocked the feature, and the `TransformToNumber` fix corrected a latent
`NaN`-producing bug across the whole codebase.

---

### 86. #4888 — Member imports failed on absent optional columns and BOM-prefixed headers
`Bug fix` · `Members, segments & import/export` · **Impact 3.1/10**
**Merged** 2026-04-28 · **Linear** [SUBRITE-38](https://linear.app/subrite/issue/SUBRITE-38) (Medium) · **Base** `develop` · 5 files, +250/-31, 8 commits

**Problem.** Member imports rejected files that omitted optional member or address columns, and files whose
first header was prefixed with a byte-order mark — which is what Excel produces when saving CSV as UTF-8, the
normal way a customer exports their data. A tenant's perfectly reasonable file failed with an unhelpful error.

**Root cause.** Header keys were used verbatim. A BOM (`﻿`) or a stray control character meant the first
column name never matched the expected field, so the importer concluded a required column was missing. Address
fields were also not modelled as optional in the import path.

**What I changed.**
- Added `normalizeHeaderKey()` to the shared `TabularDataTransformPipe`, stripping a leading BOM
  (`/^﻿/`), removing control characters (`/[\x00-\x1F]+/g`) and trimming — applied to every header before
  matching, so the fix covers all tabular imports rather than just members.
- Used `MemberAddressDto` in `MembersImportService` so address fields are properly optional.
- Extended `UpdateMemberDto` and the frontend `CSVFileUploader` accordingly.

**Impact.** Fixed a class of import failure that hit customers using the most common CSV export path. Putting
the header normalisation in the shared pipe meant every import type benefited, and it is invisible when a file
is already clean.

---

### 87. #4903 — Empty CSV columns and untrimmed identifiers broke member imports
`Bug fix` · `Members, segments & import/export` · **Impact 3.1/10**
**Merged** 2026-04-30 · **Linear** [SUBRITE-58](https://linear.app/subrite/issue/SUBRITE-58) (Medium · Bug) · **Base** `develop` · 5 files, +59/-15, 4 commits

**Problem.** Follow-up to #4888. Spreadsheets routinely contain trailing empty columns, which the parser
turned into headers named `__EMPTY`, `__EMPTY_1` and so on. The importer treated these as unrecognised fields
and rejected the file. Identifier values with surrounding whitespace also failed to match existing members, so
an update import created duplicates instead of updating.

**Root cause.** Empty columns were not distinguishable from real ones by name alone, and the identifier was
lower-cased but never trimmed — `" user@example.com"` did not match `"user@example.com"`.

**What I changed.**
- Added `isEmptyHeader(key)` — matching `''` and the `/^__EMPTY(_\d+)?$/` pattern — in **both** the backend
  `TabularDataTransformPipe` and the frontend `CSVFileUploader`, so phantom columns are dropped at parse time
  and the preview the operator sees matches what the backend will process.
- Added `.trim()` before `.toLowerCase()` on the identifier, so whitespace no longer causes a failed match and
  a duplicate member.
- Replaced the blanket `unsupportedFieldsExists` rejection with a `presentColumns` set, so the importer
  reasons about which columns are actually present rather than failing on any unexpected key.
- Added an `isCsvFile()` check accepting both the MIME type and a `.csv` extension, since browsers report CSV
  MIME types inconsistently.

**Impact.** Removed the remaining common reasons a genuinely valid customer file was rejected. The duplicate
-creation bug from untrimmed identifiers is the most consequential part: it silently produced wrong data
rather than failing, which is worse than an error.

---

### 88. #5006 — Login code emails claimed to come from the wrong address
`Bug fix` · `Auth & access control` · **Impact 3.1/10**
**Merged** 2026-06-02 · **Linear** [SUBRITE-69](https://linear.app/subrite/issue/SUBRITE-69) (High · Bug) · **Base** `develop` · 8 files, +98/-22, 10 commits

**Problem.** The auth code verification screen — shown while a member waits for a login code — told the user
which address to look for, and named the wrong one. It displayed the tenant's *contact* address rather than
the address the email was actually sent from. Members looking for an email from the stated sender could not
find it, on the login path.

**Root cause.** Two mismatched sources. The UI rendered `tenant?.tenantContact`, a general contact address
with no relationship to email sending. Meanwhile `getNextAuthOptions` sent from the
`DEFAULT_SENDER_EMAIL` environment variable, ignoring the tenant's configured email sender entirely — so even
the send itself was not using the tenant's sender.

**What I changed.**
- Made the send use the tenant's configured sender with a fallback:
  `tenant.emailSender?.senderEmail || getEnv('DEFAULT_SENDER_EMAIL')` — so mail comes from the tenant's own
  address where one is configured.
- Passed the same default through to the UI as an explicit `defaultSenderEmail` prop on `AuthCodeForm`, wired
  from all three code pages (`auth/code`, `auth/signin/code`, `auth/signup/code`) using `getOptionalEnv`, so
  the message reflects the actual sender rather than an unrelated contact field.
- Exposed the needed sender fields on `CommunicationSenderResponseDto`.

**Impact.** Fixed a wrong instruction on the login flow, where a confused user simply cannot get in. Making
the send prefer the tenant's configured sender also improved deliverability and brand consistency — the email
now comes from the tenant's own verified address rather than a platform default.

---

### 89. #3797 — Paid invoices left marked as undelivered
`Bug fix` · `Payments & providers` · **Impact 3.0/10**
**Merged** 2025-09-30 · **Base** `subrite/develop` · 1 file, +7/-1, 3 commits

**Problem.** Refinement of #3793. That PR stopped an invoice document being generated for an already-paid
order by returning early — but the early return left the invoice's `deliveryStatus` unset. The invoice was
correctly not sent, yet appeared undelivered, so it showed up in any view or process looking for invoices
awaiting delivery.

**Root cause.** The `if (isPaid) return;` guard skipped the send but also skipped the status update that
normally follows it, so the record did not reflect the decision that had been made.

**What I changed.**
- Replaced the bare early return with one that records the outcome first:
  `await this.manager.update(Invoice, invoice.id, { deliveryStatus: INVOICE_DELIVERY_STATUS.SENT_MANUALLY })`
  before returning — so the invoice is marked as handled out-of-band rather than left in limbo.

**Impact.** Closed the loop on #3793 so that skipping a send is recorded rather than merely happening. Seven
lines, and a good illustration that an early-return fix has to account for the bookkeeping it bypasses,
otherwise it trades a visible bug for an invisible data inconsistency.

---

### 90. #3891 — Dunning reminders sent for invoices that had been paid
`Bug fix` · `Automation & workflows` · **Impact 3.0/10**
**Merged** 2025-10-28 · **Linear** SUBRITE-3543 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 2 files, +9/-0, 3 commits

**Problem.** Payment-reminder emails were sent for invoices the customer had already paid. Chasing a customer
for money they have already sent is a direct trust problem, and it generates support contacts.

**Root cause.** The automation event that triggers the reminder — "e-invoice unpaid one day after sending" —
had no validation step. The `EINVOICE_SENDING_FAILED` case in `AutomationEventProcessor` fell through without
re-checking payment state, so the reminder fired on the schedule regardless of what had happened since.

**What I changed.**
- Added an explicit `case EINVOICE_SENDING_FAILED:` branch routing to
  `validateNewOrderEinvoiceUnpaid1DayAfterSent(payload)`, so the event is validated against current payment
  state immediately before the reminder is sent rather than trusting the state at scheduling time.

**Impact.** Stopped customers being chased for settled invoices. The pattern is the same one that appears in
several of the higher-ranked entries: any scheduled action must re-verify its precondition at execution time,
because the state it was scheduled against will sometimes have changed.

---

### 91. #3855 — Delivery-address export for physical fulfilment
`Feature` · `Members, segments & import/export` · **Impact 3.0/10**
**Merged** 2025-11-03 · **Linear** SUBRITE-3363 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 16 files, +908/-5, 17 commits

**Problem.** Tenants shipping physical products — printed magazines, merchandise — had no way to get a
delivery-address list out of the platform. Fulfilment houses need a file of who receives what, where, and
addresses were only visible one member at a time in the admin UI.

**Root cause.** The export module had no delivery-address export type. Nothing about the address data was
missing; the export pipeline simply had no report for it.

**What I changed.**
- Added `EXPORT_TYPE.DELIVERY_ADDRESS`, a `POST delivery-address` endpoint, and
  `exportDeliveryAddress` wired into the existing async `ExportReportConsumer` — so the report runs as a
  background job like the other exports rather than blocking a request.
- Added `DeliveryAddressExportFilterDto` filtering by subscription status, shipping provider, product and date,
  since a fulfilment run is always a subset (this month, this product, this carrier).
- Added `DeliveryAddressExportResponseDto` (~115 lines) using the existing `@Exportable()` /
  `@ExportableAsNumber()` decorators, and `COUNTRY_LOOKUP` so the file carries readable country names as well
  as codes — a fulfilment house cannot act on `NO`.
- Corrected `MemberPackageExportFilterDto`, where `paymentProvider` and `createdAt` were typed as required
  when the filters are genuinely optional.

**Impact.** Made physical fulfilment self-service, which is a precondition for selling print products on the
platform. Building it on the existing async export pipeline and `@Exportable()` decorators meant it inherited
job handling, delivery and formatting rather than reinventing them.

---

### 92. #4047 — Subscriptions with no stored card were never stopped
`Bug fix` · `Payments & providers` · **Impact 3.0/10**
**Merged** 2025-11-21 · **Linear** SUBRITE-3604 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 7 files, +53/-2, 12 commits

**Problem.** A subscription whose stored payment details had gone missing was never stopped. The recurring
charge failed in a way the dunning machinery did not recognise, so the subscription stayed active
indefinitely: the customer kept their access, no payment was ever collected, and the account never progressed
to deactivation. Free service, indefinitely, invisible.

**Root cause.** All three providers dereferenced `paymentInfo`/`cardInfo` without checking they were populated.
The resulting generic error was not identifiable by the queue consumers, whose failure handling recognised only
provider-specific errors (`Stripe.errors.StripeCardError`) — so the dunning path was never entered, and the
job failure produced no state change.

**What I changed.**
- Introduced a shared `ERROR_MESSAGES.PAYMENT_INFO_NOT_FOUND` constant so the condition has one identity
  across the codebase rather than being matched on message text in each place.
- Added explicit guards in the Stripe, Nets and Vipps providers —
  `if (!paymentInfo || Object.keys(paymentInfo).length === 0)` — logging the member package id and throwing the
  named error. The empty-object check matters: `paymentInfo` was often `{}` rather than null, which a plain
  null check would have missed.
- Extended all three recurrent-payment consumers to recognise that error and route it into
  `handleFailedRecurringPayment`, so a missing-card subscription now enters dunning and is eventually stopped
  like any other failed payment.

**Impact.** Closed a silent revenue leak where subscriptions with lost payment details continued to deliver
service for free forever. Fixing it consistently across all three card providers, with one shared error
identity, is why the fix holds rather than covering only the reported provider. Later refined by #4402 and
#4792.

---

### 93. #3640 — Tenant postal address not maintained on update
`Bug fix` · `Shared platform code` · **Impact 2.8/10** · **Hotfix branch**
**Merged** 2025-08-19 · **Base** `subrite/develop` · 2 files, +13/-0, 3 commits

**Problem.** The tenant's composed postal address — the single-line form used where a full address must be
rendered as text, such as on invoices and in email footers — was not regenerated when the tenant's address
changed. After an address edit, documents continued showing the old address.

**Root cause.** The composed string was stored rather than derived, and `TenantService`'s update path did not
recompute it. Nothing kept the derived value in step with its source.

**What I changed.**
- Added a shared `generatePostalAddress(address)` utility joining `line1`, `postCode`, `city` and `country`
  with `, `, filtering out empty, undefined and whitespace-only parts — so a partially filled address produces
  a clean string rather than one with stray commas.
- Called it from the tenant update path so the composed address is regenerated whenever the underlying address
  changes.

**Impact.** Stopped stale addresses appearing on tenant-branded documents after an address change. The
whitespace-and-empty filtering is what makes the output usable for the many tenants who have no second address
line.

---

### 94. #3754 — Vipps checkout redirect failed silently, stranding the buyer
`Bug fix` · `Checkout` · **Impact 2.8/10** · **Hotfix branch**
**Merged** 2025-09-16 · **Linear** SUBRITE-3322 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 3 files, +97/-2, 15 commits

**Problem.** After starting a Vipps purchase, the browser redirect to the Vipps confirmation URL sometimes did
not happen. The customer was left on the payment form with no error and no indication of what to do — a
payment begun on Subrite's side that the customer could not complete. On a checkout flow this is
straightforwardly lost revenue.

**Root cause.** The code called Next.js's `push(responseData.vippsConfirmationUrl)` once and assumed it
succeeded. A client-side navigation can fail or be swallowed (hydration timing, an interrupted transition),
and with a single fire-and-forget call there was no retry and no failure signal.

**What I changed.**
- Added a `redirectWithRetry({ url, push, onFailure, maxAttempts, delayMs })` utility that retries the
  navigation on a delay and invokes `onFailure` once attempts are exhausted, so a failed redirect surfaces to
  the user instead of leaving them stranded.
- Applied it at both Vipps redirect sites — the main checkout `PaymentForm` and the subscription-change
  `PaymentForm` — rather than only the reported one.

**Impact.** Converted a silent dead end on the payment path into a retried navigation with a visible failure
state. Fixing both call sites matters because the two flows fail identically and only one had been reported.

---

### 95. #3768 — Exports carried country codes only
`Improvement` · `Members, segments & import/export` · **Impact 2.8/10**
**Merged** 2025-09-19 · **Linear** SUBRITE-3029 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 6 files, +71/-20, 5 commits

**Problem.** Every export emitted a two-letter country code and nothing else. Recipients of these files —
fulfilment houses, mailing services, finance teams — need the country name, and were mapping codes by hand or
guessing.

**Root cause.** The export DTOs exposed the raw stored code, and no lookup from code to readable name existed.

**What I changed.**
- Built a `COUNTRY_LOOKUP` record derived from the existing `COUNTRIES` constant, title-casing each entry
  (`split('_')`, upper-case initial, lower-case remainder) so `NORWAY`/`NEW_ZEALAND` become `Norway` and
  `New Zealand`. Deriving it from the existing constant means the two cannot drift apart.
- Renamed the field to `memberAddressCountryCode` and added a separate `@Exportable()` country-name column
  across all five export DTOs — member, member package, member package products, member package users and
  segment members — so every export gained both columns, not just the one that prompted the request.

**Impact.** Made exports directly usable by their recipients instead of requiring a manual mapping step.
Applying it to all five exports, and deriving the lookup from the existing country constant, is what makes it a
consistent improvement rather than a one-off column.

---

### 96. #3760 — OCR import receiver email invisible in the admin panel
`Feature` · `Communication & notifications` · **Impact 2.8/10**
**Merged** 2025-09-17 · **Linear** SUBRITE-3252 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 18 files, +1119/-0, 12 commits

**Problem.** OCR payment files — bank payment reconciliation data — arrive at a per-tenant email address.
Tenants needed to know that address to configure their bank, and could not see it anywhere in the admin panel,
so every setup required asking Subrite.

**Root cause.** Communication *receivers* (inbound addresses) had no representation at all. Senders were
modelled; the receiving side was configuration held outside the product.

**What I changed.**
- Built the communication-receiver feature: entity, repository, service, controller and create/update/response
  DTOs, registered in the automation module — an additive change (+1119/−0).
- Added controller tests and registered the new pieces in the testing module.
- Built the admin UI with a `saveCommunicationReceiver` action in the communication settings area, so the
  receiver address is visible and manageable alongside the senders it belongs with.

**Impact.** Made the OCR reconciliation address self-service, removing a support step from every tenant's
bank-integration setup. It also completed the communication model: having modelled outbound senders, the
inbound side existed as configuration nobody could see.

---

### 97. #3777 — Vipps login could not be enabled per tenant
`Feature` · `Auth & access control` · **Impact 2.8/10**
**Merged** 2025-09-26 · **Linear** SUBRITE-3230 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 20 files, +1154/-4, 25 commits

**Problem.** "Log in with Vipps" — a major convenience in the Norwegian market, where nearly everyone has
Vipps — could not be configured per tenant. OAuth provider configuration was not part of the product, so
enabling it for a tenant was a manual, staff-mediated change.

**Root cause.** No OAuth provider configuration existed in the data model or the API. Provider credentials and
enablement had no home.

**What I changed.**
- Built OAuth provider configuration end-to-end: repository, service, controller and create/update/response
  DTOs registered in the setting module, with controller tests.
- Built the admin settings UI so a tenant can enable Vipps login and supply its credentials themselves.
- Also corrected a receiver-service issue in `communication-receiver.service.ts` encountered alongside it.

**Impact.** Made a market-significant login method self-service. In Norway, offering Vipps login materially
affects signup completion, so moving it from a staff task to a tenant setting removes a delay on something with
direct conversion impact.

---

### 98. #3143 — Tenant preference settings surfaced in the admin panel
`Feature` · `Admin UI` · **Impact 2.7/10**
**Merged** 2025-05-22 · **Linear** SUBRITE-2903 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 42 files, +2744/-74, 33 commits

**Problem.** Tenant preferences — custom properties, invoice field customisation, e-invoice payment settings
and related configuration — had no administrative surface. Tenants could not see or change their own
preferences.

**Root cause.** Feature gap: the preference model existed but nothing exposed it.

**What I changed.**
- Built the preferences area across 42 files: `TenantPreferenceUpdateDto`,
  `PreferenceCustomPropertyConfigDto` and the `EInvoicePaymentPreference` model on the backend; on the frontend
  a tabbed preferences UI (`PreferenceTabs` plus a `PreferenceTabsSmallScreen` variant so it works on narrow
  viewports), a custom-properties table, and invoice-field customisation screens (`CustomFieldsOnInvoiceList`,
  `EditInvoiceFields`, `InvoiceFieldTable`).
- Added translations for the new surfaces.

**Impact.** Another substantial block of configuration moved from support-mediated to self-service. Part of the
2025 tenant-settings programme (#2949, #2959, #3119, #3143, #3194, #3214), and the area later consolidated and
corrected by #3213.

---

### 99. #3925 — Registered-member export lacked revenue and tenure
`Improvement` · `Members, segments & import/export` · **Impact 2.7/10**
**Merged** 2025-11-05 · **Linear** SUBRITE-3537 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 9 files, +258/-7, 9 commits

**Problem.** The registered-members export listed who the members were but not what they were worth or how
long they had been active. Without revenue per member and active days, tenants could not segment by value or
tenure — the two dimensions most marketing and retention analysis depends on — so they exported the list and
computed it themselves.

**Root cause.** Both figures are aggregates over related payment and subscription records, and the export's
member query fetched member fields only. Neither value was computed anywhere.

**What I changed.**
- Extended `MemberRepository` to compute revenue and active days as part of the export query, so the aggregates
  are produced in SQL in one pass rather than per member in application code.
- Added the fields to `MemberExportResponseDto` and extended `MemberExportFilterDto`.
- Added the options to the `RegisteredMembersExport` admin UI with English and Norwegian translations.

**Impact.** Made value- and tenure-based analysis possible directly from the platform's own export. Computing
the aggregates in the query rather than per member is what keeps the export viable for tenants with large
member bases.

---

### 100. #3860 — Hosted widget forms with shareable URLs
`Feature` · `Widgets & branding` · **Impact 2.7/10**
**Merged** 2025-11-12 · **Linear** SUBRITE-3520 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 35 files, +1150/-66, 42 commits

**Problem.** Subrite widgets — signup and subscription forms — could only be embedded in a tenant's own
website. Tenants also needed a hosted, linkable version to use in email campaigns, social posts and QR codes,
where there is no page to embed into.

**Root cause.** Widgets were addressable only by numeric id, which is neither shareable nor stable-looking in a
public URL, and there was no hosted rendering route.

**What I changed.**
- Migration `AddSlugToWidget` adding a slug, plus a shared `slugify` utility, so each widget gets a
  human-readable URL identifier rather than exposing a database id publicly.
- Extended the widget entity, request/response DTOs and controller to support slug-based lookup, with
  controller and service tests.
- Built the hosted form rendering so a widget is reachable as a standalone page.

**Impact.** Extended widgets from embed-only to shareable-link, opening the campaign and print/QR channels
tenants had asked for. Using a slug rather than an id keeps public URLs readable and avoids advertising
internal identifiers. Follow-up fixes (#3987, #3988, #3989) addressed translation issues in the hosted form.

---

### 101. #5004 — Psykologisk tenant data migration
`Data migration` · `Members, segments & import/export` · **Impact 2.7/10**
**Merged** 2026-05-21 · **Linear** [SUBRITE-122](https://linear.app/subrite/issue/SUBRITE-122) (High · Support) · **Base** `develop` · 30 files, +4622/-0, 43 commits

**Problem.** Onboarding the Psykologisk tenant required migrating their entire existing customer base —
members, subscriptions, group members, payments and IP-based access — from their previous system. A
subscription migration has to preserve billing state exactly: get a renewal date or a payment history wrong
and customers are billed incorrectly from day one.

**Root cause.** Not a defect — a substantial data-engineering task with no existing repeatable process.

**What I changed.**
- Built a staged, ordered migration as eight numbered Jupyter notebooks — cancel subscriptions, members,
  subscriptions, group members, payments, IP addresses, IP member-package-products — so each stage is
  independently inspectable and re-runnable, and the ordering (which reflects real foreign-key dependencies)
  is explicit rather than tribal knowledge.
- Added shell scripts for the insert steps and wrote `guidelines.md` and `post-import-work.md`, so the process
  and its required follow-up are documented rather than living in one person's head.
- Entirely additive (+4622/−0) under `scripts/data-ops/`, touching no application code.

**Impact.** Delivered a live tenant migration. Worth noting for what happened next: this import is also the
origin of the renewal-anchor corruption fixed in #5158 — which is precisely why the notebook-per-stage
structure and the written post-import checklist mattered, since they made the fault locatable afterwards.
The same structure was reused for later imports (#4450, #4169, #3816).

---

### 102. #5079 — Subscription panel showed no start or end date
`Bug fix` · `Admin UI` · **Impact 2.7/10**
**Merged** 2026-06-04 · **Linear** [SUBRITE-150](https://linear.app/subrite/issue/SUBRITE-150) (High) · **Base** `develop` · 7 files, +37/-0, 3 commits

**Problem.** The admin subscription panel did not show when a subscription had started or, for an ended one,
when it stopped. Support staff answering "since when has this customer been subscribed?" or "when did this
lapse?" had to look elsewhere, on one of the most frequently used screens in the product.

**Root cause.** Neither date was included in the subscription details response, so the UI had nothing to render.

**What I changed.**
- Added the dates to `SubscriptionDetails` and populated them in `MemberPackageService`.
- Rendered them conditionally in `CurrentSubscriptionDetails`: "Active since" for active subscriptions,
  "Ended on" for inactive or stopped ones — so the label states which kind of date it is rather than showing an
  ambiguous field.
- Added English and Norwegian translations for the new label.

**Impact.** Put the two dates support staff ask for most on the screen where they ask. Small and purely
additive (+37/−0), but it removes a lookup from a routine, frequent task.

---

### 103. #3319 — Onboarding field configuration would not save
`Bug fix` · `Admin UI` · **Impact 2.6/10** · **Hotfix branch**
**Merged** 2025-06-13 · **Base** `subrite/develop` · 1 file, +15/-16, 3 commits

**Problem.** Editing an onboarding input field's configuration in tenant preferences did not persist. Since
these fields define what a new customer is asked during signup, tenants could not change their onboarding form
at all.

**Root cause.** The same defect as #3792 in a neighbouring form: `EditInputFieldConfig` derived its values
from props on each render without holding editable state, so changes had nowhere to live between edit and
submit.

**What I changed.**
- Reworked `EditInputFieldConfig` to hold its edits in component state and submit from there, replacing the
  read-only derivation.

**Impact.** Restored a non-functional settings form controlling the signup experience. One of a pair with
#3792 — the same state-handling mistake had been made in two adjacent forms, and both needed hotfixing.

---

### 104. #3339 — Bank account number rejected at the correct length, and read-only fields editable
`Bug fix` · `Admin UI` · **Impact 2.6/10** · **Hotfix branch**
**Merged** 2025-06-18 · **Base** `subrite/develop` · 5 files, +14/-5, 2 commits

**Problem.** Two onboarding/company-settings defects. The bank account field's length limit did not match the
Norwegian account number format, so tenants could not enter their real account number — which blocks payouts.
And onboarding fields that should have been read-only (system fields the tenant must not change) were editable.

**Root cause.** An incorrect length constraint on the bank account input, and missing read-only handling in the
onboarding field configuration and the system-field table.

**What I changed.**
- Corrected the bank account length constraint in `BankAndCurrencyForm` to match the Norwegian format.
- Added read-only handling in `EditInputFieldConfig` and `SystemFiledTable`, so system fields are presented as
  non-editable rather than appearing changeable and silently reverting.
- Added the accompanying English and Norwegian translations.

**Impact.** Unblocked entry of a valid Norwegian bank account number — required for payouts, so a hard blocker
during tenant setup — and stopped the UI offering edits it would not honour.

---

### 105. #3388 — Duplicate Stripe credential reported as an error
`Bug fix` · `Payments & providers` · **Impact 2.6/10** · **Hotfix branch**
**Merged** 2025-06-24 · **Base** `subrite/develop` · 1 file, +1/-4, 5 commits

**Problem.** The Stripe provider reported an error to Sentry when it encountered a duplicate Stripe
credential. This is an expected, harmless condition — the same credential legitimately appearing more than once
— so it produced recurring false alerts in error tracking.

**Root cause.** The duplicate case was handled by reporting rather than by being recognised as normal, so an
ordinary condition was classified as a fault.

**What I changed.**
- Removed the Sentry report for the duplicate-credential case, leaving the surrounding handling intact.

**Impact.** Removed a source of false-positive alerts, so the remaining Stripe error signal is more
trustworthy. The earliest instance of a concern that recurs throughout this portfolio — see #5270, #5272,
#5297, #5301 and #5370 — that an alert for an expected condition is not merely noise but actively degrades the
alerts that matter.

---

# Tier 3 — Supporting work (ranks 106-253)

Smaller fixes, copy corrections, UI polish and follow-up work. Included for completeness and to show
sustained delivery volume. Same four-section treatment; where a change genuinely has a one-line root cause,
the section says so rather than being padded.

---

### 106. #3722 — Invoice access check crashed for GDPR-deleted members
`Bug fix` · `Invoicing & accounting` · **Impact 2.6/10**
**Merged** 2025-09-08 · **Linear** SUBRITE-3222 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 1 file, +2/-1, 3 commits

**Problem.** Viewing an invoice belonging to a member who had been deleted under GDPR threw an exception.
Invoices must be retained for accounting purposes after a member exercises erasure, so this is a state the
system is required to support — and it could not display those invoices at all.

**Root cause.** `InvoiceService` called
`validateOwnershipOrAdmin(currentUser, invoice.tenant.id, invoice.member.id)`. After GDPR deletion the member
relation is null, so `invoice.member.id` threw before the authorisation check could run.

**What I changed.**
- Changed the access to `invoice.member?.id`, so a retained invoice with no member resolves to undefined and
  the ownership check evaluates normally rather than throwing.

**Impact.** Made GDPR-retained invoices viewable, which is a legal retention requirement. Two lines, and it
keeps the authorisation check intact — the member simply cannot be the owner if the member no longer exists.

---

### 107. #5331 — "Unauthorized" wording replaced with an actionable message
`Improvement` · `Member portal (My Page)` · **Impact 2.6/10**
**Merged** 2026-07-20 · **Linear** [SUBRITE-599](https://linear.app/subrite/issue/SUBRITE-599) (Medium) · **Programme** *Clear error messages* · **Base** `develop` · 6 files, +6/-6, 6 commits

**Problem.** A fully logged-in user who hit a transient 401 — after brief inactivity, or on switching browser
tab — was shown "You don't have permission to perform this action". That is a false statement about their
account: they were authorised, the session verification had simply blipped. Users read it as a permissions
problem and contacted support instead of reloading.

**Root cause.** The `unauthorized` message text assumed 401 always meant "insufficient permission". The
underlying transient-401 cause is documented in SUBRITE-599 and split out from SUBRITE-298 (#5316).

**What I changed.**
- Rewrote the `unauthorized` message across all six locale files — admin English and Norwegian, My Page
  Danish, English, Norwegian and Swedish — to say the session could not be verified and to reload and try
  again. The message now describes what happened and what to do about it.

**Impact.** Replaced a misleading error with an actionable one on a path users hit routinely, removing a
support contact whose answer was always "reload the page". Scope note: the delivered change in this PR is the
wording across the six locale files — the token-preservation code described in the PR's generated summary is
not in this PR's file list.

---

### 108. #4862 — Member email and primary address email drifted apart
`Bug fix` · `Members, segments & import/export` · **Impact 2.6/10**
**Merged** 2026-04-24 · **Linear** SUBRITE-4126 *(pre-migration issue, no longer in Linear)* · **Base** `develop` · 2 files, +192/-1, 6 commits

**Problem.** A member's email address is stored in two places: on the member record and on their primary
address. Updating the member's email did not update the address, so the two diverged — and since invoices and
some communications use the address email, a member could update their email and keep receiving invoices at
the old one.

**Root cause.** `MemberService`'s update path synced related addresses using only the submitted `address`
payload. When an update changed `email` at the member level without including an email in the address payload,
the address kept its stale value.

**What I changed.**
- Set `existingAddress.email` from the incoming member email when one is supplied.
- Changed the `syncRelatedAddresses` call to pass `{ ...address, email: restOfUpdateMemberDto.email ?? address.email }`,
  so a member-level email change propagates while an explicitly supplied address email still wins.
- Added ~130 lines of tests covering the sync in both directions, including the case where the address payload
  carries no email.

**Impact.** Kept the two stored copies of a member's email consistent, so invoices and address-based
communications reach the address the member actually updated. The test coverage pins the precedence rule,
which is the part most likely to be broken by a later change.

---

### 109. #2886 — Ledger reference filter failed on pasted values
`Bug fix` · `Invoicing & accounting` · **Impact 2.5/10** · **Hotfix branch**
**Merged** 2025-03-07 · **Linear** SUBRITE-2604 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 1 file, +1/-1, 2 commits

**Problem.** Filtering ledger transactions by reference returned nothing when the reference had surrounding
whitespace — which is what happens whenever the value is pasted from a bank statement or spreadsheet, the
normal way finance staff use this filter.

**Root cause.** `TransactionRepository` used the filter value verbatim, so `" 12345"` did not match `"12345"`.

**What I changed.**
- Trimmed the value in the filter: `reference: filter.reference.trim()`.

**Impact.** Made the reference filter work with pasted input, which is how it is actually used. One line, and
part of a small cluster of the same fix across filters (#2884, #2906, and the member-email variant in the same
period) — the whitespace problem existed on several filters at once.

---

### 110. #3679 — Sender approval requests routed to the requesting user outside production
`Bug fix` · `Communication & notifications` · **Impact 2.5/10** · **Production hotfix**
**Merged** 2025-08-25 · **Base** `subrite/main` (hotfix) · 1 file, +1/-5, 2 commits

**Problem.** Sender-approval request emails — sent when a tenant asks to have a new sending address verified —
went to the requesting user's own address rather than the approvals inbox whenever `NODE_ENV` was not exactly
`'production'`. Approval requests from any environment not matching that string were therefore never seen by
whoever had to act on them.

**Root cause.** The recipient was chosen by an environment check:
`process.env.NODE_ENV === 'production' ? getEnv('SENDER_APPROVAL_REQUEST_RECEIVER_EMAIL') : currentUser.email`.
Intended as a development convenience, it silently redirected real requests in any environment where
`NODE_ENV` was not that exact value.

**What I changed.**
- Removed the branch entirely, always using `getEnv('SENDER_APPROVAL_REQUEST_RECEIVER_EMAIL')`. The
  environment variable is the right control — configure it per environment rather than branching on `NODE_ENV`
  in code.

**Impact.** Restored delivery of sender-approval requests. The general lesson is in the shape of the bug:
behaviour that differs by environment inside application code fails silently in whichever environment the
author did not think about. Paired with #3681, which set the variable in the test environment.

---

### 111. #4407 — Activity log attributed consent imports to the wrong person
`Bug fix` · `Members, segments & import/export` · **Impact 2.5/10** · **Production hotfix**
**Merged** 2026-01-16 · **Linear** SUBRITE-3838 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/main` (hotfix) · 3 files, +14/-2, 3 commits

**Problem.** When consent data was imported, the activity log recorded the *importing operator's* email and
name against each consent change rather than the member whose consent it was. Consent records are the audit
trail for a legal basis, so an entry naming the wrong person undermines exactly the evidence it exists to
provide.

**Root cause.** `MemberConsentService` logged `updatedBy.fullName || updatedBy.email` — the acting user —
because the member's own identity was not passed into the logging call at all.

**What I changed.**
- Threaded the member's email and name explicitly into the consent-logging path as `memberEmail` and
  `memberFullName` parameters, and passed `member.email` / `member.getMemberName` from both the direct consent
  path and the import path (`existingMember.email`, `existingMember.getMemberName`).
- Recorded the fix in `CHANGELOG.md`.

**Impact.** Corrected the consent audit trail so each entry identifies the member it concerns. Passing the
member identity as explicit parameters rather than inferring it from the acting user is what makes the
attribution correct in both the interactive and bulk-import paths.

---

### 112. #4351 — Seat invitations could not be resent
`Feature` · `Subscriptions & billing lifecycle` · **Impact 2.5/10**
**Merged** 2026-01-09 · **Linear** SUBRITE-3781 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 10 files, +429/-82, 7 commits

**Problem.** When a seat invitation was not received — spam filter, typo, or simply lost — there was no way to
resend it. The workaround was to remove the seat and re-invite, which loses the seat's history. For a group
subscription owner distributing seats, an unreceived invitation was effectively a dead seat.

**Root cause.** No resend endpoint existed. Invitation sending happened only as a side effect of creating a
seat, so it could not be triggered independently.

**What I changed.**
- Added `POST seats/:seatId/resend` on both the admin (`member-subscription.controller.ts`) and member-facing
  (`profile.controller.ts`) paths, with a `ResendSeatInvitationDto` and declared not-found and bad-request
  responses.
- Extracted the invitation send in `TriggerService` and `MemberPackageService` so it can be invoked
  independently of seat creation.
- Built the UI in the member-facing resend-invitation page and the admin gift-receiver and invited-member
  detail components, so both audiences can resend, optionally correcting the address at the same time.

**Impact.** Turned a dead seat into a recoverable one without losing seat history. Making the invitation send
callable independently of seat creation is the structural change — the feature was impossible while sending was
only a side effect. This resend capability was later folded into the seat-management work in #5208/#5249.

---

### 113. #3321 — Existing members could not be assigned to a seat
`Feature` · `Subscriptions & billing lifecycle` · **Impact 2.5/10**
**Merged** 2025-06-16 · **Linear** SUBRITE-3031 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 9 files, +449/-2, 17 commits

**Problem.** Filling a subscription seat always went through an email invitation, even when the person was
already a member of the platform. An operator adding an existing member to a group subscription had to send an
invitation and wait for that member to accept an invitation to something they were already part of.

**Root cause.** The seat-assignment flow only supported invite-by-email and fill-in-details. There was no path
to attach an existing member directly.

**What I changed.**
- Added `POST product/:id/assign-member/:userId` with declared not-found and bad-request responses, guarded by
  `validateOwnershipOrAdmin(user, memberPackageProduct.memberPackage.package.tenantId, …)` — so an operator
  cannot attach a member across a tenant boundary.
- Added an `AssignExistingUser` component with member search and a third `assignExistingUser` option in
  `MemberInviteModal`, alongside the existing two.
- Added controller tests creating a second user to exercise the assignment path.

**Impact.** Removed a pointless invitation round-trip for members already on the platform, which is the common
case in enterprise group subscriptions. Adding the tenant-scoped ownership check at the same time meant the new
endpoint could not become a cross-tenant assignment path.

---

### 114. #5267 — SendGrid webhook events dropped on a foreign-key violation
`Bug fix` · `Communication & notifications` · **Impact 2.4/10**
**Merged** 2026-07-06 · **Linear** [SUBRITE-552](https://linear.app/subrite/issue/SUBRITE-552) (High · Bug) · **Base** `develop` · 2 files, +25/-1, 7 commits

**Problem.** Email activity events for automated report emails were being discarded. The insert into
`communication_log` failed with a foreign-key violation on `communicationId`, so delivery, open and bounce
information for those emails was permanently lost.

**Root cause.** `AutomatedReportService` set `communicationId: 0` for emails that have no associated
communication record — automated reports are not campaign sends. `0` is not a valid `communication` id, so
every insert violated the foreign key. The correct representation of "no related communication" is `null`, not
a sentinel zero.

**What I changed.**
- Changed `communicationId: 0` to `communicationId: null`, so the nullable foreign key is genuinely absent
  rather than pointing at a non-existent row.
- Added a test asserting the webhook still creates a communication log and persists event history when
  `communicationId` is null.

**Impact.** Restored email activity tracking for automated report emails. A one-line change, but a good example
of a sentinel value chosen to avoid a null causing a hard failure at the database boundary — `null` was always
the correct value.

---

### 115. #2926 — Vipps charge check missed subscriptions outside the day window
`Bug fix` · `Subscriptions & billing lifecycle` · **Impact 2.4/10**
**Merged** 2025-03-14 · **Linear** SUBRITE-2630 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 1 file, +2/-2, 2 commits

**Problem.** The check determining whether a Vipps subscription had already been charged only considered
renewals inside a specific day window. Subscriptions whose renewal date fell outside it were mishandled, so
the already-charged determination was wrong for them.

**Root cause.** The condition combined two range predicates with `&&` where the logic required the range to be
one of several alternatives — a precedence and grouping error rather than a wrong comparison.

**What I changed.**
- Parenthesised the range check and changed the combining operator, so `(nextRenewal >= startOfTodayOslo &&
  nextRenewal <= endOfDayOfAlreadyCharged) || …` evaluates as intended rather than requiring both the range
  and the additional condition simultaneously.

**Impact.** Corrected already-charged detection for Vipps subscriptions outside the immediate day window,
which governs whether a charge is attempted. Two lines, and the sort of boolean-grouping mistake that produces
plausible behaviour for the common case and wrong behaviour at the edges.

---

### 116. #2949 — Company info settings brought into the admin panel
`Feature` · `Admin UI` · **Impact 2.4/10**
**Merged** 2025-03-21 · **Linear** SUBRITE-2692 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 33 files, +1263/-12, 29 commits

**Problem.** Tenants could not see or edit their own company information — legal details, bank and currency
configuration, addresses. All of it was staff-managed, so a change of bank account or registered address meant
a support request.

**Root cause.** Feature gap: the tenant settings area of the admin panel did not exist yet. This is the first
iteration of the work.

**What I changed.**
- Added the company-info settings area: a left-menu entry with tabbed sections, `CompanyDetailsForm` and
  `BankAndCurrencyForm`, each with its own `loading.tsx` and page.
- Extended `TenantController`, `TenantResponse` and `TenantService` to expose and update the data, and
  registered `AddressRepository` in the member and tenant modules so address data was reachable.

**Impact.** The first step of the tenant self-service programme that ran through 2025 (#2959, #3119, #3143,
#3194, #3213, #3214). It established the settings-area structure — left menu, tabs, per-section forms — that
the later settings work followed.

---

### 117. #2959 — Tenant-defined custom properties made manageable
`Feature` · `Admin UI` · **Impact 2.4/10**
**Merged** 2025-05-21 · **Linear** SUBRITE-2692 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 25 files, +897/-55, 35 commits

**Problem.** Custom properties — the tenant-defined extra fields on a member, such as a loyalty number or a
regional code — could not be created or edited by tenants. Every new field was a staff task, and the ordering
in which they appeared was not controllable.

**Root cause.** No administrative surface for custom properties, and the repository ordered them by the
caller's paging parameters rather than the tenant's intended display order.

**What I changed.**
- Built the custom-properties settings area: list, detail page, creation/edit form, with `loading.tsx` and
  `not-found.tsx` companions so navigation and bad ids behave consistently with the rest of the panel.
- Added `CustomPropertyOption` to the settings model with an `oneOf: [string, number]` API schema, so a
  select-type property can offer either string or numeric option values and the generated client types both.
- Fixed the repository ordering to `order: { order: ORDER_DIRECTION.ASC }` — using the property's own `order`
  field rather than the request's paging parameters, so tenants control the display sequence.
- Tidied the company-info tabs to key off `companyInfoTabs[0].value` instead of a translated label (a
  translated string is not a stable identifier) and switched the layout to the `ignoreNBreadcrumbItems` API.

**Impact.** Made member data extensible by tenants without developer involvement. The two substantive details
are the ordering fix — the previous behaviour meant the display order was effectively arbitrary — and typing
option values as string-or-number so the field type is honest in the API contract.

---

### 118. #3214 — Company info made editable
`Feature` · `Admin UI` · **Impact 2.4/10**
**Merged** 2025-05-30 · **Linear** SUBRITE-2949 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 33 files, +519/-333, 20 commits

**Problem.** Follow-up to #2949, which made company information visible. Tenants still could not change it, so
correcting a registered address, bank account or customer-service contact remained a support request.

**Root cause.** No update path: `UpdateTenantDto` did not accept an address, and `TenantService` had no
address-saving logic.

**What I changed.**
- Extended `UpdateTenantDto` with optional `name` and `address`, added an `AddressDto`, and added
  `saveTenantAddress(tenantId, address)` to `TenantService`, invoked when an address is present in the update.
- Made `CompanyDetailsForm`, `BankAndCurrencyForm` and `CustomerServiceForm` editable — adding form state,
  `useAuthToken`, submit buttons and toast feedback.
- Tagged the settings page fetches with `tenantSettingsTag`, so a saved change is reflected immediately rather
  than served from a stale cache.

**Impact.** Completed the company-info settings from read-only to editable. The cache tagging is what made it
feel correct rather than broken — without it, a successful save appeared to have done nothing. (This
per-form pattern was later consolidated onto a single settings endpoint in #3213.)

---

### 119. #3227 — Tenant settings review feedback
`Improvement` · `Admin UI` · **Impact 2.4/10**
**Merged** 2025-06-02 · **Base** `subrite/develop` · 31 files, +414/-236, 25 commits

**Problem.** A round of consolidated feedback on the newly built tenant settings area, dated in the branch
name. Issues included translation keys pointing at an outdated `automationSender` namespace, missing SMS
sender-name validation, and font uploads failing for valid OTF files.

**Root cause.** Several small inconsistencies from building the settings area in successive iterations: the
sender terminology had been renamed but the translation namespace had not, validation rules for SMS sender
names (which have carrier-imposed format restrictions) had not been implemented, and the multer configuration's
MIME-type allowlist did not cover the several MIME types browsers report for OTF fonts.

**What I changed.**
- Added `isValidOtf(file)` to the multer config, checking both the file extension and a list of the MIME types
  actually seen for OTF (`font/otf`, `application/x-font-otf`, …) — because browsers are inconsistent here and
  a single-MIME check rejects valid files.
- Added an `isValidSmsSenderName` utility and applied it in the sender form.
- Corrected the translation namespace from `…communications.automationSender` to `…communications.senders`
  across the sender form and list, so the labels matched the renamed concept.
- Applied the remaining feedback across the communication settings forms, lists, tabs and layout.

**Impact.** Closed out a settings area to a shippable standard rather than leaving a list of small known
defects. The OTF fix is the one users would have hit hardest — a valid font file being rejected with no
explanation.

---

### 120. #3704 — Company details rejected a blank organisation number
`Bug fix` · `Admin UI` · **Impact 2.4/10** · **Production hotfix**
**Merged** 2025-08-28 · **Base** `subrite/main` (hotfix) · 1 file, +31/-7, 4 commits

**Problem.** The company-details form validated the Norwegian organisation number format even when the field
was left empty, so a tenant that had not entered one could not save the form at all. Conversely, the form
allowed saving a company name without an organisation number or vice versa, when the two only make sense
together.

**Root cause.** The validation regex ran unconditionally: `if (!/^\d{9}$/.test(stripped) || !raw.endsWith('MVA'))`
treated an empty value as invalid rather than as absent. There was also no cross-field rule tying company name
and organisation number together. The form used a server-action `action={handleSave}`, which submits before
client validation can block it.

**What I changed.**
- Made the format check conditional on the field being non-empty:
  `if (stripped.length > 0 && (…))` — so an unset optional field is allowed while a supplied one is validated.
- Added a `dependencyCheck(companyName, registeredBusinessNo)` cross-field rule flagging the specific missing
  field when one is present without the other.
- Converted the form from `action={handleSave}` to `onSubmit` with `e.preventDefault()` and a manually
  constructed `FormData`, so validation can actually prevent submission — with the action form it could not.

**Impact.** Unblocked saving company details for tenants without an organisation number, and made the
name/number pairing rule explicit. The `action` → `onSubmit` change is the structural part: client-side
validation cannot block a form-action submission, so the guard would have been decorative otherwise.

---

### 121. #3712 — Admin create-order form could not use an existing member or return an invoice number
`Improvement` · `Payments & providers` · **Impact 2.4/10**
**Merged** 2025-09-19 · **Linear** SUBRITE-3229 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 17 files, +683/-155, 59 commits

**Problem.** The admin create-order flow always created a new member, so ordering on behalf of an existing
customer produced a duplicate. It also gave the operator no invoice number afterwards, and billing-address
company name and email could not be captured — meaning an invoice raised for a company could not carry the
company's own details.

**Root cause.** `CheckoutUserDto` required an email and had no way to reference an existing member, so the
checkout path always went down the create-member branch. `BillingAddress` lacked `companyName` and `email`
fields, `AddressInfo` lacked `organizationNo`, `email` and `phone`, and the faktura response returned no
invoice number.

**What I changed.**
- Added an optional `memberId` to `CheckoutUserDto` with
  `@ValidateIf((o) => !o.memberId)` on email — so email is required only when creating a new member, and
  supplying a member id reuses the existing one.
- Added `companyName` and `email` to `BillingAddress`, and `organizationNo`, `email` and `phone` to
  `AddressInfo`.
- Changed `OrderInvoiceDto` to prefer the address's own values over the member's
  (`invoice.addressInfo.name || invoice.memberInfo.fullName`, and likewise for email), so a company billing
  address is used on the invoice rather than the individual's personal details.
- Returned `invoiceNumber` from `FakturaPaymentResponseDto` and the faktura transaction, with an
  `isAdminAction = !!data.userData?.memberId` distinction so the admin path behaves appropriately.
- Split `EInvoicePackageSubscriptionDto` from `FakturaPackageSubscriptionDto`, separating two flows that had
  been sharing one DTO.

**Impact.** Removed duplicate-member creation from the admin ordering path, gave operators the invoice number
they need to reference the order, and made invoices for business customers carry the company's details.
Preferring address values over member values is the correct precedence for a financial document.

---

### 122. #3926 — Copy-email and list actions rebuilt on a shared select component
`Bug fix` · `Admin UI` · **Impact 2.4/10**
**Merged** 2025-11-05 · **Linear** SUBRITE-3555 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 13 files, +309/-326, 15 commits

**Problem.** The "copy email" action misbehaved, and the several admin action menus it belonged to — auth
providers, communication senders, invoices, email lists, accounting report history — each had their own
hand-rolled dropdown. Behaviour differed between them, and group headings in those menus were selectable as if
they were options. Newly created senders also did not appear at the top of the list, so the item just added was
hard to find.

**Root cause.** Duplicated bespoke dropdown implementations with no shared notion of a non-selectable group
label, and repository queries with no explicit ordering — so list order was whatever the database returned.

**What I changed.**
- Consolidated the action menus onto the shared `ui/CustomSelect` with typed action enums per surface
  (`AUTH_PROVIDER_ACTION`, `COMMUNICATION_SENDER_ACTION`, …), replacing string comparisons with enum values.
- Added `isGroupLabel: true` support so group headings render as headings rather than selectable options.
- Added explicit `order: { createdAt: 'DESC' }` to the communication-sender repository and service, so the
  most recently created sender appears first.
- Reduced the repo ESLint threshold from 82 to 81.

**Impact.** Made action menus behave identically across five admin surfaces instead of five slightly different
ways, and stopped group headings being selectable. The near-balanced diff (+309/−326) reflects consolidation:
five bespoke implementations replaced by one shared component.

---

### 123. #3989 — Hosted newsletter form rendered untranslated
`Bug fix` · `Widgets & branding` · **Impact 2.4/10**
**Merged** 2025-11-12 · **Base** `subrite/develop` · 4 files, +126/-114, 2 commits

**Problem.** The hosted newsletter subscribe form — the standalone linkable page from #3860 — showed
untranslated text. Since it is a public, customer-facing page reached from campaigns, untranslated strings are
immediately visible to the audience it exists to convert.

**Root cause.** The translation lookup happened inside a `'server-only'` server action
(`getWidgetConfigAndTheme`) using `getTranslations` from `next-intl/server`. Translated strings resolved there
did not reach the client component that rendered the form, so the form fell back to keys or defaults.

**What I changed.**
- Removed the server action entirely and fetched the widget and tenant details directly in the page components
  via `api` / `subriteInternalFetch`.
- Moved translation into the client component: `HostedFormContainer` now calls `useTranslations` itself,
  taking `widgetDetails` and `TenantDetailsResponse` as props — so text is resolved where it is rendered.

**Impact.** Fixed visible untranslated text on a public campaign landing page. The structural point is that
translations must be resolved in the component that renders them; passing resolved strings across the
server/client boundary was the wrong shape and produced this class of bug. Part of a cluster of hosted-form
fixes (#3987, #3988, #3990).

---

### 124. #4379 — Subscription import rejected unlimited quantities
`Bug fix` · `Members, segments & import/export` · **Impact 2.4/10** · **Production hotfix**
**Merged** 2026-01-12 · **Base** `subrite/main` (hotfix) · 2 files, +13/-2, 2 commits

**Problem.** Importing subscriptions failed validation for products configured with an unlimited quantity.
Since tenant onboarding depends on bulk subscription import, this blocked migration of any customer using
unlimited-quantity products.

**Root cause.** Unlimited is represented as `quantityRange = -1` — a sentinel, not a real bound. The validation
compared `quantity > quantityRange` without accounting for it, so every positive quantity exceeded `-1` and
every row failed.

**What I changed.**
- Added `&& quantityRange > 0` to the condition, so the upper-bound check applies only when there is a genuine
  upper bound and the unlimited sentinel is skipped.
- Recorded the fix in `CHANGELOG.md`.

**Impact.** Unblocked subscription import for unlimited-quantity products. A three-line condition change, and
a reminder that a sentinel value in a comparison needs an explicit guard — the code was correct for every
configuration except the one that used the sentinel.

---

### 125. #4792 — Payment-failed event crashed on missing Vipps payment info
`Bug fix` · `Payments & providers` · **Impact 2.4/10**
**Merged** 2026-04-10 · **Linear** SUBRITE-3833 *(pre-migration issue, no longer in Linear)* · **Base** `develop` · 1 file, +2/-2, 2 commits

**Problem.** A remaining unguarded path from the same issue as #4402: firing the recurring-payment-failed event
for a Vipps subscription with no stored payment info still threw, so the dunning notification was not sent.

**Root cause.** `TriggerService` dereferenced `.paymentInfo` and then `vippsPaymentInfo.phone` without null
checks — the two sites #4402 had not covered.

**What I changed.**
- Added optional chaining and an honest type: `?.paymentInfo as VippsPaymentInfo | null`, and
  `phoneNumber: vippsPaymentInfo?.phone || null`.

**Impact.** Closed the last crash on the Vipps payment-failure path, so dunning completes for subscriptions
with missing payment info. Two lines, and worth noting as the third pass over this area (#4047, #4402, #4792) —
the type had been asserted as non-null when the data was in fact optional, which is what let the same bug keep
reappearing in different call sites.

---

### 126. #4832 — Imported paid e-invoice subscriptions never got a next order date
`Bug fix` · `Invoicing & accounting` · **Impact 2.4/10**
**Merged** 2026-04-17 · **Linear** SUBRITE-4196 *(pre-migration issue, no longer in Linear)* · **Base** `develop` · 2 files, +16/-6, 3 commits

**Problem.** As I noted in the PR: when paid e-invoice subscriptions were imported through the admin UI,
`nextOrderDate` was never set. That field drives when the next invoice is generated, so imported subscriptions
were never invoiced again — silent revenue loss for a whole imported cohort. A second defect assigned optional
products by overwriting the array rather than accumulating, so only the last product survived.

**Root cause.** The import path went through `FakturaPackageTransaction`, which computed `nextOrderDate` only
for the interactive purchase flow. The import supplied a renewal date but nothing derived the order date from
it. Separately, the import built `optionalProducts` by assignment per product instead of pushing.

**What I changed.**
- In `FakturaPackageTransaction`, computed `nextOrderDate` via `getNextOrderDate(data.nextRenewalDate,
  data.package.subscriptionPlan, eInvoicePaymentPreference.orderDaysBeforeRenewal, false)` for e-invoice
  subscriptions with `autoRenew === true`, and persisted it — so an imported auto-renewing subscription is
  scheduled like any other.
- Changed the import to initialise `optionalProducts = []` and `push` each product, so all imported optional
  products are retained.

**Impact.** Stopped imported e-invoice subscriptions silently never being invoiced again — the kind of fault
that is invisible until a tenant notices missing revenue months later. The optional-products fix corrected
imported subscription contents at the same time.

---

### 127. #5231 — Seat availability compared against the wrong capacity field
`Bug fix` · `Subscriptions & billing lifecycle` · **Impact 2.4/10**
**Merged** 2026-06-29 · **Linear** [SUBRITE-446](https://linear.app/subrite/issue/SUBRITE-446) (High) · **Base** `develop` · 3 files, +9/-5, 2 commits

**Problem.** Follow-up to #5208, branch-named `fix/owner-access-disabled-wrongly`. Owner access was being
disabled incorrectly, because the seat-availability calculation compared active seats against the wrong field.
The "add member" action also stayed enabled when no seats remained, so an operator could start an invitation
that could not succeed.

**Root cause.** The condition used `groupInfo?.seatsActive === groupInfo?.maxQuantityPerOrder`.
`maxQuantityPerOrder` is a per-order purchase limit, not the subscription's seat capacity — `seatsAllowed` is.
Strict equality also meant that if `seatsActive` ever exceeded capacity the condition silently stopped holding.

**What I changed.**
- Changed the comparison to `groupInfo?.seatsActive >= groupInfo?.seatsAllowed` in both the member-facing and
  admin `MemberInvites` components — correct field, and `>=` so an over-capacity state still registers as full.
- Disabled the add-member button in `ManageMembersModal` when `counts.remaining <= 0`, so the action is not
  offered when it cannot succeed.

**Impact.** Corrected owner access for full group subscriptions and stopped the UI offering an impossible
action. Small, but it fixed a wrong-field comparison introduced in a much larger change — the sort of detail
that only shows up in use.

---

### 128. #5071 — Admin create-order failed with `notAvailableViaSlug`
`Bug fix` · `Admin UI` · **Impact 2.4/10**
**Merged** 2026-06-03 · **Linear** [SUBRITE-195](https://linear.app/subrite/issue/SUBRITE-195) (High · Bug) · **Base** `develop` · 4 files, +31/-9, 2 commits

**Problem.** The admin Create Order modal failed with `notAvailableViaSlug`. Operators could not create orders
on behalf of customers — a routine support and sales action.

**Root cause.** The modal loaded package details through the public, slug-based, unauthenticated lookup. That
endpoint deliberately refuses packages not published for public purchase, which is exactly the case an operator
needs: creating an order for a package that is not publicly available. The admin flow was using the customer
path.

**What I changed.**
- Switched the admin flow to an authenticated package-details lookup
  (`getProductPackageDetails`) rather than the public slug lookup, so operators can reach packages regardless
  of public availability.
- Added an explicit response type in `types/productPackage.ts` instead of an implicit shape, and clarified the
  parameter naming in the package-details helper.

**Impact.** Restored admin order creation. The underlying error was a correct refusal from the wrong endpoint —
the fix was to use the authenticated path the admin context is entitled to, rather than to weaken the public
endpoint's rule.

---

### 129. #3713 — Podcast image could not be removed and was not taken from the feed
`Bug fix` · `Content & podcasts` · **Impact 2.3/10**
**Merged** 2025-09-01 · **Base** `subrite/develop` · 5 files, +32/-5, 2 commits

**Problem.** Two defects on podcast management. Clearing a podcast's image in the admin UI did not remove it —
the old image persisted. And when creating a podcast from an RSS feed, the artwork the feed already provided
was not picked up, so tenants uploaded it manually despite the feed containing it.

**Root cause.** An empty image field was indistinguishable from "field not submitted", so the update path had
no way to know a removal was intended — the classic omitted-versus-cleared ambiguity. And `PodcastService`
called `fetchRssFeed` for validation only, discarding the parsed metadata including `meta.imageUrl`.

**What I changed.**
- Added an explicit `removeOldImage?: boolean` to `UpdatePodcastDto` with `@TransformToBoolean()`, and set it
  from the frontend when the image field is empty (`const removeOldImage = !formData.imageUrl || formData.imageUrl === ''`)
  — making the intent explicit rather than inferring it from an absent value.
- Captured the return of `fetchRssFeed` and used `rssContent.meta?.imageUrl` when creating a podcast, so feed
  artwork is adopted automatically.

**Impact.** Made image removal actually work and removed a manual step from podcast setup. The explicit
`removeOldImage` flag is the right resolution of the omitted-versus-cleared ambiguity — no amount of care on
the empty value could have distinguished the two cases.

---

### 130. #3898 — Public API route corrected with a documented deprecation
`Improvement` · `API contract` · **Impact 2.3/10**
**Merged** 2025-10-29 · **Linear** SUBRITE-3017 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 4 files, +140/-9, 3 commits

**Problem.** The public API route for editing an email communication was
`PATCH /api/v1/communications/email/edit/:id` — the id in the wrong position, inconsistent with every other
resource route in the API. Since this is a documented endpoint third parties integrate against, it could not
simply be changed.

**Root cause.** The route had been defined with the parameter after the action rather than after the resource,
diverging from the API's own convention. A handler name typo (`updateCommunicationThidparty`) had also
persisted.

**What I changed.**
- Added the correctly shaped route `PATCH :id/email/edit` alongside the old one, so both work.
- Marked the old route `deprecated: true` in Swagger with a description naming the replacement and the
  deprecation date, so the generated API documentation tells integrators what to do.
- Updated the public developer documentation (`docs-public/.../edit.md`) with an explicit deprecation notice
  stating the old route still functions but may be removed.
- Fixed the handler name typo to `updateCommunicationThirdParty`.

**Impact.** Corrected a public API inconsistency without breaking existing integrations — both routes work,
the old one is marked deprecated in the machine-readable contract, and the documentation gives integrators a
date and a migration target. That is the difference between fixing an API and breaking a customer's integration.

---

### 131. #3934 — Email editor capability and colour handling
`Improvement` · `Communication & notifications` · **Impact 2.3/10**
**Merged** 2025-11-24 · **Linear** SUBRITE-3552 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 32 files, +1197/-218, 48 commits

**Problem.** The rich-text email editor was limited and inconsistent. It lacked numbered lists, dividers,
highlight boxes and text colour; each place it was embedded passed its own hardcoded modal dimensions and
absolute pixel offsets for the CTA, link and emoji popovers, so the toolbar behaved differently per screen and
broke at different widths. Colour values from the editor were also serialised into email HTML without
validation.

**Root cause.** No shared positioning or sizing for the editor's popovers — each consumer supplied literal
values like `ctaCustomPosition="absolute -left-[200px] -top-48 z-10 mt-3"` — and the Slate serialiser
interpolated colour strings into inline styles unchecked.

**What I changed.**
- Added `hex-helper.ts` with `HEX_REGEX` and `sanitizeHex()`, and used it in the Slate serialiser so only valid
  hex colours are emitted — an unvalidated value from the editor was being interpolated straight into email
  HTML.
- Extended the editor with `divider`, `highlight-box`, `text-color` toolbar types and a
  `NumberedListElement` Slate type.
- Removed the per-consumer hardcoded modal dimensions and absolute positions, moving to a shared
  `RICH_TEXT_EDITOR_WRAPPER_CLASS_NAME` so positioning is defined once.
- Reduced the repo ESLint threshold from 78 to 74.

**Impact.** Gave tenants a usable email editor with the formatting they expect, and made it behave consistently
everywhere it is embedded. Hex sanitisation closed an unvalidated-value-into-HTML path on content sent to
customers' inboxes.

---

### 132. #4113 — Email preview text leaked into the visible body
`Bug fix` · `Communication & notifications` · **Impact 2.3/10**
**Merged** 2025-12-01 · **Linear** SUBRITE-3673 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 7 files, +153/-38, 6 commits

**Problem.** Email preview text — the short teaser mail clients show next to the subject — appeared inside the
visible email body for some recipients instead of staying hidden. Every email the platform sent could therefore
begin with duplicated text, in campaigns, automations and OTP mails alike.

**Root cause.** Each of four call sites built its own hidden preheader `<div>` with its own inline CSS —
`display: none; max-height: 0px; overflow: hidden` in one place, a longer variant with `font-size:1px` and
`opacity:0` in another — and prepended it to the body. Mail clients vary in which hiding techniques they
respect, so the weaker variants leaked. Preview text was also inserted as raw HTML with no sanitisation.

**What I changed.**
- Centralised preheader handling in `common/utils/email-template-utility.ts`, replacing
  `injectEmailContentToTemplate` with a version that takes the preview text and applies one correct hidden
  construction — so all senders share the same behaviour.
- Added `stripHtmlTags()`, removing `<script>` blocks and tags from the preview text, since it was previously
  interpolated as raw HTML.
- Removed the four duplicated inline preheader constructions from `CommunicationService`,
  `SendEmailExecutor`, `SendInternalEmailExecutor` and `VisualWorkflowService`, plus the frontend copy used by
  the OTP email.

**Impact.** Fixed visible duplicated text across every email the platform sends, and removed an unsanitised
HTML insertion path in the process. Four divergent implementations of one email-HTML convention became one — a
recurring theme in this portfolio and the reason the bug existed in only some of them.

---

### 133. #4861 — Dead `automationTemplateId` column removed from preferences
`Refactor / tech debt` · `Database migrations` · **Impact 2.3/10**
**Merged** 2026-04-23 · **Linear** SUBRITE-4097 *(pre-migration issue, no longer in Linear)* · **Base** `develop` · 7 files, +17/-21, 2 commits

**Problem.** `preference.automationTemplateId` was still present in the schema, the entity, the API contract and
the tenant-provisioning documentation, but nothing used it. It appeared in the generated API client, so
integrators could see a field with no effect, and every new tenant had to have it set to `NULL` explicitly.

**Root cause.** The field had been superseded and left behind — the same pattern as #3736.

**What I changed.**
- Migration `DropAutomationTemplateIdFromPreference` removing the column, after confirming nothing read it.
- Removed it from the `Preference` entity and `TenantPreferenceUpdateDto`, so it disappears from the generated
  OpenAPI and client.
- Removed it from `ADDING_TENANTS.md`, so tenant provisioning no longer references a column that does not exist.

**Impact.** Removed a dead field from schema, contract and operational documentation in one pass. Small, but
keeping the provisioning documentation in step with the schema is what stops the next tenant setup failing on a
missing column.

---

### 134. #5070 — Removal dialogs labelled their confirm button "Save"
`Bug fix` · `Member portal (My Page)` · **Impact 2.3/10**
**Merged** 2026-06-03 · **Linear** [SUBRITE-272](https://linear.app/subrite/issue/SUBRITE-272) (**Urgent** · Bug) · **Base** `develop` · 2 files, +2/-2, 3 commits

**Problem.** The confirmation dialogs for removing an invited member — on both My Page and the admin panel —
labelled their action button "Save". The button performs a destructive removal, so labelling it "Save" invites
the user to click it expecting the opposite. Filed Urgent, reasonably: a mislabelled destructive action causes
real mistakes.

**Root cause.** Both dialogs used `tCommon('save')`, presumably copied from a form dialog where it was correct.

**What I changed.**
- Changed both to `tCommon('confirm')`, in the member-facing `RemoveInvitedMember` and the admin
  `RemoveImvitedMember` components.

**Impact.** Made a destructive confirmation say what it does. Two lines with a genuine consequence — a user
clicking "Save" to avoid removing someone would have removed them.

---

### 135. #2902 — Automation trigger list unordered and re-translated on every render
`Bug fix` · `Automation & workflows` · **Impact 2.2/10**
**Merged** 2025-03-11 · **Linear** SUBRITE-2625 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 3 files, +22/-8, 7 commits

**Problem.** The automation trigger list was not alphabetically ordered, so finding a trigger among many meant
scanning the whole list. Sorting it was not straightforward: the displayed names are translated, so the list has
to be sorted by translated name, not by the underlying identifier.

**Root cause.** Translation happened at render time inside the list component
(`tTriggers(convertEmitterNameToCamelCase(event.emitterName) + '.name')`), so the translated names did not exist
at the point where the collection was assembled and could not be sorted on.

**What I changed.**
- Moved translation upstream into `fetchAndFilterEvents` in `WorkflowDetailsContext`, which now takes
  `tTriggers` and attaches `tTriggerName` and `tTriggerDescription` to each event.
- Added both fields to the `EventType` type and simplified the component to render `event.tTriggerName`
  directly.

**Impact.** Made the trigger list sortable by its displayed names and removed repeated translation work from
render. The structural point is that resolving derived display values where the collection is built, rather
than where each row is rendered, is what makes ordering by them possible at all.

---

### 136. #3060 — Consent default-subscribed checkbox behaved wrongly on purpose change
`Bug fix` · `Communication & notifications` · **Impact 2.2/10** · **Hotfix branch**
**Merged** 2025-04-16 · **Linear** SUBRITE-2709 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 2 files, +18/-1, 4 commits

**Problem.** Follow-up to #3032. In the newsletter/consent forms, changing a consent's purpose left the
`defaultSubscribed` checkbox at its previous value. Only the marketing-and-newsletter purpose may legitimately
default to subscribed, so switching to another purpose could leave a consent silently configured to opt members
in by default — a consent-compliance problem, not a UI nuisance.

**Root cause.** The purpose field's change handler did not reset the dependent `defaultSubscribed` value, so the
two could end up in a combination the domain does not allow.

**What I changed.**
- In the create form, reset `defaultSubscribed` to `false` whenever `purpose` changes.
- In the edit form, made it conditional: when the purpose changes to `PURPOSE_MARKETING_AND_NEWSLETTER` restore
  the previous value, otherwise force `false` — so the only purpose permitted to default to subscribed keeps the
  operator's setting and the others cannot.

**Impact.** Prevented a consent being left opted-in-by-default under a purpose that does not permit it. The
comment left in the code recording *why* the rule is asymmetric is doing real work here — the behaviour would
otherwise look arbitrary to the next reader.

---

### 137. #3336 — Email text shrank when an image was added
`Bug fix` · `Communication & notifications` · **Impact 2.2/10**
**Merged** 2025-06-18 · **Linear** SUBRITE-3059 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 1 file, +43/-45, 5 commits

**Problem.** Adding an image to a communication email caused the surrounding text to shrink. Emails went out
with inconsistent typography — visible to every recipient of that campaign.

**Root cause.** The serialised email HTML had no wrapper establishing font and line-height, so the text
inherited styling from whatever context it landed in. An image's own styles then influenced the surrounding
text.

**What I changed.**
- Wrapped the serialised output in a container explicitly inheriting typography:
  `<div style="max-width: 100%; font-family: inherit; font-size: inherit; color: inherit; line-height: inherit;">`,
  so text styling is anchored regardless of what else the body contains.
- Condensed the mark serialisation (`bold`, `code`, `italic`, `underline`, `strikethrough`) from multi-line
  blocks to single-line returns while restructuring.

**Impact.** Fixed inconsistent text sizing in emails whenever an image was present. Anchoring typography in an
explicit wrapper is the standard fix for email HTML, where inheritance is unpredictable across clients.

---

### 138. #3830 — Test emails showed a raw CTA placeholder
`Bug fix` · `Automation & workflows` · **Impact 2.2/10**
**Merged** 2025-10-13 · **Linear** SUBRITE-3367 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 2 files, +16/-4, 3 commits

**Problem.** Sending a test email stripped the invitation-link CTA to bare text, so the tenant previewing their
email did not see the call-to-action button that recipients would get. Testing an email is meant to show what
will actually be sent.

**Root cause.** `replacePlaceholdersInTemplate` replaced every placeholder with its key name for previews —
correct for data tokens like a member's name, but wrong for `InvitationLinkCTA`, which is a rendered button
rather than a text substitution.

**What I changed.**
- Made the replacement skip that one token:
  `key.trim() === TokenName.INVITATION_LINK_CTA ? match : key.trim()` — leaving the placeholder intact so the
  CTA renders, while all other tokens still show as readable names in the preview.

**Impact.** Made test emails represent what recipients receive. The distinction the fix encodes — data tokens
substitute to their names in a preview, structural tokens must render — is the right one, and was previously
not expressed at all.

---

### 139. #3903 — One unverified sender stopped every scheduled email
`Bug fix` · `Communication & notifications` · **Impact 2.2/10**
**Merged** 2025-10-29 · **Linear** SUBRITE-3550 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 1 file, +19/-1, 2 commits

**Problem.** The cron job that executes scheduled communications processed them in a loop with no per-item
error handling. A single failing communication — most commonly one whose sender was not verified — threw and
aborted the entire run, so every *other* tenant's scheduled email for that cycle was never sent. One tenant's
misconfiguration silently suppressed everyone's campaigns.

**Root cause.** `await this.communicate(communication.id)` sat unguarded inside the loop, so any throw
propagated out and terminated the batch. The failing communication was also left in its scheduled state,
so it would be retried on the next run and abort that one too.

**What I changed.**
- Wrapped the per-communication call in `try/catch`, reporting the failure with `reportEvent` and its
  `communicationId` and continuing to the next item.
- Marked the failed communication `COMMUNICATION_STATUS.FAILED`, so it stops being retried and blocking future
  runs — with that update itself wrapped in a nested `try/catch` and reported, since a failure to record the
  failure must not re-break the loop.

**Impact.** Stopped one tenant's misconfigured sender from silently suppressing scheduled emails for every
tenant — a genuine cross-tenant blast radius from a per-item error. The nested guard on the status update is the
detail that makes the isolation complete.

---

### 140. #3914 — Invoice address shown to invited members
`Improvement` · `Member portal (My Page)` · **Impact 2.2/10** · **Production hotfix**
**Merged** 2025-10-30 · **Linear** SUBRITE-3062 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/main` (hotfix) · 1 file, +3/-1

**Problem.** The subscription details page on My Page showed the invoice address to *invited* members — people
occupying a seat on someone else's subscription. An invited member is not the payer, so this exposed the paying
owner's billing address, potentially a home or company address, to everyone holding a seat.

**Root cause.** `CurrentSubscriptionDetails` rendered `<InvoiceAddress isPayingMember={isPayingMember} …>`
unconditionally, passing the flag down instead of using it to decide whether to render at all — so the component
received the information but the address was still displayed.

**What I changed.**
- Made the render conditional: `{isPayingMember && (<InvoiceAddress … />)}`, so the section is not rendered for
  non-paying members.

**Impact.** Stopped a paying member's billing address being visible to seat holders. Three lines, but it is a
disclosure of one person's address to others, which is why it went out as a hotfix — and it illustrates that
passing a permission flag into a component is not the same as gating on it.

---

### 141. #4034 — Invalid image URL crashed the whole page
`Bug fix` · `Shared platform code` · **Impact 2.2/10** · **Production hotfix**
**Merged** 2025-11-21 · **Linear** SUBRITE-3645 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/main` (hotfix) · 2 files, +4/-28, 4 commits

**Problem.** A malformed image URL crashed the entire page rather than degrading to a missing image. One bad
value in tenant content took down the whole view.

**Root cause.** `CloudflareImage` and `UploadInput` each carried their own copy of a large hand-written
`isValidUrl` regex — identical, and each constructing a `RegExp` from a concatenated string on every call. The
duplicated validation did not correctly reject the malformed values that were reaching it.

**What I changed.**
- Deleted both inline copies (−28 lines) and imported the shared `utils/isValidUrl`, so validation is defined
  once and behaves consistently for both components.

**Impact.** Turned a full page crash into a gracefully absent image. Consolidating two duplicated
regex implementations into the shared utility means the next component gets the correct behaviour for free —
the duplication was why the two could disagree in the first place.

---

### 142. #4405 — Consent list did not show draft status
`Bug fix` · `Communication & notifications` · **Impact 2.2/10**
**Merged** 2026-01-15 · **Base** `subrite/develop` · 1 file, +16/-0, 1 commit

**Problem.** The consent reorder list showed no status column, so active and draft consents looked identical.
An operator reordering consents could not tell which were live and which were unpublished drafts — and consent
ordering determines what members are shown.

**Root cause.** The list rendered names only; the status field was available but not displayed.

**What I changed.**
- Added a status column header and a per-row badge: green "Active" or grey "Draft" depending on
  `consent.status`, using the existing `tableStatus` and settings translation namespaces.

**Impact.** Made the live/draft distinction visible where consents are ordered, so operators can see which
entries members actually see. Purely additive (+16/−0).

---

### 143. #4418 — Automation CTA buttons rendered with hardcoded English labels
`Bug fix` · `Automation & workflows` · **Impact 2.2/10**
**Merged** 2026-01-22 · **Linear** SUBRITE-3814 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 2 files, +29/-1, 4 commits

**Problem.** Automation emails containing an invitation or card-change call-to-action rendered the button label
in English regardless of the tenant's language. Norwegian members received an otherwise Norwegian email with an
English button — on transactional emails where the button is the entire point of the message.

**Root cause.** The CTA button text had no translation path. `TokenProcessingService` did not supply a
`buttonText`, so the template fell back to a hardcoded English default.

**What I changed.**
- Injected `TranslationService` into `TokenProcessingService` and, when the content contains
  `{{InvitationLinkCTA}}` or `{{cardChangeLink}}` and no button text has been supplied, translated
  `automation.invitationBtnText` (and the card-change equivalent) into the tenant's language, defaulting to
  `'en'`.
- Passed `buttonText` and `cardChangeBtnText` through to `replacePlaceholdersInTemplate` conditionally, so an
  explicitly supplied label still wins over the translated default.

**Impact.** Made transactional CTA buttons appear in the member's language. Detecting the token in the content
before doing translation work, and letting an explicit value override, keeps the change cheap and
non-overriding.

---

### 144. #4841 — Filternyheter subscription plan consolidation scripts
`Data migration` · `Members, segments & import/export` · **Impact 2.2/10**
**Merged** 2026-04-21 · **Linear** SUBRITE-4204 *(pre-migration issue, no longer in Linear)* · **Base** `develop` · 3 files, +833/-0, 5 commits

**Problem.** The Filternyheter tenant had accumulated redundant subscription plans that needed consolidating.
Moving live subscriptions between plans changes what customers are billed, so it could not be done with an
ad-hoc `UPDATE`.

**Root cause.** Not a defect — a production data operation on billing-relevant records, needing a safe
procedure.

**What I changed.**
- Three shell scripts under `scripts/data-ops/`, entirely additive: `list-migration-candidates.sh` (which
  subscriptions qualify), `list-non-qualifying.sh` (which do not, **and why** — so exclusions are reviewable
  rather than silent), and `apply-plan-migration.sh` to perform the move.
- Same structure as the other data-ops work here: enumerate and review first, apply second.

**Impact.** Let a billing-affecting plan consolidation be reviewed before execution. The non-qualifying listing
is the part that matters — being able to see what was *not* migrated, and why, is what makes the operation
auditable afterwards.

---

### 145. #5354 — Postal code validation error not shown, only a generic system error
`Bug fix` · `Admin UI` · **Impact 2.2/10**
**Merged** 2026-07-22 · **Linear** [SUBRITE-649](https://linear.app/subrite/issue/SUBRITE-649) (High · Bug) · **Base** `develop` · 1 file, +25/-1

**Problem.** Entering an invalid postal code on the company-details form with the country set to Sweden showed
"Failed to update due to system error". The value was wrong in a specific, correctable way — Swedish postal
codes are five digits — but the operator was told the system had broken, giving them no idea what to change.

**Root cause.** The form treated any API error as fatal: `if (response.error) throw new Error('updateFailed')`,
discarding the structured field errors the API had returned. There was also no client-side postal-code check,
so a locally detectable mistake made a round trip only to come back as a generic failure.

**What I changed.**
- Added a market-aware client-side check using `getPostalCodeErrorKey(postCode, country, market)` with the
  tenant's market from `useTenant()`, so the specific message ("Postal code must have 5 digits") is shown
  immediately beneath the input.
- Replaced the blanket throw with real handling: extract field errors via `getFieldErrors`, map nested DTO
  paths with `stripNestedFieldPrefix` (both from #5044), attach them to the corresponding inputs, and show a
  translated `validationError` toast. Genuinely unexpected failures still surface the API's `messageCode`
  rather than a hardcoded string.
- Passed `errorMessage={errors?.['postCode']}` to the input so the error renders in place.

**Impact.** Turned an unactionable "system error" into a specific, market-correct validation message on the
field at fault. It also reused the two utilities built in #5044 rather than adding a third error-handling
approach — part of the same *Clear error messages* effort as #5316 and #5331.

---

### 146. #3702 — Company info save failed on the address object
`Bug fix` · `Admin UI` · **Impact 2.1/10** · **Production hotfix**
**Merged** 2025-08-28 · **Base** `subrite/main` (hotfix) · 1 file, +6/-2, 1 commit

**Problem.** Saving company info failed. Tenants could not update their own company details at all.

**Root cause.** `TenantService.update` passed the whole `updateTenantDto` — including the nested `address`
object — to `tenantRepository.update()`. TypeORM's `update` takes a flat partial entity and cannot handle a
nested relation object, so the query failed. The address was already being persisted separately by
`saveTenantAddress`, so it had no business being in the tenant update payload.

**What I changed.**
- Deleted the nested key before the update: `delete updateTenantDto.address;` — the address is saved by its own
  path, so removing it from the tenant payload is the correct fix rather than a workaround.

**Impact.** Restored company-info saving. Six lines. Paired with #3701, the same class of bug on the brand-kit
form, both hotfixed the same day.

---

### 147. #3701 — Brand kit settings save failed on file upload fields
`Bug fix` · `Widgets & branding` · **Impact 2.1/10** · **Production hotfix**
**Merged** 2025-08-28 · **Linear** SUBRITE-3250 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/main` (hotfix) · 2 files, +17/-2, 2 commits

**Problem.** Updating brand-kit settings failed, so tenants could not change their branding — logo, favicon,
fonts, hostnames.

**Root cause.** The same shape of bug as #3702. `BrandKitUpdateDto` legitimately carries non-entity fields —
`logoImage`, `faviconImage`, `primaryFontFile`, `secondaryFontFile` (uploaded files) and `hostnameIds` (a
relation) — but the whole DTO was passed to `brandKitRepository.update()`, which expects only real entity
columns.

**What I changed.**
- Added a static `BrandKitUpdateDto.removeNonEntityFields(dto)` stripping the five non-column fields, and
  called it before the update. Putting it on the DTO as a static method keeps the knowledge of which fields are
  not columns next to their declaration, rather than as a list of `delete` calls in the service.

**Impact.** Restored brand-kit updating. The DTO-level helper is a slightly better answer than #3702's inline
delete, because the list of non-entity fields lives with the DTO that defines them.

---

### 148. #3788 — Company details save and a misleading settings icon
`Bug fix` · `Admin UI` · **Impact 2.1/10**
**Merged** 2025-09-26 · **Linear** SUBRITE-3381 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 2 files, +2/-2, 1 commit

**Problem.** Company details still failed to save on `develop` — the third instance of the address-in-update
problem (#3702, #3701) — and the settings submenu used a key icon for a section about access control, where a
key conventionally means credentials rather than restriction.

**Root cause.** A residual `tenantRepository.update(tenant.id, updateTenantDto)` call not covered by the earlier
hotfixes, plus an icon choice that did not match the section's meaning.

**What I changed.**
- Removed the remaining problematic update call in `TenantService`.
- Changed the submenu icon from `KeyIcon` to `LockClosedIcon`.

**Impact.** Closed out the company-details save issue on the mainline after two hotfixes had patched
production, and corrected a small piece of visual vocabulary. Worth including for the pattern: the same defect
appearing in three places over four weeks is the signature of a fix applied to symptoms — #3701's DTO-level
helper is what finally addressed it structurally.

---

### 149. #3883 — Vipps login configuration could not be deleted
`Feature` · `Auth & access control` · **Impact 2.1/10**
**Merged** 2025-11-03 · **Linear** SUBRITE-3542 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 7 files, +178/-5, 7 commits

**Problem.** Follow-up to #3777. Tenants could add and edit an OAuth provider such as Vipps login, but not
remove one. A misconfigured provider stayed live and offered to members on the login screen with no way to
withdraw it.

**Root cause.** No delete endpoint existed. The tenant configuration is also cached, so simply deleting the row
would have left the provider still being offered until the cache expired.

**What I changed.**
- Added `DELETE :id` on `OauthProviderController`, guarded with
  `@CheckPermissions([ACTIONS.UPDATE, SECTIONS.ACCOUNT_SETTINGS])`.
- Injected `TenantService` into `OauthProviderService` and called
  `resetTenantCache(tenant.hostName)` after deletion and mutation, so the login screen stops offering the
  provider immediately rather than after a cache expiry.
- Added a `ConfirmationDialog` to `AuthProviderList` with toast feedback and `revalidateApiTag(authProvidersTag)`,
  plus translations — so a destructive action is confirmed and the list refreshes.

**Impact.** Completed OAuth provider management with removal. The cache reset is the part that makes it actually
work: without it a deleted provider would keep appearing on the login screen, which is worse than not being able
to delete it.

---

### 150. #4000 — Hardcoded CDN hostname removed from image handling
`Refactor / tech debt` · `Shared platform code` · **Impact 2.1/10**
**Merged** 2025-11-19 · **Linear** SUBRITE-3628 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 7 files, +46/-102, 7 commits

**Problem.** Six components branched on whether an image URL contained the literal string `cdn.united.no` — a
specific external CDN — and rendered a plain `next/image` in that case instead of the platform's
`CloudflareImage`. The hostname of one external provider was hardcoded into avatar and member-image rendering
across the frontend.

**Root cause.** A special case added to accommodate legacy avatar URLs, then copied to every component that
renders a member image, so the exception spread rather than being handled once.

**What I changed.**
- Removed the `cdn.united.no` conditional and the duplicate `next/image` branches from all six components
  (profile section, member avatar, user menu, onboarding upload, admin member info) and let `CloudflareImage`
  handle every URL.
- Kept `FILE.WHITE_LISTED_BASE_URLS` with an explanatory comment — the hostname still legitimately appears in
  stored data, so the allowlist is still needed; what was removed is the rendering special case.

**Impact.** −102/+46: removed a hardcoded third-party hostname from six rendering paths, so member images now
go through one component. Keeping the allowlist with a comment saying why is the right call — deleting it would
have broken existing stored URLs.

---

### 151. #4141 — Email preview text truncated and padded incorrectly
`Bug fix` · `Communication & notifications` · **Impact 2.1/10**
**Merged** 2025-12-01 · **Base** `subrite/develop` · 2 files, +21/-12, 2 commits

**Problem.** Refinement of #4113. Preview text was still not displaying correctly in mail clients. When the
author had supplied preview text, the body content was appended to it, so clients showed the preview followed by
the start of the email body. And short preview text left the client free to pull visible body content in after
it, so the preview line was not what the author wrote.

**Root cause.** Two issues in `injectEmailContentToTemplate`. The logic concatenated
`${previewText} ${sanitizedEmailContent}` rather than treating supplied preview text as authoritative. And mail
clients pad a short preheader by pulling in following content — the standard remedy is to pad the preheader with
zero-width characters, which was not being done.

**What I changed.**
- Made supplied preview text authoritative: if `previewText?.trim()?.length > 0`, use it alone; only fall back
  to `stripHtmlTags(emailContent)` when none was supplied.
- Extracted `PREVIEW_TEXT_LENGTH = 300` as a named constant in both the backend and frontend utilities,
  replacing the magic number.
- Padded a short preview to the full length with `'&nbsp;&zwnj;'.repeat(paddingLength)` — non-breaking space
  plus zero-width non-joiner, the conventional technique — so clients do not pull visible body text into the
  preview line.

**Impact.** Made the preview line in recipients' inboxes match what the author wrote, across every email the
platform sends. The zero-width padding is a specific piece of email-client knowledge that no amount of general
tidying would have produced.

---

### 152. #3753 — Admin podcast page crashed on an empty response
`Bug fix` · `Content & podcasts` · **Impact 2.0/10** · **Production hotfix**
**Merged** 2025-09-12 · **Linear** SUBRITE-3321 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/main` (hotfix) · 1 file, +2/-2, 1 commit

**Problem.** The admin podcasts page threw `Cannot read properties of null (reading 'filter')`. A tenant with no
podcasts — or a failed fetch — got a crashed page instead of an empty list.

**Root cause.** `podcastsResponse?.data` and `productsResponse?.data` were passed straight to code calling
`.filter()`. The optional chaining guarded the response but not the payload, so a null `data` reached the array
operation.

**What I changed.**
- Defaulted both to empty arrays: `podcastsResponse?.data || []` and `productsResponse?.data || []`, so
  downstream array operations always have an array.

**Impact.** Replaced a crashed page with an empty list — which is the correct rendering for a tenant with no
podcasts. Two lines, and it illustrates that `?.` on the response is not enough when the payload itself can be
null.

---

### 153. #4001 — Single payment provider not pre-selected at checkout
`Improvement` · `Checkout` · **Impact 2.0/10**
**Merged** 2025-11-19 · **Linear** SUBRITE-755 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 2 files, +9/-9, 4 commits

**Problem.** When a package offered exactly one payment provider, the checkout form still presented it as an
unselected choice the customer had to click. An unnecessary interaction on the payment step, where every extra
click costs conversions.

**Root cause.** `paymentMethod` was initialised to `''` unconditionally, with no consideration of how many
options existed. Separately, intro and recurring price state were initialised to `0` and then set by a
`useEffect`, causing an avoidable render with zero prices before the real values appeared.

**What I changed.**
- Initialised the selection from the data:
  `useState<string>(packageDetails?.paymentProviders?.length === 1 ? packageDetails.paymentProviders[0] : '')`
  — so a sole provider is pre-selected and multiple providers still require a choice.
- Replaced the price-setting `useEffect` with direct `useState` initialisers from `packageDetailsResponse`,
  removing the render pass that displayed `0`.

**Impact.** Removed a pointless click from checkout and a flash of zero prices. Initialising state from
available data rather than setting it in an effect is both correct and cheaper — the effect version always
renders once with the wrong value.

---

### 154. #4794 — Dashboard subscription graph unreadable with many plans
`Improvement` · `Analytics & reporting` · **Impact 2.0/10**
**Merged** 2026-04-16 · **Linear** SUBRITE-3869 *(pre-migration issue, no longer in Linear)* · **Base** `develop` · 7 files, +24/-21, 9 commits

**Problem.** As stated in the PR: the root dashboard subscription graph needed to be stacked so the total is
readable, and needed to show at most four series — the three largest by last month plus an "other" bucket.
Tenants with many subscription plans got an unreadable tangle of lines with no visible total.

**Root cause.** The chart rendered one series per plan with no cap and no stacking, so both readability and the
aggregate total degraded as plan count grew. The series-naming logic also had no case for the aggregated bucket.

**What I changed.**
- Added `stacked?: boolean` to `StatsView` and adjusted the chart-type branch to
  `(isAreaChart && !stacked) || isLineChart`, so an area chart can stack and show a readable total.
- Changed the series cap from 5 to 4 and added `FOUR = 4` to the backend `NoOfLine` enum, so the API and UI
  agree on the limit.
- Added handling for the aggregated `id === -1` bucket in series naming, so "other" is labelled rather than
  falling through to a `Plan -1` placeholder.

**Impact.** Made the dashboard's main chart readable for tenants with many plans, with the total visible. The
`id === -1` naming case is the detail that mattered — without it the aggregate series would have displayed a
nonsense label.

---

### 155. #4902 — Admin create-order did not default to EHF
`Improvement` · `Admin UI` · **Impact 2.0/10**
**Merged** 2026-04-30 · **Linear** [SUBRITE-44](https://linear.app/subrite/issue/SUBRITE-44) (Medium · Improvement) · **Base** `develop` · 1 file, +13/-2, 2 commits

**Problem.** Orders created from the admin panel are almost always invoiced by EHF/Avtalegiro, but the form
defaulted the payment method to unselected and "use main address" to true. Operators re-made the same two
choices on every order.

**Root cause.** Both pieces of state were initialised to fixed values with no reference to which providers the
tenant actually has enabled.

**What I changed.**
- Initialised `paymentMethod` with a lazy initialiser that pre-selects `EfakturaAvtalegiroEhf` when the tenant
  has it enabled, and leaves the field empty otherwise — so tenants without EHF are not given a
  wrong default.
- Initialised `useMainAddress` conditionally on the same check, since EHF orders generally need a separate
  invoice address.

**Impact.** Removed two repeated clicks from a frequent operator task, and defaulted correctly per tenant
rather than globally. Using lazy `useState` initialisers keyed off the tenant's actual providers is what makes
the default right rather than merely convenient.

---

### 156. #3734 — Payment confirmation duplicated its heading on mobile
`Bug fix` · `Checkout` · **Impact 2.0/10**
**Merged** 2025-09-11 · **Linear** SUBRITE-3295 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 3 files, +6/-25, 10 commits

**Problem.** On the payment-complete screen, mobile users saw the title and help text rendered twice — once from
a mobile-only block and once from the shared layout — with the image below the text rather than above it.

**Root cause.** Each of the three payment-complete components carried a duplicate `md:hidden` heading block
alongside the shared responsive grid. Rather than making the grid reorder on small screens, a second copy of the
content had been added for mobile — so both rendered where the hiding rule did not apply as intended.

**What I changed.**
- Deleted the duplicated `md:hidden` heading blocks from all three components (payment preview, auth sign-in
  form, payment confirmed) and adjusted the grid gap so the shared layout handles both breakpoints.

**Impact.** Fixed duplicated headings on the payment confirmation screen for mobile users. −25/+6: removing the
duplicated markup was the fix, and having it in three components was why it was worth doing properly.

---

### 157. #3955 — Podcast search could not be turned off per tenant
`Feature` · `Content & podcasts` · **Impact 2.0/10**
**Merged** 2025-11-12 · **Linear** SUBRITE-3594 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 16 files, +397/-50, 21 commits

**Problem.** The podcast search box on My Page was always shown. Tenants with only a handful of podcasts did not
want it — search over five items is clutter — but there was no setting.

**Root cause.** No preference existed for it, and the podcast repository query was written as a single chained
expression ending in `.getMany()`, leaving no place to conditionally add search behaviour.

**What I changed.**
- Migration `ShowPodcastSearchSettings` adding `showPodcastSearch` to `preference`, defaulting to `true` so
  existing tenants keep their current behaviour.
- Refactored `PodcastRepository` to assign the query builder to a variable before executing, so conditions can be
  added before `getMany()`.
- Added a new "Podcast rules" preferences tab with a `PodcastRulesConfigForm` and its `loading.tsx`, following
  the established settings-area structure.
- Noted in the PR that the tenant settings cache must be cleared for all tenants after deploy — the setting is
  cached, so without that step the change would not take effect.

**Impact.** Gave tenants control over a customer-facing UI element. The deployment note is the notable part:
identifying and communicating the required cache clear is what made the release actually work rather than
appearing to do nothing.

---

### 158. #5059 — Default Stripe card correction and subscription reactivation tooling
`Recovery tooling` · `Payments & providers` · **Impact 2.0/10** · **Hotfix branch**
**Merged** 2026-06-03 · **Base** `develop` · 3 files, +416/-0, 6 commits

**Problem.** After the Psykologisk migration (#5004), some members' Stripe default payment methods did not match
the card the platform believed was default. A recurring charge would then use the wrong card, and affected
subscriptions had been deactivated as a result.

**Root cause.** The migration imported payment methods without reconciling which one Stripe considered default
for each customer, so the local and remote notions of "default card" diverged.

**What I changed.**
- Added notebook `7-default-card-correction.ipynb` reconciling each customer's Stripe default payment method
  against the local record and generating the corrective SQL — continuing the numbered-stage structure of the
  original migration.
- Added `8-update-default-cards.sh`, which refuses to run if the generated SQL file is absent
  (`Missing $SQL_FILE — run default-card-correction.py first`) rather than silently doing nothing.
- Added `9-reactivate-subscriptions.sh` with an interactive dry-run prompt and a CSV proof-of-work output, so the
  reactivation is reviewable and evidenced.

**Impact.** Corrected payment-method state for affected customers and reactivated the subscriptions that had
been deactivated because of it. The guard on the missing SQL file and the dry-run default are what made a
payment-data repair safe to run — a script that quietly does nothing is more dangerous than one that fails.

---

### 159. #5238 — Member management pagination and focus states
`Improvement` · `Member portal (My Page)` · **Impact 2.0/10**
**Merged** 2026-06-30 · **Linear** [SUBRITE-518](https://linear.app/subrite/issue/SUBRITE-518) (High · Improvement) · **Base** `develop` · 4 files, +88/-73, 3 commits

**Problem.** Follow-up feedback on the seat-management work (#5208/#5249). The page size of 9 meant excessive
pagination for large subscriptions, the cards lacked a background so they read as flat, and the interactive
elements had no visible keyboard focus indicators — so the screen was not usable without a mouse.

**Root cause.** `PAGE_SIZE = 9` was set conservatively during the initial build, and the new components had been
styled without `focus:` states or an explicit card background.

**What I changed.**
- Raised `PAGE_SIZE` from 9 to 20, roughly halving the pagination needed.
- Added `bg-white` to the members card so it reads as a surface.
- Added keyboard focus indicators across the member-management actions, search, filters and pagination, and made
  the open state of the member row action menu visibly highlighted.

**Impact.** Made the seat-management screens keyboard-navigable and less tedious to page through. Accessibility
work of this kind is the difference between a screen being usable and merely present for anyone not using a
mouse.

---

### 160. #3313 — Invoice table column layout and wrapping
`Bug fix` · `Admin UI` · **Impact 1.9/10** · **Production hotfix**
**Merged** 2025-06-12 · **Linear** SUBRITE-2955 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/main` (hotfix) · 2 files, +3/-3

**Problem.** The total-amount column in the member invoice table was laid out with a half-width wrapper, so
amounts were cramped and wrapped onto two lines. Currency amounts split across lines are hard to read and easy
to misread.

**Root cause.** The cell used `w-full sm:w-1/2 text-right`, constraining the value to half the column, and
neither the cell nor the shared table header prevented wrapping.

**What I changed.**
- Changed the cell to `w-full text-right whitespace-nowrap pr-2`, giving the amount the full column width and
  preventing the wrap.
- Added `whitespace-nowrap` to the shared `TableWrapper` header styling, so column headings do not wrap either.

**Impact.** Made invoice amounts readable at a glance. Presentational, three lines, and included honestly at its
real weight — the heuristic scoring initially over-ranked it because it touches invoices; reading the diff showed
it is a Tailwind layout fix.

---

### 161. #3662 — Owner-only products with zero seats handled correctly
`Bug fix` · `Subscriptions & billing lifecycle` · **Impact 1.9/10**
**Merged** 2025-08-25 · **Linear** SUBRITE-1752 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 9 files, +52/-17, 38 commits

**Problem.** Products configured with zero additional seats — owner-only products — were mishandled throughout.
The owner did not get access, the product could still be added as an optional extra, and the group-members UI
appeared for a product with no seats to manage. Product creation also defaulted to one seat and rejected zero as
invalid, so the configuration was hard to create in the first place.

**Root cause.** `maxQuantityPerOrder` was treated purely as an upper bound, with `> 1` meaning "has seats". The
value `0` — meaning "no distributable seats, owner only" — was not recognised as a distinct case anywhere.

**What I changed.**
- Made `hasAccess` in `order-utility.ts` account for it: `maxQuantityPerOrder > 1 || maxQuantityPerOrder === 0`,
  with a comment recording that zero means owner-only access.
- Added `&& memPackProd.product.maxQuantityPerOrder > 0` to the optional-product conditions in both the faktura
  and purchase transactions, so a zero-seat product cannot be added as an extra.
- Gated the group-info UI on `(groupInfo?.seatsAllowed ?? 0) > 0` in both the member-facing and admin product
  detail components, so seat management is hidden when there are no seats.
- Changed product creation to default `maxQuantityPerOrder` to `0` and validate `< 0` rather than `<= 0`, making
  the owner-only configuration expressible.
- Added form validation to `AccountingAndDelivery` and corrected a misspelled variable (`tApiMesageCode`).

**Impact.** Made owner-only products work end-to-end, from creation through purchase to display. This is the
first pass at the problem; #4400 later corrected the same `hasAccess` derivation again, which suggests the
concept needed the clearer treatment it eventually got.

---

### 162. #3844 — Selected communication sender disappeared from the form
`Bug fix` · `Communication & notifications` · **Impact 1.9/10**
**Merged** 2025-10-14 · **Base** `subrite/develop` · 6 files, +12/-2, 2 commits

**Problem.** Follow-up to #3803, which introduced the archived sender status. Existing communications whose
sender had since been archived showed no sender at all — the field appeared empty, so opening and saving such a
communication risked clearing its sender.

**Root cause.** `CommunicationSenderRepository` fetched only `status: APPROVED`. An archived sender was therefore
absent from the options list, so a communication referencing one had nothing to display. Filtering the archived
sender out of the *options* is correct; filtering it out of the *data* is not.

**What I changed.**
- Changed the query to `status: Not(COMMUNICATION_SENDER_STATUS.PENDING)`, so approved and archived senders are
  both returned and an existing selection can be displayed.
- Added `hidden: sender.status !== COMMUNICATION_SENDER_STATUS.Approved` at each of the five form sites (email
  setup, create email, SMS content, add-new-communication, payment-link SMS), so an archived sender remains
  visible when already selected but cannot be newly chosen.

**Impact.** Stopped existing communications appearing to have no sender after their sender was archived. The
distinction the fix draws — return the data, hide it as a new option — is the correct way to handle soft-deleted
references, and applying it at all five form sites kept the behaviour consistent.

---

### 163. #3910 — Global member search fired a request per keystroke
`Performance` · `Admin UI` · **Impact 1.9/10**
**Merged** 2025-11-03 · **Linear** SUBRITE-3506 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 4 files, +75/-55, 7 commits

**Problem.** The admin global member search issued an API request on every keystroke. Typing a name produced a
dozen requests, with responses arriving out of order so the visible results could belong to a prefix of what had
been typed. Numeric searches also ran an inappropriate lookup for long digit strings.

**Root cause.** No debouncing and no request cancellation. On the backend, the numeric branch of
`MemberService`'s search applied to any numeric query, including long strings that cannot be a member number.

**What I changed.**
- Added `useDebouncedCallback` with a 350 ms `DEBOUNCE_DELAY` to `MemberSearchBox`, and an
  `abortControllerRef` so in-flight requests are cancelled when superseded — displayed results always match the
  current input.
- Threaded a `RequestInit` through `getMembersSearchData` so the abort signal reaches the API call.
- Constrained the backend numeric branch to `isNumber && query.length < 10`, so a long digit string is not
  treated as a member-number lookup.
- Reduced the repo ESLint threshold from 83 to 82.

**Impact.** Cut search request volume by roughly an order of magnitude and eliminated out-of-order results in a
frequently used admin control. The same debounce-plus-abort pattern was later applied to the list filters
(#5029, #5346).

---

### 164. #3796 — Request-log cleanup timed out mid-vacuum
`Bug fix` · `Backend (other)` · **Impact 1.9/10**
**Merged** 2025-09-30 · **Linear** SUBRITE-3380 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 2 files, +27/-1, 4 commits

**Problem.** The scheduled cleanup of old request logs failed with a query read timeout. `VACUUM` on a large
table takes longer than the connection's statement timeout, so the maintenance job never completed — and request
logs kept growing.

**Root cause.** `this.dataSource.query('VACUUM request_log')` ran on a pooled connection carrying the
application's default statement timeout. That timeout is correct for request-serving queries and wrong for a
maintenance operation.

**What I changed.**
- Added `runVacuumNoTimeout()`, which creates a dedicated `QueryRunner`, connects, and runs the vacuum on that
  connection without the application's statement timeout — so the maintenance operation is not bound by a limit
  intended for user-facing queries, and the pooled connections keep theirs.
- Updated the service test to provide `createQueryRunner`.

**Impact.** Restored the request-log cleanup job, preventing unbounded table growth. Using a dedicated query
runner rather than raising the global timeout is the right shape: it exempts only the operation that needs it.

---

### 165. #4450 — Petro tenant data migration
`Data migration` · `Members, segments & import/export` · **Impact 1.8/10**
**Merged** 2026-02-11 · **Linear** SUBRITE-3666 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 31 files, +4175/-1, 24 commits

**Problem.** Onboarding the Petro tenant required migrating members, subscriptions, Stripe payment methods,
payment history, group members and whitelisted email domains from their previous system.

**Root cause.** Not a defect — a tenant onboarding data-engineering task.

**What I changed.**
- Seven numbered notebooks following the pattern established by #5004: cancel subscriptions, users,
  subscriptions, Stripe payments, payment history, group members, whitelist domains — ordered to respect real
  data dependencies and each independently inspectable.
- Shell scripts for the insert stages and a `notes.md` documenting the tenant's specifics.
- Also corrected a `"version": "3.11.undefined"` metadata value in an unrelated Filter Nyheter notebook.

**Impact.** Delivered another tenant migration. The value in these entries is cumulative rather than
individual: reusing the same numbered-stage structure across Psykologisk, Petro, Minerva, Filter Nyheter and
Dog Trainer meant each subsequent migration was faster and more reviewable than a bespoke script would have
been.

---

### 166. #4780 — Minerva export analysis and migration preparation
`Data migration` · `Members, segments & import/export` · **Impact 1.8/10**
**Merged** 2026-04-10 · **Linear** SUBRITE-3861 *(pre-migration issue, no longer in Linear)* · **Base** `develop` · 27 files, +3492/-0, 16 commits

**Problem.** Preparing the Minerva tenant migration: analysing their export, splitting it into workable
datasets, and mapping members, subscriptions and payment history. Minerva also had ongoing Vipps charges that
had to be found and cancelled before migration, otherwise customers would be charged by both systems.

**Root cause.** Not a defect — migration preparation, with the added complication of in-flight recurring
charges at the source system.

**What I changed.**
- A `checklist.md` stating the required inputs and the order of operations, naming each expected file — so the
  migration is repeatable rather than dependent on the author's memory.
- `split-tabs.ipynb` to divide the customer's multi-tab export into the individual CSVs the pipeline consumes.
- Six numbered notebooks: members, unique subscriptions, subscriptions, payment history, and — specific to this
  tenant — `5-find-ongoing-vipps-charges` and `6-cancel-ongoing-vipps-charges`, separated so the charges to be
  cancelled are reviewed before anything is cancelled.
- Shell scripts for the insert stages.

**Impact.** Prepared a tenant migration including the double-charging risk from in-flight Vipps agreements.
Splitting "find the charges" from "cancel the charges" into two reviewable stages is the notable judgement —
cancelling live payment agreements is not something to do in one unreviewed pass.

---

### 167. #2884 — Ledger reference filter had no effect
`Bug fix` · `Invoicing & accounting` · **Impact 1.8/10**
**Merged** 2025-03-07 · **Linear** SUBRITE-2604 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 1 file, +1/-0, 4 commits

**Problem.** The reference filter on the ledger view did nothing — entering a reference returned the unfiltered
list, so finance staff could not locate a transaction by its reference.

**Root cause.** The filter was accepted by the UI and the DTO but never applied in
`TransactionRepository`'s where clause. The parameter was plumbed through and then dropped.

**What I changed.**
- Added the missing clause: `...(filter?.reference && { reference: filter.reference })`.

**Impact.** Made the reference filter functional. One line, and the first of the ledger-filter cluster —
#2886 then trimmed whitespace from the value and #2906 fixed the amount and email variants, so the filter went
from non-functional to correct over three PRs in the first fortnight.

---

### 168. #2885 — Ledger email link went to the wrong page
`Bug fix` · `Invoicing & accounting` · **Impact 1.8/10**
**Merged** 2025-03-07 · **Linear** SUBRITE-2533 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 1 file, +1/-1, 2 commits

**Problem.** Clicking a member's email in the ledger list navigated to the member's overview page rather than
their ledger detail. Someone working through ledger entries lost their place on every click and had to navigate
back in.

**Root cause.** The link target omitted the `/ledger` segment.

**What I changed.**
- Changed the `href` from `/admin/members/${memberId}` to `/admin/members/${memberId}/ledger`.

**Impact.** Made the ledger list's links land where the user is working. One line, entirely a workflow fix —
the destination was valid, just not the one that made sense from a ledger context.

---

### 169. #2906 — Ledger amount and email filters, and pagination
`Bug fix` · `Invoicing & accounting` · **Impact 1.8/10** · **Hotfix branch**
**Merged** 2025-03-13 · **Linear** SUBRITE-2604 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 3 files, +11/-14, 5 commits

**Problem.** The remaining ledger filter defects. Filtering by amount did not match the values shown, the email
filter failed on pasted input with whitespace, and pagination lost the active filters — so paging to the second
page returned unfiltered results.

**Root cause.** Ledger amounts are stored signed (a credit is negative) but displayed as absolute values, so
`Equal(filter.amount)` never matched a credit the user could see. The email filter used the value untrimmed.
And the frontend passed filters to pagination by enumerating each field by name, so any filter not listed was
silently dropped.

**What I changed.**
- Replaced `Equal`/`MoreThan`/`LessThan` with TypeORM `Raw` comparisons on the absolute value —
  `Raw((alias) => \`ABS(${alias}) = :amount\`, { amount: filter.amount })` — so filtering matches what the user
  sees rather than the stored sign.
- Trimmed the email filter: `Like(\`%${filter.memberEmail.trim()}%\`)`.
- Replaced the hand-enumerated filter fields in the pagination call with `...(filters || {})`, so every filter is
  carried through and adding a new one does not require remembering to list it here.

**Impact.** Completed the ledger filtering fixes so amount, email and reference all work and survive pagination.
The `ABS()` comparison is the substantive part — the mismatch between stored sign and displayed value meant the
amount filter could not work for credits at all.

---

### 170. #2911 — Help text support for custom properties
`Feature` · `Shared platform code` · **Impact 1.8/10**
**Merged** 2025-03-13 · **Linear** SUBRITE-2632 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 5 files, +32/-3, 9 commits

**Problem.** Tenant-defined custom properties had a label but no way to explain themselves. A field like
"Region code" or "Membership tier" needs a hint telling the member what to enter, and there was nowhere to put
one — so tenants either used a long confusing label or left members guessing.

**Root cause.** `CustomPropertySettings` had no help-text field, and it was not exposed through the view
response DTO.

**What I changed.**
- Added an optional `helpText?: Translation` to `CustomPropertySettings` and surfaced it on
  `CustomPropertyViewResponse` (`this.helpText = property.settings?.helpText`) — typed as `Translation`, so the
  hint is per-language rather than one string, which matters for a Norwegian/English platform.
- Consumed it in the shared `ui/CustomProperty` component, adding `useLocale` so the correct language is
  rendered.

**Impact.** Let tenants explain their own custom fields to members, in both supported languages. Using the
`Translation` type from the outset avoided the retrofit that a plain string would have needed later.

---

### 171. #3341 — Email link styling and image URL copying
`Improvement` · `Communication & notifications` · **Impact 1.8/10**
**Merged** 2025-06-19 · **Linear** SUBRITE-3057 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 4 files, +33/-2, 6 commits

**Problem.** Two issues around email content authoring. Links in serialised emails carried no styling, so in
mail clients that do not apply default link colours they rendered as ordinary text — recipients could not tell
they were clickable. And after uploading an image in settings there was no way to get its URL, which authors
needed in order to reference it in email templates.

**Root cause.** The Slate serialiser emitted a bare `<a href target>` with no inline style; email clients cannot
be relied on to style links, so inline styling is required. And `UploadInput` displayed a preview without
exposing the URL.

**What I changed.**
- Added inline link styling to the serialiser:
  `style="color: #1a0dab; text-decoration: underline;"` — inline because email clients ignore stylesheets.
- Added a copy-to-clipboard button to `UploadInput`, shown when a preview exists, with a `TooltipWrapper`
  confirming "image URL copied" and translations for it.

**Impact.** Made email links visibly clickable across mail clients, and gave content authors access to the URLs
of images they upload. Both are small, and both remove a step that authors were otherwise working around.

---

### 172. #3516 — Invoice reference input rendered as an uncontrolled React field
`Bug fix` · `Checkout` · **Impact 1.8/10** · **Hotfix branch**
**Merged** 2025-07-23 · **Linear** SUBRITE-2856 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 2 files, +2/-2, 2 commits

**Problem.** Production follow-up to the invoice-reference feature (#3485). The reference input on the billing
address and e-invoice payment forms misbehaved: when no reference had been entered React received `undefined`
as the input's value, which switches the field from controlled to uncontrolled and produces a console warning
and unreliable editing behaviour.

**Root cause.** `value={billingAddressData.invoiceReference}` — the field is optional, so the value is
`undefined` until something is typed, and React treats an `undefined` value as "uncontrolled".

**What I changed.**
- Defaulted the value in both forms: `value={billingAddressData.invoiceReference ?? ''}`, so the input is
  controlled from first render.

**Impact.** Made the new invoice-reference input behave correctly on the checkout and e-invoice forms. Two
lines, and the standard fix for an optional field bound to a controlled React input — worth including because
it is the production follow-up that finished #3485.

---

### 173. #3689 — Podcast management: ordering, own artwork and QR codes
`Feature` · `Content & podcasts` · **Impact 1.8/10**
**Merged** 2025-09-01 · **Linear** SUBRITE-3234 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 34 files, +1312/-516, 85 commits

**Problem.** Podcast management was thin. Tenants could not control the order podcasts appear in, could not
upload their own artwork (the image was always taken from the RSS feed and overwritten on every feed refresh),
and the podcast DTO exposed only a handful of fields so descriptions were unavailable to the frontend.

**Root cause.** `PodcastDto` was constructed from four positional arguments
(`new PodcastDto(feed.id, feed.name, podcastUrlSignature, feed.imageUrl)`), so adding a field meant changing
every call site. There was no sort order. And the feed sweep unconditionally wrote
`imageUrl: meta.imageUrl`, so a tenant-uploaded image was replaced by the feed's on the next refresh.

**What I changed.**
- Changed `PodcastDto` to take the `Podcast` entity plus the URL signature (`new PodcastDto(feed, podcastUrlSignature)`)
  and added `description` and `sortOrder`, so the DTO grows without touching call sites.
- Added tenant image upload: `imageFile?: Express.Multer.File` on `UpdatePodcastDto`, a
  `PODCAST_IMAGE` S3 file category, and `S3FileUploadService` injected into `PodcastService`.
- Made the feed sweep respect a tenant-supplied image:
  `imageUrl: podcast.imageUrl ? podcast.imageUrl : meta.imageUrl` — the feed only provides artwork when the
  tenant has not chosen their own.
- Added podcast sort-order support and a bulk sort-order update, and `react-qr-code` for generating podcast
  feed QR codes.

**Impact.** Turned podcasts from a read-only mirror of an RSS feed into something tenants control — ordering,
their own artwork, and shareable QR codes. The image precedence fix is the important one: without it a
tenant's uploaded artwork was silently reverted at the next feed refresh.

---

### 174. #3911 — Doubled slash in package URLs from a trailing-slash hostname
`Bug fix` · `Shared platform code` · **Impact 1.8/10** · **Hotfix branch**
**Merged** 2025-10-29 · **Base** `subrite/develop` · 2 files, +11/-1, 4 commits

**Problem.** Package URLs shown in the admin panel contained a doubled slash. A malformed purchase URL that is
copied into a campaign or shared with a customer is a broken link on a revenue path.

**Root cause.** `getSite()` can return a hostname with a trailing slash depending on how the tenant's host is
configured. Concatenating that with a path beginning with `/` produced `//`, and nothing normalised it.

**What I changed.**
- Added a shared `removeTrailingSlashes(str)` utility (`str.replace(/\/+$/, '')`, null-safe) and applied it to
  the hostname: `removeTrailingSlashes(await getSite())` — normalising at the source rather than patching each
  concatenation.

**Impact.** Fixed shareable package links. Normalising the hostname once, in a shared utility, means every
future URL built from it is correct — the alternative would have been fixing each construction site as it was
reported.

---

### 175. #3746 — Email communication authoring flow: validation and review feedback
`Improvement` · `Communication & notifications` · **Impact 1.8/10**
**Merged** 2025-09-19 · **Linear** SUBRITE-3262 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 19 files, +209/-351, 11 commits

**Problem.** A round of consolidated feedback on the email communication authoring flow — the multi-step
content / receivers / send wizard. Validation was inconsistent between steps, the subject field could hold an
undefined value, and the tab navigation, review and send-test screens each handled state and errors their own
way.

**Root cause.** The wizard's steps had been built incrementally, each with its own inline validation and value
handling, so behaviour differed between steps and validation logic was duplicated.

**What I changed.**
- Extracted the validation into dedicated hooks — `useContentValidation` and
  `useSendConfigurationValidation` — so each step's rules live in one place instead of inline in the form.
- Defaulted the subject to `''` (`communicationDetails.subject ?? ''`) rather than passing a possibly
  undefined value into a controlled input.
- Applied the remaining feedback across the content form and fields, emoji subject input, tab navigation,
  email review, send-test handler, receivers form and send configuration.

**Impact.** −351/+209 across 19 files: the authoring wizard behaves consistently step to step, with validation
extracted into reusable hooks rather than duplicated inline. Net code reduction, which is the shape a
consolidation pass should have.

---

### 176. #3062 — Harvest Magazine export analysis and migration preparation
`Data migration` · `Members, segments & import/export` · **Impact 1.8/10**
**Merged** 2025-05-02 · **Linear** SUBRITE-2761 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 15 files, +4863/-0

**Problem.** Preparing the Harvest Magazine tenant migration: analysing their system's export and mapping it to
Subrite's member and subscription model.

**Root cause.** Not a defect — migration analysis and preparation.

**What I changed.**
- Notebooks and scripts under `scripts/data-ops/` analysing the source export and preparing the import,
  entirely additive (+4863/−0) with no application code touched.

**Impact.** The earliest of the tenant-migration pipelines in this portfolio, from May 2025. The structure
established here — analyse, map, stage, insert, all in reviewable numbered steps — is what the later migrations
(#3459, #3816, #4169, #4450, #4780, #5004) followed.

---

### 177. #3459 — Media tenant import manifest preparation
`Data migration` · `Members, segments & import/export` · **Impact 1.8/10**
**Merged** 2025-09-11 · **Linear** SUBRITE-2969 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 31 files, +5931/-0

**Problem.** Preparing the import manifest for a media tenant migration — mapping their export into the staged
datasets the import pipeline consumes.

**Root cause.** Not a defect — migration preparation.

**What I changed.**
- The largest of these preparation sets: 31 files of notebooks and scripts (+5931/−0) building the import
  manifest, again entirely under `scripts/data-ops/` with no application code changed.

**Impact.** Prepared another tenant migration. Volume alone is not impact, which is why these sit here rather
than higher — but the cumulative effect of five such pipelines is that tenant onboarding became a repeatable
process rather than a bespoke project each time.

---

### 178. #3816 — Dog Trainer tenant import script preparation
`Data migration` · `Members, segments & import/export` · **Impact 1.8/10**
**Merged** 2025-10-24 · **Linear** SUBRITE-3338 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 22 files, +3207/-0

**Problem.** Preparing the import scripts for the Dog Trainer tenant migration.

**Root cause.** Not a defect — migration preparation.

**What I changed.**
- 22 files of notebooks and shell scripts under `scripts/data-ops/`, following the established numbered-stage
  pattern, entirely additive.

**Impact.** Prepared another tenant migration using the shared pipeline structure. Included for completeness.

---

### 179. #4269 — Area chart folded into the shared stats view
`Refactor / tech debt` · `Analytics & reporting` · **Impact 1.7/10**
**Merged** 2025-12-22 · **Base** `subrite/develop` · 3 files, +185/-224, 6 commits

**Problem.** The payment-flow analytics screen had its own bespoke Recharts area-chart implementation, separate
from the shared `StatsView` used by the platform's other statistics screens. Two implementations of the same
chart type meant two places to fix anything.

**Root cause.** `StatsView` supported bar and line charts only, so anything needing an area chart had to build
its own.

**What I changed.**
- Added `AREA` to the `StatsViewType` enum and implemented area-chart rendering inside `StatsView`.
- Deleted the bespoke `PaymentFlowChart` implementation and pointed the screen at the shared component.
- Flagged in the PR that all statistics screens needed testing on stage before production, since the change
  touches the component every stats view renders through.

**Impact.** −224/+185: one shared chart component instead of two implementations, and the groundwork that let
#4300 and #4303 delete three more bespoke dashboard charts (−824 lines) shortly afterwards. The explicit
"test everything on stage" note is the right instinct for a change to a widely shared component.

---

### 180. #5356 — Unsubscribe pages said "newsletter" for SMS
`Feature` · `Communication & notifications` · **Impact 1.7/10**
**Merged** 2026-07-23 · **Linear** [SUBRITE-97](https://linear.app/subrite/issue/SUBRITE-97) (High) · **Base** `develop` · 10 files, +61/-16, 10 commits

**Problem.** A member unsubscribing from SMS was taken to a page that talked about newsletters. The unsubscribe
flow had one set of wording regardless of channel, so an SMS opt-out told the recipient they were unsubscribing
from something they had not received. Getting an unsubscribe wrong is both a compliance and a trust problem.

**Root cause.** The unsubscribe pages had no concept of a channel. The SMS short-link resolver forwarded only a
token, so by the time the page rendered there was nothing to distinguish an SMS opt-out from an email one.

**What I changed.**
- Added an `unsubscribeChannel.ts` module with a narrow `UnsubscribeChannel = 'sms' | 'email'` type and a
  `parseUnsubscribeChannel(value)` that returns `'sms'` only for exactly that value and otherwise defaults to
  `'email'` — so an unknown or missing channel keeps the existing email behaviour rather than breaking.
- Threaded `channel` through the unsubscribe page, its success page and both container components.
- Made the SMS short-link resolver (`/u/[code]`) forward `channel: 'sms'` alongside the token, so the channel is
  known from the entry point.
- Added SMS-specific wording for the already-unsubscribed, in-progress and success states in English, Danish,
  Norwegian and Swedish.

**Impact.** Made SMS unsubscribe pages describe what the member is actually unsubscribing from, in all four
supported languages. Defaulting an unrecognised channel to email is what made this safe to ship — no existing
unsubscribe link changed behaviour.

---

### 181. #3623 — Cancelling a subscription change had no confirmation
`Improvement` · `Admin UI` · **Impact 1.6/10**
**Merged** 2025-08-18 · **Linear** SUBRITE-3205 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 3 files, +21/-1, 5 commits

**Problem.** The "withdraw subscription change" action in the admin subscription table fired immediately on
click. Cancelling a pending subscription change is destructive and not trivially reversible, and a misclick did
it with no warning.

**Root cause.** The button called `handleWithdrawSubscriptionChange` directly, with no interstitial.

**What I changed.**
- Added modal state and a shared `ConfirmationDialog` with a title, description and translated yes/no buttons,
  and repointed the button to open it — so the action requires deliberate confirmation.

**Impact.** Put a confirmation in front of a destructive action on a frequently used screen. Small, and the kind
of guard that only proves its worth when someone misclicks.

---

### 182. #3252 — Theme colour settings allowed inaccessible combinations
`Feature` · `Widgets & branding` · **Impact 1.6/10**
**Merged** 2025-06-02 · **Base** `subrite/develop` · 7 files, +167/-35, 5 commits

**Problem.** Tenants configuring their theme colours could pick foreground/background combinations with
insufficient contrast, producing customer-facing pages that fail WCAG accessibility standards and are genuinely
hard to read. Nothing warned them.

**Root cause.** The colour settings form validated only that a value was a valid colour, with no contrast
evaluation — there was no way to know whether a chosen pair was readable.

**What I changed.**
- Added the `accessible-colors` dependency and a `getContrastCheck(fg, bg)` utility returning the contrast
  ratio plus `passesAA` and `passesAAA` flags, so the form can report both the standard level reached and the
  ratio itself.
- Wired it into `ColorsForm`, replacing the generic `fieldErrors` record with per-colour error state so the
  warning attaches to the specific colour at fault.
- Added a `required` prop to the shared `ColorPicker` and the accompanying translations.

**Impact.** Gave tenants immediate feedback when their brand colours would produce inaccessible pages, at the
point of choosing them. Reporting the WCAG level rather than a bare pass/fail lets a tenant make an informed
decision about their own branding.

---

### 183. #3369 — Member invite forms simplified
`Refactor / tech debt` · `Admin UI` · **Impact 1.6/10**
**Merged** 2025-06-26 · **Linear** SUBRITE-3031 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 8 files, +153/-196, 12 commits

**Problem.** Follow-up to #3321. The member-invite and gift-receiver forms each carried their own copy of a
`checkIfFieldIsRequired(fieldName)` helper that walked the tenant's input-field configuration. Error state was
also updated with a stale-closure pattern (`setErrors({ ...errors, … })`), so two validation failures in quick
succession could lose the first.

**Root cause.** Duplicated required-field logic across forms, and object-spread error updates reading a possibly
stale `errors` value rather than using the functional updater.

**What I changed.**
- Removed the duplicated `checkIfFieldIsRequired` implementations from both the member-invite and the
  member-facing details form.
- Switched error updates to the functional form: `setErrors((prevErrors) => ({ ...prevErrors, … }))`, so
  concurrent validation failures accumulate correctly.
- Guarded `PhoneNumberInput` against an empty value, and tidied `AssignExistingUser` and `UploadInput`.

**Impact.** −196/+153 across the invite forms: duplicated logic removed and a real stale-state bug in error
handling fixed. The functional-updater change is the substantive part — the previous pattern silently dropped
errors under fast input.

---

### 184. #3646 — Active subscriptions not shown first
`Improvement` · `Admin UI` · **Impact 1.6/10**
**Merged** 2025-08-20 · **Linear** SUBRITE-3209 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 2 files, +32/-1, 1 commit

**Problem.** The admin subscription table sorted purely by descending id, so a member's active subscription
could appear below several stopped or expired ones. Support staff looking at a member's subscriptions had to
scan for the one that is actually live — on a screen used constantly.

**Root cause.** `(data || []).sort((a, b) => b.id - a.id)` — newest first, with no regard for status. Recency is
a poor proxy for relevance here, since a member's active subscription may well be an older record.

**What I changed.**
- Extracted a shared `sortPackagesByStatus(data)` utility ordering by subscription status first and id second,
  and used it in place of the inline id sort — so active subscriptions surface to the top and the rule is
  reusable wherever subscriptions are listed.

**Impact.** Put the subscription that matters at the top of a screen support staff use constantly. Extracting
the comparator rather than inlining it means other subscription lists can adopt the same ordering.

---

### 185. #3665 — Sender approval recipient hardcoded in source
`Improvement` · `Communication & notifications` · **Impact 1.6/10**
**Merged** 2025-08-22 · **Base** `subrite/develop` · 3 files, +3/-3, 1 commit

**Problem.** The address receiving sender-approval requests was a hardcoded constant,
`APPROVAL_REQUEST_RECEIVER = 'support@subrite.no'`, compiled into the application. Changing where approval
requests go — or pointing them somewhere else in a non-production environment — required a code change and a
deploy.

**Root cause.** Environment-specific configuration expressed as a source constant.

**What I changed.**
- Deleted the constant and its module, replacing it with `getEnv('SENDER_APPROVAL_REQUEST_RECEIVER_EMAIL')`.
- Documented the new variable in `secrets.env.example`, so it is discoverable when setting up an environment.

**Impact.** Moved an environment-specific address out of source into configuration. Three lines, and it is the
prerequisite for #3679 — which removed the `NODE_ENV` branch that had existed precisely because the value was not
configurable.

---

### 186. #3750 — Email preview text wrongly required
`Bug fix` · `Communication & notifications` · **Impact 1.6/10** · **Production hotfix**
**Merged** 2025-09-12 · **Linear** SUBRITE-3320 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/main` (hotfix) · 2 files, +0/-21, 4 commits

**Problem.** Email preview text was enforced as mandatory in both the review checklist and the send-configuration
validation, so a communication could not be sent without it. Preview text is genuinely optional — the platform
falls back to the start of the body (see #4113, #4141) — so tenants were blocked from sending on a field that
does not need a value.

**Root cause.** Both validation hooks treated an empty `emailPreviewText` as an error, contradicting the
fallback behaviour implemented on the sending side.

**What I changed.**
- Removed the preview-text requirement from `useEmailValidation` and from both branches of
  `useSendConfigurationValidation` (initial validation and the modified-fields path).

**Impact.** Unblocked sending for communications without preview text. A pure deletion (+0/−21) — the correct
fix was to remove validation that contradicted the system's actual behaviour, not to add a workaround.

---

### 187. #3795 — Norwegian payment-provider labels
`Improvement` · `Translations & copy` · **Impact 1.6/10** · **Production hotfix**
**Merged** 2025-09-30 · **Base** `subrite/main` (hotfix) · 1 file, +10/-10, 6 commits

**Problem.** Payment-provider labels shown to Norwegian users were wrong or untranslated. These strings appear at
checkout, where the customer chooses how to pay, so incorrect naming is confusing at the point of payment.

**Root cause.** Incorrect entries in the Norwegian translation file.

**What I changed.**
- Corrected ten Norwegian payment-provider strings in `shared/translations/no.json`.

**Impact.** Correct provider names for Norwegian customers at checkout. Translation-only; included for
completeness at its real weight.

---

### 188. #3799 — Custom period text not applied to product items
`Bug fix` · `Checkout` · **Impact 1.6/10**
**Merged** 2025-09-30 · **Linear** SUBRITE-3243 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 5 files, +32/-11, 9 commits

**Problem.** Follow-up to #3771, which let tenants write their own billing-period wording. The override was
applied in some places but not on individual product items or the total-price line, so a checkout page showed the
tenant's custom wording in one place and the platform's generated wording in another — inconsistent copy on the
purchase page.

**Root cause.** `customPeriodText` was not threaded into `OptionalProduct` or `TotalPriceInformation`, and the
generated recurring-period text was rendered unconditionally, so it appeared alongside the override rather than
being replaced by it.

**What I changed.**
- Passed `customPeriodText` (typed as `Translation`) from `PaymentForm` into both `OptionalProduct` and
  `TotalPriceInformation`.
- Made the generated text conditional — `{autoRenew && !customPeriodText && …}` — with a comment noting that the
  generated wording is unnecessary when an override exists, so the two never render together.

**Impact.** Made the tenant's custom period wording consistent across the whole checkout page. Completes #3771:
an override applied in only some places is arguably worse than none, because the page contradicts itself.

---

### 189. #3869 — Unimplemented landing-pages section removed from the admin panel
`Refactor / tech debt` · `Admin UI` · **Impact 1.6/10**
**Merged** 2025-10-23 · **Linear** SUBRITE-3522 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 8 files, +0/-288, 2 commits

**Problem.** The admin sidebar advertised a "Landing pages" section that led to a "Coming soon" placeholder with
hardcoded sample data. Tenants clicked into it expecting a feature and found nothing — the navigation was
promising something that did not exist.

**Root cause.** A placeholder shipped in anticipation of a feature that was not built, left in the navigation
indefinitely.

**What I changed.**
- Deleted the entire landing-pages route tree (page, layout, loading, error, and the detail route), the shared
  `ComingSoon` component it was the only consumer of, and the sidebar navigation entry — 288 lines removed, no
  additions.

**Impact.** Removed a feature the UI promised and did not deliver. A pure deletion, and the right response to a
placeholder that has outlived its purpose: an empty section in the navigation costs tenant confidence and support
questions.

---

### 190. #4061 — Tenant logo rendered blurry
`Bug fix` · `Widgets & branding` · **Impact 1.6/10** · **Production hotfix**
**Merged** 2025-11-24 · **Base** `subrite/main` (hotfix) · 2 files, +6/-6, 2 commits

**Problem.** Tenant logos rendered soft and blurry in the site header and the brand-kit preview. A blurry logo is
a visible quality problem on every page of a media brand's customer-facing site.

**Root cause.** Two causes. The member-facing header requested the image at `dpr: 1`, so on high-density displays
the browser upscaled a standard-resolution image. And `classStack="object-scale-down"` alone left the element's
height unconstrained, so the image was scaled rather than fitted.

**What I changed.**
- Raised the member-facing header to `dpr: 2`, matching the brand-kit preview, so the logo is fetched at
  retina resolution.
- Changed the brand-kit preview's format from `webp` to `auto`, letting Cloudflare pick the best format the
  client supports.
- Constrained the element in both places with `classStack="object-scale-down h-full max-h-10 w-auto"` so the
  logo is fitted rather than stretched.
- Adjusted the container padding from `py-2.5` to `py-2` to accommodate the corrected height.

**Impact.** Fixed logo rendering quality across the customer-facing header and the admin preview. Presentational,
but for a media brand the logo is on every page — and the `dpr: 1` request meant it was guaranteed to be soft on
most modern displays.

---

### 191. #4291 — Prices at checkout had no thousands separator
`Improvement` · `Checkout` · **Impact 1.6/10**
**Merged** 2025-12-24 · **Linear** SUBRITE-3705 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 6 files, +29/-14, 5 commits

**Problem.** Prices were rendered as unformatted numbers, so a four- or five-figure amount appeared as `12500`
rather than `12 500`. On a payment page the amount is the single most important thing on screen, and an
unseparated figure is easy to misread by an order of magnitude.

**Root cause.** The amounts were interpolated directly with no formatting, in six separate places across
checkout, the subscription-change form and the admin create-order total.

**What I changed.**
- Applied the shared `formatNumberWithSpace` formatter (space as the thousands separator, the Norwegian
  convention) at all six sites: the member-facing change PaymentForm, the admin `TotalPriceInformation`, the
  checkout `OptionalProduct`, `PaymentForm`, `TotalPriceInformation` and payment index.

**Impact.** Made prices legible wherever a customer or operator sees a total. Applying it at every site rather
than the one reported is what makes the formatting consistent — a page showing `12 500` in one place and `12500`
in another is its own kind of wrong.

---

### 192. #3800 — Request-log test not updated for the new vacuum path
`Bug fix` · `Tests` · **Impact 1.6/10**
**Merged** 2025-09-30 · **Base** `subrite/develop` · 1 file, +14/-3, 1 commit

**Problem.** The `RequestLogService` test broke after #3796 changed the vacuum to run on a dedicated query
runner. The mock returned `undefined` from `createQueryRunner()`, so the test failed rather than verifying the new
behaviour.

**Root cause.** #3796 added `createQueryRunner: jest.fn()` to the mock without a return value, so the service's
call chain had nothing to operate on. The assertion also still checked the old `dataSource.query('VACUUM …')`
path.

**What I changed.**
- Built a proper `Partial<QueryRunner>` mock with `connect`, `release` and `query`, and returned it from
  `createQueryRunner`.
- Rewrote the assertions to verify the full intended sequence: `connect()`,
  `SET statement_timeout TO 0`, `VACUUM request_log`, `RESET statement_timeout`, `release()`.

**Impact.** Restored the test and made it actually pin the behaviour that matters — that the timeout is
disabled for the vacuum and reset afterwards, and the connection released. The original mock would have passed
even if the reset were dropped.

---

### 193. #4274 — Settings navigation lost its active tab
`Bug fix` · `Admin UI` · **Impact 1.6/10**
**Merged** 2025-12-23 · **Linear** SUBRITE-3783 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 38 files, +17/-18, 7 commits

**Problem.** Selecting a tab inside the member settings area did not mark the corresponding navigator item
active, so the sidebar and the content disagreed about where the user was.

**Root cause.** The communication settings area had been renamed from `communications` to
`communication-settings`, but the rename was incomplete: server-action imports, redirect targets, tab
`prefixRoute` values, breadcrumb resolution and several `router.push` calls still referenced the old path. The
navigation matched on route, so a stale path meant no active item. A stale email-widgets route was also pointing
into the settings tree rather than the content tree.

**What I changed.**
- Completed the rename across 38 files: action imports, the page redirect, `CommunicationTabs`'s `prefixRoute`,
  the settings `Header` breadcrumb resolution (adding an explicit `communication-settings` case) and the
  newsletter-integration `router.push`.
- Corrected the email-widgets navigation to `/admin/content/email-widgets/list/...` instead of the settings path
  it had been pointing at.

**Impact.** Restored active-state highlighting in the settings navigation. The diff shape (+17/−18 across 38
files) is characteristic of a half-finished rename — the work is finding every reference, and the cost of not
doing so was a navigation that quietly stopped indicating where you were.

---

### 194. #3748 — Seat rows blank when the member had no name
`Bug fix` · `Subscriptions & billing lifecycle` · **Impact 1.5/10** · **Production hotfix**
**Merged** 2025-09-12 · **Linear** SUBRITE-3319 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/main` (hotfix) · 4 files, +4/-4, 3 commits

**Problem.** In the seat-member and gift-receiver lists, a member who had accepted an invitation but not yet
entered a name rendered as an empty row. The owner saw a blank entry with no way to tell who it was.

**Root cause.** The row rendered `{seat.name}` with no fallback. Name is optional until the invitee completes
their profile, but email is always known.

**What I changed.**
- Changed the render to `{seat.name || seat.email}` in all four affected components — the member-facing and admin
  versions of both the member-invites and gift-receiver lists.

**Impact.** Made every seat row identifiable. Four lines, and the fix is applied to all four components rather
than the one reported, which is what stops the same blank row reappearing on a neighbouring screen.

---

### 195. #3769 — Image component crashed on an undefined source
`Bug fix` · `Shared platform code` · **Impact 1.5/10** · **Production hotfix**
**Merged** 2025-09-18 · **Base** `subrite/main` (hotfix) · 1 file, +1/-1, 1 commit

**Problem.** `ImgixImage` threw `Cannot read properties of undefined (reading 'startsWith')` when rendered
without a source. Because it is a shared component, the throw took down whichever page rendered it.

**Root cause.** `src.startsWith('blob:')` was called before checking that `src` existed — a shared component
assuming a prop that callers do not always provide.

**What I changed.**
- Added optional chaining: `src?.startsWith('blob:')`.

**Impact.** Stopped a missing image source crashing the page. One character, and typical of shared UI components:
the assumption holds for every caller until one passes nothing.

---

### 196. #4074 — Logo centred instead of left-aligned on iPhone
`Bug fix` · `Widgets & branding` · **Impact 1.5/10** · **Production hotfix**
**Merged** 2025-11-25 · **Base** `subrite/main` (hotfix) · 2 files, +2/-2, 1 commit

**Problem.** Follow-up to #4064. On narrow screens — iPhone in particular — the tenant logo appeared centred in
the header rather than left-aligned, so the header looked wrong on mobile for every tenant.

**Root cause.** #4064 had changed the logo to `w-full` to fix scaling. With a full-width box and no alignment
specified, the image defaults to centred within it, which is only visible when the box is wider than the logo —
i.e. on small screens.

**What I changed.**
- Added `object-left` to the class stack in both the brand-kit preview and the member-facing `TenantIntro`, so
  the image aligns left within its box.

**Impact.** Corrected mobile header alignment. Two lines, and a clean illustration of a fix introducing a
regression: `w-full` solved the scaling problem in #4064 and created the alignment one, which needed this
follow-up.

---

### 197. #4265 — Cashflow chart month labels untranslated
`Bug fix` · `Analytics & reporting` · **Impact 1.5/10**
**Merged** 2025-12-18 · **Linear** SUBRITE-3702 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 4 files, +17/-20, 2 commits

**Problem.** The payment-flow (cashflow) chart showed month names in English regardless of the user's language,
so a Norwegian admin saw "December 2025" on an otherwise Norwegian screen. The analytics submenu also used a
trending-up icon for a flow comparison, which misrepresents what the view shows.

**Root cause.** Month names were interpolated raw (`${currentMonthName}`) rather than passed through the
translation function, in both the axis labels and the series labels.

**What I changed.**
- Wrapped the month names in the `common` translation namespace: `${tCommon(currentMonthName)} ${year}`, for the
  current and previous month labels and the series labels.
- Changed the submenu icon from `ArrowTrendingUpIcon` to `ArrowsRightLeftIcon`, which represents flow rather than
  growth.
- Removed an unnecessary `React` import while there.

**Impact.** Localised the cashflow chart labels for Norwegian users. Small, but a chart with English month names
in an otherwise translated interface reads as unfinished.

---

### 198. #2976 — Company info inputs stretched to full page width
`Improvement` · `Admin UI` · **Impact 1.4/10** · **Hotfix branch**
**Merged** 2025-04-15 · **Linear** SUBRITE-2692 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 6 files, +212/-228, 3 commits

**Problem.** The company-info settings forms laid their inputs out in responsive two-column rows that stretched to
the full width of the page. On a wide monitor a postcode field was hundreds of pixels wide, which looks broken and
makes the form harder to scan.

**Root cause.** Each of the five forms had its own nested `flex ... lg:flex-row` / `flex-1 w-full` wrapper
structure with no maximum width, so the layout was driven by viewport width rather than by a sensible field
width.

**What I changed.**
- Added a `max-w-input: 680px` value to `tailwind.config.js`, giving the codebase one definition of a sensible
  form width.
- Replaced the per-form nested flex wrappers with `flex flex-col gap-4 w-full max-w-input` across all five forms
  (bank and currency, company details, customer service, domains and URLs, tracking) — simplifying the markup
  and constraining the width in one shared way.
- Removed a stale `defaultValue` comment referencing a field that did not exist.

**Impact.** Made the settings forms readable at any window size, with the width defined once in the Tailwind
config rather than five times in markup. Near-balanced diff (+212/−228) — mostly simplification of duplicated
layout wrappers.

---

### 199. #3218 — Theme font preview failed to load
`Bug fix` · `Widgets & branding` · **Impact 1.4/10** · **Hotfix branch**
**Merged** 2025-05-23 · **Base** `subrite/develop` · 1 file, +3/-3, 1 commit

**Problem.** Fonts did not render in the theme settings preview, so a tenant choosing typography could not see
what they were choosing.

**Root cause.** The font-loader URL was built against `tenant.siteUrl` — the tenant's public site — rather than
the admin panel's own origin. From the admin panel that is a cross-origin request to a different host, so the
font resource did not load.

**What I changed.**
- Built the URL against the current origin instead:
  `const baseUrl = typeof window !== 'undefined' ? window.location.origin : ''`, used for both font-loader URLs.
  The `typeof window` guard is needed because the component also renders during SSR, where `window` is absent.

**Impact.** Made the font preview work, so tenants can see their typography before committing to it. Three
lines; the SSR guard is the part that would have bitten a naive fix.

---

### 200. #3361 — Tenant settings feedback: template type and onboarding fields
`Bug fix` · `Admin UI` · **Impact 1.4/10**
**Merged** 2025-06-20 · **Linear** SUBRITE-3058 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 6 files, +75/-66, 12 commits

**Problem.** A feedback round on the settings area. The communication-template form defaulted its channel to an
empty string cast to the enum, so the form began in an invalid state. The member-card fields form crashed when a
tenant had no custom properties. And the onboarding field-config editor required a selected config, so opening it
to add a new one was awkward.

**Root cause.** `'' as COMMUNICATION_CHANNEL` — an invalid enum value used as a placeholder — plus
`customProperties.map(...)` with no null guard, and a `selectedConfig: PageInputFieldConfig` prop typed as
required when adding a new config legitimately has none.

**What I changed.**
- Defaulted the channel to `COMMUNICATION_CHANNEL.Email` with a comment noting email is the only supported
  channel, rather than an invalid cast, and built the template-type options list with an explicit placeholder
  entry.
- Guarded the custom-properties mapping: `customProperties?.map(...)`.
- Made `selectedConfig` optional in `EditInputFieldConfig` so the add-new path is representable, and removed the
  `addNewConfig` workaround that had been constructing an empty config to satisfy the type.

**Impact.** Fixed a crash for tenants without custom properties and removed two invalid-placeholder patterns.
Making the type honest (`selectedConfig?`) let the workaround that existed to satisfy it be deleted.

---

### 201. #3639 — Email communication content reverted after saving
`Bug fix` · `Communication & notifications` · **Impact 1.4/10**
**Merged** 2025-08-19 · **Linear** SUBRITE-3211 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 4 files, +10/-1, 3 commits

**Problem.** Saving email communication content appeared to work but the form then showed the previous content —
so authors could not tell whether their edit had persisted, and reasonably assumed it had not.

**Root cause.** Two halves of the same problem. The page's fetch was untagged, so Next.js served the cached
pre-save response after a successful save. And `CommunicationForm` initialised its state from
`initialFormData` on mount only, so even fresh data would not have been reflected.

**What I changed.**
- Added a `communicationContentTag`, tagged the content page's fetch with it, and called
  `revalidateApiTag(communicationContentTag)` after a successful save — so the server data is refetched.
- Added `useEffect(() => setFormData(initialFormData), [initialFormData])` so the form picks up the refreshed
  data.

**Impact.** Made saved email content visibly persist. Both halves were needed: revalidating without the effect
would have refreshed the server data into a form that ignored it.

---

### 202. #3681 — Sender approval test missing its environment variable
`Bug fix` · `Tests` · **Impact 1.4/10** · **Production hotfix**
**Merged** 2025-08-25 · **Base** `subrite/main` (hotfix) · 1 file, +1/-0, 2 commits

**Problem.** The `CommunicationSenderController` test broke after #3665 moved the approval-request recipient from a
source constant to an environment variable — the variable was unset in the test environment.

**Root cause.** #3665 replaced the constant with `getEnv('SENDER_APPROVAL_REQUEST_RECEIVER_EMAIL')` and documented
it in `secrets.env.example`, but the test environment had no value for it.

**What I changed.**
- Set `process.env.SENDER_APPROVAL_REQUEST_RECEIVER_EMAIL = 'test@example.com'` in the test setup.

**Impact.** Restored the test suite. One line, included for completeness — and a reminder that moving a constant
into configuration means every environment, including the test one, needs the value.

---

### 203. #3773 — Podcast page not refreshed after an update
`Bug fix` · `Content & podcasts` · **Impact 1.4/10**
**Merged** 2025-09-24 · **Linear** SUBRITE-3310 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 4 files, +18/-11, 2 commits

**Problem.** Raised as "uploading a large image to a podcast fails with an unclear error message". After a podcast
update — including a failed one — the page did not reflect the current state, so the tenant could not tell what
had happened.

**Root cause.** Cache revalidation was embedded inside the `updatePodcast` server action, so it only ran on that
one path. The Spotify information form updated podcasts through the same action but the revalidation was
positioned such that the affected pages were not consistently refreshed.

**What I changed.**
- Extracted revalidation into its own `'use server'` action, `revalidatePodcastPaths(podcastId)`, revalidating
  both the podcast list and the specific podcast page — so the refresh is a named, reusable operation rather than
  a side effect buried in the update action.
- Pointed `updatePodcast` at it and normalised the action's import naming to camelCase
  (`UpdatePodcastAction` → `updatePodcastAction`) at both call sites.

**Impact.** Made podcast pages reflect their true state after an update attempt, so a failed upload is visible
rather than masked by stale cached content. Extracting the revalidation makes it reusable by any future podcast
mutation.

---

### 204. #3820 — List query builder buried the most-used field and left the operator unset
`Improvement` · `Members, segments & import/export` · **Impact 1.4/10**
**Merged** 2025-10-10 · **Linear** SUBRITE-3384 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 3 files, +26/-1, 8 commits

**Problem.** Building a segment query for inactive subscribers — one of the most common list operations — was
tedious. Subscription status was buried among all the other `member_package` fields, and after choosing a field
the operator dropdown started empty, so the user had to select an operator before the rule was valid.

**Root cause.** Fields were listed in their natural order with no notion of which are most used, and the operator
select bound to `currentOperator` with no default when that value was absent or not applicable to the chosen
field.

**What I changed.**
- Added a priority list — `['member_package.status', 'member_package.statusOfSubscription']` — hoisting those two
  fields to the top of the `member_package` group while preserving the order of the rest.
- Made the operator selector fall back to the first valid option when the current value is not among them, and
  call `handleChangeOperator` so the parent state matches what is displayed — previously the visible selection and
  the stored value could disagree.
- Enabled `autoSelectOperator` in the query builder.

**Impact.** Made the most common segmentation query quicker to build and removed a state mismatch where the
operator shown was not the operator stored. Prioritising by usage rather than by data order is a small piece of
product judgement that shows in daily use.

---

### 205. #3824 — Whitespace-only product titles accepted
`Bug fix` · `Admin UI` · **Impact 1.4/10**
**Merged** 2025-10-13 · **Linear** SUBRITE-3258 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 2 files, +15/-11, 7 commits

**Problem.** A product package could be saved with a sales title or name consisting only of spaces. The
validation checked presence, not content, so a package appeared in customer-facing listings with a blank title.
Worse, `validateFormData` was called without its result being checked, so the form submitted even when validation
had failed.

**Root cause.** `if (!salesTitle)` passes for `'   '`. And the submit handler invoked `validateFormData(formData)`
while discarding the return value, so validation was decorative.

**What I changed.**
- Changed both checks in both forms to `!salesTitle || salesTitle?.trim()?.length === 0`, and likewise for `name`.
- Made the submit handler act on the result: `const { isInvalid } = validateFormData(formData); if (isInvalid) return;`
- Trimmed the values on save (`salesTitle?.trim()`, `name?.trim()`, `slug?.trim()`) so leading/trailing whitespace
  is not persisted even when the value is otherwise valid.

**Impact.** Stopped blank-titled packages reaching customer-facing listings. The submit-handler fix is the more
significant of the two — validation whose result is ignored blocks nothing, so tightening the checks alone would
have changed nothing.

---

### 206. #3951 — Stray text in the reports section
`Improvement` · `Analytics & reporting` · **Impact 1.4/10** · **Production hotfix**
**Merged** 2025-11-07 · **Base** `subrite/main` (hotfix) · 4 files, +6/-3, 2 commits

**Problem.** The reports screen displayed a bare, untranslated `Delivery address` fragment left over from
development, plus a redundant title and help text on the report-history section.

**Root cause.** A hardcoded JSX fragment (`<>Delivery address</>`) left in from building the delivery-address
export (#3855), and duplicated section headings.

**What I changed.**
- Removed the stray fragment and the redundant `SectionTitle`/help-text block from the report-history component,
  with the corresponding translation cleanup.

**Impact.** Removed visible development leftovers from a tenant-facing screen. Trivial, and included at its real
weight — but untranslated stray text in a shipped UI reads as carelessness to the customer looking at it.

---

### 207. #4276 — Tenant id and build version not visible anywhere
`Feature` · `Admin UI` · **Impact 1.4/10**
**Merged** 2025-12-23 · **Linear** SUBRITE-3675 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 11 files, +136/-1, 18 commits

**Problem.** Neither the tenant's id nor the deployed build version was visible in the product. Support
conversations began with "which tenant are you?" and "which version are you on?", and neither the customer nor the
support agent could answer.

**Root cause.** No system-information surface existed, and the frontend had no build-time commit reference.

**What I changed.**
- Added a "System" tab to the company-info settings with a `SystemForm` displaying the tenant id and build
  version, plus the `loading.tsx` companion.
- Made the commit SHA available at build time via a `set-commit-sha.sh` script that uses `NEXT_PUBLIC_COMMIT_SHA`
  if CI has already set it and otherwise derives it from `git rev-parse --short HEAD`, guarded by
  `command -v git` and `git rev-parse --git-dir` so a build outside a git checkout does not fail.
- Wired `NEXT_PUBLIC_COMMIT_SHA: ${{ github.sha }}` into the CI workflow and made the npm `build` script invoke
  the helper — so it works both in CI and locally with no manual configuration.
- Documented the behaviour in `README.md` and `.cursorrules`.

**Impact.** Made the two facts support always needs self-serve. The build script's fallback chain is why it works
everywhere: CI value first, git second, and a build with neither still succeeds.

---

### 208. #4467 — Product list showed price instead of type and delivery
`Improvement` · `Admin UI` · **Impact 1.4/10**
**Merged** 2026-02-02 · **Linear** SUBRITE-3853 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 3 files, +46/-13, 4 commits

**Problem.** The admin product list showed a price column, but a product's price is set per package and plan
rather than on the product itself — so the column was of little use. What operators actually needed to see was
whether a product is a main or optional product, and how a digital subscription is delivered.

**Root cause.** The list had been built with the columns that were easiest to render rather than the ones that
identify a product.

**What I changed.**
- Replaced the price column with an `isOptional` type column rendering "Optional" or "Main".
- Added a delivery column, rendered only for `digital_subscription` products (returning `null` otherwise, since
  delivery type is meaningless for other categories), with a `TooltipWrapper` and `InfoIcon` explaining the value.
- Removed the now-unused locale and number-formatting imports.

**Impact.** Made the product list show the attributes that actually distinguish products. Rendering the delivery
column only where it applies avoids the empty-column noise a blanket addition would have produced.

---

### 209. #3556 — Member profile API omitted the user id
`Improvement` · `API contract` · **Impact 1.4/10**
**Merged** 2025-08-04 · **Linear** SUBRITE-1740 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 4 files, +18/-0, 5 commits

**Problem.** `GET /api/v1/members/profile` returned member details without the underlying user id. Integrators
correlating a Subrite member with a user in their own system had no stable identifier to key on.

**Root cause.** `MemberPersonalInfo` exposed member fields only; the associated user's id was available on the
entity but not mapped into the response.

**What I changed.**
- Added `userId` to `MemberPersonalInfo`, populated as `member.user?.id` — optional chaining because the user
  relation is not guaranteed to be loaded on every path.
- Updated the theme-preview fixture that constructs a `MemberPersonalInfo` to include the new field.

**Impact.** Gave API consumers a stable identifier for cross-system correlation. Purely additive (+18/−0), so no
existing integration was affected.

---

### 210. #3884 — Communication list sorted by status change, not send date
`Bug fix` · `Communication & notifications` · **Impact 1.3/10**
**Merged** 2025-10-28 · **Linear** SUBRITE-3503 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 6 files, +13/-2, 4 commits

**Problem.** A member's communication history sorted by `lastUpdatedAt`, which changes whenever a delivery-status
webhook arrives. An old email that had just received a bounce or open event jumped to the top, so the list did not
read as a chronology of what was sent when.

**Root cause.** The response DTO exposed only `lastUpdatedAt`. The original send time (`createdAt`) was on the
communication log but not surfaced, so the UI had nothing else to sort or display.

**What I changed.**
- Added `createdAt` to `MemberCommunicationActivitiesResponseDto`, populated from the communication log.
- Changed the admin communication table's column from `lastUpdatedAt` to `createdAt`, with the corresponding
  translations.

**Impact.** Made the member communication history a genuine chronology of sends. Sorting a history by a field that
mutates on every downstream event is a subtle but real modelling error — the fix was to expose and use the time
the thing actually happened.

---

### 211. #3987 — Hosted widget form action missing its server directive
`Bug fix` · `Widgets & branding` · **Impact 1.3/10**
**Merged** 2025-11-12 · **Base** `subrite/develop` · 1 file, +2/-0, 1 commit

**Problem.** The hosted newsletter widget form rendered incorrectly — the first of the hosted-form translation
issues, later resolved properly in #3989.

**Root cause.** `getWidgetConfigAndTheme.ts` lacked the `'use server'` directive, so Next.js did not treat it as a
server module. It used `next-intl/server` APIs that require a server context.

**What I changed.**
- Added `'use server'` to the module.

**Impact.** Made the module execute in the right context. Two lines, and it turned out to be a partial fix — the
underlying problem was that translations were being resolved on the server and passed to a client component at
all, which #3989 addressed by moving translation into the component that renders.

---

### 212. #4121 — Communication receivers preview errored for unsegmented communications
`Bug fix` · `Communication & notifications` · **Impact 1.3/10**
**Merged** 2025-11-28 · **Linear** SUBRITE-3653 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 1 file, +8/-0, 3 commits

**Problem.** Previewing the recipients of a communication that had no segment attached produced an error instead of
an empty list. Since a communication starts with no segment, previewing recipients before choosing one — a natural
thing to do — errored.

**Root cause.** The recipient query assumed `communication.segment` existed and dereferenced it without a guard.

**What I changed.**
- Added an early return of a properly formed empty page —
  `new Paginate<CommunicationMembersResponseDto>([], new PageMeta({ pageOptions, count: 0 }))` — when no segment is
  set. Returning a valid paginated response rather than null means the client needs no special case.

**Impact.** Replaced an error with a correct empty result for a communication without a segment. Eight lines, and
the choice to return a well-formed empty page rather than null is what keeps the client simple.

---

### 213. #4169 — Filter Nyheter migration with documented business decisions
`Data migration` · `Members, segments & import/export` · **Impact 1.3/10**
**Merged** 2026-01-15 · **Linear** SUBRITE-3613 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 35 files, +7821/-0, 57 commits

**Problem.** Preparing the Filter Nyheter migration, which raised genuine business questions rather than only
technical ones: some records had no product id, 221 subscriptions had no payment provider at all, and imported
Vipps subscriptions risked being charged immediately if their next renewal fell close to the import date.

**Root cause.** Not a defect — a migration whose source data was incomplete in ways that required decisions, not
just transformation.

**What I changed.**
- Wrote `clarification.md` recording each question and the decision taken: records with a missing product id are
  skipped and listed; the 221 without a payment provider are imported as Stripe; and Vipps subscriptions get a
  buffer — *"if importDate − 8 days ≤ nextRenewal ≤ importDate + 3 days, set nextRenewal to importDate + 3 days"* —
  so a member is not charged immediately on import and has time to add a payment method.
- Nine numbered notebooks covering users, subscription parsing and processing, payment info, email masking, member
  id resolution, payment history and team subscriptions, plus the input merge step.

**Impact.** Delivered the migration with its judgement calls written down. The Vipps renewal-buffer rule is the
substantive piece of thinking here: without it, importing would have charged customers within days of migration,
and the decision is recorded with its exact boundaries rather than living in one person's memory.

---

### 214. #3671 — Sender approval email did not say who must act
`Improvement` · `Communication & notifications` · **Impact 1.3/10**
**Merged** 2025-08-22 · **Base** `subrite/develop` · 1 file, +1/-1, 2 commits

**Problem.** The sender-approval request email said a new sender "needs your approval" without stating that only a
system administrator can grant it. Recipients who could not act on it had no way to know that from the message.

**Root cause.** Wording written from the sender's perspective rather than the recipient's, with the required role
left implicit.

**What I changed.**
- Rewrote the sentence to state the required action and who performs it: *"A new … sender requires approval for the
  tenant listed below. This action must be completed by a system administrator."* — and lower-cased the type label
  so it reads naturally inline.

**Impact.** Made a transactional email actionable by telling the reader what is needed and who can do it. One
line, but an approval request that does not say who approves generates a reply rather than an approval.

---

### 215. #4288 — Dashboard chart y-axis did not fit the data
`Bug fix` · `Analytics & reporting` · **Impact 1.3/10**
**Merged** 2025-12-26 · **Linear** SUBRITE-3788 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 2 files, +73/-39, 3 commits

**Problem.** The dashboard chart's y-axis did not fit the actual values, so series were compressed into a fraction
of the available height and trends were hard to read.

**Root cause.** A hand-rolled `getNiceMax` in the bespoke chart component rounded the maximum up to the next 1/2/5
× power of ten — so a maximum of 210 produced an axis to 500, wasting more than half the chart. The stacked-total
calculation summed positive and negative values separately in a way that did not reflect what was rendered.

**What I changed.**
- Replaced `getNiceMax` with domain-and-tick calculation that fits the data, including an explicit zero case
  returning `{ domain: [0, 100], ticks: [0, 25, 50, 75, 100] }` so an empty chart still renders a sensible axis.
- Moved the logic into the shared `StatsView`, deriving the chart type from the view configuration
  (`showTotal ? view.total?.graph : view.details?.graph`) so line and area charts each scale correctly, and
  reworked the entity-sum calculation to account for hidden series.

**Impact.** Made dashboard charts use the available height, so trends are legible. Moving the axis logic into the
shared component meant the subsequent dashboard consolidation (#4300, #4303) inherited it rather than needing it
reimplemented.

---

### 216. #4873 — Backspace could not delete a CTA block in the email editor
`Bug fix` · `Communication & notifications` · **Impact 1.3/10**
**Merged** 2026-04-23 · **Linear** SUBRITE-3977 *(pre-migration issue, no longer in Linear)* · **Base** `develop` · 1 file, +39/-0, 2 commits

**Problem.** A call-to-action block inserted into the email editor could not be removed with backspace. Authors had
to delete surrounding content or start over — a genuinely stuck state in a content editor.

**Root cause.** Slate treats a CTA as a void element. Backspace at its start, or at the start of the block
following it, has no default behaviour for removing it, so the keystroke did nothing.

**What I changed.**
- Added backspace handling for two cases. When the cursor is inside a CTA at its start
  (`Point.equals(selection.anchor, Editor.start(editor, ctaPath))`), prevent the default and
  `Transforms.removeNodes` at the CTA's path. When the cursor is at the start of a block whose previous sibling is a
  CTA, remove that CTA — the behaviour a user expects from backspace at the start of a line.
- Guarded both paths on a collapsed selection so a range delete is unaffected.

**Impact.** Made CTA blocks deletable by the key users reach for. Handling both the inside-at-start and
after-the-block cases is what makes it feel natural rather than partially working.

---

### 217. #3885 — Email statistics route parameter in the wrong position
`Improvement` · `API contract` · **Impact 1.3/10**
**Merged** 2025-10-28 · **Linear** SUBRITE-3016 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 3 files, +4/-4, 2 commits

**Problem.** The public route was `GET email/statistics/:id` — id after the action — inconsistent with REST
convention and with the rest of the API.

**Root cause.** The route had been defined with the parameter trailing the action rather than following the
resource.

**What I changed.**
- Changed the route to `GET :id/email/statistics`, so the resource id precedes the sub-resource.

**Impact.** Brought the route in line with the API's conventions. Paired with #3887 and #3898 as a small
consistency pass over the communication endpoints — worth noting that #3898, the one with external integrators,
was given a documented deprecation rather than a straight change.

---

### 218. #3887 — SMS test route segment order
`Improvement` · `API contract` · **Impact 1.3/10**
**Merged** 2025-10-28 · **Linear** SUBRITE-3014 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 3 files, +4/-4, 3 commits

**Problem.** The route was `POST :id/test/sms`, putting the action before the channel. The corresponding email
route used the opposite order, so the two were inconsistent with each other.

**Root cause.** Segment ordering chosen ad hoc when the endpoint was added.

**What I changed.**
- Changed the route to `POST :id/sms/test`, so channel precedes action and the SMS and email test routes match.

**Impact.** Made the two test-send routes consistent. Part of the same convention pass as #3885 and #3898.

---

### 219. #3794 — Error flashed when closing the segment creation modal
`Bug fix` · `Members, segments & import/export` · **Impact 1.2/10**
**Merged** 2025-09-29 · **Linear** SUBRITE-3364 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 1 file, +7/-2, 3 commits

**Problem.** Closing the segment creation modal in the automation workflow briefly flashed an error before the
modal disappeared — visible enough for users to report it, and it made a successful action look like a failure.

**Root cause.** `setOpenSegmentCreationModal(false)` ran at the start of the close, so the modal's content
re-rendered without its data while still visible during the closing transition, momentarily rendering an error
state.

**What I changed.**
- Moved the state reset into an `onExited` handler (`handleSegmentModalExited`), so it runs after the closing
  transition completes and the content is never re-rendered while on screen.

**Impact.** Removed a visible error flash on a successful action. The fix is the right one for animated dismissal:
tearing down state after the exit transition rather than at its start.

---

### 220. #3867 — Email widget feedback and configurable labels
`Improvement` · `Widgets & branding` · **Impact 1.2/10**
**Merged** 2025-11-12 · **Linear** SUBRITE-3288 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 24 files, +590/-79, 31 commits

**Problem.** A feedback round on the email signup widget. Tenants could not change the email field's label, so the
widget embedded on their site used wording they did not choose — on a component whose whole purpose is to convert
visitors.

**Root cause.** The widget's theme configuration had no label fields, so text was fixed in the bundled widget
script.

**What I changed.**
- Added an optional `emailLabel` to the widget `ThemeObjType` configuration, so tenants control the field label.
- Rebuilt and published the widget bundle as `subrite-email-widget-1.0.1.js` — versioned in the filename, so an
  embedded widget on a tenant's site is not silently changed under them and they can adopt the new version
  deliberately.
- Applied the remaining feedback across the widget configuration UI and translations.

**Impact.** Gave tenants control of the widget's wording. The versioned bundle filename is the notable decision:
for a script embedded on third-party sites, replacing the file in place would change behaviour on live sites
without warning.

---

### 221. #4304 — Dashboard "show more" spacing
`Improvement` · `Analytics & reporting` · **Impact 1.2/10** · **Production hotfix**
**Merged** 2025-12-30 · **Base** `subrite/main` (hotfix) · 1 file, +1/-1, 1 commit

**Problem.** Excessive vertical whitespace between the dashboard subscription chart and its "show more" footer,
left over from the chart consolidation.

**Root cause.** The chart component's own bottom spacing plus the card footer's spacing compounded after the
consolidation in #4303 replaced the chart implementation.

**What I changed.**
- Added `-mt-10` to the `CardFooter` class list to absorb the duplicated spacing.

**Impact.** Cosmetic spacing correction. One line, listed at its real weight — a negative-margin patch is an
acknowledged shortcut rather than a structural fix, which is honestly why it is a one-line hotfix and not a
refactor.

---

### 222. #4380 — Unsupported import columns reported as an unstructured error
`Bug fix` · `Members, segments & import/export` · **Impact 1.2/10**
**Merged** 2026-01-13 · **Linear** SUBRITE-3824 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 1 file, +7/-2, 1 commit

**Problem.** When a subscription import file contained unrecognised columns, the backend threw a
`BadRequestException` with the column names embedded in a message string. The frontend could not attach that to any
field, so the operator saw a generic error rather than which columns were the problem.

**Root cause.** `throw new BadRequestException(\`Unsupported columns: ${...}\`)` — the information existed but only
inside a human-readable string, so it was not machine-readable by the client.

**What I changed.**
- Replaced it with the platform's structured `FieldException`, carrying `field` (the offending column names) and
  `messageCode: 'unsupportedFieldsExists'` — so the frontend can render a localised message against the right
  place, and the message text lives in translations rather than in the backend.

**Impact.** Made the error actionable and localisable. Part of the same import error-reporting improvement as
#4922 — putting the detail in structured fields rather than a message string is what lets any client present it
properly.

---

### 223. #5005 — Change vs switch subscription not explained
`Improvement` · `Member portal (My Page)` · **Impact 1.2/10**
**Merged** 2026-05-19 · **Linear** [SUBRITE-151](https://linear.app/subrite/issue/SUBRITE-151) (Medium) · **Base** `develop` · 6 files, +2/-6, 6 commits

**Problem.** My Page offered both "change subscription" and "switch subscription" without explaining the
difference. Members could not tell which they wanted, and picking wrong has billing consequences.

**Root cause.** The two actions were labelled but not described, and the existing copy did not distinguish them.

**What I changed.**
- Revised the wording across all six locale files — admin English and Norwegian, My Page Danish, English,
  Norwegian and Swedish — so each action describes what it does. Net −4 lines: clearer copy, not more of it.

**Impact.** Removed a genuine choice-ambiguity from a member-facing action with billing consequences.
Translation-only, and the right kind of fix — the confusion was in the words, so words are what needed changing.

---

### 224. #3544 — Package list showed the sales title rather than the internal name
`Improvement` · `Admin UI` · **Impact 1.1/10**
**Merged** 2025-07-30 · **Linear** SUBRITE-3049 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 5 files, +14/-2, 4 commits

**Problem.** Admin package lists displayed the sales title — the customer-facing marketing name — rather than the
internal name or the name used on invoices. Operators looking for a package by the name they use internally, or
reconciling against an invoice, could not find it.

**Root cause.** `MinimalPackageResponse` exposed the sales title only; `nameOnInvoice` was on the entity but not
mapped into the response.

**What I changed.**
- Added `nameOnInvoice` to `MinimalPackageResponse` (`this.nameOnInvoice = pack.nameOnInvoice`) and surfaced it in
  the admin list.

**Impact.** Let operators find packages by the name they actually use. Small, and the sort of internal-versus-
customer-facing naming distinction that only surfaces once real operators use the screen.

---

### 225. #3663 — Sender validation applied email rules to SMS senders
`Bug fix` · `Communication & notifications` · **Impact 1.1/10**
**Merged** 2025-08-21 · **Base** `subrite/develop` · 2 files, +12/-0, 1 commit

**Problem.** Creating or updating a communication sender validated the sender-email and reply-to fields regardless
of channel, so an SMS sender — which has no email address — could not be saved. Invalid email addresses were also
accepted on the frontend and only rejected by the backend.

**Root cause.** The DTO's email validators were unconditional, and the frontend form performed no email validation
of its own.

**What I changed.**
- Added `@ValidateIf((o) => o.communicationChannel === COMMUNICATION_CHANNEL.EMAIL)` to the sender-email
  validation, and a compound condition for `replyTo`
  (`channel === EMAIL && o.replyTo?.length > 0`) so an optional reply-to is only validated when supplied.
- Added client-side `validateEmail` checks for both fields in `CommunicationSenderForm`, gated on the email
  channel, so the error appears immediately rather than after a round trip.

**Impact.** Made SMS senders saveable and gave immediate feedback on malformed email addresses. Conditional
validation keyed on channel is the correct model — the fields are genuinely not applicable to SMS.

---

### 226. #3644 — Push notification statistics styled inconsistently
`Improvement` · `Communication & notifications` · **Impact 1.1/10**
**Merged** 2025-08-19 · **Linear** SUBRITE-3186 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 3 files, +46/-35, 3 commits

**Problem.** The push-notification statistics page used its own bespoke stat cards with emoji icons (📤, ❌, 👁️, ⏳,
📊) and per-metric pastel colours, looking nothing like the email statistics page. The same information was
presented two different ways in the same product.

**Root cause.** The page had been built independently rather than reusing the existing `StatisticsBox` component.

**What I changed.**
- Imported the shared `StatisticsBox` from the email statistics area and removed the bespoke card markup, the
  per-metric colour classes and the emoji icons, centring the labels and values consistently.

**Impact.** Made push and email statistics look like the same product. Emoji as data-visualisation iconography also
reads as unpolished in a B2B admin panel — removing it was the right call, not just a consolidation.

---

### 227. #4309 — Dashboard "show more" link unreachable
`Bug fix` · `Analytics & reporting` · **Impact 1.1/10** · **Production hotfix**
**Merged** 2025-12-31 · **Base** `subrite/main` (hotfix) · 2 files, +4/-4, 2 commits

**Problem.** The "show more" link on the dashboard subscription card could not be clicked. The chart above it
overlapped the footer, so the link was covered.

**Root cause.** #4304 had patched the spacing with a `-mt-10` negative margin on the footer, which pulled the
footer under the chart rather than removing the excess space. The real cause was the chart's own bottom margin and
x-axis height reserving space for a label that was not present.

**What I changed.**
- Removed the `-mt-10` hack and set `CardContent` to `p-0` instead.
- Fixed the underlying spacing in the shared `StatsView`: the chart's bottom margin now collapses to `0` when
  there is no x-axis label (`xAxisLabel ? 30 : 0`) and the axis height to `40` rather than `50` — so the space is
  not reserved in the first place.

**Impact.** Made the link clickable and replaced a negative-margin workaround with a fix at the source. Worth
reading as a pair with #4304: the first hotfix moved the symptom, this one removed the cause.

---

### 228. #3314 — Tooltips rendered behind other content
`Bug fix` · `Shared platform code` · **Impact 1.0/10** · **Production hotfix**
**Merged** 2025-06-12 · **Linear** SUBRITE-2110 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/main` (hotfix) · 2 files, +9/-1

**Problem.** Tooltips were partially or wholly hidden behind adjacent content — reported for the member package
slug tooltip. A tooltip that cannot be read serves no purpose, and this affected the shared component used
throughout the admin panel.

**Root cause.** `TooltipWrapper` set no `zIndex`, so it participated in the default stacking order and could be
overlapped by any positioned sibling.

**What I changed.**
- Added `zIndex: 10` to the tooltip's inline style.
- Recorded both this and the invoice-table amount fix from #3313 in `CHANGELOG.md`.

**Impact.** Fixed tooltip visibility everywhere the shared component is used, not just the reported location. One
property, with a wide reach because it is a shared component.

---

### 229. #3762 — Token values shown raw in email previews
`Bug fix` · `Automation & workflows` · **Impact 1.0/10**
**Merged** 2025-09-19 · **Linear** SUBRITE-3318 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 3 files, +6/-2, 5 commits

**Problem.** Email previews rendered token values unformatted — a date appeared as a raw timestamp rather than a
formatted date — so the preview did not show what a recipient would receive. A separate seat-access condition was
also inverted.

**Root cause.** `replacePlaceholdersInTemplate` substituted the raw value, ignoring the `formatter` already
defined for each token in `TOKEN_DEFINITIONS`. And a seat comparison used `&&` where the logic required `||`.

**What I changed.**
- Looked the token up in `TOKEN_DEFINITIONS` and applied its `formatter` when one exists, so preview values are
  formatted the same way as in a real send — reusing the definitions that already existed rather than duplicating
  formatting logic.
- Removed an unused `date-fns/locale` import.
- Corrected the seat-access condition in `MemberPackageService` from `&&` to `||`.

**Impact.** Made email previews represent the real output. Reusing the token definitions' own formatters means
preview and send cannot drift apart.

---

### 230. #3928 — Gift delivery address name field misbehaved
`Bug fix` · `Checkout` · **Impact 1.0/10**
**Merged** 2025-11-03 · **Base** `subrite/develop` · 2 files, +4/-4, 1 commit

**Problem.** In the gift-receiver delivery address form, the name field became uncontrolled when empty, and its
read-only state was decided by comparing `product.memberId !== ''` — which is true for `undefined` as well as for a
real id, so the field could be locked when it should have been editable.

**Root cause.** `value={product?.address?.name}` with no default (undefined → uncontrolled input), and a string
comparison used as a presence check where `memberId` may be undefined rather than empty.

**What I changed.**
- Defaulted the value: `value={product?.address?.name || ''}`.
- Replaced both `product.memberId !== ''` checks with `!!product.memberId`, which is true only for an actual id,
  and updated the accompanying comments — the previous comments described the opposite condition.
- Guarded the validator: `validateName(name || '')`.

**Impact.** Made the name field editable when it should be and controlled throughout. Small, but the
`!== ''` versus truthiness distinction was producing a locked field on a checkout form.

---

### 231. #3316 — Tenant provisioning documentation missing podcast card settings
`Improvement` · `Tooling, deps & docs` · **Impact 0.9/10** · **Hotfix branch**
**Merged** 2025-06-12 · **Base** `subrite/develop` · 1 file, +9/-0, 1 commit

**Problem.** `ADDING_TENANTS.md` — the runbook for provisioning a new tenant — omitted the My Page podcasts card
settings, so a newly provisioned tenant was missing that card and someone had to notice and add it manually.

**Root cause.** The runbook had not been updated when the podcasts card was added.

**What I changed.**
- Added the `mypage_cards_settings` insert for the podcasts card, including its display order and `{tenantId}`
  placeholder, to the provisioning documentation.

**Impact.** Made new tenants come up complete. Documentation-only and the lowest-impact entry in the portfolio by
diff, but an incomplete provisioning runbook produces a subtly broken tenant every time it is followed.

---

### 232. #3393 — Creating a user with an existing email silently overwrote it
`Bug fix` · `Auth & access control` · **Impact 0.9/10**
**Merged** 2025-06-26 · **Linear** SUBRITE-3058 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 4 files, +4/-1, 4 commits

**Problem.** Creating a user with an email address already in use did not fail. Instead it assigned the existing
user's id to the incoming DTO and proceeded — quietly updating the existing user with the new data rather than
reporting a conflict. The operator believed they had created a user; in fact they had modified someone else's
record.

**Root cause.** `UserService` handled the duplicate case with `Object.assign(userDto, { id: existingUser.id })`,
converting a create into an update. That may have been deliberate at some point, but from the caller's perspective
it is a silent, surprising mutation of an unrelated record.

**What I changed.**
- Replaced the assignment with an explicit rejection:
  `throw new HttpException('User email already exists', HttpStatus.BAD_REQUEST)`.
- Declared `@ApiBadRequestResponse({ type: ErrorResponse, description: 'userEmailAlreadyExists' })` on the
  controller so the contract states the failure, and added the corresponding translated message.

**Impact.** Turned a silent overwrite of an existing user record into a clear error. Four lines, and among the more
consequential small changes here: the previous behaviour modified data the operator had not intended to touch, with
no indication it had happened.

---

### 233. #3838 — Modals could not be closed with Escape
`Improvement` · `Admin UI` · **Impact 0.8/10**
**Merged** 2025-10-14 · **Linear** SUBRITE-2034 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 1 file, +14/-0, 2 commits

**Problem.** Admin modals could not be dismissed with the Escape key — users had to find and click the close
button. Escape-to-dismiss is a baseline expectation and an accessibility consideration for keyboard users.

**Root cause.** The shared `ModalWrapper` had no keyboard handling.

**What I changed.**
- Added an Escape key handler to `ModalWrapper`, so every modal in the admin panel gained the behaviour from one
  change.

**Impact.** Made every admin modal keyboard-dismissable. Fourteen lines in a shared component covering the whole
panel — the right place for a behaviour that should be universal.

---

### 234. #3868 — Copied package URL missing its scheme
`Bug fix` · `Admin UI` · **Impact 0.8/10**
**Merged** 2025-10-23 · **Linear** SUBRITE-3042 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 1 file, +2/-2, 2 commits

**Problem.** Copying a product package's URL produced an address with no `https://` prefix. Pasted into an email or
a browser, a scheme-less URL is often treated as relative or plain text and does not resolve — so shared purchase
links were broken.

**Root cause.** The copied value was built from the hostname without prefixing a scheme.

**What I changed.**
- Included the `https://` scheme in the copied package URL.

**Impact.** Made copied purchase links work when pasted. Two lines; one of the small cluster of package-URL fixes
alongside #3911, both affecting links tenants share with customers.

---

### 235. #5073 — Product package list showed two loading indicators
`Bug fix` · `Admin UI` · **Impact 0.8/10**
**Merged** 2026-06-04 · **Base** `develop` · 1 file, +0/-7, 1 commit

**Problem.** Sorting the product package list showed two overlapping loading indicators — the route-level loading
state and the list's own — which read as a stuck or double-loading screen.

**Root cause.** The list had gained its own loading state during the filtering work (#5029), which made the
route-level `loading.tsx` redundant rather than complementary.

**What I changed.**
- Deleted the route-level `loading.tsx` (+0/−7), leaving the list's own in-context indicator.

**Impact.** One loading indicator instead of two. A pure deletion, and the correct resolution — the newer
in-context state is the better one, so the older route-level file was simply obsolete.

---

### 236. #3557 — Stripe publishable key fetched unconditionally
`Improvement` · `Checkout` · **Impact 0.8/10**
**Merged** 2025-08-04 · **Linear** SUBRITE-1466 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 4 files, +64/-41, 9 commits

**Problem.** Four payment-related pages fetched the tenant's Stripe publishable key on every render, including for
tenants that do not use Stripe at all. An unnecessary request on the payment path, and for a non-Stripe tenant a
request that can only fail.

**Root cause.** The key fetch was unconditional in each page, with no check of whether Stripe is among the
tenant's enabled providers.

**What I changed.**
- Made the fetch conditional on Stripe being an available provider, across all four pages: subscription-change
  checkout, payment-method change, subscription payment and the main checkout.

**Impact.** Removed a redundant request from four payment pages and a guaranteed-failing one for non-Stripe
tenants. Applying it to all four pages rather than one is what makes the behaviour consistent across the payment
surfaces.

---

### 237. #3826 — Large profile image upload failed unhelpfully
`Bug fix` · `Member portal (My Page)` · **Impact 0.8/10**
**Merged** 2025-10-10 · **Linear** SUBRITE-3476 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 2 files, +20/-46, 3 commits

**Problem.** Uploading a large profile image failed without telling the member why. They retried the same file,
with the same result.

**Root cause.** The avatar update action and its component carried duplicated, inconsistent handling of the upload
result, so a size rejection was not surfaced as a message the member could act on.

**What I changed.**
- Simplified the avatar upload path (+20/−46), consolidating the duplicated result handling so the failure reason
  reaches the member.

**Impact.** Turned a silent upload failure into a comprehensible one. Net code reduction — the duplicated handling
was itself the reason the error was being lost.

---

### 238. #5075 — Subscription change cards broke at the wrong breakpoint
`Bug fix` · `Member portal (My Page)` · **Impact 0.8/10**
**Merged** 2026-06-04 · **Linear** [SUBRITE-249](https://linear.app/subrite/issue/SUBRITE-249) (High · Bug) · **Base** `develop` · 2 files, +35/-37, 2 commits

**Problem.** On the My Page subscription-change screen, the package cards switched layout at the wrong viewport
width, so on tablet-sized screens the cards were cramped or misaligned — on the screen where a member chooses which
plan to move to.

**Root cause.** The responsive breakpoint on `PackageCardList` did not match the cards' actual content width.

**What I changed.**
- Corrected the breakpoint in `PackageCardList` and updated its loading skeleton to match, so the placeholder and
  the loaded content break at the same width — otherwise the layout visibly jumps when data arrives.

**Impact.** Fixed the plan-selection layout on tablet-width screens. Keeping the skeleton in step with the content
is the detail that removes the visible reflow.

---

### 239. #3747 — Tenant name and My Page link missing from the admin sidebar
`Improvement` · `Admin UI` · **Impact 0.7/10**
**Merged** 2025-09-15 · **Linear** SUBRITE-3315 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 3 files, +32/-4, 4 commits

**Problem.** The admin sidebar did not show which tenant the user was administering, and offered no link to that
tenant's My Page. Staff working across several tenants had no confirmation of which one they were in — a real risk
when making configuration changes.

**Root cause.** The sidebar user-profile area displayed the user but not the tenant context.

**What I changed.**
- Added the tenant name and a link to the tenant's My Page to the sidebar `UserProfile` component, with
  translations.

**Impact.** Made the active tenant visible at all times in the admin panel. Small, and it removes a genuine class
of mistake — changing settings while believing you are in a different tenant.

---

### 240. #3841 — Member search results not announced to screen readers
`Bug fix` · `Admin UI` · **Impact 0.7/10**
**Merged** 2025-10-29 · **Linear** SUBRITE-2471 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 2 files, +59/-48, 4 commits

**Problem.** The member search results were not exposed correctly to assistive technology, so a screen-reader user
searching for a member received no indication that results had appeared or how many.

**Root cause.** The results list was rendered as plain markup with no accessible semantics or announcement.

**What I changed.**
- Reworked `MemberSearchBox` and `MemberSearchResult` (+59/−48) so the results are structured and announced
  appropriately.

**Impact.** Made the admin member search usable with a screen reader. Accessibility work on a control used
constantly — for the users who need it, this is the difference between the feature working and not existing.

---

### 241. #4937 — Onboarding rules could not be saved without selecting a package
`Bug fix` · `Admin UI` · **Impact 0.6/10**
**Merged** 2026-05-07 · **Linear** [SUBRITE-95](https://linear.app/subrite/issue/SUBRITE-95) (High · Bug) · **Base** `develop` · 2 files, +2/-4, 3 commits

**Problem.** The onboarding-rules form required a package selection before it could be saved, but onboarding rules
apply tenant-wide and are not package-specific. Tenants could not configure their onboarding rules at all without
attaching an arbitrary package.

**Root cause.** A validation rule requiring the package field, which is genuinely optional for this form.

**What I changed.**
- Removed the package requirement from `OnboardingRulesConfigForm` (+2/−4).

**Impact.** Unblocked onboarding-rules configuration. A deletion of validation that did not reflect the domain —
the same shape of fix as #3750.

---

### 242. #5260 — Two more dependencies unpinned from the upgrade exclusion list
`Refactor / tech debt` · `Tooling, deps & docs` · **Impact 0.6/10**
**Merged** 2026-07-03 · **Linear** [SUBRITE-399](https://linear.app/subrite/issue/SUBRITE-399) (High) · **Base** `develop` · 4 files, +47/-33, 3 commits

**Problem.** Follow-up to #5133. `next-intl` and `eslint-plugin-react-hooks` remained on the nightly
upgrade-exclusion list, still pinned to older versions.

**Root cause.** Both upgrades required code changes — as with the packages handled in #5133 — so they had been
deferred.

**What I changed.**
- Removed both from `.ncurc.json`, upgraded them, and made the required code changes including an update to
  `frontend/src/proxy.ts` for the `next-intl` version change.

**Impact.** Cleared the remaining entries from the frontend's dependency-exclusion list, so the nightly automation
can keep these current. Completes the work started in #5133 rather than leaving it half-done.

---

### 243. #3763 — Gift receiver phone number requirement
`Improvement` · `Checkout` · **Impact 0.5/10**
**Merged** 2025-09-18 · **Linear** SUBRITE-3323 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 1 file, +6/-11, 3 commits

**Problem.** The add-gift-receiver form required a phone number for the recipient, which the gift buyer often does
not have — the same over-strict requirement later fixed more thoroughly in #3924.

**Root cause.** Phone treated as mandatory in the gift-receiver information form.

**What I changed.**
- Relaxed the phone requirement in `GiftReceiverInformationForm` (+6/−11).

**Impact.** Removed an obstacle from the gift-purchase flow. Partial — the same requirement persisted in the
checkout path until #3924 removed it there and on the server as well.

---

### 244. #3839 — Translation correction
`Bug fix` · `Translations & copy` · **Impact 0.5/10**
**Merged** 2025-10-14 · **Base** `subrite/develop` · 2 files, +4/-1, 6 commits

**Problem.** Incorrect or missing translation strings visible in the UI.

**Root cause.** Translation file entries that were wrong or absent.

**What I changed.**
- Corrected the affected strings.

**Impact.** Correct text in the affected locale. Included for completeness at its real weight.

---

### 245. #3840 — Follow-up translation correction
`Bug fix` · `Translations & copy` · **Impact 0.5/10**
**Merged** 2025-10-14 · **Base** `subrite/develop` · 1 file, +1/-0, 1 commit

**Problem.** A remaining missing translation string after #3839.

**Root cause.** One entry missed in the previous pass.

**What I changed.**
- Added the missing string.

**Impact.** Completed the translation correction. One line; included for completeness.

---

### 246. #3913 — Quantity input unusable on mobile
`Bug fix` · `Checkout` · **Impact 0.5/10**
**Merged** 2025-11-03 · **Linear** SUBRITE-3052 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 1 file, +1/-1, 4 commits

**Problem.** Extra products could not be added on mobile — the quantity input's controls were not usable at small
viewport sizes, blocking an upsell at the point of purchase.

**Root cause.** A sizing/layout value in the shared `QuantityInput` that did not work at mobile widths.

**What I changed.**
- Corrected the value in `QuantityInput` (one line), fixing it wherever the shared component is used.

**Impact.** Made extra products addable on mobile — directly a revenue path, since mobile is where a large share of
purchases happen. One line in a shared component.

---

### 247. #3990 — Widget tag manager translation
`Bug fix` · `Translations & copy` · **Impact 0.5/10**
**Merged** 2025-11-13 · **Base** `subrite/develop` · 2 files, +4/-4, 2 commits

**Problem.** Incorrect translation strings in the widget tag-manager UI, part of the same hosted-widget cluster as
#3987, #3988 and #3989.

**Root cause.** Wrong translation entries for the tag-manager labels.

**What I changed.**
- Corrected the four affected strings.

**Impact.** Correct labels in the widget embedding UI. Included for completeness.

---

### 248. #2876 — Duplicate subscription-plan setting removed
`Refactor / tech debt` · `Admin UI` · **Impact 0.4/10**
**Merged** 2025-03-07 · **Linear** SUBRITE-2589 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 1 file, +0/-13, 4 commits

**Problem.** The subscription-plan settings form rendered the `showAsOptionOnSubscriptionSwitch` control twice.
Two controls bound to one value is confusing and invites the assumption that they mean different things.

**Root cause.** A duplicated block in the settings component.

**What I changed.**
- Deleted the duplicate (+0/−13).

**Impact.** One control for one setting. The earliest PR in this portfolio and a pure deletion. (The setting itself
was later split into two genuinely distinct flags in #4947, which is what the duplication had superficially
resembled.)

---

### 249. #3745 — Member number not shown for group members
`Improvement` · `Admin UI` · **Impact 0.4/10**
**Merged** 2025-09-15 · **Linear** SUBRITE-3264 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 3 files, +6/-0, 4 commits

**Problem.** The invited-member detail view did not show the member number. Support staff use the member number to
identify people, and for a group member it was absent — so a seat holder could not be cross-referenced.

**Root cause.** The field was available but not rendered in `UpdateInvitedMemberDetails`.

**What I changed.**
- Displayed the member number in the invited-member details, with translations (+6/−0).

**Impact.** Made group members identifiable by the same number as everyone else. Purely additive and small; it
removes a lookup from a routine support task.

---

### 250. #3866 — Upside-down icons
`Bug fix` · `Admin UI` · **Impact 0.4/10**
**Merged** 2025-10-23 · **Linear** SUBRITE-2912 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 3 files, +3/-3, 2 commits

**Problem.** Icons appeared inverted in three places — email statistics boxes, the product missing-info alert and
the member-invites list. An upside-down arrow indicates the opposite of what is meant, so it is misleading rather
than merely untidy.

**Root cause.** Incorrect icon variants or rotation in the three components.

**What I changed.**
- Corrected the icon usage in all three (+3/−3).

**Impact.** Icons now indicate what they mean. Three lines; an inverted directional icon in a statistics box is
actively misleading, which is why it was worth fixing.

---

### 251. #3720 — Unsaved-changes alert interfered with the date picker
`Bug fix` · `Shared platform code` · **Impact 0.4/10**
**Merged** 2025-09-04 · **Linear** SUBRITE-3228 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 1 file, +7/-5, 3 commits

**Problem.** On the extend-due-date screen, interacting with the date picker triggered the unsaved-changes alert,
so users were warned about leaving while they were still in the middle of choosing a date.

**Root cause.** `UnsavedChangesAlert` treated the date picker's internal interactions as navigation intent.

**What I changed.**
- Adjusted the alert's detection so date-picker interaction is not treated as leaving the page (+7/−5).

**Impact.** Removed a spurious warning from the due-date flow. Small, and the sort of false positive that trains
users to dismiss warnings without reading them — which is what makes the genuine ones ineffective.

---

### 252. #3988 — Hosted widget form and version reference
`Bug fix` · `Widgets & branding` · **Impact 0.3/10**
**Merged** 2025-11-12 · **Base** `subrite/develop` · 2 files, +2/-2, 2 commits

**Problem.** Part of the hosted-widget cluster (#3987, #3989, #3990): the embed tag manager referenced the wrong
widget version, and the hosted-form action needed a corresponding correction.

**Root cause.** A stale version reference in `EmbedTagManager` after the widget bundle was re-versioned in #3867,
plus a follow-on adjustment in the widget config action.

**What I changed.**
- Corrected the widget version reference in the embed tag manager and the corresponding line in
  `getWidgetConfigAndTheme` (+2/−2).

**Impact.** Made the embed snippet reference the correct widget bundle — which matters, because that snippet is what
tenants copy onto their own sites. Two lines.

---

### 253. #3790 — Vipps settings form feedback
`Improvement` · `Auth & access control` · **Impact 0.2/10**
**Merged** 2025-09-29 · **Linear** SUBRITE-3230 *(pre-migration issue, no longer in Linear)* · **Base** `subrite/develop` · 4 files, +18/-18, 6 commits

**Problem.** Review feedback on the Vipps login settings screens built in #3777 — labels and copy in the auth
provider form and list needed correcting before the feature was considered complete.

**Root cause.** Wording and labelling chosen during initial implementation and revised on review.

**What I changed.**
- Applied the feedback to `AuthProviderForm` and `AuthProviderList` with the corresponding translations
  (+18/−18).

**Impact.** Finished the Vipps login settings to a shippable standard. The lowest-ranked entry in the portfolio and
listed as such — a copy-and-labelling pass closing out a feature.

---

# In progress (not yet raised as a PR)

### #— SUBRITE-632 — Communication module error-handling compliance
`Improvement` · `Communication & notifications` + `API contract` · **Impact: est. 5.5/10** · **IN PROGRESS**
**Branch** `feature/subrite-632-communication-part-1-error-handling-compliance-review` · **Linear**
[SUBRITE-632](https://linear.app/subrite/issue/SUBRITE-632) (Medium · estimate M) · **Programme**
*API error-handling compliance* · 5 commits, 28 files, +2927/-353 · **as of 2026-07-28**

**Problem.** The communication module's error handling did not meet the platform's own contract. Controller
endpoints returned generic failures, route parameters were unvalidated, error responses were undeclared or
inaccurately declared in Swagger, and provider failures (SendGrid) surfaced to the user as opaque errors. This
is the communication-module instalment of the *API error-handling compliance* programme that #5316, #5331 and
#5354 belong to — the same problem being worked module by module.

**Root cause.** Error handling had grown per-endpoint rather than from a shared contract: no registry of
domain error codes, no field-scoped exception type, no parameter-validation pipe, and no exception filter
translating provider errors into the platform's response shape. Each endpoint therefore did its own thing, and
the Swagger declarations drifted from what the code actually returns.

**What I have changed so far** (five commits, each a coherent step):
1. **Registry-backed error codes** — added `communication.errors.ts` as the module's error registry,
   `domain-field-exception.ts` for field-scoped domain errors and `expect-domain-error.ts` as a test helper, so
   error codes are declared once and asserted against.
2. **Parameter and DTO validation** — added a reusable `parse-id.pipe.ts` and closed the validation gaps on
   `CommunicationController`, with `communication-param-validation.test.ts` covering them.
3. **API contract declarations** — declared the controller's real error responses in Swagger, plus
   `communication-wire-compat.test.ts` asserting the wire format does not regress.
4. **Surfacing provider failures** — added `communication-provider-error.ts` and a
   `communication-provider-exception.filter.ts` (with tests) translating SendGrid failures into meaningful
   responses, `temporal-failure.util.ts` for failure classification in Temporal activities, and
   `unwrapApiResponse.ts` on the frontend so the message reaches the user.
5. **Accurate Swagger** — corrected the field-scoped codes so the documentation matches the implementation
   rather than approximating it.

**Impact (expected).** Brings the communication module up to the platform's error-handling contract: validated
parameters, declared and accurate error responses, and provider failures that reach the user as actionable
messages instead of generic errors. The reusable pieces — `parse-id.pipe`, `domain-field-exception`,
`expect-domain-error`, `temporal-failure.util` — are deliberately module-agnostic, so the payment module work
staged alongside this branch and any subsequent module reuse them rather than reimplementing.
**Not yet raised as a PR; the figures above are the branch state on 2026-07-28.**

---

# Appendix A — Chronological timeline

Every PR by month, newest first. `H` marks a production hotfix; `#N` links to the ranked entry above.

### July 2026 — 24 PRs

- `2026-07-28` **#5374** *(open)* — Tenant isolation: dedicated TenantScopedRepository for TenantUsage · *Security / tenant isolation* · rank 28
- `2026-07-28` **#5370** — Failed to fetch podcast feed · *Bug fix* · rank 40
- `2026-07-24` **#5368** *(open)* — Align gift subscription member UI with the new group subscription design (admin + mypage) · *Feature* · rank 24
- `2026-07-24` **#5362** — Issues on members list filter · *Feature* · rank 56
- `2026-07-23` **#5351** — Tenant isolation: Accounting module · *Security / tenant isolation* · rank 17
- `2026-07-23` **#5356** — Use SMS terminology instead of newsletter in unsubscribe wording · *Feature* · rank 180
- `2026-07-22` **#5346** — Members list — apply the Product Packages filter design (Filter bar + chips) · *Feature* · rank 44
- `2026-07-22` **#5354** — Postal code validation error not shown on Company details settings form · *Bug fix* · rank 145
- `2026-07-20` **#5323** — Push notifications silently dropped when FCM returns transient internal errors · *Bug fix* · rank 20
- `2026-07-20` **#5331** — Misleading "no permission" error from spurious 401 · *Security* · rank 107
- `2026-07-17` **#5329** — Tenant isolation: Push notification · *Security / tenant isolation* · rank 10
- `2026-07-16` **#5317** — Tenant isolation: Product / Plan / Content / Segmentation · *Security / tenant isolation* · rank 2
- `2026-07-15` **#5316** — Nonsensical and unauthorized error message · *Security* · rank 16
- `2026-07-13` **#5301** — Recurring e-invoice: drop over-strict phone mandatory check (blocks EHF delivery for private members) · *Bug fix* · rank 43
- `2026-07-10` **#5282** — reconcileVippsCharge: Cannot read properties of null (reading 'tenantId') · *Bug fix* · rank 48
- `2026-07-10` **#5297** — OIDC token endpoint: refresh-token rotation force-logs-out members (invalid_grant flood) · *Bug fix* · rank 60
- `2026-07-09` **#5278** — Change cancellation blocked due to last invoice being CREDITED · *Bug fix* · rank 19
- `2026-07-07` **#5272** — Automation "No recipient email address" should be a silent traceable log, not a Datadog error · *Improvement* · rank 31
- `2026-07-06` **#5270** — Skip Logging duplicate SendGrid webhook events, no warning/error · *Bug fix* · rank 9
- `2026-07-06` **#5267** — SendGrid webhook drops events: communicationId FK violation on communication_log insert · *Bug fix* · rank 114
- `2026-07-03` **#5237** — SubscriptionSwitch wizard: token refresh silently resets quantities and discards hand edited seat transfer map · *Feature* · rank 72
- `2026-07-03` **#5260** — Clean up next-intl and eslint-plugin-react-hooks from .ncurc.json in frontend · *Refactor / tech debt* · rank 242
- `2026-07-02` **#5249** — Optimize My Account (mypage) subscription seat-member UI for 3000+ members · *Performance* · rank 22
- `2026-07-01` **#5229** — Error when switching subscription · *Bug fix* · rank 11

### June 2026 — 24 PRs

- `2026-06-30` **#5238** — UI feedback on seat optimization task · *Performance* · rank 159
- `2026-06-29` **#5194** — When we manually stop a subscription for the customer, do we also stop further attempts to charge · *Bug fix* · rank 1
- `2026-06-29` **#5208** — Optimize subscription/seat-member list for 3000+ members · *Performance* · rank 6
- `2026-06-29` **#5231** — Optimize subscription/seat-member list for 3000+ members · *Performance* · rank 127
- `2026-06-23` **#5200** — Credit note editor: editing a decimal price silently inflates the amount 10x or 100x, øre amounts cannot be entered · *Bug fix* · rank 5
- `2026-06-22` **#5185** — SubscriptionUpgradeDowngrade: Fix frontend state errors · *Bug fix* · rank 30
- `2026-06-19` **#5188** `H` — Bug on Invoice Already Paid for Next Period · *Bug fix* · rank 4
- `2026-06-19` **#5170** — Checkout PaymentForm: Fix frontend state errors · *Bug fix* · rank 47
- `2026-06-17` **#5166** — Export segmentation list not showing the dropdown options · *Bug fix* · rank 52
- `2026-06-16` **#5158** — Psykologisk: Strange renewal-data · *Data migration* · rank 12
- `2026-06-12` **#5133** — Clean up .ncurc.json in frontend · *Refactor / tech debt* · rank 80
- `2026-06-11` **#5114** — List export · *Feature* · rank 39
- `2026-06-10` **#5095** — Fix invoice reactivation periods · *Bug fix* · rank 36
- `2026-06-05` **#5082** — E-invoice reactivation advances nextRenewal a full billing period beyond paid coverage (subscriptions get ~2 years to next renewal) · *Bug fix* · rank 18
- `2026-06-04` **#5079** — Subscription missing start date · *Bug fix* · rank 102
- `2026-06-04` **#5073** — Double loading on sorting product package · *Bug fix* · rank 235
- `2026-06-04` **#5075** — Wrong breakpoint when changing subscription on my page · *Bug fix* · rank 238
- `2026-06-03` **#5068** — Access to survey list without admin access · *Security* · rank 8
- `2026-06-03` **#5029** — UI: Optimize Plans & Products->Product packages · *Performance* · rank 32
- `2026-06-03` **#5044** — Einvoice payment shows harcoded values instead of api · *Bug fix* · rank 63
- `2026-06-03` **#5071** — Fix Admin Create Order modal failing with notAvailableViaSlug · *Bug fix* · rank 128
- `2026-06-03` **#5070** — Wrong text on Confirm button · *Bug fix* · rank 134
- `2026-06-03` **#5059** `H` — Find default stripe card for psykologisk · *Recovery tooling* · rank 158
- `2026-06-02` **#5006** — Send Email From "email" is incorrect on Auth Code Verification UI · *Bug fix* · rank 88

### May 2026 — 8 PRs

- `2026-05-21` **#5024** `H` — Recover stripe recurring invoices · *Recovery tooling* · rank 7
- `2026-05-21` **#5004** — Psykologisk import · *Data migration* · rank 101
- `2026-05-19` **#5005** — Describe the difference between change and switch subscription · *Improvement* · rank 223
- `2026-05-14` **#4893** — Tenant isolation: Foundations and Member module · *Security / tenant isolation* · rank 3
- `2026-05-11` **#4947** — Decouple subscription visibility between Admin UI and My Page for enterprise subscriptions · *Improvement* · rank 42
- `2026-05-11` **#4922** — Feedback in import section · *Bug fix* · rank 59
- `2026-05-08` **#4944** — Stripe user charged thrice during downtime via temporal · *Improvement* · rank 29
- `2026-05-07` **#4937** — Admin should be able to save onboarding rules without selecting a package · *Bug fix* · rank 241

### April 2026 — 11 PRs

- `2026-04-30` **#4903** — CSV encoding issue in member import · *Bug fix* · rank 87
- `2026-04-30` **#4902** — Default payment method should be EHF on Create order from admin · *Improvement* · rank 155
- `2026-04-28` **#4888** — Support missing member and address fields during member import from admin UI · *Bug fix* · rank 86
- `2026-04-24` **#4862** — Subrite 4126 keep member email and primary email synced · *Improvement* · rank 108
- `2026-04-23` **#4861** — 4097 remove automation template id from preference · *Improvement* · rank 133
- `2026-04-23` **#4873** — 3977 editor cta trouble · *Improvement* · rank 216
- `2026-04-21` **#4841** — 4204 filternyheter plan clean up · *Improvement* · rank 144
- `2026-04-17` **#4832** — 4196 filter and josimar invoice not sent expected · *Improvement* · rank 126
- `2026-04-16` **#4794** — Fix/subrite 3869/adjust graph on dashboard · *Bug fix* · rank 154
- `2026-04-10` **#4792** — Error while recurring payment failed event · *Bug fix* · rank 125
- `2026-04-10` **#4780** — Subrite 3861/minerva export api analysis and data investigation · *Data migration* · rank 166

### February 2026 — 8 PRs

- `2026-02-24` **#4543** — Subrite 3904/adjust ongoing upgrade downgrade recurring price if subscription plan price updates · *Improvement* · rank 27
- `2026-02-20` **#4556** — Subrite 3942/subscription plan price update affects vipps in progress invoice already sent · *Improvement* · rank 55
- `2026-02-13` **#4528** `H` — Subrite 3912/prevent override discounted prices · *Bug fix* · rank 21
- `2026-02-11` **#4526** `H` — Prevent updating member package prices · *Bug fix* · rank 38
- `2026-02-11` **#4450** — 3666 petro import · *Data migration* · rank 165
- `2026-02-10` **#4506** `H` — Prevent subscription plan price update if not intendent · *Bug fix* · rank 15
- `2026-02-09` **#4493** — Fix / subrite-3903 / prevent-subscription-plan-price-update-if-not-intendent · *Bug fix* · rank 54
- `2026-02-02` **#4467** — 3853 show different info on product list · *Improvement* · rank 208

### January 2026 — 15 PRs

- `2026-01-27` **#4419** — Option to override cta text on checkout · *Improvement* · rank 64
- `2026-01-26` **#4439** — Bugs on restart vipps payment · *Bug fix* · rank 53
- `2026-01-23` **#4400** — 3827 bugs related to price change · *Improvement* · rank 51
- `2026-01-22` **#4432** `H` — Prevent cross user data access · *Security / tenant isolation* · rank 13
- `2026-01-22` **#4418** — Replace english text on cta · *Bug fix* · rank 143
- `2026-01-16` **#4407** `H` — Activity log import member consent data feed email and fullname is incorrect · *Bug fix* · rank 111
- `2026-01-15` **#4402** `H` — Error while recurring payment failed · *Bug fix* · rank 14
- `2026-01-15` **#4296** — Subrite 3711/more flexibility for consent · *Improvement* · rank 77
- `2026-01-15` **#4405** — Add draft status in consent list · *Bug fix* · rank 142
- `2026-01-15` **#4169** — Subrite 3613/filter nyheter preparing import script · *Data migration* · rank 213
- `2026-01-13` **#4380** — Unsupported error not shown correctly · *Bug fix* · rank 222
- `2026-01-12` **#4379** `H` — Quantity issue in subscription import · *Bug fix* · rank 124
- `2026-01-09` **#4351** — 3781 resend member invitation not working as expected v2 · *Improvement* · rank 112
- `2026-01-08` **#4341** — Upgrade downgrade issue on manual price subscription · *Bug fix* · rank 79
- `2026-01-02` **#4287** — Subrite 2871/fixed discounted price on creation · *Improvement* · rank 68

### December 2025 — 13 PRs

- `2025-12-31` **#4309** `H` — Show more link not working in dashboard subscriptions · *Improvement* · rank 227
- `2025-12-30` **#4303** `H` — Show more in dashboard subscription chart · *Improvement* · rank 62
- `2025-12-30` **#4300** `H` — Dashboard area chart · *Improvement* · rank 67
- `2025-12-30` **#4304** `H` — Vertical space on show more · *Improvement* · rank 221
- `2025-12-29` **#4292** — 3758 handing of soft bounce dropped · *Improvement* · rank 76
- `2025-12-26` **#4288** — 3788 adjust y axis to fit actual numbers · *Improvement* · rank 215
- `2025-12-24` **#4291** — 3705 add thousand separator on checkout · *Improvement* · rank 191
- `2025-12-23` **#4274** — subrite-3783 / subscription-navigator-get-active-when-tab-selected-inside-member · *Improvement* · rank 193
- `2025-12-23` **#4276** — 3675 display tenant id in ui · *Improvement* · rank 207
- `2025-12-22` **#4269** — Refactor stats view for area chart · *Refactor / tech debt* · rank 179
- `2025-12-18` **#4265** — Subrite 3702 / Fixed cashflow feedbacks · *Improvement* · rank 197
- `2025-12-01` **#4113** — subrite-3673 / preview-text-bug-in-email · *Bug fix* · rank 132
- `2025-12-01` **#4141** — Fix / preview text · *Bug fix* · rank 151

### November 2025 — 28 PRs

- `2025-11-28` **#4118** `H` — Hotfix/email communication consent bug · *Bug fix* · rank 58
- `2025-11-28` **#4121** — Subrite-3653 / communication-receivers-preview-error · *Bug fix* · rank 212
- `2025-11-26` **#3992** — Subrite 3235/list quality hard bounced emails · *Improvement* · rank 57
- `2025-11-25` **#4074** `H` — hotfix / logo-should-not-be-centered-in-iphone · *Improvement* · rank 196
- `2025-11-24` **#4064** `H` — Fix / Support duplicate email senders · *Bug fix* · rank 75
- `2025-11-24` **#3934** — Subrite 3552 / Email sending improvements · *Improvement* · rank 131
- `2025-11-24` **#4061** `H` — Hotfix / Logo blurr issue · *Bug fix* · rank 190
- `2025-11-21` **#4047** — Subrite 3604 / subscription-is-not-stopped-if-card-detaills-are-missing · *Bug fix* · rank 92
- `2025-11-21` **#4034** `H` — Subrite 3645 / page-crushed-for-invalid-url-on-cloudflare-image-component · *Improvement* · rank 141
- `2025-11-19` **#4000** — Subrite 3628 / Remove hardcoded cdn.united.no · *Improvement* · rank 150
- `2025-11-19` **#4001** — Subrite 755 / When only one payment provider we should select it for the user · *Improvement* · rank 153
- `2025-11-17` **#4003** `H` — Subrite 3634/Bug on image delivery · *Bug fix* · rank 74
- `2025-11-13` **#3990** — Fix/widget tag manager translation · *Bug fix* · rank 247
- `2025-11-12` **#3860** — Subrite/3520 hosted widget form · *Improvement* · rank 100
- `2025-11-12` **#3989** — Fixed hosted form translation issue · *Bug fix* · rank 123
- `2025-11-12` **#3955** — Subrite 3594/search under mypage podcast · *Improvement* · rank 157
- `2025-11-12` **#3987** — Fix / hosted widget form translation issue · *Bug fix* · rank 211
- `2025-11-12` **#3867** — Subrite 3288 / feedback widget · *Improvement* · rank 220
- `2025-11-12` **#3988** — Fix/widget hosted form and version · *Bug fix* · rank 252
- `2025-11-07` **#3951** `H` — Hotfix / unnecessary text in report · *Improvement* · rank 206
- `2025-11-05` **#3925** — Subrite 3537/add revenue and active days per member in registered member export · *Improvement* · rank 99
- `2025-11-05` **#3926** — Subrite 3555/copy email bug · *Bug fix* · rank 122
- `2025-11-03` **#3924** `H` — Hotfix/optional phone number in invited member · *Improvement* · rank 37
- `2025-11-03` **#3855** — Subrite 3363/export for delivery address · *Improvement* · rank 91
- `2025-11-03` **#3883** — Subrite 3542 / Option to delete vipps login from admin · *Improvement* · rank 149
- `2025-11-03` **#3910** — Subrite 3506 / Global search debounce · *Improvement* · rank 163
- `2025-11-03` **#3928** — Fix / delivery address name issue · *Bug fix* · rank 230
- `2025-11-03` **#3913** — Subrite 3052 / Not able to add extra products on mobile · *Improvement* · rank 246

### October 2025 — 24 PRs

- `2025-10-30` **#3914** `H` — Subrite 3062 / Hide invoice address on invited member view · *Improvement* · rank 140
- `2025-10-29` **#3898** — Subrite-3017: wrong-path-for/api/v1/communications/email/edit/id · *Bug fix* · rank 130
- `2025-10-29` **#3903** — Subrite-3550 / Avoid one unverified sender to stop all other email · *Improvement* · rank 139
- `2025-10-29` **#3911** `H` — Hotfix / Extra slash in package url · *Improvement* · rank 174
- `2025-10-29` **#3841** — Subrite 2471/missing a for search results · *Bug fix* · rank 240
- `2025-10-28` **#3891** — Subrite 3543 / Reminder email sent for paid invoice · *Improvement* · rank 90
- `2025-10-28` **#3884** — Subrite 3503 / sort original send date not delivery status · *Improvement* · rank 210
- `2025-10-28` **#3885** — subrite-3016 / wrong-path '/api/v1/communications/email/statistics/id' · *Bug fix* · rank 217
- `2025-10-28` **#3887** — Subrite 3014 / wrong path api/v1/communications/{id}/test/sms · *Bug fix* · rank 218
- `2025-10-24` **#3787** — Subrite 2822/import tag · *Improvement* · rank 84
- `2025-10-24` **#3816** — Subrite 3338/preparing import script for dog trainer · *Data migration* · rank 178
- `2025-10-23` **#3869** — Subrite 3522 / Removed landing pages from admin · *Improvement* · rank 189
- `2025-10-23` **#3868** — Subrite 3042 / Copying package url missing https · *Bug fix* · rank 234
- `2025-10-23` **#3866** — Subrite 2912 / Upside down icons in subrite · *Improvement* · rank 250
- `2025-10-22` **#3863** `H` — Hotfix / upgrade downgrade issue on immediate apply · *Bug fix* · rank 34
- `2025-10-14` **#3803** — Subrite 2805/not possible to delete communication sender · *Improvement* · rank 85
- `2025-10-14` **#3844** — Bugfix / Selected communication sender not showing · *Improvement* · rank 162
- `2025-10-14` **#3838** — Subrite 2034 / not able to close modal popups with esc key · *Improvement* · rank 233
- `2025-10-14` **#3839** — Bugfix translation · *Bug fix* · rank 244
- `2025-10-14` **#3840** — bugfix/translation fix 2 · *Bug fix* · rank 245
- `2025-10-13` **#3830** — Subrite 3367 / Show CTA in send test email · *Improvement* · rank 138
- `2025-10-13` **#3824** — Subrite 3258/whitespace as salestitle shouldnt alllow · *Improvement* · rank 205
- `2025-10-10` **#3820** — Subrite 3384/imporvement predefined query for inactive subscribers · *Improvement* · rank 204
- `2025-10-10` **#3826** — Subrite 3476 / Member profile image pload issue for large size · *Bug fix* · rank 237

### September 2025 — 35 PRs

- `2025-09-30` **#3797** — FIx / don't send invoice while already paid · *Bug fix* · rank 89
- `2025-09-30` **#3796** — Subrite 3380 / Query read timeout on cleanup old request logs · *Refactor / tech debt* · rank 164
- `2025-09-30` **#3795** `H` — Hotfix/payment provider translation · *Improvement* · rank 187
- `2025-09-30` **#3799** — Subrite 3243 / Custom period text adjustment on product item · *Improvement* · rank 188
- `2025-09-30` **#3800** — Fix request log test fail · *Bug fix* · rank 192
- `2025-09-29` **#3793** — Fix / subrite 3223 / skip sending invoice automatically when the invoice is paid · *Bug fix* · rank 50
- `2025-09-29` **#3792** `H` — Subrite 3382/bug on saving subscription rules in admin · *Bug fix* · rank 78
- `2025-09-29` **#3794** — Subrite 3364 / UI Error Blinking once Close the Automation Workflow · *Bug fix* · rank 219
- `2025-09-29` **#3790** — subrite-3230 feedbacks vipps settings · *Improvement* · rank 253
- `2025-09-26` **#3771** — Subrite 3243/allow tenant to edit or save own text for period price on product package level · *Improvement* · rank 61
- `2025-09-26` **#3777** — Subrite 3230/add enable vipps login settings in admin · *Improvement* · rank 97
- `2025-09-26` **#3788** — Fix / Subrite-3381 / tenant save and the icon · *Bug fix* · rank 148
- `2025-09-24` **#3773** — 3310 when uploading large image to podcast it fails with unclear error message · *Bug fix* · rank 203
- `2025-09-23` **#3776** `H` — Hotfix / subrite 3223 / Support paid payment provider · *Improvement* · rank 23
- `2025-09-19` **#3768** — Subrite - 3029 / Add country name in addtion to country code in exports · *Improvement* · rank 95
- `2025-09-19` **#3712** — Subrite 3229 create order form adjustment · *Improvement* · rank 121
- `2025-09-19` **#3746** — Subrite 3262/feedback newsletter section · *Improvement* · rank 175
- `2025-09-19` **#3762** — Subrite 3318 / Show cta in preview · *Improvement* · rank 229
- `2025-09-18` **#3769** `H` — Hotfix imageinx undefined issue · *Bug fix* · rank 195
- `2025-09-18` **#3763** — Subrite 3323 - Phone number requirements gift receivers · *Improvement* · rank 243
- `2025-09-17` **#3736** — Subrite 3259 / deprecate non functional fields from db and ui · *Improvement* · rank 45
- `2025-09-17` **#3760** — Subrite 3252 / Show ocr import receiver email in admin · *Improvement* · rank 96
- `2025-09-16` **#3754** `H` — Hotfix / subrite 3322 / retry on vipps purchase · *Improvement* · rank 94
- `2025-09-15` **#3747** — Subrite 3315 - Mypage and tenant name in admin layout · *Improvement* · rank 239
- `2025-09-15` **#3745** — Subrite 3264 / display member number for group members · *Improvement* · rank 249
- `2025-09-12` **#3753** `H` — Hotfix / subrite 3321 / cannot read null properites of "filter" in admin podcast · *Improvement* · rank 152
- `2025-09-12` **#3750** `H` — Hotfix / Subrite 3320 / Makes teaser text optional · *Improvement* · rank 186
- `2025-09-12` **#3748** `H` — Hotfix/subrite 3319/seat member shows empty space if accepted member name empty · *Improvement* · rank 194
- `2025-09-11` **#3734** — Subrite 3295/move payment image above text on mobile · *Improvement* · rank 156
- `2025-09-11` **#3459** — Subrite 2969/prepare import manifest media · *Data migration* · rank 177
- `2025-09-08` **#3722** — Subrite 3222 - gdpr deleted member and invoice · *Improvement* · rank 106
- `2025-09-04` **#3720** — Subrite 3228 / Extend due date "date picker" issues · *Improvement* · rank 251
- `2025-09-03` **#3705** `H` — Subrite 3249 - user turned off autorenewal still receiving reminders · *Bug fix* · rank 46
- `2025-09-01` **#3713** — Fix/podcast image update · *Bug fix* · rank 129
- `2025-09-01` **#3689** — Subrite 3234/podcast frontend changes · *Improvement* · rank 173

### August 2025 — 18 PRs

- `2025-08-28` **#3704** `H` — Hotfix/company details name org · *Improvement* · rank 120
- `2025-08-28` **#3702** `H` — Surbite 3250 issue on company info save · *Bug fix* · rank 146
- `2025-08-28` **#3701** `H` — Hotfix/subrite 3250 failed to update brandkit settings · *Improvement* · rank 147
- `2025-08-25` **#3679** `H` — hotfix - communication sender receiver check · *Improvement* · rank 110
- `2025-08-25` **#3662** — Subrite 1752 zero seat · *Improvement* · rank 161
- `2025-08-25` **#3681** `H` — Hotfix - Set environment variable for sender approval request email in tests · *Improvement* · rank 202
- `2025-08-22` **#3665** — Added env for request receiver · *Bug fix* · rank 185
- `2025-08-22` **#3671** — Refactor: Update communication sender approval message for clarity and specificity · *Refactor / tech debt* · rank 214
- `2025-08-21` **#3595** — Subrite 3187/secure sender/schema update and data migration · *Improvement* · rank 41
- `2025-08-21` **#3663** — Fix: Communication sender create update · *Bug fix* · rank 225
- `2025-08-20` **#3646** — Subrite 3209 - Show active subscription on top in admin · *Improvement* · rank 184
- `2025-08-19` **#3640** `H` — Add postal address in tenant update process · *Improvement* · rank 93
- `2025-08-19` **#3639** — Subrite 3211 - Fix fixed save email on admin issue · *Bug fix* · rank 201
- `2025-08-19` **#3644** — Remove icons from push notificaton stats · *Improvement* · rank 226
- `2025-08-18` **#3623** — Add confirmation before cancel subscription changes · *Improvement* · rank 181
- `2025-08-15` **#3620** `H` — Fix / include tenantId in payment provider settings query · *Bug fix* · rank 26
- `2025-08-04` **#3556** — Subrite 1740 - include user id on api/v1/members/profile · *Improvement* · rank 209
- `2025-08-04` **#3557** — Subrite 1466 - Get stripe publishable key conditionally · *Improvement* · rank 236

### July 2025 — 5 PRs

- `2025-07-30` **#3544** — Subrite 3049 - Show product package name instead of sales title in list · *Improvement* · rank 224
- `2025-07-29` **#3509** — Subrite 3134/Brandkit configuration in admin, brand specific sender and otp template · *Improvement* · rank 49
- `2025-07-24` **#3522** — Subrite 3111 use company name on invoice · *Improvement* · rank 71
- `2025-07-23` **#3516** `H` — Reference on invoice address · *Improvement* · rank 172
- `2025-07-22` **#3485** — Subrite 2856/inoice reference on invoice address · *Improvement* · rank 25

### June 2025 — 18 PRs

- `2025-06-26` **#3373** — Fix/subrite 3058 default preferences for ehf payment method · *Bug fix* · rank 83
- `2025-06-26` **#3369** — Feat/subrite 3031 simplify member invite · *Improvement* · rank 183
- `2025-06-26` **#3393** — Subrite-3058 - Add proper error message on user create · *Bug fix* · rank 232
- `2025-06-24` **#3388** `H` — Remove sentry error on duplicate stripe credential · *Bug fix* · rank 105
- `2025-06-20` **#3361** — Fix/Subrite-3058 Feedback tenant seetings - Email template type and onboarding fields · *Bug fix* · rank 200
- `2025-06-19` **#3341** — Fix/subrite 3057 missing logo url to be used in template · *Bug fix* · rank 171
- `2025-06-18` **#3339** `H` — Fixes / bank account length and readonly options for the onboarding fields · *Improvement* · rank 104
- `2025-06-18` **#3336** — Fix/subrite 3059 communication email when image is added text shrinks · *Bug fix* · rank 137
- `2025-06-17` **#3333** `H` — Hotfix / clear webhooksecret on payment mehtod update · *Improvement* · rank 33
- `2025-06-16` **#3321** — Subrite 3031 - Simplify member invite · *Improvement* · rank 113
- `2025-06-13` **#3317** `H` — Hotfix/style issue in invoice generation · *Bug fix* · rank 82
- `2025-06-13` **#3319** `H` — Hotfix/saving onboarding fields issue · *Bug fix* · rank 103
- `2025-06-12` **#3313** `H` — Fix/subrite 2955 css bug on invoice page · *Bug fix* · rank 160
- `2025-06-12` **#3314** `H` — Fix/Subrite 2110 - tooltiop issue · *Bug fix* · rank 228
- `2025-06-12` **#3316** `H` — hotfix/Updated add tenants.md with subrite standard podcasts · *Improvement* · rank 231
- `2025-06-02` **#3242** — Feat/subrite-2994-edit-payment-methods · *Improvement* · rank 66
- `2025-06-02` **#3227** — Tenant settings feedbacks 2025-05-26 · *Improvement* · rank 119
- `2025-06-02` **#3252** — Wcag standard check on colors settings · *Improvement* · rank 182

### May 2025 — 8 PRs

- `2025-05-30` **#3214** — Feat/subrite 2949 edit company info · *Improvement* · rank 118
- `2025-05-23` **#3213** `H` — Hotfix/theme settings · *Improvement* · rank 35
- `2025-05-23` **#3218** `H` — Hotfix - Font preview on admin panel · *Improvement* · rank 199
- `2025-05-22` **#3119** — Subrite - 2888: Theme settings · *Improvement* · rank 70
- `2025-05-22` **#3143** — Feat/Subrite 2903 - Tenant preference settings · *Improvement* · rank 98
- `2025-05-21` **#3194** — Feat/subrite 2938 communication settings · *Improvement* · rank 69
- `2025-05-21` **#2959** — Subrite 2692 custom properties · *Improvement* · rank 117
- `2025-05-02` **#3062** — Subrite - 2761 - Export api analysis and data investigation for harvest magazine · *Data migration* · rank 176

### April 2025 — 4 PRs

- `2025-04-16` **#3032** — Feat/subrite 2709 improve tenant setting flexibility on newsletter · *Improvement* · rank 73
- `2025-04-16` **#3060** `H` — Hotfix/subrite 2709 improve tenant setting flexibility on newsletter · *Improvement* · rank 136
- `2025-04-15` **#2952** — Feat/subrite 2692 payment methods show in admin · *Improvement* · rank 65
- `2025-04-15` **#2976** `H` — Hotfix/subrite 2692 company details input length · *Improvement* · rank 198

### March 2025 — 10 PRs

- `2025-03-21` **#2949** — Feat/subrite-2692: iteration 1 show tenants settings in admin panel · *Improvement* · rank 116
- `2025-03-14` **#2926** — Fix (Subrite - 2630): VIPPS charge check · *Bug fix* · rank 115
- `2025-03-13` **#2906** `H` — Fix (Subrite - 2604): All filter variant test on ledger · *Bug fix* · rank 169
- `2025-03-13` **#2911** — Subrite - 2632: Add support for help text in settings · *Improvement* · rank 170
- `2025-03-11` **#2902** — Fix (Subrite - 2625): Order trigger list alphabetically · *Bug fix* · rank 135
- `2025-03-10` **#2899** — Fix(Subrite-2606): Payment report missing company name and org. number · *Bug fix* · rank 81
- `2025-03-07` **#2886** `H` — Hotfix(subrite-2604): Reference filter on ledger not working as expected · *Bug fix* · rank 109
- `2025-03-07` **#2884** — Fix/subrite 2604 reference filter on ledger not working as expected · *Bug fix* · rank 167
- `2025-03-07` **#2885** — Fix(subrite-2533): Click on email should be linked to ledger detail for user · *Bug fix* · rank 168
- `2025-03-07` **#2876** — refactor(SubscriptionPlans): Remove duplicate showAsOptionOnSubscript… · *Refactor / tech debt* · rank 248

---

# Appendix B — Drafted CV bullets (Tier 1)

One drafted bullet per Tier 1 item, written to be lifted and edited. They deliberately state **what was wrong,
what you built, and what it changed** — no invented percentages, because no production metrics were recorded for
most of this work. Where a real number exists (file counts, line counts, entity counts, Datadog hit counts,
tenant scale) it is used.

Pick 4-8 of these. Anything more and they compete with each other.

**Strongest four, if you only take four:** #4893 (architecture), #5194 (revenue-critical bug),
#5208 (performance at scale), #5024 (money recovery). They cover architecture, correctness, performance and
operational judgement without overlapping.

---

### Architecture and security

- **#4893** — Designed and built the tenant-isolation layer for a multi-tenant subscription platform
  (135 entities): a CLS-backed `TenantScopedRepository` that auto-scopes every read and stamps every write, with
  an explicit `unscoped()` escape hatch, an abstract tenant-scoped entity, and a `withTenantContext` helper for
  workers and cron jobs. Shipped with ~830 lines of unit and integration tests and documented as the codebase
  default, turning per-query convention into an enforced invariant.

- **#5317, #5329, #5351, #5374** — Rolled tenant isolation out across catalog, content, segmentation, push
  notifications, the accounting ledger and usage tracking, taking tenant-scoped entities from 78 to 90 of 136 via
  add-then-backfill-then-`NOT NULL` migration pairs. Closed a live cross-tenant write path in the podcast feed
  sweep and replaced a tenant-blind unique index on the accounting chart of accounts.

- **#3620, #4432** — Found and closed two production data-exposure paths: payment-provider settings readable
  across tenants by id (3-line fix), and cross-user access to subscription records, by moving the isolation
  predicate into the query and replacing a post-fetch check with an explicit ownership-or-seat-invitation
  authorisation.

- **#5068** — Closed an authorisation gap where survey data was reachable by anyone with communications
  permission, by introducing a dedicated permission section with an idempotent migration and adding server-side
  enforcement on the route in addition to the API guard.

### Revenue and billing correctness

- **#5194** — Diagnosed and fixed a compound defect where manually stopped subscriptions continued to be charged
  and were silently reverted from `STOPPED` to `PAUSED`: added a pre-charge status re-check to the Temporal
  workflow, made the failure handler terminal-state-aware, cancelled the in-flight workflow on stop, and
  extracted the guard predicates into one shared, tested module applied uniformly across all four card providers.

- **#5188** — Fixed suppressed invoicing caused by an incorrect date comparison duplicated in three places
  (wrong field plus a timezone-granularity mismatch between SQL and application code), replacing all three with
  one tested utility.

- **#5200** — Fixed a silent 10×/100× inflation of credit-note amounts caused by a locale-formatted price being
  parsed with dot-decimal logic, and migrated the persisted amount from `integer` to `numeric` so sub-krone
  values became representable end-to-end. Left behind a tested locale-tolerant price parser.

- **#5024** — Built operator recovery tooling for recurring Stripe charges that succeeded remotely but failed
  locally: matched confirmed payment intents by subscription metadata, reconstructed the missing invoices
  without double-charging, and exposed it as an idempotent batch-capped operator endpoint returning per-item
  outcomes.

- **#5082** — Eliminated roughly a full billing period of free service granted on every e-invoice reactivation by
  anchoring the renewal period to the paid invoice rather than to mutable subscription state, with an explicit
  logged fallback for legacy invoices lacking period dates.

- **#4493, #4506, #4528, #4526** — Stopped routine subscription-plan edits repricing live subscribers and
  overwriting negotiated discounts, by adding a price-change comparison, recording deliberate-discount intent as
  stored state (`hasFixedDiscount`) instead of inferring it from a price mismatch, and disabling the price input
  in the UI when a plan is in use.

- **#4402, #4047, #4792** — Hardened the recurring-payment failure path across Stripe, Nets and Vipps: guarded
  optional payment data consistently, introduced a shared named error for missing payment info so queue
  consumers could route it into dunning, and stopped subscriptions with lost card details delivering service
  indefinitely for free.

### Performance and scale

- **#5208** — Made group subscriptions of 3000+ seats usable: introduced a paginated, searchable, filterable
  seats API with a deprecation path for the embedded collection it replaced, removed an N+1 in alert generation
  by batching member resolution, and built the admin seat-management UI. 37 files, +2980/-310, with unit and
  integration coverage.

- **#5249** — Delivered the member-facing counterpart on My Page, replacing client-side full-list loading with
  server-action paginated fetches and consolidating ~200 lines of bespoke logic into a reusable member-management
  component set.

- **#5029, #5346** — Rebuilt admin list filtering: created a reusable filter framework with URL-persisted state
  and a declarative `IsLessThanOrEqualToField` validator, then migrated the Members list onto it, adding consent
  and custom-property filtering and removing over 1200 net lines while fixing multi-value and open-ended range
  mapping that had been returning wrong result sets.

### Reliability and operations

- **#5323** — Stopped silent loss of push notifications on transient FCM failures by classifying errors into
  drop/retry/fail, adding durable per-row retry state with a SQL-computed backoff, and documenting why framework
  (Temporal) retry was the wrong mechanism — it would have re-sent to subscribers already delivered in the batch.

- **#5270, #5272, #5297, #5301, #5370** — Reduced false-positive production alerting so the remaining signal is
  trustworthy: reclassified expected duplicate-key webhook races (114 Datadog hits in 14 days) using a shared
  SQLSTATE helper, introduced a first-class "skipped" outcome so automation workflows no longer abort on members
  missing a contact channel, and replaced consecutive-failure alert escalation with continuous outage duration
  for third-party feed outages (133 Datadog errors).

- **#5158, #5095** — Repaired billing-field data corruption across affected tenants using a
  preview → snapshot → transactional-apply → verify structure, deliberately scoping the correction to the
  demonstrably safe subset and leaving rows with competing renewal signals untouched.

### Product and features

- **#5229** — Unblocked group-plan subscription switching by deriving group capability from optional-product
  quantity ranges instead of a per-order limit, and removed a path that silently revoked the paying owner's own
  access during a switch.

- **#5278** — Split an overloaded "is this invoice paid?" boolean into distinct `isEInvoicePaid` and
  `isEInvoiceSettled` predicates, unblocking cancellation for customers whose invoice had been credited and
  stopping payment prompts for already-settled invoices.

- **#5316** — Replaced six per-form hardcoded error maps across admin money-handling screens with one centralised,
  tested error normaliser and a consistent alert component, eliminating misleading "unauthorized" messages and
  stale field errors.

- **#5368** — Consolidated divergent gift- and group-subscription seat-management UIs onto one shared component
  set across admin and member portal (40 files, net −1200 lines), so gift subscriptions inherited the
  3000-member scalability work without it being reimplemented.

- **#3485** — Delivered customer invoice references end-to-end — migration, DTO chain, rendered PDF and EHF
  document, and all six address-capture surfaces — removing a concrete blocker to being paid by business
  customers whose AP systems reject unreferenced invoices.

- **#3776** — Corrected payment attribution for manually registered payments by making the paid provider an
  explicit request field instead of a hardcoded literal, so financial reporting reflected how money actually
  arrived.

---

## Framing notes

- **Scale and domain, for context in a summary line:** multi-tenant subscription-management SaaS for Nordic
  media; NestJS/TypeScript, PostgreSQL/TypeORM, Temporal, Redis/Bull backend; Next.js 16/React 19 frontend;
  136 entities, 605 API routes, 623 frontend routes; four card providers (Stripe, Vipps, Nets, SwedbankPay) plus
  EHF/Avtalegiro e-invoicing.
- **Volume, if a "delivery" line is useful:** 251 merged PRs over 17 months (March 2025 – July 2026), including
  56 production hotfixes, across backend, frontend, database migrations and production data operations.
- **What the record actually shows**, if you want a one-line self-description: someone who finds root causes
  rather than symptoms (repeatedly extracting one shared, tested implementation where three or four divergent
  ones existed), works safely on money-affecting production data (preview/snapshot/verify), and treats alerting
  and error messages as part of the product.
- **Honest caveat to keep in mind:** the impact scores here are relative rankings, not measured business
  outcomes. If an interviewer asks for numbers, the defensible ones are the diff/file/entity counts, the Datadog
  hit counts quoted in #5270 and #5370, and the 3000+ member scale target — not a percentage improvement.

---

# Appendix C — Themes and programmes

These are the multi-PR bodies of work. On a CV a coherent programme reads considerably stronger than the
same PRs listed individually, because it shows sustained ownership of a problem rather than a series of tickets.

### Tenant isolation programme

*Linear project: *Tenant isolation: add tenantId to all tenant-scoped entities** · **7 PRs**

Converted multi-tenant isolation in a 135-entity NestJS/TypeORM codebase from a per-query convention into an
enforced platform primitive, then rolled it out module by module. #4893 built the foundation
(`TenantScopedRepository`, `TenantScopedEntity`, `withTenantContext`, ~830 lines of tests); #5317, #5329, #5351
and #5374 applied it to catalog/content/segmentation, push notifications, the accounting ledger and tenant
usage. #3620 and #4432 are the concrete cross-tenant and cross-user exposures that motivated the programme.
Tenant-scoped entities went from 78 to 90 of 136 over the course of it.

**PRs:** #4893, #5317, #5329, #5351, #5374, #3620, #4432

### Error-handling and clear error messages

*Linear projects: *Clear error messages*, *API error-handling compliance** · **5 PRs**

A programme to replace generic and misleading errors with accurate, localised, actionable ones. #5316 built
the centralised `getActionError` normaliser and replaced six per-form hardcoded error maps; #5354 and #5331
applied it to postal-code validation and the misleading transient-401 message; #5229 widened API error
declarations so clients can distinguish cases. The in-progress SUBRITE-632 branch extends the same contract to
the communication module with an error registry, parameter validation, accurate Swagger and provider-error
surfacing.

**PRs:** #5316, #5331, #5354, #5229, #SUBRITE-632

### Datadog noise reduction

*Linear project: *Datadog cleanup** · **8 PRs**

Systematic work to make production alerting trustworthy by removing false positives while preserving real
signal. Each entry diagnoses why an expected condition was being reported as a fault and fixes the
classification rather than silencing the log: duplicate SendGrid webhooks (#5270), missing recipient contact
info (#5272), expected OIDC grant errors (#5297), an over-strict EHF phone check (#5301), transient FCM
failures (#5323), third-party feed outages (#5370) and reconciliation scope (#5282). #3388 is the earliest
instance of the same concern, from June 2025.

**PRs:** #5270, #5272, #5297, #5301, #5323, #5370, #5282, #3388

### Performance and scale for 3000+ member subscriptions

*Linear: SUBRITE-446, SUBRITE-489, SUBRITE-254, SUBRITE-197* · **7 PRs**

Made the platform's largest enterprise subscriptions usable. #5208 introduced a paginated seats API with a
deprecation path, killed an N+1 in alert generation and built the admin seat-management UI; #5249 delivered the
member-facing counterpart; #5029 rebuilt the product-packages list and created the reusable filter framework
that #5346 then migrated the Members list onto (net −1200 lines). #3910 applied debouncing and request
cancellation to global search.

**PRs:** #5208, #5249, #5029, #5346, #5238, #5231, #3910

### Payment correctness and recovery

*Payments, billing and invoicing* · **12 PRs**

The highest-stakes cluster: subscriptions charged after being stopped (#5194), suppressed invoicing from a
wrong date comparison (#5188), credit notes inflated 100× (#5200), charges taken at Stripe but not recorded
locally (#5024), a full billing period of free service on e-invoice reactivation (#5082), plan edits repricing
live subscribers and overwriting negotiated discounts (#4493/#4506/#4526/#4528), crashes on the dunning path
(#4402), triple-charging during an outage (#4944) and subscriptions never stopped when card details went
missing (#4047).

**PRs:** #5194, #5188, #5200, #5024, #5082, #4506, #4528, #4493, #4526, #4402, #4944, #4047

### Tenant self-service settings

*2025 programme across the admin panel* · **15 PRs**

A sustained 2025 effort moving tenant configuration from staff-mediated to self-service: company info, custom
properties, themes, preferences, communication settings, brand kits, payment providers and OAuth login. #3213
later consolidated a dozen divergent save paths onto a single settings endpoint that derives the tenant from
the session rather than the request.

**PRs:** #2949, #2959, #3119, #3143, #3194, #3213, #3214, #3227, #3509, #3595, #3777, #3883, #2952, #3242, #5044

### Tenant onboarding data migrations

*scripts/data-ops — repeatable migration pipelines* · **11 PRs**

Eleven PRs building and running tenant data migrations, each as numbered, individually inspectable stages with
written clarifications and post-import checklists. Two of them (#5158, #5095) are the preview/snapshot/verify
data repairs for billing fields damaged by an earlier import — the structure is what made the fault locatable
and the correction reviewable.

**PRs:** #5004, #4450, #4780, #4169, #3816, #3459, #3062, #5158, #5095, #5059, #4841

---

# Appendix D — Closed without merge

26 PRs were closed without being merged — superseded by a rewrite, replaced after a branch
rename, or abandoned in favour of a different approach. Listed for completeness only; none of them shipped.

| PR | Closed | Title |
| --- | --- | --- |
| #5336 | 2026-07-20 | feature/subrite-485-surface-specific-error-instead-of-internal-server-error |
| #5308 | 2026-07-13 | Temp stage test |
| #5292 | 2026-07-08 | Merge develop into main |
| #5189 | 2026-06-19 | feature/subrite-299-bug-on-invoice-already-paid-for-next-period-1 |
| #5180 | 2026-06-18 | feature/subrite-299-bug-on-invoice-already-paid-for-next-period |
| #5087 | 2026-06-05 | fix/subrite-276-fix-einvoice-reactivation-periods |
| #4916 | 2026-05-04 | feature/subrite-39-make-a-plan-to-standardize-the-import-process-for-a-new-tenant |
| #4883 | 2026-04-27 | Subrite 3854 / Tenant isolation via tenantId for all tenant scoped entity |
| #4793 | 2026-04-10 | fix/subrite-3869/adjust-graph-on-dashboard |
| #4549 | 2026-02-18 | subrite-3946/hold-the-price-snapshot-during-the-upgrade-switch-action |
| #4521 | 2026-02-11 | subrite 3912/do not update discounted member subscriptions when subscription plan price is |
| #4465 | 2026-01-29 | subrite-3871 - improve-ui-in-draft-mode-so-tenant-is-not-confused |
| #4404 | 2026-01-15 | fix / filter-payment-info |
| #4328 | 2026-01-02 | subrite-3781/resend-member-invitation-not-working-as-expected |
| #4126 | 2025-11-28 | Silent failure of webhook |
| #4105 | 2025-11-27 | Hotfix resolve race condition |
| #4062 | 2025-11-24 | Fix / Support duplicate email senders |
| #3695 | 2025-08-27 | Hotfix / Subrite 3245 - Adjust alignment on checkout |
| #3678 | 2025-08-25 | Fix/merge conflict with develop |
| #3652 | 2025-08-20 | Subrite 3223 - new order in admin with already paid for einvoice |
| #3579 | 2025-08-05 | Subrite 1752 digital upgrades that are not a new user |
| #3551 | 2025-07-30 | Subrite 1833 - Missing nick names on triggers |
| #3500 | 2025-07-15 | Feat/subrite 3130/brand kit configuration in admin |
| #3337 | 2025-06-18 | Fix/subrite 3059 communication email - when image is added text shrinks |
| #3224 | 2025-05-26 | Hotfix/font preview on tenant settings |
| #2978 | 2025-03-25 | Hotfix/account settings navigation issue |
